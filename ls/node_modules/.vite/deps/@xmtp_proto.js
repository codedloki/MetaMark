import {
  require_minimal
} from "./chunk-IDAXVCIJ.js";
import {
  __asyncGenerator,
  __asyncValues,
  __await,
  __awaiter,
  __extends,
  __generator,
  __read,
  __spreadArray,
  __values,
  init_tslib_es6
} from "./chunk-SZASF7UD.js";
import {
  __export,
  __toESM
} from "./chunk-ONY6HBPH.js";

// node_modules/@xmtp/proto/ts/dist/esm/message_api/v1/message_api.pb.js
var message_api_pb_exports = {};
__export(message_api_pb_exports, {
  MessageApi: () => MessageApi,
  SortDirection: () => SortDirection
});

// node_modules/@xmtp/proto/ts/dist/esm/fetch.pb.js
var fetch_pb_exports = {};
__export(fetch_pb_exports, {
  b64Decode: () => b64Decode,
  b64Encode: () => b64Encode,
  fetchReq: () => fetchReq,
  fetchStreamingRequest: () => fetchStreamingRequest,
  renderURLSearchParams: () => renderURLSearchParams,
  replacer: () => replacer
});
var __awaiter2 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var __rest = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var b64 = new Array(64);
var s64 = new Array(123);
for (let i = 0; i < 64; )
  s64[b64[i] = i < 26 ? i + 65 : i < 52 ? i + 71 : i < 62 ? i - 4 : i - 59 | 43] = i++;
function b64Encode(buffer2, start, end) {
  let parts = null;
  const chunk = [];
  let i = 0, j = 0, t;
  while (start < end) {
    const b = buffer2[start++];
    switch (j) {
      case 0:
        chunk[i++] = b64[b >> 2];
        t = (b & 3) << 4;
        j = 1;
        break;
      case 1:
        chunk[i++] = b64[t | b >> 4];
        t = (b & 15) << 2;
        j = 2;
        break;
      case 2:
        chunk[i++] = b64[t | b >> 6];
        chunk[i++] = b64[b & 63];
        j = 0;
        break;
    }
    if (i > 8191) {
      (parts || (parts = [])).push(String.fromCharCode.apply(String, chunk));
      i = 0;
    }
  }
  if (j) {
    chunk[i++] = b64[t];
    chunk[i++] = 61;
    if (j === 1)
      chunk[i++] = 61;
  }
  if (parts) {
    if (i)
      parts.push(String.fromCharCode.apply(String, chunk.slice(0, i)));
    return parts.join("");
  }
  return String.fromCharCode.apply(String, chunk.slice(0, i));
}
var invalidEncoding = "invalid encoding";
function b64Decode(s) {
  const buffer2 = [];
  let offset = 0;
  let j = 0, t;
  for (let i = 0; i < s.length; ) {
    let c = s.charCodeAt(i++);
    if (c === 61 && j > 1)
      break;
    if ((c = s64[c]) === void 0)
      throw Error(invalidEncoding);
    switch (j) {
      case 0:
        t = c;
        j = 1;
        break;
      case 1:
        buffer2[offset++] = t << 2 | (c & 48) >> 4;
        t = c;
        j = 2;
        break;
      case 2:
        buffer2[offset++] = (t & 15) << 4 | (c & 60) >> 2;
        t = c;
        j = 3;
        break;
      case 3:
        buffer2[offset++] = (t & 3) << 6 | c;
        j = 0;
        break;
    }
  }
  if (j === 1)
    throw Error(invalidEncoding);
  return new Uint8Array(buffer2);
}
function replacer(key, value) {
  if (value && value.constructor === Uint8Array) {
    return b64Encode(value, 0, value.length);
  }
  return value;
}
function fetchReq(path, init) {
  const _a = init || {}, { pathPrefix } = _a, req = __rest(_a, ["pathPrefix"]);
  const url = pathPrefix ? `${pathPrefix}${path}` : path;
  return fetch(url, req).then((r) => r.json().then((body) => {
    if (!r.ok) {
      throw body;
    }
    return body;
  }));
}
function fetchStreamingRequest(path, callback, init) {
  return __awaiter2(this, void 0, void 0, function* () {
    const _a = init || {}, { pathPrefix } = _a, req = __rest(_a, ["pathPrefix"]);
    const url = pathPrefix ? `${pathPrefix}${path}` : path;
    const result = yield fetch(url, req);
    if (!result.ok) {
      const resp = yield result.json();
      const errMsg = resp.error && resp.error.message ? resp.error.message : "";
      throw new Error(errMsg);
    }
    if (!result.body) {
      throw new Error("response doesnt have a body");
    }
    yield result.body.pipeThrough(new TextDecoderStream()).pipeThrough(getNewLineDelimitedJSONDecodingStream()).pipeTo(getNotifyEntityArrivalSink((e) => {
      if (callback) {
        callback(e);
      }
    }));
    return;
  });
}
function getNewLineDelimitedJSONDecodingStream() {
  return new TransformStream({
    start(controller) {
      controller.buf = "";
      controller.pos = 0;
    },
    transform(chunk, controller) {
      if (controller.buf === void 0) {
        controller.buf = "";
      }
      if (controller.pos === void 0) {
        controller.pos = 0;
      }
      controller.buf += chunk;
      while (controller.pos < controller.buf.length) {
        if (controller.buf[controller.pos] === "\n") {
          const line = controller.buf.substring(0, controller.pos);
          const response = JSON.parse(line);
          controller.enqueue(response.result);
          controller.buf = controller.buf.substring(controller.pos + 1);
          controller.pos = 0;
        } else {
          ++controller.pos;
        }
      }
    }
  });
}
function getNotifyEntityArrivalSink(notifyCallback) {
  return new WritableStream({
    write(entity) {
      notifyCallback(entity);
    }
  });
}
function isPlainObject(value) {
  const isObject8 = Object.prototype.toString.call(value).slice(8, -1) === "Object";
  const isObjLike = value !== null && isObject8;
  if (!isObjLike || !isObject8) {
    return false;
  }
  const proto = Object.getPrototypeOf(value);
  const hasObjectConstructor = typeof proto === "object" && proto.constructor === Object.prototype.constructor;
  return hasObjectConstructor;
}
function isPrimitive(value) {
  return ["string", "number", "boolean"].some((t) => typeof value === t);
}
function isZeroValuePrimitive(value) {
  return value === false || value === 0 || value === "";
}
function flattenRequestPayload(requestPayload, path = "") {
  return Object.keys(requestPayload).reduce((acc, key) => {
    const value = requestPayload[key];
    const newPath = path ? [path, key].join(".") : key;
    const isNonEmptyPrimitiveArray = Array.isArray(value) && value.every((v) => isPrimitive(v)) && value.length > 0;
    const isNonZeroValuePrimitive = isPrimitive(value) && !isZeroValuePrimitive(value);
    let objectToMerge = {};
    if (isPlainObject(value)) {
      objectToMerge = flattenRequestPayload(value, newPath);
    } else if (isNonZeroValuePrimitive || isNonEmptyPrimitiveArray) {
      objectToMerge = { [newPath]: value };
    }
    return Object.assign(Object.assign({}, acc), objectToMerge);
  }, {});
}
function renderURLSearchParams(requestPayload, urlPathParams = []) {
  const flattenedRequestPayload = flattenRequestPayload(requestPayload);
  const urlSearchParams = Object.keys(flattenedRequestPayload).reduce((acc, key) => {
    const value = flattenedRequestPayload[key];
    if (urlPathParams.find((f) => f === key)) {
      return acc;
    }
    return Array.isArray(value) ? [...acc, ...value.map((m) => [key, m.toString()])] : acc = [...acc, [key, value.toString()]];
  }, []);
  return new URLSearchParams(urlSearchParams).toString();
}

// node_modules/@xmtp/proto/ts/dist/esm/message_api/v1/message_api.pb.js
var SortDirection;
(function(SortDirection3) {
  SortDirection3["SORT_DIRECTION_UNSPECIFIED"] = "SORT_DIRECTION_UNSPECIFIED";
  SortDirection3["SORT_DIRECTION_ASCENDING"] = "SORT_DIRECTION_ASCENDING";
  SortDirection3["SORT_DIRECTION_DESCENDING"] = "SORT_DIRECTION_DESCENDING";
})(SortDirection || (SortDirection = {}));
var MessageApi = class {
  static Publish(req, initReq) {
    return fetchReq(`/message/v1/publish`, Object.assign(Object.assign({}, initReq), { method: "POST", body: JSON.stringify(req, replacer) }));
  }
  static Subscribe(req, entityNotifier, initReq) {
    return fetchStreamingRequest(`/message/v1/subscribe`, entityNotifier, Object.assign(Object.assign({}, initReq), { method: "POST", body: JSON.stringify(req, replacer) }));
  }
  static SubscribeAll(req, entityNotifier, initReq) {
    return fetchStreamingRequest(`/message/v1/subscribe-all`, entityNotifier, Object.assign(Object.assign({}, initReq), { method: "POST", body: JSON.stringify(req, replacer) }));
  }
  static Query(req, initReq) {
    return fetchReq(`/message/v1/query`, Object.assign(Object.assign({}, initReq), { method: "POST", body: JSON.stringify(req, replacer) }));
  }
  static BatchQuery(req, initReq) {
    return fetchReq(`/message/v1/batch-query`, Object.assign(Object.assign({}, initReq), { method: "POST", body: JSON.stringify(req, replacer) }));
  }
};

// node_modules/@xmtp/proto/ts/dist/esm/message_api/v1/authn.pb.js
var authn_pb_exports = {};
__export(authn_pb_exports, {
  AuthData: () => AuthData,
  Token: () => Token,
  protobufPackage: () => protobufPackage3
});

// node_modules/long/index.js
var wasm = null;
try {
  wasm = new WebAssembly.Instance(
    new WebAssembly.Module(
      new Uint8Array([
        // \0asm
        0,
        97,
        115,
        109,
        // version 1
        1,
        0,
        0,
        0,
        // section "type"
        1,
        13,
        2,
        // 0, () => i32
        96,
        0,
        1,
        127,
        // 1, (i32, i32, i32, i32) => i32
        96,
        4,
        127,
        127,
        127,
        127,
        1,
        127,
        // section "function"
        3,
        7,
        6,
        // 0, type 0
        0,
        // 1, type 1
        1,
        // 2, type 1
        1,
        // 3, type 1
        1,
        // 4, type 1
        1,
        // 5, type 1
        1,
        // section "global"
        6,
        6,
        1,
        // 0, "high", mutable i32
        127,
        1,
        65,
        0,
        11,
        // section "export"
        7,
        50,
        6,
        // 0, "mul"
        3,
        109,
        117,
        108,
        0,
        1,
        // 1, "div_s"
        5,
        100,
        105,
        118,
        95,
        115,
        0,
        2,
        // 2, "div_u"
        5,
        100,
        105,
        118,
        95,
        117,
        0,
        3,
        // 3, "rem_s"
        5,
        114,
        101,
        109,
        95,
        115,
        0,
        4,
        // 4, "rem_u"
        5,
        114,
        101,
        109,
        95,
        117,
        0,
        5,
        // 5, "get_high"
        8,
        103,
        101,
        116,
        95,
        104,
        105,
        103,
        104,
        0,
        0,
        // section "code"
        10,
        191,
        1,
        6,
        // 0, "get_high"
        4,
        0,
        35,
        0,
        11,
        // 1, "mul"
        36,
        1,
        1,
        126,
        32,
        0,
        173,
        32,
        1,
        173,
        66,
        32,
        134,
        132,
        32,
        2,
        173,
        32,
        3,
        173,
        66,
        32,
        134,
        132,
        126,
        34,
        4,
        66,
        32,
        135,
        167,
        36,
        0,
        32,
        4,
        167,
        11,
        // 2, "div_s"
        36,
        1,
        1,
        126,
        32,
        0,
        173,
        32,
        1,
        173,
        66,
        32,
        134,
        132,
        32,
        2,
        173,
        32,
        3,
        173,
        66,
        32,
        134,
        132,
        127,
        34,
        4,
        66,
        32,
        135,
        167,
        36,
        0,
        32,
        4,
        167,
        11,
        // 3, "div_u"
        36,
        1,
        1,
        126,
        32,
        0,
        173,
        32,
        1,
        173,
        66,
        32,
        134,
        132,
        32,
        2,
        173,
        32,
        3,
        173,
        66,
        32,
        134,
        132,
        128,
        34,
        4,
        66,
        32,
        135,
        167,
        36,
        0,
        32,
        4,
        167,
        11,
        // 4, "rem_s"
        36,
        1,
        1,
        126,
        32,
        0,
        173,
        32,
        1,
        173,
        66,
        32,
        134,
        132,
        32,
        2,
        173,
        32,
        3,
        173,
        66,
        32,
        134,
        132,
        129,
        34,
        4,
        66,
        32,
        135,
        167,
        36,
        0,
        32,
        4,
        167,
        11,
        // 5, "rem_u"
        36,
        1,
        1,
        126,
        32,
        0,
        173,
        32,
        1,
        173,
        66,
        32,
        134,
        132,
        32,
        2,
        173,
        32,
        3,
        173,
        66,
        32,
        134,
        132,
        130,
        34,
        4,
        66,
        32,
        135,
        167,
        36,
        0,
        32,
        4,
        167,
        11
      ])
    ),
    {}
  ).exports;
} catch {
}
function Long(low, high, unsigned) {
  this.low = low | 0;
  this.high = high | 0;
  this.unsigned = !!unsigned;
}
Long.prototype.__isLong__;
Object.defineProperty(Long.prototype, "__isLong__", { value: true });
function isLong(obj) {
  return (obj && obj["__isLong__"]) === true;
}
function ctz32(value) {
  var c = Math.clz32(value & -value);
  return value ? 31 - c : c;
}
Long.isLong = isLong;
var INT_CACHE = {};
var UINT_CACHE = {};
function fromInt(value, unsigned) {
  var obj, cachedObj, cache;
  if (unsigned) {
    value >>>= 0;
    if (cache = 0 <= value && value < 256) {
      cachedObj = UINT_CACHE[value];
      if (cachedObj) return cachedObj;
    }
    obj = fromBits(value, 0, true);
    if (cache) UINT_CACHE[value] = obj;
    return obj;
  } else {
    value |= 0;
    if (cache = -128 <= value && value < 128) {
      cachedObj = INT_CACHE[value];
      if (cachedObj) return cachedObj;
    }
    obj = fromBits(value, value < 0 ? -1 : 0, false);
    if (cache) INT_CACHE[value] = obj;
    return obj;
  }
}
Long.fromInt = fromInt;
function fromNumber(value, unsigned) {
  if (isNaN(value)) return unsigned ? UZERO : ZERO;
  if (unsigned) {
    if (value < 0) return UZERO;
    if (value >= TWO_PWR_64_DBL) return MAX_UNSIGNED_VALUE;
  } else {
    if (value <= -TWO_PWR_63_DBL) return MIN_VALUE;
    if (value + 1 >= TWO_PWR_63_DBL) return MAX_VALUE;
  }
  if (value < 0) return fromNumber(-value, unsigned).neg();
  return fromBits(
    value % TWO_PWR_32_DBL | 0,
    value / TWO_PWR_32_DBL | 0,
    unsigned
  );
}
Long.fromNumber = fromNumber;
function fromBits(lowBits, highBits, unsigned) {
  return new Long(lowBits, highBits, unsigned);
}
Long.fromBits = fromBits;
var pow_dbl = Math.pow;
function fromString(str, unsigned, radix) {
  if (str.length === 0) throw Error("empty string");
  if (typeof unsigned === "number") {
    radix = unsigned;
    unsigned = false;
  } else {
    unsigned = !!unsigned;
  }
  if (str === "NaN" || str === "Infinity" || str === "+Infinity" || str === "-Infinity")
    return unsigned ? UZERO : ZERO;
  radix = radix || 10;
  if (radix < 2 || 36 < radix) throw RangeError("radix");
  var p;
  if ((p = str.indexOf("-")) > 0) throw Error("interior hyphen");
  else if (p === 0) {
    return fromString(str.substring(1), unsigned, radix).neg();
  }
  var radixToPower = fromNumber(pow_dbl(radix, 8));
  var result = ZERO;
  for (var i = 0; i < str.length; i += 8) {
    var size = Math.min(8, str.length - i), value = parseInt(str.substring(i, i + size), radix);
    if (size < 8) {
      var power = fromNumber(pow_dbl(radix, size));
      result = result.mul(power).add(fromNumber(value));
    } else {
      result = result.mul(radixToPower);
      result = result.add(fromNumber(value));
    }
  }
  result.unsigned = unsigned;
  return result;
}
Long.fromString = fromString;
function fromValue(val, unsigned) {
  if (typeof val === "number") return fromNumber(val, unsigned);
  if (typeof val === "string") return fromString(val, unsigned);
  return fromBits(
    val.low,
    val.high,
    typeof unsigned === "boolean" ? unsigned : val.unsigned
  );
}
Long.fromValue = fromValue;
var TWO_PWR_16_DBL = 1 << 16;
var TWO_PWR_24_DBL = 1 << 24;
var TWO_PWR_32_DBL = TWO_PWR_16_DBL * TWO_PWR_16_DBL;
var TWO_PWR_64_DBL = TWO_PWR_32_DBL * TWO_PWR_32_DBL;
var TWO_PWR_63_DBL = TWO_PWR_64_DBL / 2;
var TWO_PWR_24 = fromInt(TWO_PWR_24_DBL);
var ZERO = fromInt(0);
Long.ZERO = ZERO;
var UZERO = fromInt(0, true);
Long.UZERO = UZERO;
var ONE = fromInt(1);
Long.ONE = ONE;
var UONE = fromInt(1, true);
Long.UONE = UONE;
var NEG_ONE = fromInt(-1);
Long.NEG_ONE = NEG_ONE;
var MAX_VALUE = fromBits(4294967295 | 0, 2147483647 | 0, false);
Long.MAX_VALUE = MAX_VALUE;
var MAX_UNSIGNED_VALUE = fromBits(4294967295 | 0, 4294967295 | 0, true);
Long.MAX_UNSIGNED_VALUE = MAX_UNSIGNED_VALUE;
var MIN_VALUE = fromBits(0, 2147483648 | 0, false);
Long.MIN_VALUE = MIN_VALUE;
var LongPrototype = Long.prototype;
LongPrototype.toInt = function toInt() {
  return this.unsigned ? this.low >>> 0 : this.low;
};
LongPrototype.toNumber = function toNumber() {
  if (this.unsigned)
    return (this.high >>> 0) * TWO_PWR_32_DBL + (this.low >>> 0);
  return this.high * TWO_PWR_32_DBL + (this.low >>> 0);
};
LongPrototype.toString = function toString(radix) {
  radix = radix || 10;
  if (radix < 2 || 36 < radix) throw RangeError("radix");
  if (this.isZero()) return "0";
  if (this.isNegative()) {
    if (this.eq(MIN_VALUE)) {
      var radixLong = fromNumber(radix), div = this.div(radixLong), rem1 = div.mul(radixLong).sub(this);
      return div.toString(radix) + rem1.toInt().toString(radix);
    } else return "-" + this.neg().toString(radix);
  }
  var radixToPower = fromNumber(pow_dbl(radix, 6), this.unsigned), rem = this;
  var result = "";
  while (true) {
    var remDiv = rem.div(radixToPower), intval = rem.sub(remDiv.mul(radixToPower)).toInt() >>> 0, digits = intval.toString(radix);
    rem = remDiv;
    if (rem.isZero()) return digits + result;
    else {
      while (digits.length < 6) digits = "0" + digits;
      result = "" + digits + result;
    }
  }
};
LongPrototype.getHighBits = function getHighBits() {
  return this.high;
};
LongPrototype.getHighBitsUnsigned = function getHighBitsUnsigned() {
  return this.high >>> 0;
};
LongPrototype.getLowBits = function getLowBits() {
  return this.low;
};
LongPrototype.getLowBitsUnsigned = function getLowBitsUnsigned() {
  return this.low >>> 0;
};
LongPrototype.getNumBitsAbs = function getNumBitsAbs() {
  if (this.isNegative())
    return this.eq(MIN_VALUE) ? 64 : this.neg().getNumBitsAbs();
  var val = this.high != 0 ? this.high : this.low;
  for (var bit = 31; bit > 0; bit--) if ((val & 1 << bit) != 0) break;
  return this.high != 0 ? bit + 33 : bit + 1;
};
LongPrototype.isSafeInteger = function isSafeInteger() {
  var top11Bits = this.high >> 21;
  if (!top11Bits) return true;
  if (this.unsigned) return false;
  return top11Bits === -1 && !(this.low === 0 && this.high === -2097152);
};
LongPrototype.isZero = function isZero() {
  return this.high === 0 && this.low === 0;
};
LongPrototype.eqz = LongPrototype.isZero;
LongPrototype.isNegative = function isNegative() {
  return !this.unsigned && this.high < 0;
};
LongPrototype.isPositive = function isPositive() {
  return this.unsigned || this.high >= 0;
};
LongPrototype.isOdd = function isOdd() {
  return (this.low & 1) === 1;
};
LongPrototype.isEven = function isEven() {
  return (this.low & 1) === 0;
};
LongPrototype.equals = function equals(other) {
  if (!isLong(other)) other = fromValue(other);
  if (this.unsigned !== other.unsigned && this.high >>> 31 === 1 && other.high >>> 31 === 1)
    return false;
  return this.high === other.high && this.low === other.low;
};
LongPrototype.eq = LongPrototype.equals;
LongPrototype.notEquals = function notEquals(other) {
  return !this.eq(
    /* validates */
    other
  );
};
LongPrototype.neq = LongPrototype.notEquals;
LongPrototype.ne = LongPrototype.notEquals;
LongPrototype.lessThan = function lessThan(other) {
  return this.comp(
    /* validates */
    other
  ) < 0;
};
LongPrototype.lt = LongPrototype.lessThan;
LongPrototype.lessThanOrEqual = function lessThanOrEqual(other) {
  return this.comp(
    /* validates */
    other
  ) <= 0;
};
LongPrototype.lte = LongPrototype.lessThanOrEqual;
LongPrototype.le = LongPrototype.lessThanOrEqual;
LongPrototype.greaterThan = function greaterThan(other) {
  return this.comp(
    /* validates */
    other
  ) > 0;
};
LongPrototype.gt = LongPrototype.greaterThan;
LongPrototype.greaterThanOrEqual = function greaterThanOrEqual(other) {
  return this.comp(
    /* validates */
    other
  ) >= 0;
};
LongPrototype.gte = LongPrototype.greaterThanOrEqual;
LongPrototype.ge = LongPrototype.greaterThanOrEqual;
LongPrototype.compare = function compare(other) {
  if (!isLong(other)) other = fromValue(other);
  if (this.eq(other)) return 0;
  var thisNeg = this.isNegative(), otherNeg = other.isNegative();
  if (thisNeg && !otherNeg) return -1;
  if (!thisNeg && otherNeg) return 1;
  if (!this.unsigned) return this.sub(other).isNegative() ? -1 : 1;
  return other.high >>> 0 > this.high >>> 0 || other.high === this.high && other.low >>> 0 > this.low >>> 0 ? -1 : 1;
};
LongPrototype.comp = LongPrototype.compare;
LongPrototype.negate = function negate() {
  if (!this.unsigned && this.eq(MIN_VALUE)) return MIN_VALUE;
  return this.not().add(ONE);
};
LongPrototype.neg = LongPrototype.negate;
LongPrototype.add = function add(addend) {
  if (!isLong(addend)) addend = fromValue(addend);
  var a48 = this.high >>> 16;
  var a32 = this.high & 65535;
  var a16 = this.low >>> 16;
  var a00 = this.low & 65535;
  var b48 = addend.high >>> 16;
  var b32 = addend.high & 65535;
  var b16 = addend.low >>> 16;
  var b00 = addend.low & 65535;
  var c48 = 0, c32 = 0, c16 = 0, c00 = 0;
  c00 += a00 + b00;
  c16 += c00 >>> 16;
  c00 &= 65535;
  c16 += a16 + b16;
  c32 += c16 >>> 16;
  c16 &= 65535;
  c32 += a32 + b32;
  c48 += c32 >>> 16;
  c32 &= 65535;
  c48 += a48 + b48;
  c48 &= 65535;
  return fromBits(c16 << 16 | c00, c48 << 16 | c32, this.unsigned);
};
LongPrototype.subtract = function subtract(subtrahend) {
  if (!isLong(subtrahend)) subtrahend = fromValue(subtrahend);
  return this.add(subtrahend.neg());
};
LongPrototype.sub = LongPrototype.subtract;
LongPrototype.multiply = function multiply(multiplier) {
  if (this.isZero()) return this;
  if (!isLong(multiplier)) multiplier = fromValue(multiplier);
  if (wasm) {
    var low = wasm["mul"](this.low, this.high, multiplier.low, multiplier.high);
    return fromBits(low, wasm["get_high"](), this.unsigned);
  }
  if (multiplier.isZero()) return this.unsigned ? UZERO : ZERO;
  if (this.eq(MIN_VALUE)) return multiplier.isOdd() ? MIN_VALUE : ZERO;
  if (multiplier.eq(MIN_VALUE)) return this.isOdd() ? MIN_VALUE : ZERO;
  if (this.isNegative()) {
    if (multiplier.isNegative()) return this.neg().mul(multiplier.neg());
    else return this.neg().mul(multiplier).neg();
  } else if (multiplier.isNegative()) return this.mul(multiplier.neg()).neg();
  if (this.lt(TWO_PWR_24) && multiplier.lt(TWO_PWR_24))
    return fromNumber(this.toNumber() * multiplier.toNumber(), this.unsigned);
  var a48 = this.high >>> 16;
  var a32 = this.high & 65535;
  var a16 = this.low >>> 16;
  var a00 = this.low & 65535;
  var b48 = multiplier.high >>> 16;
  var b32 = multiplier.high & 65535;
  var b16 = multiplier.low >>> 16;
  var b00 = multiplier.low & 65535;
  var c48 = 0, c32 = 0, c16 = 0, c00 = 0;
  c00 += a00 * b00;
  c16 += c00 >>> 16;
  c00 &= 65535;
  c16 += a16 * b00;
  c32 += c16 >>> 16;
  c16 &= 65535;
  c16 += a00 * b16;
  c32 += c16 >>> 16;
  c16 &= 65535;
  c32 += a32 * b00;
  c48 += c32 >>> 16;
  c32 &= 65535;
  c32 += a16 * b16;
  c48 += c32 >>> 16;
  c32 &= 65535;
  c32 += a00 * b32;
  c48 += c32 >>> 16;
  c32 &= 65535;
  c48 += a48 * b00 + a32 * b16 + a16 * b32 + a00 * b48;
  c48 &= 65535;
  return fromBits(c16 << 16 | c00, c48 << 16 | c32, this.unsigned);
};
LongPrototype.mul = LongPrototype.multiply;
LongPrototype.divide = function divide(divisor) {
  if (!isLong(divisor)) divisor = fromValue(divisor);
  if (divisor.isZero()) throw Error("division by zero");
  if (wasm) {
    if (!this.unsigned && this.high === -2147483648 && divisor.low === -1 && divisor.high === -1) {
      return this;
    }
    var low = (this.unsigned ? wasm["div_u"] : wasm["div_s"])(
      this.low,
      this.high,
      divisor.low,
      divisor.high
    );
    return fromBits(low, wasm["get_high"](), this.unsigned);
  }
  if (this.isZero()) return this.unsigned ? UZERO : ZERO;
  var approx, rem, res;
  if (!this.unsigned) {
    if (this.eq(MIN_VALUE)) {
      if (divisor.eq(ONE) || divisor.eq(NEG_ONE))
        return MIN_VALUE;
      else if (divisor.eq(MIN_VALUE)) return ONE;
      else {
        var halfThis = this.shr(1);
        approx = halfThis.div(divisor).shl(1);
        if (approx.eq(ZERO)) {
          return divisor.isNegative() ? ONE : NEG_ONE;
        } else {
          rem = this.sub(divisor.mul(approx));
          res = approx.add(rem.div(divisor));
          return res;
        }
      }
    } else if (divisor.eq(MIN_VALUE)) return this.unsigned ? UZERO : ZERO;
    if (this.isNegative()) {
      if (divisor.isNegative()) return this.neg().div(divisor.neg());
      return this.neg().div(divisor).neg();
    } else if (divisor.isNegative()) return this.div(divisor.neg()).neg();
    res = ZERO;
  } else {
    if (!divisor.unsigned) divisor = divisor.toUnsigned();
    if (divisor.gt(this)) return UZERO;
    if (divisor.gt(this.shru(1)))
      return UONE;
    res = UZERO;
  }
  rem = this;
  while (rem.gte(divisor)) {
    approx = Math.max(1, Math.floor(rem.toNumber() / divisor.toNumber()));
    var log2 = Math.ceil(Math.log(approx) / Math.LN2), delta = log2 <= 48 ? 1 : pow_dbl(2, log2 - 48), approxRes = fromNumber(approx), approxRem = approxRes.mul(divisor);
    while (approxRem.isNegative() || approxRem.gt(rem)) {
      approx -= delta;
      approxRes = fromNumber(approx, this.unsigned);
      approxRem = approxRes.mul(divisor);
    }
    if (approxRes.isZero()) approxRes = ONE;
    res = res.add(approxRes);
    rem = rem.sub(approxRem);
  }
  return res;
};
LongPrototype.div = LongPrototype.divide;
LongPrototype.modulo = function modulo(divisor) {
  if (!isLong(divisor)) divisor = fromValue(divisor);
  if (wasm) {
    var low = (this.unsigned ? wasm["rem_u"] : wasm["rem_s"])(
      this.low,
      this.high,
      divisor.low,
      divisor.high
    );
    return fromBits(low, wasm["get_high"](), this.unsigned);
  }
  return this.sub(this.div(divisor).mul(divisor));
};
LongPrototype.mod = LongPrototype.modulo;
LongPrototype.rem = LongPrototype.modulo;
LongPrototype.not = function not() {
  return fromBits(~this.low, ~this.high, this.unsigned);
};
LongPrototype.countLeadingZeros = function countLeadingZeros() {
  return this.high ? Math.clz32(this.high) : Math.clz32(this.low) + 32;
};
LongPrototype.clz = LongPrototype.countLeadingZeros;
LongPrototype.countTrailingZeros = function countTrailingZeros() {
  return this.low ? ctz32(this.low) : ctz32(this.high) + 32;
};
LongPrototype.ctz = LongPrototype.countTrailingZeros;
LongPrototype.and = function and(other) {
  if (!isLong(other)) other = fromValue(other);
  return fromBits(this.low & other.low, this.high & other.high, this.unsigned);
};
LongPrototype.or = function or(other) {
  if (!isLong(other)) other = fromValue(other);
  return fromBits(this.low | other.low, this.high | other.high, this.unsigned);
};
LongPrototype.xor = function xor(other) {
  if (!isLong(other)) other = fromValue(other);
  return fromBits(this.low ^ other.low, this.high ^ other.high, this.unsigned);
};
LongPrototype.shiftLeft = function shiftLeft(numBits) {
  if (isLong(numBits)) numBits = numBits.toInt();
  if ((numBits &= 63) === 0) return this;
  else if (numBits < 32)
    return fromBits(
      this.low << numBits,
      this.high << numBits | this.low >>> 32 - numBits,
      this.unsigned
    );
  else return fromBits(0, this.low << numBits - 32, this.unsigned);
};
LongPrototype.shl = LongPrototype.shiftLeft;
LongPrototype.shiftRight = function shiftRight(numBits) {
  if (isLong(numBits)) numBits = numBits.toInt();
  if ((numBits &= 63) === 0) return this;
  else if (numBits < 32)
    return fromBits(
      this.low >>> numBits | this.high << 32 - numBits,
      this.high >> numBits,
      this.unsigned
    );
  else
    return fromBits(
      this.high >> numBits - 32,
      this.high >= 0 ? 0 : -1,
      this.unsigned
    );
};
LongPrototype.shr = LongPrototype.shiftRight;
LongPrototype.shiftRightUnsigned = function shiftRightUnsigned(numBits) {
  if (isLong(numBits)) numBits = numBits.toInt();
  if ((numBits &= 63) === 0) return this;
  if (numBits < 32)
    return fromBits(
      this.low >>> numBits | this.high << 32 - numBits,
      this.high >>> numBits,
      this.unsigned
    );
  if (numBits === 32) return fromBits(this.high, 0, this.unsigned);
  return fromBits(this.high >>> numBits - 32, 0, this.unsigned);
};
LongPrototype.shru = LongPrototype.shiftRightUnsigned;
LongPrototype.shr_u = LongPrototype.shiftRightUnsigned;
LongPrototype.rotateLeft = function rotateLeft(numBits) {
  var b;
  if (isLong(numBits)) numBits = numBits.toInt();
  if ((numBits &= 63) === 0) return this;
  if (numBits === 32) return fromBits(this.high, this.low, this.unsigned);
  if (numBits < 32) {
    b = 32 - numBits;
    return fromBits(
      this.low << numBits | this.high >>> b,
      this.high << numBits | this.low >>> b,
      this.unsigned
    );
  }
  numBits -= 32;
  b = 32 - numBits;
  return fromBits(
    this.high << numBits | this.low >>> b,
    this.low << numBits | this.high >>> b,
    this.unsigned
  );
};
LongPrototype.rotl = LongPrototype.rotateLeft;
LongPrototype.rotateRight = function rotateRight(numBits) {
  var b;
  if (isLong(numBits)) numBits = numBits.toInt();
  if ((numBits &= 63) === 0) return this;
  if (numBits === 32) return fromBits(this.high, this.low, this.unsigned);
  if (numBits < 32) {
    b = 32 - numBits;
    return fromBits(
      this.high << b | this.low >>> numBits,
      this.low << b | this.high >>> numBits,
      this.unsigned
    );
  }
  numBits -= 32;
  b = 32 - numBits;
  return fromBits(
    this.low << b | this.high >>> numBits,
    this.high << b | this.low >>> numBits,
    this.unsigned
  );
};
LongPrototype.rotr = LongPrototype.rotateRight;
LongPrototype.toSigned = function toSigned() {
  if (!this.unsigned) return this;
  return fromBits(this.low, this.high, false);
};
LongPrototype.toUnsigned = function toUnsigned() {
  if (this.unsigned) return this;
  return fromBits(this.low, this.high, true);
};
LongPrototype.toBytes = function toBytes(le) {
  return le ? this.toBytesLE() : this.toBytesBE();
};
LongPrototype.toBytesLE = function toBytesLE() {
  var hi = this.high, lo = this.low;
  return [
    lo & 255,
    lo >>> 8 & 255,
    lo >>> 16 & 255,
    lo >>> 24,
    hi & 255,
    hi >>> 8 & 255,
    hi >>> 16 & 255,
    hi >>> 24
  ];
};
LongPrototype.toBytesBE = function toBytesBE() {
  var hi = this.high, lo = this.low;
  return [
    hi >>> 24,
    hi >>> 16 & 255,
    hi >>> 8 & 255,
    hi & 255,
    lo >>> 24,
    lo >>> 16 & 255,
    lo >>> 8 & 255,
    lo & 255
  ];
};
Long.fromBytes = function fromBytes(bytes, unsigned, le) {
  return le ? Long.fromBytesLE(bytes, unsigned) : Long.fromBytesBE(bytes, unsigned);
};
Long.fromBytesLE = function fromBytesLE(bytes, unsigned) {
  return new Long(
    bytes[0] | bytes[1] << 8 | bytes[2] << 16 | bytes[3] << 24,
    bytes[4] | bytes[5] << 8 | bytes[6] << 16 | bytes[7] << 24,
    unsigned
  );
};
Long.fromBytesBE = function fromBytesBE(bytes, unsigned) {
  return new Long(
    bytes[4] << 24 | bytes[5] << 16 | bytes[6] << 8 | bytes[7],
    bytes[0] << 24 | bytes[1] << 16 | bytes[2] << 8 | bytes[3],
    unsigned
  );
};
if (typeof BigInt === "function") {
  Long.fromBigInt = function fromBigInt(value, unsigned) {
    var lowBits = Number(BigInt.asIntN(32, value));
    var highBits = Number(BigInt.asIntN(32, value >> BigInt(32)));
    return fromBits(lowBits, highBits, unsigned);
  };
  Long.fromValue = function fromValueWithBigInt(value, unsigned) {
    if (typeof value === "bigint") return Long.fromBigInt(value, unsigned);
    return fromValue(value, unsigned);
  };
  LongPrototype.toBigInt = function toBigInt() {
    var lowBigInt = BigInt(this.low >>> 0);
    var highBigInt = BigInt(this.unsigned ? this.high >>> 0 : this.high);
    return highBigInt << BigInt(32) | lowBigInt;
  };
}
var long_default = Long;

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/public_key.pb.js
var public_key_pb_exports = {};
__export(public_key_pb_exports, {
  PublicKey: () => PublicKey,
  PublicKeyBundle: () => PublicKeyBundle,
  PublicKey_Secp256k1Uncompressed: () => PublicKey_Secp256k1Uncompressed,
  SignedPublicKey: () => SignedPublicKey,
  SignedPublicKeyBundle: () => SignedPublicKeyBundle,
  UnsignedPublicKey: () => UnsignedPublicKey,
  UnsignedPublicKey_Secp256k1Uncompressed: () => UnsignedPublicKey_Secp256k1Uncompressed,
  protobufPackage: () => protobufPackage2
});

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/signature.pb.js
var signature_pb_exports = {};
__export(signature_pb_exports, {
  Signature: () => Signature,
  Signature_ECDSACompact: () => Signature_ECDSACompact,
  Signature_WalletECDSACompact: () => Signature_WalletECDSACompact,
  protobufPackage: () => protobufPackage
});
var import_minimal = __toESM(require_minimal());
var protobufPackage = "xmtp.message_contents";
function createBaseSignature() {
  return { ecdsaCompact: void 0, walletEcdsaCompact: void 0 };
}
var Signature = {
  encode(message, writer = import_minimal.default.Writer.create()) {
    if (message.ecdsaCompact !== void 0) {
      Signature_ECDSACompact.encode(message.ecdsaCompact, writer.uint32(10).fork()).ldelim();
    }
    if (message.walletEcdsaCompact !== void 0) {
      Signature_WalletECDSACompact.encode(message.walletEcdsaCompact, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal.default.Reader ? input : new import_minimal.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.ecdsaCompact = Signature_ECDSACompact.decode(reader, reader.uint32());
          break;
        case 2:
          message.walletEcdsaCompact = Signature_WalletECDSACompact.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      ecdsaCompact: isSet(object.ecdsaCompact) ? Signature_ECDSACompact.fromJSON(object.ecdsaCompact) : void 0,
      walletEcdsaCompact: isSet(object.walletEcdsaCompact) ? Signature_WalletECDSACompact.fromJSON(object.walletEcdsaCompact) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.ecdsaCompact !== void 0 && (obj.ecdsaCompact = message.ecdsaCompact ? Signature_ECDSACompact.toJSON(message.ecdsaCompact) : void 0);
    message.walletEcdsaCompact !== void 0 && (obj.walletEcdsaCompact = message.walletEcdsaCompact ? Signature_WalletECDSACompact.toJSON(message.walletEcdsaCompact) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseSignature();
    message.ecdsaCompact = object.ecdsaCompact !== void 0 && object.ecdsaCompact !== null ? Signature_ECDSACompact.fromPartial(object.ecdsaCompact) : void 0;
    message.walletEcdsaCompact = object.walletEcdsaCompact !== void 0 && object.walletEcdsaCompact !== null ? Signature_WalletECDSACompact.fromPartial(object.walletEcdsaCompact) : void 0;
    return message;
  }
};
function createBaseSignature_ECDSACompact() {
  return { bytes: new Uint8Array(), recovery: 0 };
}
var Signature_ECDSACompact = {
  encode(message, writer = import_minimal.default.Writer.create()) {
    if (message.bytes.length !== 0) {
      writer.uint32(10).bytes(message.bytes);
    }
    if (message.recovery !== 0) {
      writer.uint32(16).uint32(message.recovery);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal.default.Reader ? input : new import_minimal.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignature_ECDSACompact();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.bytes = reader.bytes();
          break;
        case 2:
          message.recovery = reader.uint32();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      bytes: isSet(object.bytes) ? bytesFromBase64(object.bytes) : new Uint8Array(),
      recovery: isSet(object.recovery) ? Number(object.recovery) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.bytes !== void 0 && (obj.bytes = base64FromBytes(message.bytes !== void 0 ? message.bytes : new Uint8Array()));
    message.recovery !== void 0 && (obj.recovery = Math.round(message.recovery));
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseSignature_ECDSACompact();
    message.bytes = (_a = object.bytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.recovery = (_b = object.recovery) !== null && _b !== void 0 ? _b : 0;
    return message;
  }
};
function createBaseSignature_WalletECDSACompact() {
  return { bytes: new Uint8Array(), recovery: 0 };
}
var Signature_WalletECDSACompact = {
  encode(message, writer = import_minimal.default.Writer.create()) {
    if (message.bytes.length !== 0) {
      writer.uint32(10).bytes(message.bytes);
    }
    if (message.recovery !== 0) {
      writer.uint32(16).uint32(message.recovery);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal.default.Reader ? input : new import_minimal.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignature_WalletECDSACompact();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.bytes = reader.bytes();
          break;
        case 2:
          message.recovery = reader.uint32();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      bytes: isSet(object.bytes) ? bytesFromBase64(object.bytes) : new Uint8Array(),
      recovery: isSet(object.recovery) ? Number(object.recovery) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.bytes !== void 0 && (obj.bytes = base64FromBytes(message.bytes !== void 0 ? message.bytes : new Uint8Array()));
    message.recovery !== void 0 && (obj.recovery = Math.round(message.recovery));
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseSignature_WalletECDSACompact();
    message.bytes = (_a = object.bytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.recovery = (_b = object.recovery) !== null && _b !== void 0 ? _b : 0;
    return message;
  }
};
var globalThis = (() => {
  if (typeof globalThis !== "undefined")
    return globalThis;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase64(b642) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
if (import_minimal.default.util.Long !== long_default) {
  import_minimal.default.util.Long = long_default;
  import_minimal.default.configure();
}
function isSet(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/public_key.pb.js
var import_minimal2 = __toESM(require_minimal());
var protobufPackage2 = "xmtp.message_contents";
function createBaseUnsignedPublicKey() {
  return { createdNs: long_default.UZERO, secp256k1Uncompressed: void 0 };
}
var UnsignedPublicKey = {
  encode(message, writer = import_minimal2.default.Writer.create()) {
    if (!message.createdNs.isZero()) {
      writer.uint32(8).uint64(message.createdNs);
    }
    if (message.secp256k1Uncompressed !== void 0) {
      UnsignedPublicKey_Secp256k1Uncompressed.encode(message.secp256k1Uncompressed, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal2.default.Reader ? input : new import_minimal2.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUnsignedPublicKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.createdNs = reader.uint64();
          break;
        case 3:
          message.secp256k1Uncompressed = UnsignedPublicKey_Secp256k1Uncompressed.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      createdNs: isSet2(object.createdNs) ? long_default.fromValue(object.createdNs) : long_default.UZERO,
      secp256k1Uncompressed: isSet2(object.secp256k1Uncompressed) ? UnsignedPublicKey_Secp256k1Uncompressed.fromJSON(object.secp256k1Uncompressed) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.createdNs !== void 0 && (obj.createdNs = (message.createdNs || long_default.UZERO).toString());
    message.secp256k1Uncompressed !== void 0 && (obj.secp256k1Uncompressed = message.secp256k1Uncompressed ? UnsignedPublicKey_Secp256k1Uncompressed.toJSON(message.secp256k1Uncompressed) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseUnsignedPublicKey();
    message.createdNs = object.createdNs !== void 0 && object.createdNs !== null ? long_default.fromValue(object.createdNs) : long_default.UZERO;
    message.secp256k1Uncompressed = object.secp256k1Uncompressed !== void 0 && object.secp256k1Uncompressed !== null ? UnsignedPublicKey_Secp256k1Uncompressed.fromPartial(object.secp256k1Uncompressed) : void 0;
    return message;
  }
};
function createBaseUnsignedPublicKey_Secp256k1Uncompressed() {
  return { bytes: new Uint8Array() };
}
var UnsignedPublicKey_Secp256k1Uncompressed = {
  encode(message, writer = import_minimal2.default.Writer.create()) {
    if (message.bytes.length !== 0) {
      writer.uint32(10).bytes(message.bytes);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal2.default.Reader ? input : new import_minimal2.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUnsignedPublicKey_Secp256k1Uncompressed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.bytes = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      bytes: isSet2(object.bytes) ? bytesFromBase642(object.bytes) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.bytes !== void 0 && (obj.bytes = base64FromBytes2(message.bytes !== void 0 ? message.bytes : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseUnsignedPublicKey_Secp256k1Uncompressed();
    message.bytes = (_a = object.bytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseSignedPublicKey() {
  return { keyBytes: new Uint8Array(), signature: void 0 };
}
var SignedPublicKey = {
  encode(message, writer = import_minimal2.default.Writer.create()) {
    if (message.keyBytes.length !== 0) {
      writer.uint32(10).bytes(message.keyBytes);
    }
    if (message.signature !== void 0) {
      Signature.encode(message.signature, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal2.default.Reader ? input : new import_minimal2.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignedPublicKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyBytes = reader.bytes();
          break;
        case 2:
          message.signature = Signature.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyBytes: isSet2(object.keyBytes) ? bytesFromBase642(object.keyBytes) : new Uint8Array(),
      signature: isSet2(object.signature) ? Signature.fromJSON(object.signature) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.keyBytes !== void 0 && (obj.keyBytes = base64FromBytes2(message.keyBytes !== void 0 ? message.keyBytes : new Uint8Array()));
    message.signature !== void 0 && (obj.signature = message.signature ? Signature.toJSON(message.signature) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSignedPublicKey();
    message.keyBytes = (_a = object.keyBytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.signature = object.signature !== void 0 && object.signature !== null ? Signature.fromPartial(object.signature) : void 0;
    return message;
  }
};
function createBaseSignedPublicKeyBundle() {
  return { identityKey: void 0, preKey: void 0 };
}
var SignedPublicKeyBundle = {
  encode(message, writer = import_minimal2.default.Writer.create()) {
    if (message.identityKey !== void 0) {
      SignedPublicKey.encode(message.identityKey, writer.uint32(10).fork()).ldelim();
    }
    if (message.preKey !== void 0) {
      SignedPublicKey.encode(message.preKey, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal2.default.Reader ? input : new import_minimal2.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignedPublicKeyBundle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.identityKey = SignedPublicKey.decode(reader, reader.uint32());
          break;
        case 2:
          message.preKey = SignedPublicKey.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityKey: isSet2(object.identityKey) ? SignedPublicKey.fromJSON(object.identityKey) : void 0,
      preKey: isSet2(object.preKey) ? SignedPublicKey.fromJSON(object.preKey) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.identityKey !== void 0 && (obj.identityKey = message.identityKey ? SignedPublicKey.toJSON(message.identityKey) : void 0);
    message.preKey !== void 0 && (obj.preKey = message.preKey ? SignedPublicKey.toJSON(message.preKey) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseSignedPublicKeyBundle();
    message.identityKey = object.identityKey !== void 0 && object.identityKey !== null ? SignedPublicKey.fromPartial(object.identityKey) : void 0;
    message.preKey = object.preKey !== void 0 && object.preKey !== null ? SignedPublicKey.fromPartial(object.preKey) : void 0;
    return message;
  }
};
function createBasePublicKey() {
  return {
    timestamp: long_default.UZERO,
    signature: void 0,
    secp256k1Uncompressed: void 0
  };
}
var PublicKey = {
  encode(message, writer = import_minimal2.default.Writer.create()) {
    if (!message.timestamp.isZero()) {
      writer.uint32(8).uint64(message.timestamp);
    }
    if (message.signature !== void 0) {
      Signature.encode(message.signature, writer.uint32(18).fork()).ldelim();
    }
    if (message.secp256k1Uncompressed !== void 0) {
      PublicKey_Secp256k1Uncompressed.encode(message.secp256k1Uncompressed, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal2.default.Reader ? input : new import_minimal2.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePublicKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.timestamp = reader.uint64();
          break;
        case 2:
          message.signature = Signature.decode(reader, reader.uint32());
          break;
        case 3:
          message.secp256k1Uncompressed = PublicKey_Secp256k1Uncompressed.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      timestamp: isSet2(object.timestamp) ? long_default.fromValue(object.timestamp) : long_default.UZERO,
      signature: isSet2(object.signature) ? Signature.fromJSON(object.signature) : void 0,
      secp256k1Uncompressed: isSet2(object.secp256k1Uncompressed) ? PublicKey_Secp256k1Uncompressed.fromJSON(object.secp256k1Uncompressed) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.timestamp !== void 0 && (obj.timestamp = (message.timestamp || long_default.UZERO).toString());
    message.signature !== void 0 && (obj.signature = message.signature ? Signature.toJSON(message.signature) : void 0);
    message.secp256k1Uncompressed !== void 0 && (obj.secp256k1Uncompressed = message.secp256k1Uncompressed ? PublicKey_Secp256k1Uncompressed.toJSON(message.secp256k1Uncompressed) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBasePublicKey();
    message.timestamp = object.timestamp !== void 0 && object.timestamp !== null ? long_default.fromValue(object.timestamp) : long_default.UZERO;
    message.signature = object.signature !== void 0 && object.signature !== null ? Signature.fromPartial(object.signature) : void 0;
    message.secp256k1Uncompressed = object.secp256k1Uncompressed !== void 0 && object.secp256k1Uncompressed !== null ? PublicKey_Secp256k1Uncompressed.fromPartial(object.secp256k1Uncompressed) : void 0;
    return message;
  }
};
function createBasePublicKey_Secp256k1Uncompressed() {
  return { bytes: new Uint8Array() };
}
var PublicKey_Secp256k1Uncompressed = {
  encode(message, writer = import_minimal2.default.Writer.create()) {
    if (message.bytes.length !== 0) {
      writer.uint32(10).bytes(message.bytes);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal2.default.Reader ? input : new import_minimal2.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePublicKey_Secp256k1Uncompressed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.bytes = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      bytes: isSet2(object.bytes) ? bytesFromBase642(object.bytes) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.bytes !== void 0 && (obj.bytes = base64FromBytes2(message.bytes !== void 0 ? message.bytes : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePublicKey_Secp256k1Uncompressed();
    message.bytes = (_a = object.bytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBasePublicKeyBundle() {
  return { identityKey: void 0, preKey: void 0 };
}
var PublicKeyBundle = {
  encode(message, writer = import_minimal2.default.Writer.create()) {
    if (message.identityKey !== void 0) {
      PublicKey.encode(message.identityKey, writer.uint32(10).fork()).ldelim();
    }
    if (message.preKey !== void 0) {
      PublicKey.encode(message.preKey, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal2.default.Reader ? input : new import_minimal2.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePublicKeyBundle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.identityKey = PublicKey.decode(reader, reader.uint32());
          break;
        case 2:
          message.preKey = PublicKey.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityKey: isSet2(object.identityKey) ? PublicKey.fromJSON(object.identityKey) : void 0,
      preKey: isSet2(object.preKey) ? PublicKey.fromJSON(object.preKey) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.identityKey !== void 0 && (obj.identityKey = message.identityKey ? PublicKey.toJSON(message.identityKey) : void 0);
    message.preKey !== void 0 && (obj.preKey = message.preKey ? PublicKey.toJSON(message.preKey) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBasePublicKeyBundle();
    message.identityKey = object.identityKey !== void 0 && object.identityKey !== null ? PublicKey.fromPartial(object.identityKey) : void 0;
    message.preKey = object.preKey !== void 0 && object.preKey !== null ? PublicKey.fromPartial(object.preKey) : void 0;
    return message;
  }
};
var globalThis2 = (() => {
  if (typeof globalThis2 !== "undefined")
    return globalThis2;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase642(b642) {
  if (globalThis2.Buffer) {
    return Uint8Array.from(globalThis2.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis2.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes2(arr) {
  if (globalThis2.Buffer) {
    return globalThis2.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis2.btoa(bin.join(""));
  }
}
if (import_minimal2.default.util.Long !== long_default) {
  import_minimal2.default.util.Long = long_default;
  import_minimal2.default.configure();
}
function isSet2(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_api/v1/authn.pb.js
var import_minimal3 = __toESM(require_minimal());
var protobufPackage3 = "xmtp.message_api.v1";
function createBaseToken() {
  return {
    identityKey: void 0,
    authDataBytes: new Uint8Array(),
    authDataSignature: void 0
  };
}
var Token = {
  encode(message, writer = import_minimal3.default.Writer.create()) {
    if (message.identityKey !== void 0) {
      PublicKey.encode(message.identityKey, writer.uint32(10).fork()).ldelim();
    }
    if (message.authDataBytes.length !== 0) {
      writer.uint32(18).bytes(message.authDataBytes);
    }
    if (message.authDataSignature !== void 0) {
      Signature.encode(message.authDataSignature, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal3.default.Reader ? input : new import_minimal3.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseToken();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.identityKey = PublicKey.decode(reader, reader.uint32());
          break;
        case 2:
          message.authDataBytes = reader.bytes();
          break;
        case 3:
          message.authDataSignature = Signature.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityKey: isSet3(object.identityKey) ? PublicKey.fromJSON(object.identityKey) : void 0,
      authDataBytes: isSet3(object.authDataBytes) ? bytesFromBase643(object.authDataBytes) : new Uint8Array(),
      authDataSignature: isSet3(object.authDataSignature) ? Signature.fromJSON(object.authDataSignature) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.identityKey !== void 0 && (obj.identityKey = message.identityKey ? PublicKey.toJSON(message.identityKey) : void 0);
    message.authDataBytes !== void 0 && (obj.authDataBytes = base64FromBytes3(message.authDataBytes !== void 0 ? message.authDataBytes : new Uint8Array()));
    message.authDataSignature !== void 0 && (obj.authDataSignature = message.authDataSignature ? Signature.toJSON(message.authDataSignature) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseToken();
    message.identityKey = object.identityKey !== void 0 && object.identityKey !== null ? PublicKey.fromPartial(object.identityKey) : void 0;
    message.authDataBytes = (_a = object.authDataBytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.authDataSignature = object.authDataSignature !== void 0 && object.authDataSignature !== null ? Signature.fromPartial(object.authDataSignature) : void 0;
    return message;
  }
};
function createBaseAuthData() {
  return { walletAddr: "", createdNs: long_default.UZERO };
}
var AuthData = {
  encode(message, writer = import_minimal3.default.Writer.create()) {
    if (message.walletAddr !== "") {
      writer.uint32(10).string(message.walletAddr);
    }
    if (!message.createdNs.isZero()) {
      writer.uint32(16).uint64(message.createdNs);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal3.default.Reader ? input : new import_minimal3.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAuthData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.walletAddr = reader.string();
          break;
        case 2:
          message.createdNs = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      walletAddr: isSet3(object.walletAddr) ? String(object.walletAddr) : "",
      createdNs: isSet3(object.createdNs) ? long_default.fromValue(object.createdNs) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.walletAddr !== void 0 && (obj.walletAddr = message.walletAddr);
    message.createdNs !== void 0 && (obj.createdNs = (message.createdNs || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseAuthData();
    message.walletAddr = (_a = object.walletAddr) !== null && _a !== void 0 ? _a : "";
    message.createdNs = object.createdNs !== void 0 && object.createdNs !== null ? long_default.fromValue(object.createdNs) : long_default.UZERO;
    return message;
  }
};
var globalThis3 = (() => {
  if (typeof globalThis3 !== "undefined")
    return globalThis3;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase643(b642) {
  if (globalThis3.Buffer) {
    return Uint8Array.from(globalThis3.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis3.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes3(arr) {
  if (globalThis3.Buffer) {
    return globalThis3.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis3.btoa(bin.join(""));
  }
}
if (import_minimal3.default.util.Long !== long_default) {
  import_minimal3.default.util.Long = long_default;
  import_minimal3.default.configure();
}
function isSet3(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/message.pb.js
var message_pb_exports = {};
__export(message_pb_exports, {
  DecodedMessage: () => DecodedMessage,
  Message: () => Message,
  MessageHeaderV1: () => MessageHeaderV1,
  MessageHeaderV2: () => MessageHeaderV2,
  MessageV1: () => MessageV1,
  MessageV2: () => MessageV2,
  protobufPackage: () => protobufPackage7
});

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/ciphertext.pb.js
var ciphertext_pb_exports = {};
__export(ciphertext_pb_exports, {
  Ciphertext: () => Ciphertext,
  Ciphertext_Aes256gcmHkdfsha256: () => Ciphertext_Aes256gcmHkdfsha256,
  SignedEciesCiphertext: () => SignedEciesCiphertext,
  SignedEciesCiphertext_Ecies: () => SignedEciesCiphertext_Ecies,
  protobufPackage: () => protobufPackage4
});
var import_minimal4 = __toESM(require_minimal());
var protobufPackage4 = "xmtp.message_contents";
function createBaseCiphertext() {
  return { aes256GcmHkdfSha256: void 0 };
}
var Ciphertext = {
  encode(message, writer = import_minimal4.default.Writer.create()) {
    if (message.aes256GcmHkdfSha256 !== void 0) {
      Ciphertext_Aes256gcmHkdfsha256.encode(message.aes256GcmHkdfSha256, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal4.default.Reader ? input : new import_minimal4.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCiphertext();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.aes256GcmHkdfSha256 = Ciphertext_Aes256gcmHkdfsha256.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      aes256GcmHkdfSha256: isSet4(object.aes256GcmHkdfSha256) ? Ciphertext_Aes256gcmHkdfsha256.fromJSON(object.aes256GcmHkdfSha256) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.aes256GcmHkdfSha256 !== void 0 && (obj.aes256GcmHkdfSha256 = message.aes256GcmHkdfSha256 ? Ciphertext_Aes256gcmHkdfsha256.toJSON(message.aes256GcmHkdfSha256) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseCiphertext();
    message.aes256GcmHkdfSha256 = object.aes256GcmHkdfSha256 !== void 0 && object.aes256GcmHkdfSha256 !== null ? Ciphertext_Aes256gcmHkdfsha256.fromPartial(object.aes256GcmHkdfSha256) : void 0;
    return message;
  }
};
function createBaseCiphertext_Aes256gcmHkdfsha256() {
  return {
    hkdfSalt: new Uint8Array(),
    gcmNonce: new Uint8Array(),
    payload: new Uint8Array()
  };
}
var Ciphertext_Aes256gcmHkdfsha256 = {
  encode(message, writer = import_minimal4.default.Writer.create()) {
    if (message.hkdfSalt.length !== 0) {
      writer.uint32(10).bytes(message.hkdfSalt);
    }
    if (message.gcmNonce.length !== 0) {
      writer.uint32(18).bytes(message.gcmNonce);
    }
    if (message.payload.length !== 0) {
      writer.uint32(26).bytes(message.payload);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal4.default.Reader ? input : new import_minimal4.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCiphertext_Aes256gcmHkdfsha256();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.hkdfSalt = reader.bytes();
          break;
        case 2:
          message.gcmNonce = reader.bytes();
          break;
        case 3:
          message.payload = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      hkdfSalt: isSet4(object.hkdfSalt) ? bytesFromBase644(object.hkdfSalt) : new Uint8Array(),
      gcmNonce: isSet4(object.gcmNonce) ? bytesFromBase644(object.gcmNonce) : new Uint8Array(),
      payload: isSet4(object.payload) ? bytesFromBase644(object.payload) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.hkdfSalt !== void 0 && (obj.hkdfSalt = base64FromBytes4(message.hkdfSalt !== void 0 ? message.hkdfSalt : new Uint8Array()));
    message.gcmNonce !== void 0 && (obj.gcmNonce = base64FromBytes4(message.gcmNonce !== void 0 ? message.gcmNonce : new Uint8Array()));
    message.payload !== void 0 && (obj.payload = base64FromBytes4(message.payload !== void 0 ? message.payload : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseCiphertext_Aes256gcmHkdfsha256();
    message.hkdfSalt = (_a = object.hkdfSalt) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.gcmNonce = (_b = object.gcmNonce) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.payload = (_c = object.payload) !== null && _c !== void 0 ? _c : new Uint8Array();
    return message;
  }
};
function createBaseSignedEciesCiphertext() {
  return { eciesBytes: new Uint8Array(), signature: void 0 };
}
var SignedEciesCiphertext = {
  encode(message, writer = import_minimal4.default.Writer.create()) {
    if (message.eciesBytes.length !== 0) {
      writer.uint32(10).bytes(message.eciesBytes);
    }
    if (message.signature !== void 0) {
      Signature.encode(message.signature, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal4.default.Reader ? input : new import_minimal4.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignedEciesCiphertext();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.eciesBytes = reader.bytes();
          break;
        case 2:
          message.signature = Signature.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      eciesBytes: isSet4(object.eciesBytes) ? bytesFromBase644(object.eciesBytes) : new Uint8Array(),
      signature: isSet4(object.signature) ? Signature.fromJSON(object.signature) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.eciesBytes !== void 0 && (obj.eciesBytes = base64FromBytes4(message.eciesBytes !== void 0 ? message.eciesBytes : new Uint8Array()));
    message.signature !== void 0 && (obj.signature = message.signature ? Signature.toJSON(message.signature) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSignedEciesCiphertext();
    message.eciesBytes = (_a = object.eciesBytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.signature = object.signature !== void 0 && object.signature !== null ? Signature.fromPartial(object.signature) : void 0;
    return message;
  }
};
function createBaseSignedEciesCiphertext_Ecies() {
  return {
    ephemeralPublicKey: new Uint8Array(),
    iv: new Uint8Array(),
    mac: new Uint8Array(),
    ciphertext: new Uint8Array()
  };
}
var SignedEciesCiphertext_Ecies = {
  encode(message, writer = import_minimal4.default.Writer.create()) {
    if (message.ephemeralPublicKey.length !== 0) {
      writer.uint32(10).bytes(message.ephemeralPublicKey);
    }
    if (message.iv.length !== 0) {
      writer.uint32(18).bytes(message.iv);
    }
    if (message.mac.length !== 0) {
      writer.uint32(26).bytes(message.mac);
    }
    if (message.ciphertext.length !== 0) {
      writer.uint32(34).bytes(message.ciphertext);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal4.default.Reader ? input : new import_minimal4.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignedEciesCiphertext_Ecies();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.ephemeralPublicKey = reader.bytes();
          break;
        case 2:
          message.iv = reader.bytes();
          break;
        case 3:
          message.mac = reader.bytes();
          break;
        case 4:
          message.ciphertext = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      ephemeralPublicKey: isSet4(object.ephemeralPublicKey) ? bytesFromBase644(object.ephemeralPublicKey) : new Uint8Array(),
      iv: isSet4(object.iv) ? bytesFromBase644(object.iv) : new Uint8Array(),
      mac: isSet4(object.mac) ? bytesFromBase644(object.mac) : new Uint8Array(),
      ciphertext: isSet4(object.ciphertext) ? bytesFromBase644(object.ciphertext) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.ephemeralPublicKey !== void 0 && (obj.ephemeralPublicKey = base64FromBytes4(message.ephemeralPublicKey !== void 0 ? message.ephemeralPublicKey : new Uint8Array()));
    message.iv !== void 0 && (obj.iv = base64FromBytes4(message.iv !== void 0 ? message.iv : new Uint8Array()));
    message.mac !== void 0 && (obj.mac = base64FromBytes4(message.mac !== void 0 ? message.mac : new Uint8Array()));
    message.ciphertext !== void 0 && (obj.ciphertext = base64FromBytes4(message.ciphertext !== void 0 ? message.ciphertext : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d;
    const message = createBaseSignedEciesCiphertext_Ecies();
    message.ephemeralPublicKey = (_a = object.ephemeralPublicKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.iv = (_b = object.iv) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.mac = (_c = object.mac) !== null && _c !== void 0 ? _c : new Uint8Array();
    message.ciphertext = (_d = object.ciphertext) !== null && _d !== void 0 ? _d : new Uint8Array();
    return message;
  }
};
var globalThis4 = (() => {
  if (typeof globalThis4 !== "undefined")
    return globalThis4;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase644(b642) {
  if (globalThis4.Buffer) {
    return Uint8Array.from(globalThis4.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis4.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes4(arr) {
  if (globalThis4.Buffer) {
    return globalThis4.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis4.btoa(bin.join(""));
  }
}
if (import_minimal4.default.util.Long !== long_default) {
  import_minimal4.default.util.Long = long_default;
  import_minimal4.default.configure();
}
function isSet4(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/conversation_reference.pb.js
var conversation_reference_pb_exports = {};
__export(conversation_reference_pb_exports, {
  ConversationReference: () => ConversationReference,
  protobufPackage: () => protobufPackage6
});

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/invitation.pb.js
var invitation_pb_exports = {};
__export(invitation_pb_exports, {
  ConsentProofPayload: () => ConsentProofPayload,
  ConsentProofPayloadVersion: () => ConsentProofPayloadVersion,
  InvitationV1: () => InvitationV1,
  InvitationV1_Aes256gcmHkdfsha256: () => InvitationV1_Aes256gcmHkdfsha256,
  InvitationV1_Context: () => InvitationV1_Context,
  InvitationV1_Context_MetadataEntry: () => InvitationV1_Context_MetadataEntry,
  SealedInvitation: () => SealedInvitation,
  SealedInvitationHeaderV1: () => SealedInvitationHeaderV1,
  SealedInvitationV1: () => SealedInvitationV1,
  consentProofPayloadVersionFromJSON: () => consentProofPayloadVersionFromJSON,
  consentProofPayloadVersionToJSON: () => consentProofPayloadVersionToJSON,
  protobufPackage: () => protobufPackage5
});
var import_minimal5 = __toESM(require_minimal());
var protobufPackage5 = "xmtp.message_contents";
var ConsentProofPayloadVersion;
(function(ConsentProofPayloadVersion2) {
  ConsentProofPayloadVersion2[ConsentProofPayloadVersion2["CONSENT_PROOF_PAYLOAD_VERSION_UNSPECIFIED"] = 0] = "CONSENT_PROOF_PAYLOAD_VERSION_UNSPECIFIED";
  ConsentProofPayloadVersion2[ConsentProofPayloadVersion2["CONSENT_PROOF_PAYLOAD_VERSION_1"] = 1] = "CONSENT_PROOF_PAYLOAD_VERSION_1";
  ConsentProofPayloadVersion2[ConsentProofPayloadVersion2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(ConsentProofPayloadVersion || (ConsentProofPayloadVersion = {}));
function consentProofPayloadVersionFromJSON(object) {
  switch (object) {
    case 0:
    case "CONSENT_PROOF_PAYLOAD_VERSION_UNSPECIFIED":
      return ConsentProofPayloadVersion.CONSENT_PROOF_PAYLOAD_VERSION_UNSPECIFIED;
    case 1:
    case "CONSENT_PROOF_PAYLOAD_VERSION_1":
      return ConsentProofPayloadVersion.CONSENT_PROOF_PAYLOAD_VERSION_1;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ConsentProofPayloadVersion.UNRECOGNIZED;
  }
}
function consentProofPayloadVersionToJSON(object) {
  switch (object) {
    case ConsentProofPayloadVersion.CONSENT_PROOF_PAYLOAD_VERSION_UNSPECIFIED:
      return "CONSENT_PROOF_PAYLOAD_VERSION_UNSPECIFIED";
    case ConsentProofPayloadVersion.CONSENT_PROOF_PAYLOAD_VERSION_1:
      return "CONSENT_PROOF_PAYLOAD_VERSION_1";
    case ConsentProofPayloadVersion.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseInvitationV1() {
  return {
    topic: "",
    context: void 0,
    aes256GcmHkdfSha256: void 0,
    consentProof: void 0
  };
}
var InvitationV1 = {
  encode(message, writer = import_minimal5.default.Writer.create()) {
    if (message.topic !== "") {
      writer.uint32(10).string(message.topic);
    }
    if (message.context !== void 0) {
      InvitationV1_Context.encode(message.context, writer.uint32(18).fork()).ldelim();
    }
    if (message.aes256GcmHkdfSha256 !== void 0) {
      InvitationV1_Aes256gcmHkdfsha256.encode(message.aes256GcmHkdfSha256, writer.uint32(26).fork()).ldelim();
    }
    if (message.consentProof !== void 0) {
      ConsentProofPayload.encode(message.consentProof, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal5.default.Reader ? input : new import_minimal5.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInvitationV1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.topic = reader.string();
          break;
        case 2:
          message.context = InvitationV1_Context.decode(reader, reader.uint32());
          break;
        case 3:
          message.aes256GcmHkdfSha256 = InvitationV1_Aes256gcmHkdfsha256.decode(reader, reader.uint32());
          break;
        case 4:
          message.consentProof = ConsentProofPayload.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      topic: isSet5(object.topic) ? String(object.topic) : "",
      context: isSet5(object.context) ? InvitationV1_Context.fromJSON(object.context) : void 0,
      aes256GcmHkdfSha256: isSet5(object.aes256GcmHkdfSha256) ? InvitationV1_Aes256gcmHkdfsha256.fromJSON(object.aes256GcmHkdfSha256) : void 0,
      consentProof: isSet5(object.consentProof) ? ConsentProofPayload.fromJSON(object.consentProof) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.topic !== void 0 && (obj.topic = message.topic);
    message.context !== void 0 && (obj.context = message.context ? InvitationV1_Context.toJSON(message.context) : void 0);
    message.aes256GcmHkdfSha256 !== void 0 && (obj.aes256GcmHkdfSha256 = message.aes256GcmHkdfSha256 ? InvitationV1_Aes256gcmHkdfsha256.toJSON(message.aes256GcmHkdfSha256) : void 0);
    message.consentProof !== void 0 && (obj.consentProof = message.consentProof ? ConsentProofPayload.toJSON(message.consentProof) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseInvitationV1();
    message.topic = (_a = object.topic) !== null && _a !== void 0 ? _a : "";
    message.context = object.context !== void 0 && object.context !== null ? InvitationV1_Context.fromPartial(object.context) : void 0;
    message.aes256GcmHkdfSha256 = object.aes256GcmHkdfSha256 !== void 0 && object.aes256GcmHkdfSha256 !== null ? InvitationV1_Aes256gcmHkdfsha256.fromPartial(object.aes256GcmHkdfSha256) : void 0;
    message.consentProof = object.consentProof !== void 0 && object.consentProof !== null ? ConsentProofPayload.fromPartial(object.consentProof) : void 0;
    return message;
  }
};
function createBaseInvitationV1_Aes256gcmHkdfsha256() {
  return { keyMaterial: new Uint8Array() };
}
var InvitationV1_Aes256gcmHkdfsha256 = {
  encode(message, writer = import_minimal5.default.Writer.create()) {
    if (message.keyMaterial.length !== 0) {
      writer.uint32(10).bytes(message.keyMaterial);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal5.default.Reader ? input : new import_minimal5.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInvitationV1_Aes256gcmHkdfsha256();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyMaterial = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyMaterial: isSet5(object.keyMaterial) ? bytesFromBase645(object.keyMaterial) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.keyMaterial !== void 0 && (obj.keyMaterial = base64FromBytes5(message.keyMaterial !== void 0 ? message.keyMaterial : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseInvitationV1_Aes256gcmHkdfsha256();
    message.keyMaterial = (_a = object.keyMaterial) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseInvitationV1_Context() {
  return { conversationId: "", metadata: {} };
}
var InvitationV1_Context = {
  encode(message, writer = import_minimal5.default.Writer.create()) {
    if (message.conversationId !== "") {
      writer.uint32(10).string(message.conversationId);
    }
    Object.entries(message.metadata).forEach(([key, value]) => {
      InvitationV1_Context_MetadataEntry.encode({ key, value }, writer.uint32(18).fork()).ldelim();
    });
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal5.default.Reader ? input : new import_minimal5.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInvitationV1_Context();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.conversationId = reader.string();
          break;
        case 2:
          const entry2 = InvitationV1_Context_MetadataEntry.decode(reader, reader.uint32());
          if (entry2.value !== void 0) {
            message.metadata[entry2.key] = entry2.value;
          }
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      conversationId: isSet5(object.conversationId) ? String(object.conversationId) : "",
      metadata: isObject(object.metadata) ? Object.entries(object.metadata).reduce((acc, [key, value]) => {
        acc[key] = String(value);
        return acc;
      }, {}) : {}
    };
  },
  toJSON(message) {
    const obj = {};
    message.conversationId !== void 0 && (obj.conversationId = message.conversationId);
    obj.metadata = {};
    if (message.metadata) {
      Object.entries(message.metadata).forEach(([k, v]) => {
        obj.metadata[k] = v;
      });
    }
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseInvitationV1_Context();
    message.conversationId = (_a = object.conversationId) !== null && _a !== void 0 ? _a : "";
    message.metadata = Object.entries((_b = object.metadata) !== null && _b !== void 0 ? _b : {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = String(value);
      }
      return acc;
    }, {});
    return message;
  }
};
function createBaseInvitationV1_Context_MetadataEntry() {
  return { key: "", value: "" };
}
var InvitationV1_Context_MetadataEntry = {
  encode(message, writer = import_minimal5.default.Writer.create()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal5.default.Reader ? input : new import_minimal5.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInvitationV1_Context_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.key = reader.string();
          break;
        case 2:
          message.value = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet5(object.key) ? String(object.key) : "",
      value: isSet5(object.value) ? String(object.value) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.key !== void 0 && (obj.key = message.key);
    message.value !== void 0 && (obj.value = message.value);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseInvitationV1_Context_MetadataEntry();
    message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
    message.value = (_b = object.value) !== null && _b !== void 0 ? _b : "";
    return message;
  }
};
function createBaseSealedInvitationHeaderV1() {
  return { sender: void 0, recipient: void 0, createdNs: long_default.UZERO };
}
var SealedInvitationHeaderV1 = {
  encode(message, writer = import_minimal5.default.Writer.create()) {
    if (message.sender !== void 0) {
      SignedPublicKeyBundle.encode(message.sender, writer.uint32(10).fork()).ldelim();
    }
    if (message.recipient !== void 0) {
      SignedPublicKeyBundle.encode(message.recipient, writer.uint32(18).fork()).ldelim();
    }
    if (!message.createdNs.isZero()) {
      writer.uint32(24).uint64(message.createdNs);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal5.default.Reader ? input : new import_minimal5.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSealedInvitationHeaderV1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.sender = SignedPublicKeyBundle.decode(reader, reader.uint32());
          break;
        case 2:
          message.recipient = SignedPublicKeyBundle.decode(reader, reader.uint32());
          break;
        case 3:
          message.createdNs = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      sender: isSet5(object.sender) ? SignedPublicKeyBundle.fromJSON(object.sender) : void 0,
      recipient: isSet5(object.recipient) ? SignedPublicKeyBundle.fromJSON(object.recipient) : void 0,
      createdNs: isSet5(object.createdNs) ? long_default.fromValue(object.createdNs) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.sender !== void 0 && (obj.sender = message.sender ? SignedPublicKeyBundle.toJSON(message.sender) : void 0);
    message.recipient !== void 0 && (obj.recipient = message.recipient ? SignedPublicKeyBundle.toJSON(message.recipient) : void 0);
    message.createdNs !== void 0 && (obj.createdNs = (message.createdNs || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    const message = createBaseSealedInvitationHeaderV1();
    message.sender = object.sender !== void 0 && object.sender !== null ? SignedPublicKeyBundle.fromPartial(object.sender) : void 0;
    message.recipient = object.recipient !== void 0 && object.recipient !== null ? SignedPublicKeyBundle.fromPartial(object.recipient) : void 0;
    message.createdNs = object.createdNs !== void 0 && object.createdNs !== null ? long_default.fromValue(object.createdNs) : long_default.UZERO;
    return message;
  }
};
function createBaseSealedInvitationV1() {
  return { headerBytes: new Uint8Array(), ciphertext: void 0 };
}
var SealedInvitationV1 = {
  encode(message, writer = import_minimal5.default.Writer.create()) {
    if (message.headerBytes.length !== 0) {
      writer.uint32(10).bytes(message.headerBytes);
    }
    if (message.ciphertext !== void 0) {
      Ciphertext.encode(message.ciphertext, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal5.default.Reader ? input : new import_minimal5.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSealedInvitationV1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.headerBytes = reader.bytes();
          break;
        case 2:
          message.ciphertext = Ciphertext.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      headerBytes: isSet5(object.headerBytes) ? bytesFromBase645(object.headerBytes) : new Uint8Array(),
      ciphertext: isSet5(object.ciphertext) ? Ciphertext.fromJSON(object.ciphertext) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.headerBytes !== void 0 && (obj.headerBytes = base64FromBytes5(message.headerBytes !== void 0 ? message.headerBytes : new Uint8Array()));
    message.ciphertext !== void 0 && (obj.ciphertext = message.ciphertext ? Ciphertext.toJSON(message.ciphertext) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSealedInvitationV1();
    message.headerBytes = (_a = object.headerBytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.ciphertext = object.ciphertext !== void 0 && object.ciphertext !== null ? Ciphertext.fromPartial(object.ciphertext) : void 0;
    return message;
  }
};
function createBaseSealedInvitation() {
  return { v1: void 0 };
}
var SealedInvitation = {
  encode(message, writer = import_minimal5.default.Writer.create()) {
    if (message.v1 !== void 0) {
      SealedInvitationV1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal5.default.Reader ? input : new import_minimal5.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSealedInvitation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = SealedInvitationV1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet5(object.v1) ? SealedInvitationV1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? SealedInvitationV1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseSealedInvitation();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? SealedInvitationV1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseConsentProofPayload() {
  return { signature: "", timestamp: long_default.UZERO, payloadVersion: 0 };
}
var ConsentProofPayload = {
  encode(message, writer = import_minimal5.default.Writer.create()) {
    if (message.signature !== "") {
      writer.uint32(10).string(message.signature);
    }
    if (!message.timestamp.isZero()) {
      writer.uint32(16).uint64(message.timestamp);
    }
    if (message.payloadVersion !== 0) {
      writer.uint32(24).int32(message.payloadVersion);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal5.default.Reader ? input : new import_minimal5.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseConsentProofPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.signature = reader.string();
          break;
        case 2:
          message.timestamp = reader.uint64();
          break;
        case 3:
          message.payloadVersion = reader.int32();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      signature: isSet5(object.signature) ? String(object.signature) : "",
      timestamp: isSet5(object.timestamp) ? long_default.fromValue(object.timestamp) : long_default.UZERO,
      payloadVersion: isSet5(object.payloadVersion) ? consentProofPayloadVersionFromJSON(object.payloadVersion) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.signature !== void 0 && (obj.signature = message.signature);
    message.timestamp !== void 0 && (obj.timestamp = (message.timestamp || long_default.UZERO).toString());
    message.payloadVersion !== void 0 && (obj.payloadVersion = consentProofPayloadVersionToJSON(message.payloadVersion));
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseConsentProofPayload();
    message.signature = (_a = object.signature) !== null && _a !== void 0 ? _a : "";
    message.timestamp = object.timestamp !== void 0 && object.timestamp !== null ? long_default.fromValue(object.timestamp) : long_default.UZERO;
    message.payloadVersion = (_b = object.payloadVersion) !== null && _b !== void 0 ? _b : 0;
    return message;
  }
};
var globalThis5 = (() => {
  if (typeof globalThis5 !== "undefined")
    return globalThis5;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase645(b642) {
  if (globalThis5.Buffer) {
    return Uint8Array.from(globalThis5.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis5.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes5(arr) {
  if (globalThis5.Buffer) {
    return globalThis5.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis5.btoa(bin.join(""));
  }
}
if (import_minimal5.default.util.Long !== long_default) {
  import_minimal5.default.util.Long = long_default;
  import_minimal5.default.configure();
}
function isObject(value) {
  return typeof value === "object" && value !== null;
}
function isSet5(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/conversation_reference.pb.js
var import_minimal6 = __toESM(require_minimal());
var protobufPackage6 = "xmtp.message_contents";
function createBaseConversationReference() {
  return {
    topic: "",
    peerAddress: "",
    createdNs: long_default.UZERO,
    context: void 0,
    consentProofPayload: void 0
  };
}
var ConversationReference = {
  encode(message, writer = import_minimal6.default.Writer.create()) {
    if (message.topic !== "") {
      writer.uint32(10).string(message.topic);
    }
    if (message.peerAddress !== "") {
      writer.uint32(18).string(message.peerAddress);
    }
    if (!message.createdNs.isZero()) {
      writer.uint32(24).uint64(message.createdNs);
    }
    if (message.context !== void 0) {
      InvitationV1_Context.encode(message.context, writer.uint32(34).fork()).ldelim();
    }
    if (message.consentProofPayload !== void 0) {
      ConsentProofPayload.encode(message.consentProofPayload, writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal6.default.Reader ? input : new import_minimal6.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseConversationReference();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.topic = reader.string();
          break;
        case 2:
          message.peerAddress = reader.string();
          break;
        case 3:
          message.createdNs = reader.uint64();
          break;
        case 4:
          message.context = InvitationV1_Context.decode(reader, reader.uint32());
          break;
        case 5:
          message.consentProofPayload = ConsentProofPayload.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      topic: isSet6(object.topic) ? String(object.topic) : "",
      peerAddress: isSet6(object.peerAddress) ? String(object.peerAddress) : "",
      createdNs: isSet6(object.createdNs) ? long_default.fromValue(object.createdNs) : long_default.UZERO,
      context: isSet6(object.context) ? InvitationV1_Context.fromJSON(object.context) : void 0,
      consentProofPayload: isSet6(object.consentProofPayload) ? ConsentProofPayload.fromJSON(object.consentProofPayload) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.topic !== void 0 && (obj.topic = message.topic);
    message.peerAddress !== void 0 && (obj.peerAddress = message.peerAddress);
    message.createdNs !== void 0 && (obj.createdNs = (message.createdNs || long_default.UZERO).toString());
    message.context !== void 0 && (obj.context = message.context ? InvitationV1_Context.toJSON(message.context) : void 0);
    message.consentProofPayload !== void 0 && (obj.consentProofPayload = message.consentProofPayload ? ConsentProofPayload.toJSON(message.consentProofPayload) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseConversationReference();
    message.topic = (_a = object.topic) !== null && _a !== void 0 ? _a : "";
    message.peerAddress = (_b = object.peerAddress) !== null && _b !== void 0 ? _b : "";
    message.createdNs = object.createdNs !== void 0 && object.createdNs !== null ? long_default.fromValue(object.createdNs) : long_default.UZERO;
    message.context = object.context !== void 0 && object.context !== null ? InvitationV1_Context.fromPartial(object.context) : void 0;
    message.consentProofPayload = object.consentProofPayload !== void 0 && object.consentProofPayload !== null ? ConsentProofPayload.fromPartial(object.consentProofPayload) : void 0;
    return message;
  }
};
if (import_minimal6.default.util.Long !== long_default) {
  import_minimal6.default.util.Long = long_default;
  import_minimal6.default.configure();
}
function isSet6(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/message.pb.js
var import_minimal7 = __toESM(require_minimal());
var protobufPackage7 = "xmtp.message_contents";
function createBaseMessageHeaderV1() {
  return { sender: void 0, recipient: void 0, timestamp: long_default.UZERO };
}
var MessageHeaderV1 = {
  encode(message, writer = import_minimal7.default.Writer.create()) {
    if (message.sender !== void 0) {
      PublicKeyBundle.encode(message.sender, writer.uint32(10).fork()).ldelim();
    }
    if (message.recipient !== void 0) {
      PublicKeyBundle.encode(message.recipient, writer.uint32(18).fork()).ldelim();
    }
    if (!message.timestamp.isZero()) {
      writer.uint32(24).uint64(message.timestamp);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal7.default.Reader ? input : new import_minimal7.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageHeaderV1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.sender = PublicKeyBundle.decode(reader, reader.uint32());
          break;
        case 2:
          message.recipient = PublicKeyBundle.decode(reader, reader.uint32());
          break;
        case 3:
          message.timestamp = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      sender: isSet7(object.sender) ? PublicKeyBundle.fromJSON(object.sender) : void 0,
      recipient: isSet7(object.recipient) ? PublicKeyBundle.fromJSON(object.recipient) : void 0,
      timestamp: isSet7(object.timestamp) ? long_default.fromValue(object.timestamp) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.sender !== void 0 && (obj.sender = message.sender ? PublicKeyBundle.toJSON(message.sender) : void 0);
    message.recipient !== void 0 && (obj.recipient = message.recipient ? PublicKeyBundle.toJSON(message.recipient) : void 0);
    message.timestamp !== void 0 && (obj.timestamp = (message.timestamp || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    const message = createBaseMessageHeaderV1();
    message.sender = object.sender !== void 0 && object.sender !== null ? PublicKeyBundle.fromPartial(object.sender) : void 0;
    message.recipient = object.recipient !== void 0 && object.recipient !== null ? PublicKeyBundle.fromPartial(object.recipient) : void 0;
    message.timestamp = object.timestamp !== void 0 && object.timestamp !== null ? long_default.fromValue(object.timestamp) : long_default.UZERO;
    return message;
  }
};
function createBaseMessageV1() {
  return { headerBytes: new Uint8Array(), ciphertext: void 0 };
}
var MessageV1 = {
  encode(message, writer = import_minimal7.default.Writer.create()) {
    if (message.headerBytes.length !== 0) {
      writer.uint32(10).bytes(message.headerBytes);
    }
    if (message.ciphertext !== void 0) {
      Ciphertext.encode(message.ciphertext, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal7.default.Reader ? input : new import_minimal7.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageV1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.headerBytes = reader.bytes();
          break;
        case 2:
          message.ciphertext = Ciphertext.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      headerBytes: isSet7(object.headerBytes) ? bytesFromBase646(object.headerBytes) : new Uint8Array(),
      ciphertext: isSet7(object.ciphertext) ? Ciphertext.fromJSON(object.ciphertext) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.headerBytes !== void 0 && (obj.headerBytes = base64FromBytes6(message.headerBytes !== void 0 ? message.headerBytes : new Uint8Array()));
    message.ciphertext !== void 0 && (obj.ciphertext = message.ciphertext ? Ciphertext.toJSON(message.ciphertext) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseMessageV1();
    message.headerBytes = (_a = object.headerBytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.ciphertext = object.ciphertext !== void 0 && object.ciphertext !== null ? Ciphertext.fromPartial(object.ciphertext) : void 0;
    return message;
  }
};
function createBaseMessageHeaderV2() {
  return { createdNs: long_default.UZERO, topic: "" };
}
var MessageHeaderV2 = {
  encode(message, writer = import_minimal7.default.Writer.create()) {
    if (!message.createdNs.isZero()) {
      writer.uint32(8).uint64(message.createdNs);
    }
    if (message.topic !== "") {
      writer.uint32(18).string(message.topic);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal7.default.Reader ? input : new import_minimal7.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageHeaderV2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.createdNs = reader.uint64();
          break;
        case 2:
          message.topic = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      createdNs: isSet7(object.createdNs) ? long_default.fromValue(object.createdNs) : long_default.UZERO,
      topic: isSet7(object.topic) ? String(object.topic) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.createdNs !== void 0 && (obj.createdNs = (message.createdNs || long_default.UZERO).toString());
    message.topic !== void 0 && (obj.topic = message.topic);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseMessageHeaderV2();
    message.createdNs = object.createdNs !== void 0 && object.createdNs !== null ? long_default.fromValue(object.createdNs) : long_default.UZERO;
    message.topic = (_a = object.topic) !== null && _a !== void 0 ? _a : "";
    return message;
  }
};
function createBaseMessageV2() {
  return {
    headerBytes: new Uint8Array(),
    ciphertext: void 0,
    senderHmac: void 0,
    shouldPush: void 0
  };
}
var MessageV2 = {
  encode(message, writer = import_minimal7.default.Writer.create()) {
    if (message.headerBytes.length !== 0) {
      writer.uint32(10).bytes(message.headerBytes);
    }
    if (message.ciphertext !== void 0) {
      Ciphertext.encode(message.ciphertext, writer.uint32(18).fork()).ldelim();
    }
    if (message.senderHmac !== void 0) {
      writer.uint32(26).bytes(message.senderHmac);
    }
    if (message.shouldPush !== void 0) {
      writer.uint32(32).bool(message.shouldPush);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal7.default.Reader ? input : new import_minimal7.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageV2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.headerBytes = reader.bytes();
          break;
        case 2:
          message.ciphertext = Ciphertext.decode(reader, reader.uint32());
          break;
        case 3:
          message.senderHmac = reader.bytes();
          break;
        case 4:
          message.shouldPush = reader.bool();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      headerBytes: isSet7(object.headerBytes) ? bytesFromBase646(object.headerBytes) : new Uint8Array(),
      ciphertext: isSet7(object.ciphertext) ? Ciphertext.fromJSON(object.ciphertext) : void 0,
      senderHmac: isSet7(object.senderHmac) ? bytesFromBase646(object.senderHmac) : void 0,
      shouldPush: isSet7(object.shouldPush) ? Boolean(object.shouldPush) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.headerBytes !== void 0 && (obj.headerBytes = base64FromBytes6(message.headerBytes !== void 0 ? message.headerBytes : new Uint8Array()));
    message.ciphertext !== void 0 && (obj.ciphertext = message.ciphertext ? Ciphertext.toJSON(message.ciphertext) : void 0);
    message.senderHmac !== void 0 && (obj.senderHmac = message.senderHmac !== void 0 ? base64FromBytes6(message.senderHmac) : void 0);
    message.shouldPush !== void 0 && (obj.shouldPush = message.shouldPush);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseMessageV2();
    message.headerBytes = (_a = object.headerBytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.ciphertext = object.ciphertext !== void 0 && object.ciphertext !== null ? Ciphertext.fromPartial(object.ciphertext) : void 0;
    message.senderHmac = (_b = object.senderHmac) !== null && _b !== void 0 ? _b : void 0;
    message.shouldPush = (_c = object.shouldPush) !== null && _c !== void 0 ? _c : void 0;
    return message;
  }
};
function createBaseMessage() {
  return { v1: void 0, v2: void 0 };
}
var Message = {
  encode(message, writer = import_minimal7.default.Writer.create()) {
    if (message.v1 !== void 0) {
      MessageV1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    if (message.v2 !== void 0) {
      MessageV2.encode(message.v2, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal7.default.Reader ? input : new import_minimal7.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = MessageV1.decode(reader, reader.uint32());
          break;
        case 2:
          message.v2 = MessageV2.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet7(object.v1) ? MessageV1.fromJSON(object.v1) : void 0,
      v2: isSet7(object.v2) ? MessageV2.fromJSON(object.v2) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? MessageV1.toJSON(message.v1) : void 0);
    message.v2 !== void 0 && (obj.v2 = message.v2 ? MessageV2.toJSON(message.v2) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseMessage();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? MessageV1.fromPartial(object.v1) : void 0;
    message.v2 = object.v2 !== void 0 && object.v2 !== null ? MessageV2.fromPartial(object.v2) : void 0;
    return message;
  }
};
function createBaseDecodedMessage() {
  return {
    id: "",
    messageVersion: "",
    senderAddress: "",
    recipientAddress: void 0,
    sentNs: long_default.UZERO,
    contentTopic: "",
    conversation: void 0,
    contentBytes: new Uint8Array()
  };
}
var DecodedMessage = {
  encode(message, writer = import_minimal7.default.Writer.create()) {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.messageVersion !== "") {
      writer.uint32(18).string(message.messageVersion);
    }
    if (message.senderAddress !== "") {
      writer.uint32(26).string(message.senderAddress);
    }
    if (message.recipientAddress !== void 0) {
      writer.uint32(34).string(message.recipientAddress);
    }
    if (!message.sentNs.isZero()) {
      writer.uint32(40).uint64(message.sentNs);
    }
    if (message.contentTopic !== "") {
      writer.uint32(50).string(message.contentTopic);
    }
    if (message.conversation !== void 0) {
      ConversationReference.encode(message.conversation, writer.uint32(58).fork()).ldelim();
    }
    if (message.contentBytes.length !== 0) {
      writer.uint32(66).bytes(message.contentBytes);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal7.default.Reader ? input : new import_minimal7.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDecodedMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.id = reader.string();
          break;
        case 2:
          message.messageVersion = reader.string();
          break;
        case 3:
          message.senderAddress = reader.string();
          break;
        case 4:
          message.recipientAddress = reader.string();
          break;
        case 5:
          message.sentNs = reader.uint64();
          break;
        case 6:
          message.contentTopic = reader.string();
          break;
        case 7:
          message.conversation = ConversationReference.decode(reader, reader.uint32());
          break;
        case 8:
          message.contentBytes = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      id: isSet7(object.id) ? String(object.id) : "",
      messageVersion: isSet7(object.messageVersion) ? String(object.messageVersion) : "",
      senderAddress: isSet7(object.senderAddress) ? String(object.senderAddress) : "",
      recipientAddress: isSet7(object.recipientAddress) ? String(object.recipientAddress) : void 0,
      sentNs: isSet7(object.sentNs) ? long_default.fromValue(object.sentNs) : long_default.UZERO,
      contentTopic: isSet7(object.contentTopic) ? String(object.contentTopic) : "",
      conversation: isSet7(object.conversation) ? ConversationReference.fromJSON(object.conversation) : void 0,
      contentBytes: isSet7(object.contentBytes) ? bytesFromBase646(object.contentBytes) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.id !== void 0 && (obj.id = message.id);
    message.messageVersion !== void 0 && (obj.messageVersion = message.messageVersion);
    message.senderAddress !== void 0 && (obj.senderAddress = message.senderAddress);
    message.recipientAddress !== void 0 && (obj.recipientAddress = message.recipientAddress);
    message.sentNs !== void 0 && (obj.sentNs = (message.sentNs || long_default.UZERO).toString());
    message.contentTopic !== void 0 && (obj.contentTopic = message.contentTopic);
    message.conversation !== void 0 && (obj.conversation = message.conversation ? ConversationReference.toJSON(message.conversation) : void 0);
    message.contentBytes !== void 0 && (obj.contentBytes = base64FromBytes6(message.contentBytes !== void 0 ? message.contentBytes : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d, _e, _f;
    const message = createBaseDecodedMessage();
    message.id = (_a = object.id) !== null && _a !== void 0 ? _a : "";
    message.messageVersion = (_b = object.messageVersion) !== null && _b !== void 0 ? _b : "";
    message.senderAddress = (_c = object.senderAddress) !== null && _c !== void 0 ? _c : "";
    message.recipientAddress = (_d = object.recipientAddress) !== null && _d !== void 0 ? _d : void 0;
    message.sentNs = object.sentNs !== void 0 && object.sentNs !== null ? long_default.fromValue(object.sentNs) : long_default.UZERO;
    message.contentTopic = (_e = object.contentTopic) !== null && _e !== void 0 ? _e : "";
    message.conversation = object.conversation !== void 0 && object.conversation !== null ? ConversationReference.fromPartial(object.conversation) : void 0;
    message.contentBytes = (_f = object.contentBytes) !== null && _f !== void 0 ? _f : new Uint8Array();
    return message;
  }
};
var globalThis6 = (() => {
  if (typeof globalThis6 !== "undefined")
    return globalThis6;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase646(b642) {
  if (globalThis6.Buffer) {
    return Uint8Array.from(globalThis6.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis6.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes6(arr) {
  if (globalThis6.Buffer) {
    return globalThis6.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis6.btoa(bin.join(""));
  }
}
if (import_minimal7.default.util.Long !== long_default) {
  import_minimal7.default.util.Long = long_default;
  import_minimal7.default.configure();
}
function isSet7(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/content.pb.js
var content_pb_exports = {};
__export(content_pb_exports, {
  Compression: () => Compression,
  ContentTypeId: () => ContentTypeId,
  EncodedContent: () => EncodedContent,
  EncodedContent_ParametersEntry: () => EncodedContent_ParametersEntry,
  SignedContent: () => SignedContent,
  compressionFromJSON: () => compressionFromJSON,
  compressionToJSON: () => compressionToJSON,
  protobufPackage: () => protobufPackage8
});
var import_minimal8 = __toESM(require_minimal());
var protobufPackage8 = "xmtp.message_contents";
var Compression;
(function(Compression3) {
  Compression3[Compression3["COMPRESSION_DEFLATE"] = 0] = "COMPRESSION_DEFLATE";
  Compression3[Compression3["COMPRESSION_GZIP"] = 1] = "COMPRESSION_GZIP";
  Compression3[Compression3["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(Compression || (Compression = {}));
function compressionFromJSON(object) {
  switch (object) {
    case 0:
    case "COMPRESSION_DEFLATE":
      return Compression.COMPRESSION_DEFLATE;
    case 1:
    case "COMPRESSION_GZIP":
      return Compression.COMPRESSION_GZIP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Compression.UNRECOGNIZED;
  }
}
function compressionToJSON(object) {
  switch (object) {
    case Compression.COMPRESSION_DEFLATE:
      return "COMPRESSION_DEFLATE";
    case Compression.COMPRESSION_GZIP:
      return "COMPRESSION_GZIP";
    case Compression.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseContentTypeId() {
  return { authorityId: "", typeId: "", versionMajor: 0, versionMinor: 0 };
}
var ContentTypeId = {
  encode(message, writer = import_minimal8.default.Writer.create()) {
    if (message.authorityId !== "") {
      writer.uint32(10).string(message.authorityId);
    }
    if (message.typeId !== "") {
      writer.uint32(18).string(message.typeId);
    }
    if (message.versionMajor !== 0) {
      writer.uint32(24).uint32(message.versionMajor);
    }
    if (message.versionMinor !== 0) {
      writer.uint32(32).uint32(message.versionMinor);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal8.default.Reader ? input : new import_minimal8.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseContentTypeId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.authorityId = reader.string();
          break;
        case 2:
          message.typeId = reader.string();
          break;
        case 3:
          message.versionMajor = reader.uint32();
          break;
        case 4:
          message.versionMinor = reader.uint32();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      authorityId: isSet8(object.authorityId) ? String(object.authorityId) : "",
      typeId: isSet8(object.typeId) ? String(object.typeId) : "",
      versionMajor: isSet8(object.versionMajor) ? Number(object.versionMajor) : 0,
      versionMinor: isSet8(object.versionMinor) ? Number(object.versionMinor) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.authorityId !== void 0 && (obj.authorityId = message.authorityId);
    message.typeId !== void 0 && (obj.typeId = message.typeId);
    message.versionMajor !== void 0 && (obj.versionMajor = Math.round(message.versionMajor));
    message.versionMinor !== void 0 && (obj.versionMinor = Math.round(message.versionMinor));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d;
    const message = createBaseContentTypeId();
    message.authorityId = (_a = object.authorityId) !== null && _a !== void 0 ? _a : "";
    message.typeId = (_b = object.typeId) !== null && _b !== void 0 ? _b : "";
    message.versionMajor = (_c = object.versionMajor) !== null && _c !== void 0 ? _c : 0;
    message.versionMinor = (_d = object.versionMinor) !== null && _d !== void 0 ? _d : 0;
    return message;
  }
};
function createBaseEncodedContent() {
  return {
    type: void 0,
    parameters: {},
    fallback: void 0,
    compression: void 0,
    content: new Uint8Array()
  };
}
var EncodedContent = {
  encode(message, writer = import_minimal8.default.Writer.create()) {
    if (message.type !== void 0) {
      ContentTypeId.encode(message.type, writer.uint32(10).fork()).ldelim();
    }
    Object.entries(message.parameters).forEach(([key, value]) => {
      EncodedContent_ParametersEntry.encode({ key, value }, writer.uint32(18).fork()).ldelim();
    });
    if (message.fallback !== void 0) {
      writer.uint32(26).string(message.fallback);
    }
    if (message.compression !== void 0) {
      writer.uint32(40).int32(message.compression);
    }
    if (message.content.length !== 0) {
      writer.uint32(34).bytes(message.content);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal8.default.Reader ? input : new import_minimal8.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncodedContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.type = ContentTypeId.decode(reader, reader.uint32());
          break;
        case 2:
          const entry2 = EncodedContent_ParametersEntry.decode(reader, reader.uint32());
          if (entry2.value !== void 0) {
            message.parameters[entry2.key] = entry2.value;
          }
          break;
        case 3:
          message.fallback = reader.string();
          break;
        case 5:
          message.compression = reader.int32();
          break;
        case 4:
          message.content = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      type: isSet8(object.type) ? ContentTypeId.fromJSON(object.type) : void 0,
      parameters: isObject2(object.parameters) ? Object.entries(object.parameters).reduce((acc, [key, value]) => {
        acc[key] = String(value);
        return acc;
      }, {}) : {},
      fallback: isSet8(object.fallback) ? String(object.fallback) : void 0,
      compression: isSet8(object.compression) ? compressionFromJSON(object.compression) : void 0,
      content: isSet8(object.content) ? bytesFromBase647(object.content) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.type !== void 0 && (obj.type = message.type ? ContentTypeId.toJSON(message.type) : void 0);
    obj.parameters = {};
    if (message.parameters) {
      Object.entries(message.parameters).forEach(([k, v]) => {
        obj.parameters[k] = v;
      });
    }
    message.fallback !== void 0 && (obj.fallback = message.fallback);
    message.compression !== void 0 && (obj.compression = message.compression !== void 0 ? compressionToJSON(message.compression) : void 0);
    message.content !== void 0 && (obj.content = base64FromBytes7(message.content !== void 0 ? message.content : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d;
    const message = createBaseEncodedContent();
    message.type = object.type !== void 0 && object.type !== null ? ContentTypeId.fromPartial(object.type) : void 0;
    message.parameters = Object.entries((_a = object.parameters) !== null && _a !== void 0 ? _a : {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = String(value);
      }
      return acc;
    }, {});
    message.fallback = (_b = object.fallback) !== null && _b !== void 0 ? _b : void 0;
    message.compression = (_c = object.compression) !== null && _c !== void 0 ? _c : void 0;
    message.content = (_d = object.content) !== null && _d !== void 0 ? _d : new Uint8Array();
    return message;
  }
};
function createBaseEncodedContent_ParametersEntry() {
  return { key: "", value: "" };
}
var EncodedContent_ParametersEntry = {
  encode(message, writer = import_minimal8.default.Writer.create()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal8.default.Reader ? input : new import_minimal8.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncodedContent_ParametersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.key = reader.string();
          break;
        case 2:
          message.value = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet8(object.key) ? String(object.key) : "",
      value: isSet8(object.value) ? String(object.value) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.key !== void 0 && (obj.key = message.key);
    message.value !== void 0 && (obj.value = message.value);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseEncodedContent_ParametersEntry();
    message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
    message.value = (_b = object.value) !== null && _b !== void 0 ? _b : "";
    return message;
  }
};
function createBaseSignedContent() {
  return { payload: new Uint8Array(), sender: void 0, signature: void 0 };
}
var SignedContent = {
  encode(message, writer = import_minimal8.default.Writer.create()) {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.sender !== void 0) {
      SignedPublicKeyBundle.encode(message.sender, writer.uint32(18).fork()).ldelim();
    }
    if (message.signature !== void 0) {
      Signature.encode(message.signature, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal8.default.Reader ? input : new import_minimal8.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignedContent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.payload = reader.bytes();
          break;
        case 2:
          message.sender = SignedPublicKeyBundle.decode(reader, reader.uint32());
          break;
        case 3:
          message.signature = Signature.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      payload: isSet8(object.payload) ? bytesFromBase647(object.payload) : new Uint8Array(),
      sender: isSet8(object.sender) ? SignedPublicKeyBundle.fromJSON(object.sender) : void 0,
      signature: isSet8(object.signature) ? Signature.fromJSON(object.signature) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.payload !== void 0 && (obj.payload = base64FromBytes7(message.payload !== void 0 ? message.payload : new Uint8Array()));
    message.sender !== void 0 && (obj.sender = message.sender ? SignedPublicKeyBundle.toJSON(message.sender) : void 0);
    message.signature !== void 0 && (obj.signature = message.signature ? Signature.toJSON(message.signature) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSignedContent();
    message.payload = (_a = object.payload) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.sender = object.sender !== void 0 && object.sender !== null ? SignedPublicKeyBundle.fromPartial(object.sender) : void 0;
    message.signature = object.signature !== void 0 && object.signature !== null ? Signature.fromPartial(object.signature) : void 0;
    return message;
  }
};
var globalThis7 = (() => {
  if (typeof globalThis7 !== "undefined")
    return globalThis7;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase647(b642) {
  if (globalThis7.Buffer) {
    return Uint8Array.from(globalThis7.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis7.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes7(arr) {
  if (globalThis7.Buffer) {
    return globalThis7.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis7.btoa(bin.join(""));
  }
}
if (import_minimal8.default.util.Long !== long_default) {
  import_minimal8.default.util.Long = long_default;
  import_minimal8.default.configure();
}
function isObject2(value) {
  return typeof value === "object" && value !== null;
}
function isSet8(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/frames.pb.js
var frames_pb_exports = {};
__export(frames_pb_exports, {
  FrameAction: () => FrameAction,
  FrameActionBody: () => FrameActionBody,
  protobufPackage: () => protobufPackage9
});
var import_minimal9 = __toESM(require_minimal());
var protobufPackage9 = "xmtp.message_contents";
function createBaseFrameActionBody() {
  return {
    frameUrl: "",
    buttonIndex: 0,
    timestamp: long_default.UZERO,
    opaqueConversationIdentifier: "",
    unixTimestamp: 0,
    inputText: "",
    state: "",
    address: "",
    transactionId: ""
  };
}
var FrameActionBody = {
  encode(message, writer = import_minimal9.default.Writer.create()) {
    if (message.frameUrl !== "") {
      writer.uint32(10).string(message.frameUrl);
    }
    if (message.buttonIndex !== 0) {
      writer.uint32(16).int32(message.buttonIndex);
    }
    if (!message.timestamp.isZero()) {
      writer.uint32(24).uint64(message.timestamp);
    }
    if (message.opaqueConversationIdentifier !== "") {
      writer.uint32(34).string(message.opaqueConversationIdentifier);
    }
    if (message.unixTimestamp !== 0) {
      writer.uint32(40).uint32(message.unixTimestamp);
    }
    if (message.inputText !== "") {
      writer.uint32(50).string(message.inputText);
    }
    if (message.state !== "") {
      writer.uint32(58).string(message.state);
    }
    if (message.address !== "") {
      writer.uint32(66).string(message.address);
    }
    if (message.transactionId !== "") {
      writer.uint32(74).string(message.transactionId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal9.default.Reader ? input : new import_minimal9.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFrameActionBody();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.frameUrl = reader.string();
          break;
        case 2:
          message.buttonIndex = reader.int32();
          break;
        case 3:
          message.timestamp = reader.uint64();
          break;
        case 4:
          message.opaqueConversationIdentifier = reader.string();
          break;
        case 5:
          message.unixTimestamp = reader.uint32();
          break;
        case 6:
          message.inputText = reader.string();
          break;
        case 7:
          message.state = reader.string();
          break;
        case 8:
          message.address = reader.string();
          break;
        case 9:
          message.transactionId = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      frameUrl: isSet9(object.frameUrl) ? String(object.frameUrl) : "",
      buttonIndex: isSet9(object.buttonIndex) ? Number(object.buttonIndex) : 0,
      timestamp: isSet9(object.timestamp) ? long_default.fromValue(object.timestamp) : long_default.UZERO,
      opaqueConversationIdentifier: isSet9(object.opaqueConversationIdentifier) ? String(object.opaqueConversationIdentifier) : "",
      unixTimestamp: isSet9(object.unixTimestamp) ? Number(object.unixTimestamp) : 0,
      inputText: isSet9(object.inputText) ? String(object.inputText) : "",
      state: isSet9(object.state) ? String(object.state) : "",
      address: isSet9(object.address) ? String(object.address) : "",
      transactionId: isSet9(object.transactionId) ? String(object.transactionId) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.frameUrl !== void 0 && (obj.frameUrl = message.frameUrl);
    message.buttonIndex !== void 0 && (obj.buttonIndex = Math.round(message.buttonIndex));
    message.timestamp !== void 0 && (obj.timestamp = (message.timestamp || long_default.UZERO).toString());
    message.opaqueConversationIdentifier !== void 0 && (obj.opaqueConversationIdentifier = message.opaqueConversationIdentifier);
    message.unixTimestamp !== void 0 && (obj.unixTimestamp = Math.round(message.unixTimestamp));
    message.inputText !== void 0 && (obj.inputText = message.inputText);
    message.state !== void 0 && (obj.state = message.state);
    message.address !== void 0 && (obj.address = message.address);
    message.transactionId !== void 0 && (obj.transactionId = message.transactionId);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const message = createBaseFrameActionBody();
    message.frameUrl = (_a = object.frameUrl) !== null && _a !== void 0 ? _a : "";
    message.buttonIndex = (_b = object.buttonIndex) !== null && _b !== void 0 ? _b : 0;
    message.timestamp = object.timestamp !== void 0 && object.timestamp !== null ? long_default.fromValue(object.timestamp) : long_default.UZERO;
    message.opaqueConversationIdentifier = (_c = object.opaqueConversationIdentifier) !== null && _c !== void 0 ? _c : "";
    message.unixTimestamp = (_d = object.unixTimestamp) !== null && _d !== void 0 ? _d : 0;
    message.inputText = (_e = object.inputText) !== null && _e !== void 0 ? _e : "";
    message.state = (_f = object.state) !== null && _f !== void 0 ? _f : "";
    message.address = (_g = object.address) !== null && _g !== void 0 ? _g : "";
    message.transactionId = (_h = object.transactionId) !== null && _h !== void 0 ? _h : "";
    return message;
  }
};
function createBaseFrameAction() {
  return {
    signature: void 0,
    signedPublicKeyBundle: void 0,
    actionBody: new Uint8Array(),
    installationSignature: new Uint8Array(),
    installationId: new Uint8Array(),
    inboxId: ""
  };
}
var FrameAction = {
  encode(message, writer = import_minimal9.default.Writer.create()) {
    if (message.signature !== void 0) {
      Signature.encode(message.signature, writer.uint32(10).fork()).ldelim();
    }
    if (message.signedPublicKeyBundle !== void 0) {
      SignedPublicKeyBundle.encode(message.signedPublicKeyBundle, writer.uint32(18).fork()).ldelim();
    }
    if (message.actionBody.length !== 0) {
      writer.uint32(26).bytes(message.actionBody);
    }
    if (message.installationSignature.length !== 0) {
      writer.uint32(34).bytes(message.installationSignature);
    }
    if (message.installationId.length !== 0) {
      writer.uint32(42).bytes(message.installationId);
    }
    if (message.inboxId !== "") {
      writer.uint32(50).string(message.inboxId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal9.default.Reader ? input : new import_minimal9.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFrameAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.signature = Signature.decode(reader, reader.uint32());
          break;
        case 2:
          message.signedPublicKeyBundle = SignedPublicKeyBundle.decode(reader, reader.uint32());
          break;
        case 3:
          message.actionBody = reader.bytes();
          break;
        case 4:
          message.installationSignature = reader.bytes();
          break;
        case 5:
          message.installationId = reader.bytes();
          break;
        case 6:
          message.inboxId = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      signature: isSet9(object.signature) ? Signature.fromJSON(object.signature) : void 0,
      signedPublicKeyBundle: isSet9(object.signedPublicKeyBundle) ? SignedPublicKeyBundle.fromJSON(object.signedPublicKeyBundle) : void 0,
      actionBody: isSet9(object.actionBody) ? bytesFromBase648(object.actionBody) : new Uint8Array(),
      installationSignature: isSet9(object.installationSignature) ? bytesFromBase648(object.installationSignature) : new Uint8Array(),
      installationId: isSet9(object.installationId) ? bytesFromBase648(object.installationId) : new Uint8Array(),
      inboxId: isSet9(object.inboxId) ? String(object.inboxId) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.signature !== void 0 && (obj.signature = message.signature ? Signature.toJSON(message.signature) : void 0);
    message.signedPublicKeyBundle !== void 0 && (obj.signedPublicKeyBundle = message.signedPublicKeyBundle ? SignedPublicKeyBundle.toJSON(message.signedPublicKeyBundle) : void 0);
    message.actionBody !== void 0 && (obj.actionBody = base64FromBytes8(message.actionBody !== void 0 ? message.actionBody : new Uint8Array()));
    message.installationSignature !== void 0 && (obj.installationSignature = base64FromBytes8(message.installationSignature !== void 0 ? message.installationSignature : new Uint8Array()));
    message.installationId !== void 0 && (obj.installationId = base64FromBytes8(message.installationId !== void 0 ? message.installationId : new Uint8Array()));
    message.inboxId !== void 0 && (obj.inboxId = message.inboxId);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d;
    const message = createBaseFrameAction();
    message.signature = object.signature !== void 0 && object.signature !== null ? Signature.fromPartial(object.signature) : void 0;
    message.signedPublicKeyBundle = object.signedPublicKeyBundle !== void 0 && object.signedPublicKeyBundle !== null ? SignedPublicKeyBundle.fromPartial(object.signedPublicKeyBundle) : void 0;
    message.actionBody = (_a = object.actionBody) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.installationSignature = (_b = object.installationSignature) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.installationId = (_c = object.installationId) !== null && _c !== void 0 ? _c : new Uint8Array();
    message.inboxId = (_d = object.inboxId) !== null && _d !== void 0 ? _d : "";
    return message;
  }
};
var globalThis8 = (() => {
  if (typeof globalThis8 !== "undefined")
    return globalThis8;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase648(b642) {
  if (globalThis8.Buffer) {
    return Uint8Array.from(globalThis8.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis8.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes8(arr) {
  if (globalThis8.Buffer) {
    return globalThis8.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis8.btoa(bin.join(""));
  }
}
if (import_minimal9.default.util.Long !== long_default) {
  import_minimal9.default.util.Long = long_default;
  import_minimal9.default.configure();
}
function isSet9(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/composite.pb.js
var composite_pb_exports = {};
__export(composite_pb_exports, {
  Composite: () => Composite,
  Composite_Part: () => Composite_Part,
  protobufPackage: () => protobufPackage10
});
var import_minimal10 = __toESM(require_minimal());
var protobufPackage10 = "xmtp.message_contents";
function createBaseComposite() {
  return { parts: [] };
}
var Composite = {
  encode(message, writer = import_minimal10.default.Writer.create()) {
    for (const v of message.parts) {
      Composite_Part.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal10.default.Reader ? input : new import_minimal10.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseComposite();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.parts.push(Composite_Part.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      parts: Array.isArray(object === null || object === void 0 ? void 0 : object.parts) ? object.parts.map((e) => Composite_Part.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.parts) {
      obj.parts = message.parts.map((e) => e ? Composite_Part.toJSON(e) : void 0);
    } else {
      obj.parts = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseComposite();
    message.parts = ((_a = object.parts) === null || _a === void 0 ? void 0 : _a.map((e) => Composite_Part.fromPartial(e))) || [];
    return message;
  }
};
function createBaseComposite_Part() {
  return { part: void 0, composite: void 0 };
}
var Composite_Part = {
  encode(message, writer = import_minimal10.default.Writer.create()) {
    if (message.part !== void 0) {
      EncodedContent.encode(message.part, writer.uint32(10).fork()).ldelim();
    }
    if (message.composite !== void 0) {
      Composite.encode(message.composite, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal10.default.Reader ? input : new import_minimal10.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseComposite_Part();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.part = EncodedContent.decode(reader, reader.uint32());
          break;
        case 2:
          message.composite = Composite.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      part: isSet10(object.part) ? EncodedContent.fromJSON(object.part) : void 0,
      composite: isSet10(object.composite) ? Composite.fromJSON(object.composite) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.part !== void 0 && (obj.part = message.part ? EncodedContent.toJSON(message.part) : void 0);
    message.composite !== void 0 && (obj.composite = message.composite ? Composite.toJSON(message.composite) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseComposite_Part();
    message.part = object.part !== void 0 && object.part !== null ? EncodedContent.fromPartial(object.part) : void 0;
    message.composite = object.composite !== void 0 && object.composite !== null ? Composite.fromPartial(object.composite) : void 0;
    return message;
  }
};
if (import_minimal10.default.util.Long !== long_default) {
  import_minimal10.default.util.Long = long_default;
  import_minimal10.default.configure();
}
function isSet10(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/private_key.pb.js
var private_key_pb_exports = {};
__export(private_key_pb_exports, {
  EncryptedPrivateKeyBundle: () => EncryptedPrivateKeyBundle,
  EncryptedPrivateKeyBundleV1: () => EncryptedPrivateKeyBundleV1,
  PrivateKey: () => PrivateKey,
  PrivateKeyBundle: () => PrivateKeyBundle,
  PrivateKeyBundleV1: () => PrivateKeyBundleV1,
  PrivateKeyBundleV2: () => PrivateKeyBundleV2,
  PrivateKey_Secp256k1: () => PrivateKey_Secp256k1,
  SignedPrivateKey: () => SignedPrivateKey,
  SignedPrivateKey_Secp256k1: () => SignedPrivateKey_Secp256k1,
  protobufPackage: () => protobufPackage11
});
var import_minimal11 = __toESM(require_minimal());
var protobufPackage11 = "xmtp.message_contents";
function createBaseSignedPrivateKey() {
  return { createdNs: long_default.UZERO, secp256k1: void 0, publicKey: void 0 };
}
var SignedPrivateKey = {
  encode(message, writer = import_minimal11.default.Writer.create()) {
    if (!message.createdNs.isZero()) {
      writer.uint32(8).uint64(message.createdNs);
    }
    if (message.secp256k1 !== void 0) {
      SignedPrivateKey_Secp256k1.encode(message.secp256k1, writer.uint32(18).fork()).ldelim();
    }
    if (message.publicKey !== void 0) {
      SignedPublicKey.encode(message.publicKey, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal11.default.Reader ? input : new import_minimal11.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignedPrivateKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.createdNs = reader.uint64();
          break;
        case 2:
          message.secp256k1 = SignedPrivateKey_Secp256k1.decode(reader, reader.uint32());
          break;
        case 3:
          message.publicKey = SignedPublicKey.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      createdNs: isSet11(object.createdNs) ? long_default.fromValue(object.createdNs) : long_default.UZERO,
      secp256k1: isSet11(object.secp256k1) ? SignedPrivateKey_Secp256k1.fromJSON(object.secp256k1) : void 0,
      publicKey: isSet11(object.publicKey) ? SignedPublicKey.fromJSON(object.publicKey) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.createdNs !== void 0 && (obj.createdNs = (message.createdNs || long_default.UZERO).toString());
    message.secp256k1 !== void 0 && (obj.secp256k1 = message.secp256k1 ? SignedPrivateKey_Secp256k1.toJSON(message.secp256k1) : void 0);
    message.publicKey !== void 0 && (obj.publicKey = message.publicKey ? SignedPublicKey.toJSON(message.publicKey) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseSignedPrivateKey();
    message.createdNs = object.createdNs !== void 0 && object.createdNs !== null ? long_default.fromValue(object.createdNs) : long_default.UZERO;
    message.secp256k1 = object.secp256k1 !== void 0 && object.secp256k1 !== null ? SignedPrivateKey_Secp256k1.fromPartial(object.secp256k1) : void 0;
    message.publicKey = object.publicKey !== void 0 && object.publicKey !== null ? SignedPublicKey.fromPartial(object.publicKey) : void 0;
    return message;
  }
};
function createBaseSignedPrivateKey_Secp256k1() {
  return { bytes: new Uint8Array() };
}
var SignedPrivateKey_Secp256k1 = {
  encode(message, writer = import_minimal11.default.Writer.create()) {
    if (message.bytes.length !== 0) {
      writer.uint32(10).bytes(message.bytes);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal11.default.Reader ? input : new import_minimal11.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignedPrivateKey_Secp256k1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.bytes = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      bytes: isSet11(object.bytes) ? bytesFromBase649(object.bytes) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.bytes !== void 0 && (obj.bytes = base64FromBytes9(message.bytes !== void 0 ? message.bytes : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSignedPrivateKey_Secp256k1();
    message.bytes = (_a = object.bytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBasePrivateKeyBundleV2() {
  return { identityKey: void 0, preKeys: [] };
}
var PrivateKeyBundleV2 = {
  encode(message, writer = import_minimal11.default.Writer.create()) {
    if (message.identityKey !== void 0) {
      SignedPrivateKey.encode(message.identityKey, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.preKeys) {
      SignedPrivateKey.encode(v, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal11.default.Reader ? input : new import_minimal11.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivateKeyBundleV2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.identityKey = SignedPrivateKey.decode(reader, reader.uint32());
          break;
        case 2:
          message.preKeys.push(SignedPrivateKey.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityKey: isSet11(object.identityKey) ? SignedPrivateKey.fromJSON(object.identityKey) : void 0,
      preKeys: Array.isArray(object === null || object === void 0 ? void 0 : object.preKeys) ? object.preKeys.map((e) => SignedPrivateKey.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    message.identityKey !== void 0 && (obj.identityKey = message.identityKey ? SignedPrivateKey.toJSON(message.identityKey) : void 0);
    if (message.preKeys) {
      obj.preKeys = message.preKeys.map((e) => e ? SignedPrivateKey.toJSON(e) : void 0);
    } else {
      obj.preKeys = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePrivateKeyBundleV2();
    message.identityKey = object.identityKey !== void 0 && object.identityKey !== null ? SignedPrivateKey.fromPartial(object.identityKey) : void 0;
    message.preKeys = ((_a = object.preKeys) === null || _a === void 0 ? void 0 : _a.map((e) => SignedPrivateKey.fromPartial(e))) || [];
    return message;
  }
};
function createBasePrivateKey() {
  return { timestamp: long_default.UZERO, secp256k1: void 0, publicKey: void 0 };
}
var PrivateKey = {
  encode(message, writer = import_minimal11.default.Writer.create()) {
    if (!message.timestamp.isZero()) {
      writer.uint32(8).uint64(message.timestamp);
    }
    if (message.secp256k1 !== void 0) {
      PrivateKey_Secp256k1.encode(message.secp256k1, writer.uint32(18).fork()).ldelim();
    }
    if (message.publicKey !== void 0) {
      PublicKey.encode(message.publicKey, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal11.default.Reader ? input : new import_minimal11.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivateKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.timestamp = reader.uint64();
          break;
        case 2:
          message.secp256k1 = PrivateKey_Secp256k1.decode(reader, reader.uint32());
          break;
        case 3:
          message.publicKey = PublicKey.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      timestamp: isSet11(object.timestamp) ? long_default.fromValue(object.timestamp) : long_default.UZERO,
      secp256k1: isSet11(object.secp256k1) ? PrivateKey_Secp256k1.fromJSON(object.secp256k1) : void 0,
      publicKey: isSet11(object.publicKey) ? PublicKey.fromJSON(object.publicKey) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.timestamp !== void 0 && (obj.timestamp = (message.timestamp || long_default.UZERO).toString());
    message.secp256k1 !== void 0 && (obj.secp256k1 = message.secp256k1 ? PrivateKey_Secp256k1.toJSON(message.secp256k1) : void 0);
    message.publicKey !== void 0 && (obj.publicKey = message.publicKey ? PublicKey.toJSON(message.publicKey) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBasePrivateKey();
    message.timestamp = object.timestamp !== void 0 && object.timestamp !== null ? long_default.fromValue(object.timestamp) : long_default.UZERO;
    message.secp256k1 = object.secp256k1 !== void 0 && object.secp256k1 !== null ? PrivateKey_Secp256k1.fromPartial(object.secp256k1) : void 0;
    message.publicKey = object.publicKey !== void 0 && object.publicKey !== null ? PublicKey.fromPartial(object.publicKey) : void 0;
    return message;
  }
};
function createBasePrivateKey_Secp256k1() {
  return { bytes: new Uint8Array() };
}
var PrivateKey_Secp256k1 = {
  encode(message, writer = import_minimal11.default.Writer.create()) {
    if (message.bytes.length !== 0) {
      writer.uint32(10).bytes(message.bytes);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal11.default.Reader ? input : new import_minimal11.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivateKey_Secp256k1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.bytes = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      bytes: isSet11(object.bytes) ? bytesFromBase649(object.bytes) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.bytes !== void 0 && (obj.bytes = base64FromBytes9(message.bytes !== void 0 ? message.bytes : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePrivateKey_Secp256k1();
    message.bytes = (_a = object.bytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBasePrivateKeyBundleV1() {
  return { identityKey: void 0, preKeys: [] };
}
var PrivateKeyBundleV1 = {
  encode(message, writer = import_minimal11.default.Writer.create()) {
    if (message.identityKey !== void 0) {
      PrivateKey.encode(message.identityKey, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.preKeys) {
      PrivateKey.encode(v, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal11.default.Reader ? input : new import_minimal11.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivateKeyBundleV1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.identityKey = PrivateKey.decode(reader, reader.uint32());
          break;
        case 2:
          message.preKeys.push(PrivateKey.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      identityKey: isSet11(object.identityKey) ? PrivateKey.fromJSON(object.identityKey) : void 0,
      preKeys: Array.isArray(object === null || object === void 0 ? void 0 : object.preKeys) ? object.preKeys.map((e) => PrivateKey.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    message.identityKey !== void 0 && (obj.identityKey = message.identityKey ? PrivateKey.toJSON(message.identityKey) : void 0);
    if (message.preKeys) {
      obj.preKeys = message.preKeys.map((e) => e ? PrivateKey.toJSON(e) : void 0);
    } else {
      obj.preKeys = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePrivateKeyBundleV1();
    message.identityKey = object.identityKey !== void 0 && object.identityKey !== null ? PrivateKey.fromPartial(object.identityKey) : void 0;
    message.preKeys = ((_a = object.preKeys) === null || _a === void 0 ? void 0 : _a.map((e) => PrivateKey.fromPartial(e))) || [];
    return message;
  }
};
function createBasePrivateKeyBundle() {
  return { v1: void 0, v2: void 0 };
}
var PrivateKeyBundle = {
  encode(message, writer = import_minimal11.default.Writer.create()) {
    if (message.v1 !== void 0) {
      PrivateKeyBundleV1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    if (message.v2 !== void 0) {
      PrivateKeyBundleV2.encode(message.v2, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal11.default.Reader ? input : new import_minimal11.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivateKeyBundle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = PrivateKeyBundleV1.decode(reader, reader.uint32());
          break;
        case 2:
          message.v2 = PrivateKeyBundleV2.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet11(object.v1) ? PrivateKeyBundleV1.fromJSON(object.v1) : void 0,
      v2: isSet11(object.v2) ? PrivateKeyBundleV2.fromJSON(object.v2) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? PrivateKeyBundleV1.toJSON(message.v1) : void 0);
    message.v2 !== void 0 && (obj.v2 = message.v2 ? PrivateKeyBundleV2.toJSON(message.v2) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBasePrivateKeyBundle();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? PrivateKeyBundleV1.fromPartial(object.v1) : void 0;
    message.v2 = object.v2 !== void 0 && object.v2 !== null ? PrivateKeyBundleV2.fromPartial(object.v2) : void 0;
    return message;
  }
};
function createBaseEncryptedPrivateKeyBundleV1() {
  return { walletPreKey: new Uint8Array(), ciphertext: void 0 };
}
var EncryptedPrivateKeyBundleV1 = {
  encode(message, writer = import_minimal11.default.Writer.create()) {
    if (message.walletPreKey.length !== 0) {
      writer.uint32(10).bytes(message.walletPreKey);
    }
    if (message.ciphertext !== void 0) {
      Ciphertext.encode(message.ciphertext, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal11.default.Reader ? input : new import_minimal11.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncryptedPrivateKeyBundleV1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.walletPreKey = reader.bytes();
          break;
        case 2:
          message.ciphertext = Ciphertext.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      walletPreKey: isSet11(object.walletPreKey) ? bytesFromBase649(object.walletPreKey) : new Uint8Array(),
      ciphertext: isSet11(object.ciphertext) ? Ciphertext.fromJSON(object.ciphertext) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.walletPreKey !== void 0 && (obj.walletPreKey = base64FromBytes9(message.walletPreKey !== void 0 ? message.walletPreKey : new Uint8Array()));
    message.ciphertext !== void 0 && (obj.ciphertext = message.ciphertext ? Ciphertext.toJSON(message.ciphertext) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseEncryptedPrivateKeyBundleV1();
    message.walletPreKey = (_a = object.walletPreKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.ciphertext = object.ciphertext !== void 0 && object.ciphertext !== null ? Ciphertext.fromPartial(object.ciphertext) : void 0;
    return message;
  }
};
function createBaseEncryptedPrivateKeyBundle() {
  return { v1: void 0 };
}
var EncryptedPrivateKeyBundle = {
  encode(message, writer = import_minimal11.default.Writer.create()) {
    if (message.v1 !== void 0) {
      EncryptedPrivateKeyBundleV1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal11.default.Reader ? input : new import_minimal11.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncryptedPrivateKeyBundle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = EncryptedPrivateKeyBundleV1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet11(object.v1) ? EncryptedPrivateKeyBundleV1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? EncryptedPrivateKeyBundleV1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseEncryptedPrivateKeyBundle();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? EncryptedPrivateKeyBundleV1.fromPartial(object.v1) : void 0;
    return message;
  }
};
var globalThis9 = (() => {
  if (typeof globalThis9 !== "undefined")
    return globalThis9;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase649(b642) {
  if (globalThis9.Buffer) {
    return Uint8Array.from(globalThis9.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis9.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes9(arr) {
  if (globalThis9.Buffer) {
    return globalThis9.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis9.btoa(bin.join(""));
  }
}
if (import_minimal11.default.util.Long !== long_default) {
  import_minimal11.default.util.Long = long_default;
  import_minimal11.default.configure();
}
function isSet11(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/contact.pb.js
var contact_pb_exports = {};
__export(contact_pb_exports, {
  ContactBundle: () => ContactBundle,
  ContactBundleV1: () => ContactBundleV1,
  ContactBundleV2: () => ContactBundleV2,
  protobufPackage: () => protobufPackage12
});
var import_minimal12 = __toESM(require_minimal());
var protobufPackage12 = "xmtp.message_contents";
function createBaseContactBundleV1() {
  return { keyBundle: void 0 };
}
var ContactBundleV1 = {
  encode(message, writer = import_minimal12.default.Writer.create()) {
    if (message.keyBundle !== void 0) {
      PublicKeyBundle.encode(message.keyBundle, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal12.default.Reader ? input : new import_minimal12.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseContactBundleV1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyBundle = PublicKeyBundle.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyBundle: isSet12(object.keyBundle) ? PublicKeyBundle.fromJSON(object.keyBundle) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.keyBundle !== void 0 && (obj.keyBundle = message.keyBundle ? PublicKeyBundle.toJSON(message.keyBundle) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseContactBundleV1();
    message.keyBundle = object.keyBundle !== void 0 && object.keyBundle !== null ? PublicKeyBundle.fromPartial(object.keyBundle) : void 0;
    return message;
  }
};
function createBaseContactBundleV2() {
  return { keyBundle: void 0 };
}
var ContactBundleV2 = {
  encode(message, writer = import_minimal12.default.Writer.create()) {
    if (message.keyBundle !== void 0) {
      SignedPublicKeyBundle.encode(message.keyBundle, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal12.default.Reader ? input : new import_minimal12.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseContactBundleV2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyBundle = SignedPublicKeyBundle.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyBundle: isSet12(object.keyBundle) ? SignedPublicKeyBundle.fromJSON(object.keyBundle) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.keyBundle !== void 0 && (obj.keyBundle = message.keyBundle ? SignedPublicKeyBundle.toJSON(message.keyBundle) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseContactBundleV2();
    message.keyBundle = object.keyBundle !== void 0 && object.keyBundle !== null ? SignedPublicKeyBundle.fromPartial(object.keyBundle) : void 0;
    return message;
  }
};
function createBaseContactBundle() {
  return { v1: void 0, v2: void 0 };
}
var ContactBundle = {
  encode(message, writer = import_minimal12.default.Writer.create()) {
    if (message.v1 !== void 0) {
      ContactBundleV1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    if (message.v2 !== void 0) {
      ContactBundleV2.encode(message.v2, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal12.default.Reader ? input : new import_minimal12.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseContactBundle();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = ContactBundleV1.decode(reader, reader.uint32());
          break;
        case 2:
          message.v2 = ContactBundleV2.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet12(object.v1) ? ContactBundleV1.fromJSON(object.v1) : void 0,
      v2: isSet12(object.v2) ? ContactBundleV2.fromJSON(object.v2) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? ContactBundleV1.toJSON(message.v1) : void 0);
    message.v2 !== void 0 && (obj.v2 = message.v2 ? ContactBundleV2.toJSON(message.v2) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseContactBundle();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? ContactBundleV1.fromPartial(object.v1) : void 0;
    message.v2 = object.v2 !== void 0 && object.v2 !== null ? ContactBundleV2.fromPartial(object.v2) : void 0;
    return message;
  }
};
if (import_minimal12.default.util.Long !== long_default) {
  import_minimal12.default.util.Long = long_default;
  import_minimal12.default.configure();
}
function isSet12(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/keystore_api/v1/keystore.pb.js
var keystore_pb_exports = {};
__export(keystore_pb_exports, {
  CreateAuthTokenRequest: () => CreateAuthTokenRequest,
  CreateInviteRequest: () => CreateInviteRequest,
  CreateInviteResponse: () => CreateInviteResponse,
  DecryptResponse: () => DecryptResponse,
  DecryptResponse_Response: () => DecryptResponse_Response,
  DecryptResponse_Response_Success: () => DecryptResponse_Response_Success,
  DecryptV1Request: () => DecryptV1Request,
  DecryptV1Request_Request: () => DecryptV1Request_Request,
  DecryptV2Request: () => DecryptV2Request,
  DecryptV2Request_Request: () => DecryptV2Request_Request,
  EncryptResponse: () => EncryptResponse,
  EncryptResponse_Response: () => EncryptResponse_Response,
  EncryptResponse_Response_Success: () => EncryptResponse_Response_Success,
  EncryptV1Request: () => EncryptV1Request,
  EncryptV1Request_Request: () => EncryptV1Request_Request,
  EncryptV2Request: () => EncryptV2Request,
  EncryptV2Request_Request: () => EncryptV2Request_Request,
  ErrorCode: () => ErrorCode,
  GetConversationHmacKeysRequest: () => GetConversationHmacKeysRequest,
  GetConversationHmacKeysResponse: () => GetConversationHmacKeysResponse,
  GetConversationHmacKeysResponse_HmacKeyData: () => GetConversationHmacKeysResponse_HmacKeyData,
  GetConversationHmacKeysResponse_HmacKeys: () => GetConversationHmacKeysResponse_HmacKeys,
  GetConversationHmacKeysResponse_HmacKeysEntry: () => GetConversationHmacKeysResponse_HmacKeysEntry,
  GetConversationsResponse: () => GetConversationsResponse,
  GetKeystoreStatusRequest: () => GetKeystoreStatusRequest,
  GetKeystoreStatusResponse: () => GetKeystoreStatusResponse,
  GetKeystoreStatusResponse_KeystoreStatus: () => GetKeystoreStatusResponse_KeystoreStatus,
  GetPrivatePreferencesTopicIdentifierResponse: () => GetPrivatePreferencesTopicIdentifierResponse,
  GetRefreshJobRequest: () => GetRefreshJobRequest,
  GetRefreshJobResponse: () => GetRefreshJobResponse,
  InitKeystoreRequest: () => InitKeystoreRequest,
  InitKeystoreResponse: () => InitKeystoreResponse,
  JobType: () => JobType,
  KeystoreError: () => KeystoreError,
  PrivatePreferencesActionMap: () => PrivatePreferencesActionMap,
  PrivatePreferencesActionMap_ActionsEntry: () => PrivatePreferencesActionMap_ActionsEntry,
  SaveInvitesRequest: () => SaveInvitesRequest,
  SaveInvitesRequest_Request: () => SaveInvitesRequest_Request,
  SaveInvitesResponse: () => SaveInvitesResponse,
  SaveInvitesResponse_Response: () => SaveInvitesResponse_Response,
  SaveInvitesResponse_Response_Success: () => SaveInvitesResponse_Response_Success,
  SaveV1ConversationsRequest: () => SaveV1ConversationsRequest,
  SaveV1ConversationsResponse: () => SaveV1ConversationsResponse,
  SelfDecryptRequest: () => SelfDecryptRequest,
  SelfDecryptRequest_Request: () => SelfDecryptRequest_Request,
  SelfEncryptRequest: () => SelfEncryptRequest,
  SelfEncryptRequest_Request: () => SelfEncryptRequest_Request,
  SelfEncryptResponse: () => SelfEncryptResponse,
  SelfEncryptResponse_Response: () => SelfEncryptResponse_Response,
  SelfEncryptResponse_Response_Success: () => SelfEncryptResponse_Response_Success,
  SetRefeshJobRequest: () => SetRefeshJobRequest,
  SetRefreshJobResponse: () => SetRefreshJobResponse,
  SignDigestRequest: () => SignDigestRequest,
  TopicMap: () => TopicMap,
  TopicMap_TopicData: () => TopicMap_TopicData,
  TopicMap_TopicsEntry: () => TopicMap_TopicsEntry,
  errorCodeFromJSON: () => errorCodeFromJSON,
  errorCodeToJSON: () => errorCodeToJSON,
  getKeystoreStatusResponse_KeystoreStatusFromJSON: () => getKeystoreStatusResponse_KeystoreStatusFromJSON,
  getKeystoreStatusResponse_KeystoreStatusToJSON: () => getKeystoreStatusResponse_KeystoreStatusToJSON,
  jobTypeFromJSON: () => jobTypeFromJSON,
  jobTypeToJSON: () => jobTypeToJSON,
  protobufPackage: () => protobufPackage14
});

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/private_preferences.pb.js
var private_preferences_pb_exports = {};
__export(private_preferences_pb_exports, {
  PrivatePreferencesAction: () => PrivatePreferencesAction,
  PrivatePreferencesAction_AllowAddress: () => PrivatePreferencesAction_AllowAddress,
  PrivatePreferencesAction_AllowGroup: () => PrivatePreferencesAction_AllowGroup,
  PrivatePreferencesAction_AllowInboxId: () => PrivatePreferencesAction_AllowInboxId,
  PrivatePreferencesAction_DenyAddress: () => PrivatePreferencesAction_DenyAddress,
  PrivatePreferencesAction_DenyGroup: () => PrivatePreferencesAction_DenyGroup,
  PrivatePreferencesAction_DenyInboxId: () => PrivatePreferencesAction_DenyInboxId,
  PrivatePreferencesPayload: () => PrivatePreferencesPayload,
  protobufPackage: () => protobufPackage13
});
var import_minimal13 = __toESM(require_minimal());
var protobufPackage13 = "xmtp.message_contents";
function createBasePrivatePreferencesAction() {
  return {
    allowAddress: void 0,
    denyAddress: void 0,
    allowGroup: void 0,
    denyGroup: void 0,
    allowInboxId: void 0,
    denyInboxId: void 0
  };
}
var PrivatePreferencesAction = {
  encode(message, writer = import_minimal13.default.Writer.create()) {
    if (message.allowAddress !== void 0) {
      PrivatePreferencesAction_AllowAddress.encode(message.allowAddress, writer.uint32(10).fork()).ldelim();
    }
    if (message.denyAddress !== void 0) {
      PrivatePreferencesAction_DenyAddress.encode(message.denyAddress, writer.uint32(18).fork()).ldelim();
    }
    if (message.allowGroup !== void 0) {
      PrivatePreferencesAction_AllowGroup.encode(message.allowGroup, writer.uint32(26).fork()).ldelim();
    }
    if (message.denyGroup !== void 0) {
      PrivatePreferencesAction_DenyGroup.encode(message.denyGroup, writer.uint32(34).fork()).ldelim();
    }
    if (message.allowInboxId !== void 0) {
      PrivatePreferencesAction_AllowInboxId.encode(message.allowInboxId, writer.uint32(42).fork()).ldelim();
    }
    if (message.denyInboxId !== void 0) {
      PrivatePreferencesAction_DenyInboxId.encode(message.denyInboxId, writer.uint32(50).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal13.default.Reader ? input : new import_minimal13.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivatePreferencesAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.allowAddress = PrivatePreferencesAction_AllowAddress.decode(reader, reader.uint32());
          break;
        case 2:
          message.denyAddress = PrivatePreferencesAction_DenyAddress.decode(reader, reader.uint32());
          break;
        case 3:
          message.allowGroup = PrivatePreferencesAction_AllowGroup.decode(reader, reader.uint32());
          break;
        case 4:
          message.denyGroup = PrivatePreferencesAction_DenyGroup.decode(reader, reader.uint32());
          break;
        case 5:
          message.allowInboxId = PrivatePreferencesAction_AllowInboxId.decode(reader, reader.uint32());
          break;
        case 6:
          message.denyInboxId = PrivatePreferencesAction_DenyInboxId.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      allowAddress: isSet13(object.allowAddress) ? PrivatePreferencesAction_AllowAddress.fromJSON(object.allowAddress) : void 0,
      denyAddress: isSet13(object.denyAddress) ? PrivatePreferencesAction_DenyAddress.fromJSON(object.denyAddress) : void 0,
      allowGroup: isSet13(object.allowGroup) ? PrivatePreferencesAction_AllowGroup.fromJSON(object.allowGroup) : void 0,
      denyGroup: isSet13(object.denyGroup) ? PrivatePreferencesAction_DenyGroup.fromJSON(object.denyGroup) : void 0,
      allowInboxId: isSet13(object.allowInboxId) ? PrivatePreferencesAction_AllowInboxId.fromJSON(object.allowInboxId) : void 0,
      denyInboxId: isSet13(object.denyInboxId) ? PrivatePreferencesAction_DenyInboxId.fromJSON(object.denyInboxId) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.allowAddress !== void 0 && (obj.allowAddress = message.allowAddress ? PrivatePreferencesAction_AllowAddress.toJSON(message.allowAddress) : void 0);
    message.denyAddress !== void 0 && (obj.denyAddress = message.denyAddress ? PrivatePreferencesAction_DenyAddress.toJSON(message.denyAddress) : void 0);
    message.allowGroup !== void 0 && (obj.allowGroup = message.allowGroup ? PrivatePreferencesAction_AllowGroup.toJSON(message.allowGroup) : void 0);
    message.denyGroup !== void 0 && (obj.denyGroup = message.denyGroup ? PrivatePreferencesAction_DenyGroup.toJSON(message.denyGroup) : void 0);
    message.allowInboxId !== void 0 && (obj.allowInboxId = message.allowInboxId ? PrivatePreferencesAction_AllowInboxId.toJSON(message.allowInboxId) : void 0);
    message.denyInboxId !== void 0 && (obj.denyInboxId = message.denyInboxId ? PrivatePreferencesAction_DenyInboxId.toJSON(message.denyInboxId) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBasePrivatePreferencesAction();
    message.allowAddress = object.allowAddress !== void 0 && object.allowAddress !== null ? PrivatePreferencesAction_AllowAddress.fromPartial(object.allowAddress) : void 0;
    message.denyAddress = object.denyAddress !== void 0 && object.denyAddress !== null ? PrivatePreferencesAction_DenyAddress.fromPartial(object.denyAddress) : void 0;
    message.allowGroup = object.allowGroup !== void 0 && object.allowGroup !== null ? PrivatePreferencesAction_AllowGroup.fromPartial(object.allowGroup) : void 0;
    message.denyGroup = object.denyGroup !== void 0 && object.denyGroup !== null ? PrivatePreferencesAction_DenyGroup.fromPartial(object.denyGroup) : void 0;
    message.allowInboxId = object.allowInboxId !== void 0 && object.allowInboxId !== null ? PrivatePreferencesAction_AllowInboxId.fromPartial(object.allowInboxId) : void 0;
    message.denyInboxId = object.denyInboxId !== void 0 && object.denyInboxId !== null ? PrivatePreferencesAction_DenyInboxId.fromPartial(object.denyInboxId) : void 0;
    return message;
  }
};
function createBasePrivatePreferencesAction_AllowAddress() {
  return { walletAddresses: [] };
}
var PrivatePreferencesAction_AllowAddress = {
  encode(message, writer = import_minimal13.default.Writer.create()) {
    for (const v of message.walletAddresses) {
      writer.uint32(10).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal13.default.Reader ? input : new import_minimal13.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivatePreferencesAction_AllowAddress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.walletAddresses.push(reader.string());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      walletAddresses: Array.isArray(object === null || object === void 0 ? void 0 : object.walletAddresses) ? object.walletAddresses.map((e) => String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.walletAddresses) {
      obj.walletAddresses = message.walletAddresses.map((e) => e);
    } else {
      obj.walletAddresses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePrivatePreferencesAction_AllowAddress();
    message.walletAddresses = ((_a = object.walletAddresses) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBasePrivatePreferencesAction_DenyAddress() {
  return { walletAddresses: [] };
}
var PrivatePreferencesAction_DenyAddress = {
  encode(message, writer = import_minimal13.default.Writer.create()) {
    for (const v of message.walletAddresses) {
      writer.uint32(10).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal13.default.Reader ? input : new import_minimal13.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivatePreferencesAction_DenyAddress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.walletAddresses.push(reader.string());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      walletAddresses: Array.isArray(object === null || object === void 0 ? void 0 : object.walletAddresses) ? object.walletAddresses.map((e) => String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.walletAddresses) {
      obj.walletAddresses = message.walletAddresses.map((e) => e);
    } else {
      obj.walletAddresses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePrivatePreferencesAction_DenyAddress();
    message.walletAddresses = ((_a = object.walletAddresses) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBasePrivatePreferencesAction_AllowInboxId() {
  return { inboxIds: [] };
}
var PrivatePreferencesAction_AllowInboxId = {
  encode(message, writer = import_minimal13.default.Writer.create()) {
    for (const v of message.inboxIds) {
      writer.uint32(10).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal13.default.Reader ? input : new import_minimal13.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivatePreferencesAction_AllowInboxId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.inboxIds.push(reader.string());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      inboxIds: Array.isArray(object === null || object === void 0 ? void 0 : object.inboxIds) ? object.inboxIds.map((e) => String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.inboxIds) {
      obj.inboxIds = message.inboxIds.map((e) => e);
    } else {
      obj.inboxIds = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePrivatePreferencesAction_AllowInboxId();
    message.inboxIds = ((_a = object.inboxIds) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBasePrivatePreferencesAction_DenyInboxId() {
  return { inboxIds: [] };
}
var PrivatePreferencesAction_DenyInboxId = {
  encode(message, writer = import_minimal13.default.Writer.create()) {
    for (const v of message.inboxIds) {
      writer.uint32(10).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal13.default.Reader ? input : new import_minimal13.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivatePreferencesAction_DenyInboxId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.inboxIds.push(reader.string());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      inboxIds: Array.isArray(object === null || object === void 0 ? void 0 : object.inboxIds) ? object.inboxIds.map((e) => String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.inboxIds) {
      obj.inboxIds = message.inboxIds.map((e) => e);
    } else {
      obj.inboxIds = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePrivatePreferencesAction_DenyInboxId();
    message.inboxIds = ((_a = object.inboxIds) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBasePrivatePreferencesAction_AllowGroup() {
  return { groupIds: [] };
}
var PrivatePreferencesAction_AllowGroup = {
  encode(message, writer = import_minimal13.default.Writer.create()) {
    for (const v of message.groupIds) {
      writer.uint32(10).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal13.default.Reader ? input : new import_minimal13.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivatePreferencesAction_AllowGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.groupIds.push(reader.string());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      groupIds: Array.isArray(object === null || object === void 0 ? void 0 : object.groupIds) ? object.groupIds.map((e) => String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.groupIds) {
      obj.groupIds = message.groupIds.map((e) => e);
    } else {
      obj.groupIds = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePrivatePreferencesAction_AllowGroup();
    message.groupIds = ((_a = object.groupIds) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBasePrivatePreferencesAction_DenyGroup() {
  return { groupIds: [] };
}
var PrivatePreferencesAction_DenyGroup = {
  encode(message, writer = import_minimal13.default.Writer.create()) {
    for (const v of message.groupIds) {
      writer.uint32(10).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal13.default.Reader ? input : new import_minimal13.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivatePreferencesAction_DenyGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.groupIds.push(reader.string());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      groupIds: Array.isArray(object === null || object === void 0 ? void 0 : object.groupIds) ? object.groupIds.map((e) => String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.groupIds) {
      obj.groupIds = message.groupIds.map((e) => e);
    } else {
      obj.groupIds = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePrivatePreferencesAction_DenyGroup();
    message.groupIds = ((_a = object.groupIds) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBasePrivatePreferencesPayload() {
  return { v1: void 0 };
}
var PrivatePreferencesPayload = {
  encode(message, writer = import_minimal13.default.Writer.create()) {
    if (message.v1 !== void 0) {
      Ciphertext.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal13.default.Reader ? input : new import_minimal13.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivatePreferencesPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = Ciphertext.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet13(object.v1) ? Ciphertext.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? Ciphertext.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBasePrivatePreferencesPayload();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? Ciphertext.fromPartial(object.v1) : void 0;
    return message;
  }
};
if (import_minimal13.default.util.Long !== long_default) {
  import_minimal13.default.util.Long = long_default;
  import_minimal13.default.configure();
}
function isSet13(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/keystore_api/v1/keystore.pb.js
var import_minimal14 = __toESM(require_minimal());
var protobufPackage14 = "xmtp.keystore_api.v1";
var ErrorCode;
(function(ErrorCode2) {
  ErrorCode2[ErrorCode2["ERROR_CODE_UNSPECIFIED"] = 0] = "ERROR_CODE_UNSPECIFIED";
  ErrorCode2[ErrorCode2["ERROR_CODE_INVALID_INPUT"] = 1] = "ERROR_CODE_INVALID_INPUT";
  ErrorCode2[ErrorCode2["ERROR_CODE_NO_MATCHING_PREKEY"] = 2] = "ERROR_CODE_NO_MATCHING_PREKEY";
  ErrorCode2[ErrorCode2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(ErrorCode || (ErrorCode = {}));
function errorCodeFromJSON(object) {
  switch (object) {
    case 0:
    case "ERROR_CODE_UNSPECIFIED":
      return ErrorCode.ERROR_CODE_UNSPECIFIED;
    case 1:
    case "ERROR_CODE_INVALID_INPUT":
      return ErrorCode.ERROR_CODE_INVALID_INPUT;
    case 2:
    case "ERROR_CODE_NO_MATCHING_PREKEY":
      return ErrorCode.ERROR_CODE_NO_MATCHING_PREKEY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ErrorCode.UNRECOGNIZED;
  }
}
function errorCodeToJSON(object) {
  switch (object) {
    case ErrorCode.ERROR_CODE_UNSPECIFIED:
      return "ERROR_CODE_UNSPECIFIED";
    case ErrorCode.ERROR_CODE_INVALID_INPUT:
      return "ERROR_CODE_INVALID_INPUT";
    case ErrorCode.ERROR_CODE_NO_MATCHING_PREKEY:
      return "ERROR_CODE_NO_MATCHING_PREKEY";
    case ErrorCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
var JobType;
(function(JobType2) {
  JobType2[JobType2["JOB_TYPE_UNSPECIFIED"] = 0] = "JOB_TYPE_UNSPECIFIED";
  JobType2[JobType2["JOB_TYPE_REFRESH_V1"] = 1] = "JOB_TYPE_REFRESH_V1";
  JobType2[JobType2["JOB_TYPE_REFRESH_V2"] = 2] = "JOB_TYPE_REFRESH_V2";
  JobType2[JobType2["JOB_TYPE_REFRESH_PPPP"] = 3] = "JOB_TYPE_REFRESH_PPPP";
  JobType2[JobType2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(JobType || (JobType = {}));
function jobTypeFromJSON(object) {
  switch (object) {
    case 0:
    case "JOB_TYPE_UNSPECIFIED":
      return JobType.JOB_TYPE_UNSPECIFIED;
    case 1:
    case "JOB_TYPE_REFRESH_V1":
      return JobType.JOB_TYPE_REFRESH_V1;
    case 2:
    case "JOB_TYPE_REFRESH_V2":
      return JobType.JOB_TYPE_REFRESH_V2;
    case 3:
    case "JOB_TYPE_REFRESH_PPPP":
      return JobType.JOB_TYPE_REFRESH_PPPP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return JobType.UNRECOGNIZED;
  }
}
function jobTypeToJSON(object) {
  switch (object) {
    case JobType.JOB_TYPE_UNSPECIFIED:
      return "JOB_TYPE_UNSPECIFIED";
    case JobType.JOB_TYPE_REFRESH_V1:
      return "JOB_TYPE_REFRESH_V1";
    case JobType.JOB_TYPE_REFRESH_V2:
      return "JOB_TYPE_REFRESH_V2";
    case JobType.JOB_TYPE_REFRESH_PPPP:
      return "JOB_TYPE_REFRESH_PPPP";
    case JobType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
var GetKeystoreStatusResponse_KeystoreStatus;
(function(GetKeystoreStatusResponse_KeystoreStatus2) {
  GetKeystoreStatusResponse_KeystoreStatus2[GetKeystoreStatusResponse_KeystoreStatus2["KEYSTORE_STATUS_UNSPECIFIED"] = 0] = "KEYSTORE_STATUS_UNSPECIFIED";
  GetKeystoreStatusResponse_KeystoreStatus2[GetKeystoreStatusResponse_KeystoreStatus2["KEYSTORE_STATUS_UNINITIALIZED"] = 1] = "KEYSTORE_STATUS_UNINITIALIZED";
  GetKeystoreStatusResponse_KeystoreStatus2[GetKeystoreStatusResponse_KeystoreStatus2["KEYSTORE_STATUS_INITIALIZED"] = 2] = "KEYSTORE_STATUS_INITIALIZED";
  GetKeystoreStatusResponse_KeystoreStatus2[GetKeystoreStatusResponse_KeystoreStatus2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(GetKeystoreStatusResponse_KeystoreStatus || (GetKeystoreStatusResponse_KeystoreStatus = {}));
function getKeystoreStatusResponse_KeystoreStatusFromJSON(object) {
  switch (object) {
    case 0:
    case "KEYSTORE_STATUS_UNSPECIFIED":
      return GetKeystoreStatusResponse_KeystoreStatus.KEYSTORE_STATUS_UNSPECIFIED;
    case 1:
    case "KEYSTORE_STATUS_UNINITIALIZED":
      return GetKeystoreStatusResponse_KeystoreStatus.KEYSTORE_STATUS_UNINITIALIZED;
    case 2:
    case "KEYSTORE_STATUS_INITIALIZED":
      return GetKeystoreStatusResponse_KeystoreStatus.KEYSTORE_STATUS_INITIALIZED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GetKeystoreStatusResponse_KeystoreStatus.UNRECOGNIZED;
  }
}
function getKeystoreStatusResponse_KeystoreStatusToJSON(object) {
  switch (object) {
    case GetKeystoreStatusResponse_KeystoreStatus.KEYSTORE_STATUS_UNSPECIFIED:
      return "KEYSTORE_STATUS_UNSPECIFIED";
    case GetKeystoreStatusResponse_KeystoreStatus.KEYSTORE_STATUS_UNINITIALIZED:
      return "KEYSTORE_STATUS_UNINITIALIZED";
    case GetKeystoreStatusResponse_KeystoreStatus.KEYSTORE_STATUS_INITIALIZED:
      return "KEYSTORE_STATUS_INITIALIZED";
    case GetKeystoreStatusResponse_KeystoreStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseKeystoreError() {
  return { message: "", code: 0 };
}
var KeystoreError = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    if (message.code !== 0) {
      writer.uint32(16).int32(message.code);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseKeystoreError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.message = reader.string();
          break;
        case 2:
          message.code = reader.int32();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      message: isSet14(object.message) ? String(object.message) : "",
      code: isSet14(object.code) ? errorCodeFromJSON(object.code) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.message !== void 0 && (obj.message = message.message);
    message.code !== void 0 && (obj.code = errorCodeToJSON(message.code));
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseKeystoreError();
    message.message = (_a = object.message) !== null && _a !== void 0 ? _a : "";
    message.code = (_b = object.code) !== null && _b !== void 0 ? _b : 0;
    return message;
  }
};
function createBaseDecryptV1Request() {
  return { requests: [] };
}
var DecryptV1Request = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.requests) {
      DecryptV1Request_Request.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDecryptV1Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.requests.push(DecryptV1Request_Request.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      requests: Array.isArray(object === null || object === void 0 ? void 0 : object.requests) ? object.requests.map((e) => DecryptV1Request_Request.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.requests) {
      obj.requests = message.requests.map((e) => e ? DecryptV1Request_Request.toJSON(e) : void 0);
    } else {
      obj.requests = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseDecryptV1Request();
    message.requests = ((_a = object.requests) === null || _a === void 0 ? void 0 : _a.map((e) => DecryptV1Request_Request.fromPartial(e))) || [];
    return message;
  }
};
function createBaseDecryptV1Request_Request() {
  return {
    payload: void 0,
    peerKeys: void 0,
    headerBytes: new Uint8Array(),
    isSender: false
  };
}
var DecryptV1Request_Request = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.payload !== void 0) {
      Ciphertext.encode(message.payload, writer.uint32(10).fork()).ldelim();
    }
    if (message.peerKeys !== void 0) {
      PublicKeyBundle.encode(message.peerKeys, writer.uint32(18).fork()).ldelim();
    }
    if (message.headerBytes.length !== 0) {
      writer.uint32(26).bytes(message.headerBytes);
    }
    if (message.isSender === true) {
      writer.uint32(32).bool(message.isSender);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDecryptV1Request_Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.payload = Ciphertext.decode(reader, reader.uint32());
          break;
        case 2:
          message.peerKeys = PublicKeyBundle.decode(reader, reader.uint32());
          break;
        case 3:
          message.headerBytes = reader.bytes();
          break;
        case 4:
          message.isSender = reader.bool();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      payload: isSet14(object.payload) ? Ciphertext.fromJSON(object.payload) : void 0,
      peerKeys: isSet14(object.peerKeys) ? PublicKeyBundle.fromJSON(object.peerKeys) : void 0,
      headerBytes: isSet14(object.headerBytes) ? bytesFromBase6410(object.headerBytes) : new Uint8Array(),
      isSender: isSet14(object.isSender) ? Boolean(object.isSender) : false
    };
  },
  toJSON(message) {
    const obj = {};
    message.payload !== void 0 && (obj.payload = message.payload ? Ciphertext.toJSON(message.payload) : void 0);
    message.peerKeys !== void 0 && (obj.peerKeys = message.peerKeys ? PublicKeyBundle.toJSON(message.peerKeys) : void 0);
    message.headerBytes !== void 0 && (obj.headerBytes = base64FromBytes10(message.headerBytes !== void 0 ? message.headerBytes : new Uint8Array()));
    message.isSender !== void 0 && (obj.isSender = message.isSender);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseDecryptV1Request_Request();
    message.payload = object.payload !== void 0 && object.payload !== null ? Ciphertext.fromPartial(object.payload) : void 0;
    message.peerKeys = object.peerKeys !== void 0 && object.peerKeys !== null ? PublicKeyBundle.fromPartial(object.peerKeys) : void 0;
    message.headerBytes = (_a = object.headerBytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.isSender = (_b = object.isSender) !== null && _b !== void 0 ? _b : false;
    return message;
  }
};
function createBaseDecryptResponse() {
  return { responses: [] };
}
var DecryptResponse = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.responses) {
      DecryptResponse_Response.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDecryptResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.responses.push(DecryptResponse_Response.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      responses: Array.isArray(object === null || object === void 0 ? void 0 : object.responses) ? object.responses.map((e) => DecryptResponse_Response.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.responses) {
      obj.responses = message.responses.map((e) => e ? DecryptResponse_Response.toJSON(e) : void 0);
    } else {
      obj.responses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseDecryptResponse();
    message.responses = ((_a = object.responses) === null || _a === void 0 ? void 0 : _a.map((e) => DecryptResponse_Response.fromPartial(e))) || [];
    return message;
  }
};
function createBaseDecryptResponse_Response() {
  return { result: void 0, error: void 0 };
}
var DecryptResponse_Response = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.result !== void 0) {
      DecryptResponse_Response_Success.encode(message.result, writer.uint32(10).fork()).ldelim();
    }
    if (message.error !== void 0) {
      KeystoreError.encode(message.error, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDecryptResponse_Response();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.result = DecryptResponse_Response_Success.decode(reader, reader.uint32());
          break;
        case 2:
          message.error = KeystoreError.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      result: isSet14(object.result) ? DecryptResponse_Response_Success.fromJSON(object.result) : void 0,
      error: isSet14(object.error) ? KeystoreError.fromJSON(object.error) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.result !== void 0 && (obj.result = message.result ? DecryptResponse_Response_Success.toJSON(message.result) : void 0);
    message.error !== void 0 && (obj.error = message.error ? KeystoreError.toJSON(message.error) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseDecryptResponse_Response();
    message.result = object.result !== void 0 && object.result !== null ? DecryptResponse_Response_Success.fromPartial(object.result) : void 0;
    message.error = object.error !== void 0 && object.error !== null ? KeystoreError.fromPartial(object.error) : void 0;
    return message;
  }
};
function createBaseDecryptResponse_Response_Success() {
  return { decrypted: new Uint8Array() };
}
var DecryptResponse_Response_Success = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.decrypted.length !== 0) {
      writer.uint32(10).bytes(message.decrypted);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDecryptResponse_Response_Success();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.decrypted = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      decrypted: isSet14(object.decrypted) ? bytesFromBase6410(object.decrypted) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.decrypted !== void 0 && (obj.decrypted = base64FromBytes10(message.decrypted !== void 0 ? message.decrypted : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseDecryptResponse_Response_Success();
    message.decrypted = (_a = object.decrypted) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseDecryptV2Request() {
  return { requests: [] };
}
var DecryptV2Request = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.requests) {
      DecryptV2Request_Request.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDecryptV2Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.requests.push(DecryptV2Request_Request.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      requests: Array.isArray(object === null || object === void 0 ? void 0 : object.requests) ? object.requests.map((e) => DecryptV2Request_Request.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.requests) {
      obj.requests = message.requests.map((e) => e ? DecryptV2Request_Request.toJSON(e) : void 0);
    } else {
      obj.requests = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseDecryptV2Request();
    message.requests = ((_a = object.requests) === null || _a === void 0 ? void 0 : _a.map((e) => DecryptV2Request_Request.fromPartial(e))) || [];
    return message;
  }
};
function createBaseDecryptV2Request_Request() {
  return {
    payload: void 0,
    headerBytes: new Uint8Array(),
    contentTopic: ""
  };
}
var DecryptV2Request_Request = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.payload !== void 0) {
      Ciphertext.encode(message.payload, writer.uint32(10).fork()).ldelim();
    }
    if (message.headerBytes.length !== 0) {
      writer.uint32(18).bytes(message.headerBytes);
    }
    if (message.contentTopic !== "") {
      writer.uint32(26).string(message.contentTopic);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDecryptV2Request_Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.payload = Ciphertext.decode(reader, reader.uint32());
          break;
        case 2:
          message.headerBytes = reader.bytes();
          break;
        case 3:
          message.contentTopic = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      payload: isSet14(object.payload) ? Ciphertext.fromJSON(object.payload) : void 0,
      headerBytes: isSet14(object.headerBytes) ? bytesFromBase6410(object.headerBytes) : new Uint8Array(),
      contentTopic: isSet14(object.contentTopic) ? String(object.contentTopic) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.payload !== void 0 && (obj.payload = message.payload ? Ciphertext.toJSON(message.payload) : void 0);
    message.headerBytes !== void 0 && (obj.headerBytes = base64FromBytes10(message.headerBytes !== void 0 ? message.headerBytes : new Uint8Array()));
    message.contentTopic !== void 0 && (obj.contentTopic = message.contentTopic);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseDecryptV2Request_Request();
    message.payload = object.payload !== void 0 && object.payload !== null ? Ciphertext.fromPartial(object.payload) : void 0;
    message.headerBytes = (_a = object.headerBytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.contentTopic = (_b = object.contentTopic) !== null && _b !== void 0 ? _b : "";
    return message;
  }
};
function createBaseEncryptV1Request() {
  return { requests: [] };
}
var EncryptV1Request = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.requests) {
      EncryptV1Request_Request.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncryptV1Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.requests.push(EncryptV1Request_Request.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      requests: Array.isArray(object === null || object === void 0 ? void 0 : object.requests) ? object.requests.map((e) => EncryptV1Request_Request.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.requests) {
      obj.requests = message.requests.map((e) => e ? EncryptV1Request_Request.toJSON(e) : void 0);
    } else {
      obj.requests = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseEncryptV1Request();
    message.requests = ((_a = object.requests) === null || _a === void 0 ? void 0 : _a.map((e) => EncryptV1Request_Request.fromPartial(e))) || [];
    return message;
  }
};
function createBaseEncryptV1Request_Request() {
  return {
    recipient: void 0,
    payload: new Uint8Array(),
    headerBytes: new Uint8Array()
  };
}
var EncryptV1Request_Request = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.recipient !== void 0) {
      PublicKeyBundle.encode(message.recipient, writer.uint32(10).fork()).ldelim();
    }
    if (message.payload.length !== 0) {
      writer.uint32(18).bytes(message.payload);
    }
    if (message.headerBytes.length !== 0) {
      writer.uint32(26).bytes(message.headerBytes);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncryptV1Request_Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.recipient = PublicKeyBundle.decode(reader, reader.uint32());
          break;
        case 2:
          message.payload = reader.bytes();
          break;
        case 3:
          message.headerBytes = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      recipient: isSet14(object.recipient) ? PublicKeyBundle.fromJSON(object.recipient) : void 0,
      payload: isSet14(object.payload) ? bytesFromBase6410(object.payload) : new Uint8Array(),
      headerBytes: isSet14(object.headerBytes) ? bytesFromBase6410(object.headerBytes) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.recipient !== void 0 && (obj.recipient = message.recipient ? PublicKeyBundle.toJSON(message.recipient) : void 0);
    message.payload !== void 0 && (obj.payload = base64FromBytes10(message.payload !== void 0 ? message.payload : new Uint8Array()));
    message.headerBytes !== void 0 && (obj.headerBytes = base64FromBytes10(message.headerBytes !== void 0 ? message.headerBytes : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseEncryptV1Request_Request();
    message.recipient = object.recipient !== void 0 && object.recipient !== null ? PublicKeyBundle.fromPartial(object.recipient) : void 0;
    message.payload = (_a = object.payload) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.headerBytes = (_b = object.headerBytes) !== null && _b !== void 0 ? _b : new Uint8Array();
    return message;
  }
};
function createBaseEncryptResponse() {
  return { responses: [] };
}
var EncryptResponse = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.responses) {
      EncryptResponse_Response.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncryptResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.responses.push(EncryptResponse_Response.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      responses: Array.isArray(object === null || object === void 0 ? void 0 : object.responses) ? object.responses.map((e) => EncryptResponse_Response.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.responses) {
      obj.responses = message.responses.map((e) => e ? EncryptResponse_Response.toJSON(e) : void 0);
    } else {
      obj.responses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseEncryptResponse();
    message.responses = ((_a = object.responses) === null || _a === void 0 ? void 0 : _a.map((e) => EncryptResponse_Response.fromPartial(e))) || [];
    return message;
  }
};
function createBaseEncryptResponse_Response() {
  return { result: void 0, error: void 0 };
}
var EncryptResponse_Response = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.result !== void 0) {
      EncryptResponse_Response_Success.encode(message.result, writer.uint32(10).fork()).ldelim();
    }
    if (message.error !== void 0) {
      KeystoreError.encode(message.error, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncryptResponse_Response();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.result = EncryptResponse_Response_Success.decode(reader, reader.uint32());
          break;
        case 2:
          message.error = KeystoreError.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      result: isSet14(object.result) ? EncryptResponse_Response_Success.fromJSON(object.result) : void 0,
      error: isSet14(object.error) ? KeystoreError.fromJSON(object.error) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.result !== void 0 && (obj.result = message.result ? EncryptResponse_Response_Success.toJSON(message.result) : void 0);
    message.error !== void 0 && (obj.error = message.error ? KeystoreError.toJSON(message.error) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseEncryptResponse_Response();
    message.result = object.result !== void 0 && object.result !== null ? EncryptResponse_Response_Success.fromPartial(object.result) : void 0;
    message.error = object.error !== void 0 && object.error !== null ? KeystoreError.fromPartial(object.error) : void 0;
    return message;
  }
};
function createBaseEncryptResponse_Response_Success() {
  return { encrypted: void 0, senderHmac: new Uint8Array() };
}
var EncryptResponse_Response_Success = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.encrypted !== void 0) {
      Ciphertext.encode(message.encrypted, writer.uint32(10).fork()).ldelim();
    }
    if (message.senderHmac.length !== 0) {
      writer.uint32(18).bytes(message.senderHmac);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncryptResponse_Response_Success();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.encrypted = Ciphertext.decode(reader, reader.uint32());
          break;
        case 2:
          message.senderHmac = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      encrypted: isSet14(object.encrypted) ? Ciphertext.fromJSON(object.encrypted) : void 0,
      senderHmac: isSet14(object.senderHmac) ? bytesFromBase6410(object.senderHmac) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.encrypted !== void 0 && (obj.encrypted = message.encrypted ? Ciphertext.toJSON(message.encrypted) : void 0);
    message.senderHmac !== void 0 && (obj.senderHmac = base64FromBytes10(message.senderHmac !== void 0 ? message.senderHmac : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseEncryptResponse_Response_Success();
    message.encrypted = object.encrypted !== void 0 && object.encrypted !== null ? Ciphertext.fromPartial(object.encrypted) : void 0;
    message.senderHmac = (_a = object.senderHmac) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseEncryptV2Request() {
  return { requests: [] };
}
var EncryptV2Request = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.requests) {
      EncryptV2Request_Request.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncryptV2Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.requests.push(EncryptV2Request_Request.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      requests: Array.isArray(object === null || object === void 0 ? void 0 : object.requests) ? object.requests.map((e) => EncryptV2Request_Request.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.requests) {
      obj.requests = message.requests.map((e) => e ? EncryptV2Request_Request.toJSON(e) : void 0);
    } else {
      obj.requests = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseEncryptV2Request();
    message.requests = ((_a = object.requests) === null || _a === void 0 ? void 0 : _a.map((e) => EncryptV2Request_Request.fromPartial(e))) || [];
    return message;
  }
};
function createBaseEncryptV2Request_Request() {
  return {
    payload: new Uint8Array(),
    headerBytes: new Uint8Array(),
    contentTopic: ""
  };
}
var EncryptV2Request_Request = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.headerBytes.length !== 0) {
      writer.uint32(18).bytes(message.headerBytes);
    }
    if (message.contentTopic !== "") {
      writer.uint32(26).string(message.contentTopic);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncryptV2Request_Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.payload = reader.bytes();
          break;
        case 2:
          message.headerBytes = reader.bytes();
          break;
        case 3:
          message.contentTopic = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      payload: isSet14(object.payload) ? bytesFromBase6410(object.payload) : new Uint8Array(),
      headerBytes: isSet14(object.headerBytes) ? bytesFromBase6410(object.headerBytes) : new Uint8Array(),
      contentTopic: isSet14(object.contentTopic) ? String(object.contentTopic) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.payload !== void 0 && (obj.payload = base64FromBytes10(message.payload !== void 0 ? message.payload : new Uint8Array()));
    message.headerBytes !== void 0 && (obj.headerBytes = base64FromBytes10(message.headerBytes !== void 0 ? message.headerBytes : new Uint8Array()));
    message.contentTopic !== void 0 && (obj.contentTopic = message.contentTopic);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseEncryptV2Request_Request();
    message.payload = (_a = object.payload) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.headerBytes = (_b = object.headerBytes) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.contentTopic = (_c = object.contentTopic) !== null && _c !== void 0 ? _c : "";
    return message;
  }
};
function createBaseSelfEncryptRequest() {
  return { requests: [] };
}
var SelfEncryptRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.requests) {
      SelfEncryptRequest_Request.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSelfEncryptRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.requests.push(SelfEncryptRequest_Request.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      requests: Array.isArray(object === null || object === void 0 ? void 0 : object.requests) ? object.requests.map((e) => SelfEncryptRequest_Request.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.requests) {
      obj.requests = message.requests.map((e) => e ? SelfEncryptRequest_Request.toJSON(e) : void 0);
    } else {
      obj.requests = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSelfEncryptRequest();
    message.requests = ((_a = object.requests) === null || _a === void 0 ? void 0 : _a.map((e) => SelfEncryptRequest_Request.fromPartial(e))) || [];
    return message;
  }
};
function createBaseSelfEncryptRequest_Request() {
  return { payload: new Uint8Array() };
}
var SelfEncryptRequest_Request = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSelfEncryptRequest_Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.payload = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      payload: isSet14(object.payload) ? bytesFromBase6410(object.payload) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.payload !== void 0 && (obj.payload = base64FromBytes10(message.payload !== void 0 ? message.payload : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSelfEncryptRequest_Request();
    message.payload = (_a = object.payload) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseSelfEncryptResponse() {
  return { responses: [] };
}
var SelfEncryptResponse = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.responses) {
      SelfEncryptResponse_Response.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSelfEncryptResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.responses.push(SelfEncryptResponse_Response.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      responses: Array.isArray(object === null || object === void 0 ? void 0 : object.responses) ? object.responses.map((e) => SelfEncryptResponse_Response.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.responses) {
      obj.responses = message.responses.map((e) => e ? SelfEncryptResponse_Response.toJSON(e) : void 0);
    } else {
      obj.responses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSelfEncryptResponse();
    message.responses = ((_a = object.responses) === null || _a === void 0 ? void 0 : _a.map((e) => SelfEncryptResponse_Response.fromPartial(e))) || [];
    return message;
  }
};
function createBaseSelfEncryptResponse_Response() {
  return { result: void 0, error: void 0 };
}
var SelfEncryptResponse_Response = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.result !== void 0) {
      SelfEncryptResponse_Response_Success.encode(message.result, writer.uint32(10).fork()).ldelim();
    }
    if (message.error !== void 0) {
      KeystoreError.encode(message.error, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSelfEncryptResponse_Response();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.result = SelfEncryptResponse_Response_Success.decode(reader, reader.uint32());
          break;
        case 2:
          message.error = KeystoreError.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      result: isSet14(object.result) ? SelfEncryptResponse_Response_Success.fromJSON(object.result) : void 0,
      error: isSet14(object.error) ? KeystoreError.fromJSON(object.error) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.result !== void 0 && (obj.result = message.result ? SelfEncryptResponse_Response_Success.toJSON(message.result) : void 0);
    message.error !== void 0 && (obj.error = message.error ? KeystoreError.toJSON(message.error) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseSelfEncryptResponse_Response();
    message.result = object.result !== void 0 && object.result !== null ? SelfEncryptResponse_Response_Success.fromPartial(object.result) : void 0;
    message.error = object.error !== void 0 && object.error !== null ? KeystoreError.fromPartial(object.error) : void 0;
    return message;
  }
};
function createBaseSelfEncryptResponse_Response_Success() {
  return { encrypted: new Uint8Array() };
}
var SelfEncryptResponse_Response_Success = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.encrypted.length !== 0) {
      writer.uint32(10).bytes(message.encrypted);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSelfEncryptResponse_Response_Success();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.encrypted = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      encrypted: isSet14(object.encrypted) ? bytesFromBase6410(object.encrypted) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.encrypted !== void 0 && (obj.encrypted = base64FromBytes10(message.encrypted !== void 0 ? message.encrypted : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSelfEncryptResponse_Response_Success();
    message.encrypted = (_a = object.encrypted) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseSelfDecryptRequest() {
  return { requests: [] };
}
var SelfDecryptRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.requests) {
      SelfDecryptRequest_Request.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSelfDecryptRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.requests.push(SelfDecryptRequest_Request.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      requests: Array.isArray(object === null || object === void 0 ? void 0 : object.requests) ? object.requests.map((e) => SelfDecryptRequest_Request.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.requests) {
      obj.requests = message.requests.map((e) => e ? SelfDecryptRequest_Request.toJSON(e) : void 0);
    } else {
      obj.requests = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSelfDecryptRequest();
    message.requests = ((_a = object.requests) === null || _a === void 0 ? void 0 : _a.map((e) => SelfDecryptRequest_Request.fromPartial(e))) || [];
    return message;
  }
};
function createBaseSelfDecryptRequest_Request() {
  return { payload: new Uint8Array() };
}
var SelfDecryptRequest_Request = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSelfDecryptRequest_Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.payload = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      payload: isSet14(object.payload) ? bytesFromBase6410(object.payload) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.payload !== void 0 && (obj.payload = base64FromBytes10(message.payload !== void 0 ? message.payload : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSelfDecryptRequest_Request();
    message.payload = (_a = object.payload) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseGetPrivatePreferencesTopicIdentifierResponse() {
  return { identifier: "" };
}
var GetPrivatePreferencesTopicIdentifierResponse = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.identifier !== "") {
      writer.uint32(10).string(message.identifier);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetPrivatePreferencesTopicIdentifierResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.identifier = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      identifier: isSet14(object.identifier) ? String(object.identifier) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.identifier !== void 0 && (obj.identifier = message.identifier);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetPrivatePreferencesTopicIdentifierResponse();
    message.identifier = (_a = object.identifier) !== null && _a !== void 0 ? _a : "";
    return message;
  }
};
function createBaseCreateInviteRequest() {
  return {
    context: void 0,
    recipient: void 0,
    createdNs: long_default.UZERO,
    consentProof: void 0
  };
}
var CreateInviteRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.context !== void 0) {
      InvitationV1_Context.encode(message.context, writer.uint32(10).fork()).ldelim();
    }
    if (message.recipient !== void 0) {
      SignedPublicKeyBundle.encode(message.recipient, writer.uint32(18).fork()).ldelim();
    }
    if (!message.createdNs.isZero()) {
      writer.uint32(24).uint64(message.createdNs);
    }
    if (message.consentProof !== void 0) {
      ConsentProofPayload.encode(message.consentProof, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateInviteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.context = InvitationV1_Context.decode(reader, reader.uint32());
          break;
        case 2:
          message.recipient = SignedPublicKeyBundle.decode(reader, reader.uint32());
          break;
        case 3:
          message.createdNs = reader.uint64();
          break;
        case 4:
          message.consentProof = ConsentProofPayload.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      context: isSet14(object.context) ? InvitationV1_Context.fromJSON(object.context) : void 0,
      recipient: isSet14(object.recipient) ? SignedPublicKeyBundle.fromJSON(object.recipient) : void 0,
      createdNs: isSet14(object.createdNs) ? long_default.fromValue(object.createdNs) : long_default.UZERO,
      consentProof: isSet14(object.consentProof) ? ConsentProofPayload.fromJSON(object.consentProof) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.context !== void 0 && (obj.context = message.context ? InvitationV1_Context.toJSON(message.context) : void 0);
    message.recipient !== void 0 && (obj.recipient = message.recipient ? SignedPublicKeyBundle.toJSON(message.recipient) : void 0);
    message.createdNs !== void 0 && (obj.createdNs = (message.createdNs || long_default.UZERO).toString());
    message.consentProof !== void 0 && (obj.consentProof = message.consentProof ? ConsentProofPayload.toJSON(message.consentProof) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseCreateInviteRequest();
    message.context = object.context !== void 0 && object.context !== null ? InvitationV1_Context.fromPartial(object.context) : void 0;
    message.recipient = object.recipient !== void 0 && object.recipient !== null ? SignedPublicKeyBundle.fromPartial(object.recipient) : void 0;
    message.createdNs = object.createdNs !== void 0 && object.createdNs !== null ? long_default.fromValue(object.createdNs) : long_default.UZERO;
    message.consentProof = object.consentProof !== void 0 && object.consentProof !== null ? ConsentProofPayload.fromPartial(object.consentProof) : void 0;
    return message;
  }
};
function createBaseCreateInviteResponse() {
  return { conversation: void 0, payload: new Uint8Array() };
}
var CreateInviteResponse = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.conversation !== void 0) {
      ConversationReference.encode(message.conversation, writer.uint32(10).fork()).ldelim();
    }
    if (message.payload.length !== 0) {
      writer.uint32(18).bytes(message.payload);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateInviteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.conversation = ConversationReference.decode(reader, reader.uint32());
          break;
        case 2:
          message.payload = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      conversation: isSet14(object.conversation) ? ConversationReference.fromJSON(object.conversation) : void 0,
      payload: isSet14(object.payload) ? bytesFromBase6410(object.payload) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.conversation !== void 0 && (obj.conversation = message.conversation ? ConversationReference.toJSON(message.conversation) : void 0);
    message.payload !== void 0 && (obj.payload = base64FromBytes10(message.payload !== void 0 ? message.payload : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseCreateInviteResponse();
    message.conversation = object.conversation !== void 0 && object.conversation !== null ? ConversationReference.fromPartial(object.conversation) : void 0;
    message.payload = (_a = object.payload) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseSaveInvitesRequest() {
  return { requests: [] };
}
var SaveInvitesRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.requests) {
      SaveInvitesRequest_Request.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSaveInvitesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.requests.push(SaveInvitesRequest_Request.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      requests: Array.isArray(object === null || object === void 0 ? void 0 : object.requests) ? object.requests.map((e) => SaveInvitesRequest_Request.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.requests) {
      obj.requests = message.requests.map((e) => e ? SaveInvitesRequest_Request.toJSON(e) : void 0);
    } else {
      obj.requests = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSaveInvitesRequest();
    message.requests = ((_a = object.requests) === null || _a === void 0 ? void 0 : _a.map((e) => SaveInvitesRequest_Request.fromPartial(e))) || [];
    return message;
  }
};
function createBaseSaveInvitesRequest_Request() {
  return {
    contentTopic: "",
    timestampNs: long_default.UZERO,
    payload: new Uint8Array()
  };
}
var SaveInvitesRequest_Request = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.contentTopic !== "") {
      writer.uint32(10).string(message.contentTopic);
    }
    if (!message.timestampNs.isZero()) {
      writer.uint32(16).uint64(message.timestampNs);
    }
    if (message.payload.length !== 0) {
      writer.uint32(26).bytes(message.payload);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSaveInvitesRequest_Request();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.contentTopic = reader.string();
          break;
        case 2:
          message.timestampNs = reader.uint64();
          break;
        case 3:
          message.payload = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      contentTopic: isSet14(object.contentTopic) ? String(object.contentTopic) : "",
      timestampNs: isSet14(object.timestampNs) ? long_default.fromValue(object.timestampNs) : long_default.UZERO,
      payload: isSet14(object.payload) ? bytesFromBase6410(object.payload) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.contentTopic !== void 0 && (obj.contentTopic = message.contentTopic);
    message.timestampNs !== void 0 && (obj.timestampNs = (message.timestampNs || long_default.UZERO).toString());
    message.payload !== void 0 && (obj.payload = base64FromBytes10(message.payload !== void 0 ? message.payload : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseSaveInvitesRequest_Request();
    message.contentTopic = (_a = object.contentTopic) !== null && _a !== void 0 ? _a : "";
    message.timestampNs = object.timestampNs !== void 0 && object.timestampNs !== null ? long_default.fromValue(object.timestampNs) : long_default.UZERO;
    message.payload = (_b = object.payload) !== null && _b !== void 0 ? _b : new Uint8Array();
    return message;
  }
};
function createBaseSaveInvitesResponse() {
  return { responses: [] };
}
var SaveInvitesResponse = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.responses) {
      SaveInvitesResponse_Response.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSaveInvitesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.responses.push(SaveInvitesResponse_Response.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      responses: Array.isArray(object === null || object === void 0 ? void 0 : object.responses) ? object.responses.map((e) => SaveInvitesResponse_Response.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.responses) {
      obj.responses = message.responses.map((e) => e ? SaveInvitesResponse_Response.toJSON(e) : void 0);
    } else {
      obj.responses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSaveInvitesResponse();
    message.responses = ((_a = object.responses) === null || _a === void 0 ? void 0 : _a.map((e) => SaveInvitesResponse_Response.fromPartial(e))) || [];
    return message;
  }
};
function createBaseSaveInvitesResponse_Response() {
  return { result: void 0, error: void 0 };
}
var SaveInvitesResponse_Response = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.result !== void 0) {
      SaveInvitesResponse_Response_Success.encode(message.result, writer.uint32(10).fork()).ldelim();
    }
    if (message.error !== void 0) {
      KeystoreError.encode(message.error, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSaveInvitesResponse_Response();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.result = SaveInvitesResponse_Response_Success.decode(reader, reader.uint32());
          break;
        case 2:
          message.error = KeystoreError.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      result: isSet14(object.result) ? SaveInvitesResponse_Response_Success.fromJSON(object.result) : void 0,
      error: isSet14(object.error) ? KeystoreError.fromJSON(object.error) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.result !== void 0 && (obj.result = message.result ? SaveInvitesResponse_Response_Success.toJSON(message.result) : void 0);
    message.error !== void 0 && (obj.error = message.error ? KeystoreError.toJSON(message.error) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseSaveInvitesResponse_Response();
    message.result = object.result !== void 0 && object.result !== null ? SaveInvitesResponse_Response_Success.fromPartial(object.result) : void 0;
    message.error = object.error !== void 0 && object.error !== null ? KeystoreError.fromPartial(object.error) : void 0;
    return message;
  }
};
function createBaseSaveInvitesResponse_Response_Success() {
  return { conversation: void 0 };
}
var SaveInvitesResponse_Response_Success = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.conversation !== void 0) {
      ConversationReference.encode(message.conversation, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSaveInvitesResponse_Response_Success();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.conversation = ConversationReference.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      conversation: isSet14(object.conversation) ? ConversationReference.fromJSON(object.conversation) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.conversation !== void 0 && (obj.conversation = message.conversation ? ConversationReference.toJSON(message.conversation) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseSaveInvitesResponse_Response_Success();
    message.conversation = object.conversation !== void 0 && object.conversation !== null ? ConversationReference.fromPartial(object.conversation) : void 0;
    return message;
  }
};
function createBaseCreateAuthTokenRequest() {
  return { timestampNs: void 0 };
}
var CreateAuthTokenRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.timestampNs !== void 0) {
      writer.uint32(8).uint64(message.timestampNs);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateAuthTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.timestampNs = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      timestampNs: isSet14(object.timestampNs) ? long_default.fromValue(object.timestampNs) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.timestampNs !== void 0 && (obj.timestampNs = (message.timestampNs || void 0).toString());
    return obj;
  },
  fromPartial(object) {
    const message = createBaseCreateAuthTokenRequest();
    message.timestampNs = object.timestampNs !== void 0 && object.timestampNs !== null ? long_default.fromValue(object.timestampNs) : void 0;
    return message;
  }
};
function createBaseSaveV1ConversationsRequest() {
  return { conversations: [] };
}
var SaveV1ConversationsRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.conversations) {
      ConversationReference.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSaveV1ConversationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.conversations.push(ConversationReference.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      conversations: Array.isArray(object === null || object === void 0 ? void 0 : object.conversations) ? object.conversations.map((e) => ConversationReference.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.conversations) {
      obj.conversations = message.conversations.map((e) => e ? ConversationReference.toJSON(e) : void 0);
    } else {
      obj.conversations = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSaveV1ConversationsRequest();
    message.conversations = ((_a = object.conversations) === null || _a === void 0 ? void 0 : _a.map((e) => ConversationReference.fromPartial(e))) || [];
    return message;
  }
};
function createBaseSaveV1ConversationsResponse() {
  return {};
}
var SaveV1ConversationsResponse = {
  encode(_, writer = import_minimal14.default.Writer.create()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSaveV1ConversationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  fromPartial(_) {
    const message = createBaseSaveV1ConversationsResponse();
    return message;
  }
};
function createBaseGetConversationsResponse() {
  return { conversations: [] };
}
var GetConversationsResponse = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.conversations) {
      ConversationReference.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetConversationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.conversations.push(ConversationReference.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      conversations: Array.isArray(object === null || object === void 0 ? void 0 : object.conversations) ? object.conversations.map((e) => ConversationReference.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.conversations) {
      obj.conversations = message.conversations.map((e) => e ? ConversationReference.toJSON(e) : void 0);
    } else {
      obj.conversations = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetConversationsResponse();
    message.conversations = ((_a = object.conversations) === null || _a === void 0 ? void 0 : _a.map((e) => ConversationReference.fromPartial(e))) || [];
    return message;
  }
};
function createBaseGetKeystoreStatusRequest() {
  return { walletAddress: "" };
}
var GetKeystoreStatusRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.walletAddress !== "") {
      writer.uint32(10).string(message.walletAddress);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetKeystoreStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.walletAddress = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      walletAddress: isSet14(object.walletAddress) ? String(object.walletAddress) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.walletAddress !== void 0 && (obj.walletAddress = message.walletAddress);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetKeystoreStatusRequest();
    message.walletAddress = (_a = object.walletAddress) !== null && _a !== void 0 ? _a : "";
    return message;
  }
};
function createBaseGetKeystoreStatusResponse() {
  return { status: 0 };
}
var GetKeystoreStatusResponse = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetKeystoreStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.status = reader.int32();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      status: isSet14(object.status) ? getKeystoreStatusResponse_KeystoreStatusFromJSON(object.status) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.status !== void 0 && (obj.status = getKeystoreStatusResponse_KeystoreStatusToJSON(message.status));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetKeystoreStatusResponse();
    message.status = (_a = object.status) !== null && _a !== void 0 ? _a : 0;
    return message;
  }
};
function createBaseInitKeystoreRequest() {
  return { v1: void 0 };
}
var InitKeystoreRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.v1 !== void 0) {
      PrivateKeyBundleV1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInitKeystoreRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = PrivateKeyBundleV1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet14(object.v1) ? PrivateKeyBundleV1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? PrivateKeyBundleV1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseInitKeystoreRequest();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? PrivateKeyBundleV1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseInitKeystoreResponse() {
  return { error: void 0 };
}
var InitKeystoreResponse = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.error !== void 0) {
      KeystoreError.encode(message.error, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInitKeystoreResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.error = KeystoreError.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      error: isSet14(object.error) ? KeystoreError.fromJSON(object.error) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.error !== void 0 && (obj.error = message.error ? KeystoreError.toJSON(message.error) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseInitKeystoreResponse();
    message.error = object.error !== void 0 && object.error !== null ? KeystoreError.fromPartial(object.error) : void 0;
    return message;
  }
};
function createBaseSignDigestRequest() {
  return {
    digest: new Uint8Array(),
    identityKey: void 0,
    prekeyIndex: void 0
  };
}
var SignDigestRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.digest.length !== 0) {
      writer.uint32(10).bytes(message.digest);
    }
    if (message.identityKey !== void 0) {
      writer.uint32(16).bool(message.identityKey);
    }
    if (message.prekeyIndex !== void 0) {
      writer.uint32(24).uint32(message.prekeyIndex);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignDigestRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.digest = reader.bytes();
          break;
        case 2:
          message.identityKey = reader.bool();
          break;
        case 3:
          message.prekeyIndex = reader.uint32();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      digest: isSet14(object.digest) ? bytesFromBase6410(object.digest) : new Uint8Array(),
      identityKey: isSet14(object.identityKey) ? Boolean(object.identityKey) : void 0,
      prekeyIndex: isSet14(object.prekeyIndex) ? Number(object.prekeyIndex) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.digest !== void 0 && (obj.digest = base64FromBytes10(message.digest !== void 0 ? message.digest : new Uint8Array()));
    message.identityKey !== void 0 && (obj.identityKey = message.identityKey);
    message.prekeyIndex !== void 0 && (obj.prekeyIndex = Math.round(message.prekeyIndex));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseSignDigestRequest();
    message.digest = (_a = object.digest) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.identityKey = (_b = object.identityKey) !== null && _b !== void 0 ? _b : void 0;
    message.prekeyIndex = (_c = object.prekeyIndex) !== null && _c !== void 0 ? _c : void 0;
    return message;
  }
};
function createBaseGetRefreshJobRequest() {
  return { jobType: 0 };
}
var GetRefreshJobRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.jobType !== 0) {
      writer.uint32(8).int32(message.jobType);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetRefreshJobRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.jobType = reader.int32();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      jobType: isSet14(object.jobType) ? jobTypeFromJSON(object.jobType) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.jobType !== void 0 && (obj.jobType = jobTypeToJSON(message.jobType));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetRefreshJobRequest();
    message.jobType = (_a = object.jobType) !== null && _a !== void 0 ? _a : 0;
    return message;
  }
};
function createBaseGetRefreshJobResponse() {
  return { lastRunNs: long_default.ZERO };
}
var GetRefreshJobResponse = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (!message.lastRunNs.isZero()) {
      writer.uint32(8).int64(message.lastRunNs);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetRefreshJobResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.lastRunNs = reader.int64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      lastRunNs: isSet14(object.lastRunNs) ? long_default.fromValue(object.lastRunNs) : long_default.ZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.lastRunNs !== void 0 && (obj.lastRunNs = (message.lastRunNs || long_default.ZERO).toString());
    return obj;
  },
  fromPartial(object) {
    const message = createBaseGetRefreshJobResponse();
    message.lastRunNs = object.lastRunNs !== void 0 && object.lastRunNs !== null ? long_default.fromValue(object.lastRunNs) : long_default.ZERO;
    return message;
  }
};
function createBaseSetRefeshJobRequest() {
  return { jobType: 0, lastRunNs: long_default.ZERO };
}
var SetRefeshJobRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.jobType !== 0) {
      writer.uint32(8).int32(message.jobType);
    }
    if (!message.lastRunNs.isZero()) {
      writer.uint32(16).int64(message.lastRunNs);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetRefeshJobRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.jobType = reader.int32();
          break;
        case 2:
          message.lastRunNs = reader.int64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      jobType: isSet14(object.jobType) ? jobTypeFromJSON(object.jobType) : 0,
      lastRunNs: isSet14(object.lastRunNs) ? long_default.fromValue(object.lastRunNs) : long_default.ZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.jobType !== void 0 && (obj.jobType = jobTypeToJSON(message.jobType));
    message.lastRunNs !== void 0 && (obj.lastRunNs = (message.lastRunNs || long_default.ZERO).toString());
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSetRefeshJobRequest();
    message.jobType = (_a = object.jobType) !== null && _a !== void 0 ? _a : 0;
    message.lastRunNs = object.lastRunNs !== void 0 && object.lastRunNs !== null ? long_default.fromValue(object.lastRunNs) : long_default.ZERO;
    return message;
  }
};
function createBaseSetRefreshJobResponse() {
  return {};
}
var SetRefreshJobResponse = {
  encode(_, writer = import_minimal14.default.Writer.create()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetRefreshJobResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  fromPartial(_) {
    const message = createBaseSetRefreshJobResponse();
    return message;
  }
};
function createBaseTopicMap() {
  return { topics: {} };
}
var TopicMap = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    Object.entries(message.topics).forEach(([key, value]) => {
      TopicMap_TopicsEntry.encode({ key, value }, writer.uint32(10).fork()).ldelim();
    });
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTopicMap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          const entry1 = TopicMap_TopicsEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.topics[entry1.key] = entry1.value;
          }
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      topics: isObject3(object.topics) ? Object.entries(object.topics).reduce((acc, [key, value]) => {
        acc[key] = TopicMap_TopicData.fromJSON(value);
        return acc;
      }, {}) : {}
    };
  },
  toJSON(message) {
    const obj = {};
    obj.topics = {};
    if (message.topics) {
      Object.entries(message.topics).forEach(([k, v]) => {
        obj.topics[k] = TopicMap_TopicData.toJSON(v);
      });
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseTopicMap();
    message.topics = Object.entries((_a = object.topics) !== null && _a !== void 0 ? _a : {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = TopicMap_TopicData.fromPartial(value);
      }
      return acc;
    }, {});
    return message;
  }
};
function createBaseTopicMap_TopicData() {
  return { createdNs: long_default.UZERO, peerAddress: "", invitation: void 0 };
}
var TopicMap_TopicData = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (!message.createdNs.isZero()) {
      writer.uint32(8).uint64(message.createdNs);
    }
    if (message.peerAddress !== "") {
      writer.uint32(18).string(message.peerAddress);
    }
    if (message.invitation !== void 0) {
      InvitationV1.encode(message.invitation, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTopicMap_TopicData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.createdNs = reader.uint64();
          break;
        case 2:
          message.peerAddress = reader.string();
          break;
        case 3:
          message.invitation = InvitationV1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      createdNs: isSet14(object.createdNs) ? long_default.fromValue(object.createdNs) : long_default.UZERO,
      peerAddress: isSet14(object.peerAddress) ? String(object.peerAddress) : "",
      invitation: isSet14(object.invitation) ? InvitationV1.fromJSON(object.invitation) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.createdNs !== void 0 && (obj.createdNs = (message.createdNs || long_default.UZERO).toString());
    message.peerAddress !== void 0 && (obj.peerAddress = message.peerAddress);
    message.invitation !== void 0 && (obj.invitation = message.invitation ? InvitationV1.toJSON(message.invitation) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseTopicMap_TopicData();
    message.createdNs = object.createdNs !== void 0 && object.createdNs !== null ? long_default.fromValue(object.createdNs) : long_default.UZERO;
    message.peerAddress = (_a = object.peerAddress) !== null && _a !== void 0 ? _a : "";
    message.invitation = object.invitation !== void 0 && object.invitation !== null ? InvitationV1.fromPartial(object.invitation) : void 0;
    return message;
  }
};
function createBaseTopicMap_TopicsEntry() {
  return { key: "", value: void 0 };
}
var TopicMap_TopicsEntry = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== void 0) {
      TopicMap_TopicData.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseTopicMap_TopicsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.key = reader.string();
          break;
        case 2:
          message.value = TopicMap_TopicData.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet14(object.key) ? String(object.key) : "",
      value: isSet14(object.value) ? TopicMap_TopicData.fromJSON(object.value) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.key !== void 0 && (obj.key = message.key);
    message.value !== void 0 && (obj.value = message.value ? TopicMap_TopicData.toJSON(message.value) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseTopicMap_TopicsEntry();
    message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
    message.value = object.value !== void 0 && object.value !== null ? TopicMap_TopicData.fromPartial(object.value) : void 0;
    return message;
  }
};
function createBaseGetConversationHmacKeysRequest() {
  return { topics: [] };
}
var GetConversationHmacKeysRequest = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.topics) {
      writer.uint32(10).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetConversationHmacKeysRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.topics.push(reader.string());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      topics: Array.isArray(object === null || object === void 0 ? void 0 : object.topics) ? object.topics.map((e) => String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.topics) {
      obj.topics = message.topics.map((e) => e);
    } else {
      obj.topics = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetConversationHmacKeysRequest();
    message.topics = ((_a = object.topics) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBaseGetConversationHmacKeysResponse() {
  return { hmacKeys: {} };
}
var GetConversationHmacKeysResponse = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    Object.entries(message.hmacKeys).forEach(([key, value]) => {
      GetConversationHmacKeysResponse_HmacKeysEntry.encode({ key, value }, writer.uint32(10).fork()).ldelim();
    });
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetConversationHmacKeysResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          const entry1 = GetConversationHmacKeysResponse_HmacKeysEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.hmacKeys[entry1.key] = entry1.value;
          }
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      hmacKeys: isObject3(object.hmacKeys) ? Object.entries(object.hmacKeys).reduce((acc, [key, value]) => {
        acc[key] = GetConversationHmacKeysResponse_HmacKeys.fromJSON(value);
        return acc;
      }, {}) : {}
    };
  },
  toJSON(message) {
    const obj = {};
    obj.hmacKeys = {};
    if (message.hmacKeys) {
      Object.entries(message.hmacKeys).forEach(([k, v]) => {
        obj.hmacKeys[k] = GetConversationHmacKeysResponse_HmacKeys.toJSON(v);
      });
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetConversationHmacKeysResponse();
    message.hmacKeys = Object.entries((_a = object.hmacKeys) !== null && _a !== void 0 ? _a : {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = GetConversationHmacKeysResponse_HmacKeys.fromPartial(value);
      }
      return acc;
    }, {});
    return message;
  }
};
function createBaseGetConversationHmacKeysResponse_HmacKeyData() {
  return { thirtyDayPeriodsSinceEpoch: 0, hmacKey: new Uint8Array() };
}
var GetConversationHmacKeysResponse_HmacKeyData = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.thirtyDayPeriodsSinceEpoch !== 0) {
      writer.uint32(8).int32(message.thirtyDayPeriodsSinceEpoch);
    }
    if (message.hmacKey.length !== 0) {
      writer.uint32(18).bytes(message.hmacKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetConversationHmacKeysResponse_HmacKeyData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.thirtyDayPeriodsSinceEpoch = reader.int32();
          break;
        case 2:
          message.hmacKey = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      thirtyDayPeriodsSinceEpoch: isSet14(object.thirtyDayPeriodsSinceEpoch) ? Number(object.thirtyDayPeriodsSinceEpoch) : 0,
      hmacKey: isSet14(object.hmacKey) ? bytesFromBase6410(object.hmacKey) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.thirtyDayPeriodsSinceEpoch !== void 0 && (obj.thirtyDayPeriodsSinceEpoch = Math.round(message.thirtyDayPeriodsSinceEpoch));
    message.hmacKey !== void 0 && (obj.hmacKey = base64FromBytes10(message.hmacKey !== void 0 ? message.hmacKey : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseGetConversationHmacKeysResponse_HmacKeyData();
    message.thirtyDayPeriodsSinceEpoch = (_a = object.thirtyDayPeriodsSinceEpoch) !== null && _a !== void 0 ? _a : 0;
    message.hmacKey = (_b = object.hmacKey) !== null && _b !== void 0 ? _b : new Uint8Array();
    return message;
  }
};
function createBaseGetConversationHmacKeysResponse_HmacKeys() {
  return { values: [] };
}
var GetConversationHmacKeysResponse_HmacKeys = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    for (const v of message.values) {
      GetConversationHmacKeysResponse_HmacKeyData.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetConversationHmacKeysResponse_HmacKeys();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.values.push(GetConversationHmacKeysResponse_HmacKeyData.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      values: Array.isArray(object === null || object === void 0 ? void 0 : object.values) ? object.values.map((e) => GetConversationHmacKeysResponse_HmacKeyData.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.values) {
      obj.values = message.values.map((e) => e ? GetConversationHmacKeysResponse_HmacKeyData.toJSON(e) : void 0);
    } else {
      obj.values = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetConversationHmacKeysResponse_HmacKeys();
    message.values = ((_a = object.values) === null || _a === void 0 ? void 0 : _a.map((e) => GetConversationHmacKeysResponse_HmacKeyData.fromPartial(e))) || [];
    return message;
  }
};
function createBaseGetConversationHmacKeysResponse_HmacKeysEntry() {
  return { key: "", value: void 0 };
}
var GetConversationHmacKeysResponse_HmacKeysEntry = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== void 0) {
      GetConversationHmacKeysResponse_HmacKeys.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetConversationHmacKeysResponse_HmacKeysEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.key = reader.string();
          break;
        case 2:
          message.value = GetConversationHmacKeysResponse_HmacKeys.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet14(object.key) ? String(object.key) : "",
      value: isSet14(object.value) ? GetConversationHmacKeysResponse_HmacKeys.fromJSON(object.value) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.key !== void 0 && (obj.key = message.key);
    message.value !== void 0 && (obj.value = message.value ? GetConversationHmacKeysResponse_HmacKeys.toJSON(message.value) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetConversationHmacKeysResponse_HmacKeysEntry();
    message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
    message.value = object.value !== void 0 && object.value !== null ? GetConversationHmacKeysResponse_HmacKeys.fromPartial(object.value) : void 0;
    return message;
  }
};
function createBasePrivatePreferencesActionMap() {
  return { actions: {} };
}
var PrivatePreferencesActionMap = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    Object.entries(message.actions).forEach(([key, value]) => {
      PrivatePreferencesActionMap_ActionsEntry.encode({ key, value }, writer.uint32(10).fork()).ldelim();
    });
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivatePreferencesActionMap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          const entry1 = PrivatePreferencesActionMap_ActionsEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.actions[entry1.key] = entry1.value;
          }
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      actions: isObject3(object.actions) ? Object.entries(object.actions).reduce((acc, [key, value]) => {
        acc[key] = PrivatePreferencesAction.fromJSON(value);
        return acc;
      }, {}) : {}
    };
  },
  toJSON(message) {
    const obj = {};
    obj.actions = {};
    if (message.actions) {
      Object.entries(message.actions).forEach(([k, v]) => {
        obj.actions[k] = PrivatePreferencesAction.toJSON(v);
      });
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePrivatePreferencesActionMap();
    message.actions = Object.entries((_a = object.actions) !== null && _a !== void 0 ? _a : {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = PrivatePreferencesAction.fromPartial(value);
      }
      return acc;
    }, {});
    return message;
  }
};
function createBasePrivatePreferencesActionMap_ActionsEntry() {
  return { key: "", value: void 0 };
}
var PrivatePreferencesActionMap_ActionsEntry = {
  encode(message, writer = import_minimal14.default.Writer.create()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== void 0) {
      PrivatePreferencesAction.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal14.default.Reader ? input : new import_minimal14.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePrivatePreferencesActionMap_ActionsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.key = reader.string();
          break;
        case 2:
          message.value = PrivatePreferencesAction.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet14(object.key) ? String(object.key) : "",
      value: isSet14(object.value) ? PrivatePreferencesAction.fromJSON(object.value) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.key !== void 0 && (obj.key = message.key);
    message.value !== void 0 && (obj.value = message.value ? PrivatePreferencesAction.toJSON(message.value) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBasePrivatePreferencesActionMap_ActionsEntry();
    message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
    message.value = object.value !== void 0 && object.value !== null ? PrivatePreferencesAction.fromPartial(object.value) : void 0;
    return message;
  }
};
var globalThis10 = (() => {
  if (typeof globalThis10 !== "undefined")
    return globalThis10;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6410(b642) {
  if (globalThis10.Buffer) {
    return Uint8Array.from(globalThis10.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis10.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes10(arr) {
  if (globalThis10.Buffer) {
    return globalThis10.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis10.btoa(bin.join(""));
  }
}
if (import_minimal14.default.util.Long !== long_default) {
  import_minimal14.default.util.Long = long_default;
  import_minimal14.default.configure();
}
function isObject3(value) {
  return typeof value === "object" && value !== null;
}
function isSet14(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/signed_payload.pb.js
var signed_payload_pb_exports = {};
__export(signed_payload_pb_exports, {
  SignedPayload: () => SignedPayload,
  protobufPackage: () => protobufPackage15
});
var import_minimal15 = __toESM(require_minimal());
var protobufPackage15 = "xmtp.message_contents";
function createBaseSignedPayload() {
  return { payload: new Uint8Array(), signature: void 0 };
}
var SignedPayload = {
  encode(message, writer = import_minimal15.default.Writer.create()) {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.signature !== void 0) {
      Signature.encode(message.signature, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal15.default.Reader ? input : new import_minimal15.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignedPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.payload = reader.bytes();
          break;
        case 2:
          message.signature = Signature.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      payload: isSet15(object.payload) ? bytesFromBase6411(object.payload) : new Uint8Array(),
      signature: isSet15(object.signature) ? Signature.fromJSON(object.signature) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.payload !== void 0 && (obj.payload = base64FromBytes11(message.payload !== void 0 ? message.payload : new Uint8Array()));
    message.signature !== void 0 && (obj.signature = message.signature ? Signature.toJSON(message.signature) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSignedPayload();
    message.payload = (_a = object.payload) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.signature = object.signature !== void 0 && object.signature !== null ? Signature.fromPartial(object.signature) : void 0;
    return message;
  }
};
var globalThis11 = (() => {
  if (typeof globalThis11 !== "undefined")
    return globalThis11;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6411(b642) {
  if (globalThis11.Buffer) {
    return Uint8Array.from(globalThis11.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis11.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes11(arr) {
  if (globalThis11.Buffer) {
    return globalThis11.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis11.btoa(bin.join(""));
  }
}
if (import_minimal15.default.util.Long !== long_default) {
  import_minimal15.default.util.Long = long_default;
  import_minimal15.default.configure();
}
function isSet15(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/message_contents/ecies.pb.js
var ecies_pb_exports = {};
__export(ecies_pb_exports, {
  EciesMessage: () => EciesMessage,
  protobufPackage: () => protobufPackage16
});
var import_minimal16 = __toESM(require_minimal());
var protobufPackage16 = "xmtp.message_contents";
function createBaseEciesMessage() {
  return { v1: void 0 };
}
var EciesMessage = {
  encode(message, writer = import_minimal16.default.Writer.create()) {
    if (message.v1 !== void 0) {
      writer.uint32(10).bytes(message.v1);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal16.default.Reader ? input : new import_minimal16.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEciesMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet16(object.v1) ? bytesFromBase6412(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 !== void 0 ? base64FromBytes12(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseEciesMessage();
    message.v1 = (_a = object.v1) !== null && _a !== void 0 ? _a : void 0;
    return message;
  }
};
var globalThis12 = (() => {
  if (typeof globalThis12 !== "undefined")
    return globalThis12;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6412(b642) {
  if (globalThis12.Buffer) {
    return Uint8Array.from(globalThis12.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis12.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes12(arr) {
  if (globalThis12.Buffer) {
    return globalThis12.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis12.btoa(bin.join(""));
  }
}
if (import_minimal16.default.util.Long !== long_default) {
  import_minimal16.default.util.Long = long_default;
  import_minimal16.default.configure();
}
function isSet16(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls/api/v1/mls.pb.js
var mls_pb_exports = {};
__export(mls_pb_exports, {
  BatchPublishCommitLogRequest: () => BatchPublishCommitLogRequest,
  BatchQueryCommitLogRequest: () => BatchQueryCommitLogRequest,
  BatchQueryCommitLogResponse: () => BatchQueryCommitLogResponse,
  FetchKeyPackagesRequest: () => FetchKeyPackagesRequest,
  FetchKeyPackagesResponse: () => FetchKeyPackagesResponse,
  FetchKeyPackagesResponse_KeyPackage: () => FetchKeyPackagesResponse_KeyPackage,
  GetIdentityUpdatesRequest: () => GetIdentityUpdatesRequest,
  GetIdentityUpdatesResponse: () => GetIdentityUpdatesResponse,
  GetIdentityUpdatesResponse_NewInstallationUpdate: () => GetIdentityUpdatesResponse_NewInstallationUpdate,
  GetIdentityUpdatesResponse_RevokedInstallationUpdate: () => GetIdentityUpdatesResponse_RevokedInstallationUpdate,
  GetIdentityUpdatesResponse_Update: () => GetIdentityUpdatesResponse_Update,
  GetIdentityUpdatesResponse_WalletUpdates: () => GetIdentityUpdatesResponse_WalletUpdates,
  GroupMessage: () => GroupMessage,
  GroupMessageInput: () => GroupMessageInput,
  GroupMessageInput_V1: () => GroupMessageInput_V1,
  GroupMessage_V1: () => GroupMessage_V1,
  KeyPackageUpload: () => KeyPackageUpload,
  MlsApiClientImpl: () => MlsApiClientImpl,
  PagingInfo: () => PagingInfo,
  PublishCommitLogRequest: () => PublishCommitLogRequest,
  QueryCommitLogRequest: () => QueryCommitLogRequest,
  QueryCommitLogResponse: () => QueryCommitLogResponse,
  QueryGroupMessagesRequest: () => QueryGroupMessagesRequest,
  QueryGroupMessagesResponse: () => QueryGroupMessagesResponse,
  QueryWelcomeMessagesRequest: () => QueryWelcomeMessagesRequest,
  QueryWelcomeMessagesResponse: () => QueryWelcomeMessagesResponse,
  RegisterInstallationRequest: () => RegisterInstallationRequest,
  RegisterInstallationResponse: () => RegisterInstallationResponse,
  RevokeInstallationRequest: () => RevokeInstallationRequest,
  SendGroupMessagesRequest: () => SendGroupMessagesRequest,
  SendWelcomeMessagesRequest: () => SendWelcomeMessagesRequest,
  SortDirection: () => SortDirection2,
  SubscribeGroupMessagesRequest: () => SubscribeGroupMessagesRequest,
  SubscribeGroupMessagesRequest_Filter: () => SubscribeGroupMessagesRequest_Filter,
  SubscribeWelcomeMessagesRequest: () => SubscribeWelcomeMessagesRequest,
  SubscribeWelcomeMessagesRequest_Filter: () => SubscribeWelcomeMessagesRequest_Filter,
  UploadKeyPackageRequest: () => UploadKeyPackageRequest,
  WelcomeMessage: () => WelcomeMessage,
  WelcomeMessageInput: () => WelcomeMessageInput,
  WelcomeMessageInput_V1: () => WelcomeMessageInput_V1,
  WelcomeMessage_V1: () => WelcomeMessage_V1,
  WelcomeMetadata: () => WelcomeMetadata,
  protobufPackage: () => protobufPackage17,
  sortDirectionFromJSON: () => sortDirectionFromJSON,
  sortDirectionToJSON: () => sortDirectionToJSON
});

// node_modules/@xmtp/proto/ts/dist/esm/mls/message_contents/wrapper_encryption.pb.js
var import_minimal17 = __toESM(require_minimal());
var WelcomeWrapperAlgorithm;
(function(WelcomeWrapperAlgorithm2) {
  WelcomeWrapperAlgorithm2[WelcomeWrapperAlgorithm2["WELCOME_WRAPPER_ALGORITHM_UNSPECIFIED"] = 0] = "WELCOME_WRAPPER_ALGORITHM_UNSPECIFIED";
  WelcomeWrapperAlgorithm2[WelcomeWrapperAlgorithm2["WELCOME_WRAPPER_ALGORITHM_CURVE25519"] = 1] = "WELCOME_WRAPPER_ALGORITHM_CURVE25519";
  WelcomeWrapperAlgorithm2[WelcomeWrapperAlgorithm2["WELCOME_WRAPPER_ALGORITHM_XWING_MLKEM_768_DRAFT_6"] = 2] = "WELCOME_WRAPPER_ALGORITHM_XWING_MLKEM_768_DRAFT_6";
  WelcomeWrapperAlgorithm2[WelcomeWrapperAlgorithm2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(WelcomeWrapperAlgorithm || (WelcomeWrapperAlgorithm = {}));
function welcomeWrapperAlgorithmFromJSON(object) {
  switch (object) {
    case 0:
    case "WELCOME_WRAPPER_ALGORITHM_UNSPECIFIED":
      return WelcomeWrapperAlgorithm.WELCOME_WRAPPER_ALGORITHM_UNSPECIFIED;
    case 1:
    case "WELCOME_WRAPPER_ALGORITHM_CURVE25519":
      return WelcomeWrapperAlgorithm.WELCOME_WRAPPER_ALGORITHM_CURVE25519;
    case 2:
    case "WELCOME_WRAPPER_ALGORITHM_XWING_MLKEM_768_DRAFT_6":
      return WelcomeWrapperAlgorithm.WELCOME_WRAPPER_ALGORITHM_XWING_MLKEM_768_DRAFT_6;
    case -1:
    case "UNRECOGNIZED":
    default:
      return WelcomeWrapperAlgorithm.UNRECOGNIZED;
  }
}
function welcomeWrapperAlgorithmToJSON(object) {
  switch (object) {
    case WelcomeWrapperAlgorithm.WELCOME_WRAPPER_ALGORITHM_UNSPECIFIED:
      return "WELCOME_WRAPPER_ALGORITHM_UNSPECIFIED";
    case WelcomeWrapperAlgorithm.WELCOME_WRAPPER_ALGORITHM_CURVE25519:
      return "WELCOME_WRAPPER_ALGORITHM_CURVE25519";
    case WelcomeWrapperAlgorithm.WELCOME_WRAPPER_ALGORITHM_XWING_MLKEM_768_DRAFT_6:
      return "WELCOME_WRAPPER_ALGORITHM_XWING_MLKEM_768_DRAFT_6";
    case WelcomeWrapperAlgorithm.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
var globalThis13 = (() => {
  if (typeof globalThis13 !== "undefined")
    return globalThis13;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
if (import_minimal17.default.util.Long !== long_default) {
  import_minimal17.default.util.Long = long_default;
  import_minimal17.default.configure();
}

// node_modules/@xmtp/proto/ts/dist/esm/identity/associations/signature.pb.js
var import_minimal18 = __toESM(require_minimal());
function createBaseRecoverableEcdsaSignature() {
  return { bytes: new Uint8Array() };
}
var RecoverableEcdsaSignature = {
  encode(message, writer = import_minimal18.default.Writer.create()) {
    if (message.bytes.length !== 0) {
      writer.uint32(10).bytes(message.bytes);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal18.default.Reader ? input : new import_minimal18.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRecoverableEcdsaSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.bytes = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      bytes: isSet17(object.bytes) ? bytesFromBase6413(object.bytes) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.bytes !== void 0 && (obj.bytes = base64FromBytes13(message.bytes !== void 0 ? message.bytes : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseRecoverableEcdsaSignature();
    message.bytes = (_a = object.bytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseRecoverableEd25519Signature() {
  return { bytes: new Uint8Array(), publicKey: new Uint8Array() };
}
var RecoverableEd25519Signature = {
  encode(message, writer = import_minimal18.default.Writer.create()) {
    if (message.bytes.length !== 0) {
      writer.uint32(10).bytes(message.bytes);
    }
    if (message.publicKey.length !== 0) {
      writer.uint32(18).bytes(message.publicKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal18.default.Reader ? input : new import_minimal18.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRecoverableEd25519Signature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.bytes = reader.bytes();
          break;
        case 2:
          message.publicKey = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      bytes: isSet17(object.bytes) ? bytesFromBase6413(object.bytes) : new Uint8Array(),
      publicKey: isSet17(object.publicKey) ? bytesFromBase6413(object.publicKey) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.bytes !== void 0 && (obj.bytes = base64FromBytes13(message.bytes !== void 0 ? message.bytes : new Uint8Array()));
    message.publicKey !== void 0 && (obj.publicKey = base64FromBytes13(message.publicKey !== void 0 ? message.publicKey : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseRecoverableEd25519Signature();
    message.bytes = (_a = object.bytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.publicKey = (_b = object.publicKey) !== null && _b !== void 0 ? _b : new Uint8Array();
    return message;
  }
};
function createBaseSmartContractWalletSignature() {
  return {
    accountId: "",
    blockNumber: long_default.UZERO,
    signature: new Uint8Array()
  };
}
var SmartContractWalletSignature = {
  encode(message, writer = import_minimal18.default.Writer.create()) {
    if (message.accountId !== "") {
      writer.uint32(10).string(message.accountId);
    }
    if (!message.blockNumber.isZero()) {
      writer.uint32(16).uint64(message.blockNumber);
    }
    if (message.signature.length !== 0) {
      writer.uint32(26).bytes(message.signature);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal18.default.Reader ? input : new import_minimal18.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSmartContractWalletSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.accountId = reader.string();
          break;
        case 2:
          message.blockNumber = reader.uint64();
          break;
        case 3:
          message.signature = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      accountId: isSet17(object.accountId) ? String(object.accountId) : "",
      blockNumber: isSet17(object.blockNumber) ? long_default.fromValue(object.blockNumber) : long_default.UZERO,
      signature: isSet17(object.signature) ? bytesFromBase6413(object.signature) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.accountId !== void 0 && (obj.accountId = message.accountId);
    message.blockNumber !== void 0 && (obj.blockNumber = (message.blockNumber || long_default.UZERO).toString());
    message.signature !== void 0 && (obj.signature = base64FromBytes13(message.signature !== void 0 ? message.signature : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseSmartContractWalletSignature();
    message.accountId = (_a = object.accountId) !== null && _a !== void 0 ? _a : "";
    message.blockNumber = object.blockNumber !== void 0 && object.blockNumber !== null ? long_default.fromValue(object.blockNumber) : long_default.UZERO;
    message.signature = (_b = object.signature) !== null && _b !== void 0 ? _b : new Uint8Array();
    return message;
  }
};
function createBaseRecoverablePasskeySignature() {
  return {
    publicKey: new Uint8Array(),
    signature: new Uint8Array(),
    authenticatorData: new Uint8Array(),
    clientDataJson: new Uint8Array()
  };
}
var RecoverablePasskeySignature = {
  encode(message, writer = import_minimal18.default.Writer.create()) {
    if (message.publicKey.length !== 0) {
      writer.uint32(10).bytes(message.publicKey);
    }
    if (message.signature.length !== 0) {
      writer.uint32(18).bytes(message.signature);
    }
    if (message.authenticatorData.length !== 0) {
      writer.uint32(26).bytes(message.authenticatorData);
    }
    if (message.clientDataJson.length !== 0) {
      writer.uint32(34).bytes(message.clientDataJson);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal18.default.Reader ? input : new import_minimal18.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRecoverablePasskeySignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.publicKey = reader.bytes();
          break;
        case 2:
          message.signature = reader.bytes();
          break;
        case 3:
          message.authenticatorData = reader.bytes();
          break;
        case 4:
          message.clientDataJson = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      publicKey: isSet17(object.publicKey) ? bytesFromBase6413(object.publicKey) : new Uint8Array(),
      signature: isSet17(object.signature) ? bytesFromBase6413(object.signature) : new Uint8Array(),
      authenticatorData: isSet17(object.authenticatorData) ? bytesFromBase6413(object.authenticatorData) : new Uint8Array(),
      clientDataJson: isSet17(object.clientDataJson) ? bytesFromBase6413(object.clientDataJson) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.publicKey !== void 0 && (obj.publicKey = base64FromBytes13(message.publicKey !== void 0 ? message.publicKey : new Uint8Array()));
    message.signature !== void 0 && (obj.signature = base64FromBytes13(message.signature !== void 0 ? message.signature : new Uint8Array()));
    message.authenticatorData !== void 0 && (obj.authenticatorData = base64FromBytes13(message.authenticatorData !== void 0 ? message.authenticatorData : new Uint8Array()));
    message.clientDataJson !== void 0 && (obj.clientDataJson = base64FromBytes13(message.clientDataJson !== void 0 ? message.clientDataJson : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d;
    const message = createBaseRecoverablePasskeySignature();
    message.publicKey = (_a = object.publicKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.signature = (_b = object.signature) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.authenticatorData = (_c = object.authenticatorData) !== null && _c !== void 0 ? _c : new Uint8Array();
    message.clientDataJson = (_d = object.clientDataJson) !== null && _d !== void 0 ? _d : new Uint8Array();
    return message;
  }
};
function createBaseLegacyDelegatedSignature() {
  return { delegatedKey: void 0, signature: void 0 };
}
var LegacyDelegatedSignature = {
  encode(message, writer = import_minimal18.default.Writer.create()) {
    if (message.delegatedKey !== void 0) {
      SignedPublicKey.encode(message.delegatedKey, writer.uint32(10).fork()).ldelim();
    }
    if (message.signature !== void 0) {
      RecoverableEcdsaSignature.encode(message.signature, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal18.default.Reader ? input : new import_minimal18.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseLegacyDelegatedSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.delegatedKey = SignedPublicKey.decode(reader, reader.uint32());
          break;
        case 2:
          message.signature = RecoverableEcdsaSignature.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      delegatedKey: isSet17(object.delegatedKey) ? SignedPublicKey.fromJSON(object.delegatedKey) : void 0,
      signature: isSet17(object.signature) ? RecoverableEcdsaSignature.fromJSON(object.signature) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.delegatedKey !== void 0 && (obj.delegatedKey = message.delegatedKey ? SignedPublicKey.toJSON(message.delegatedKey) : void 0);
    message.signature !== void 0 && (obj.signature = message.signature ? RecoverableEcdsaSignature.toJSON(message.signature) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseLegacyDelegatedSignature();
    message.delegatedKey = object.delegatedKey !== void 0 && object.delegatedKey !== null ? SignedPublicKey.fromPartial(object.delegatedKey) : void 0;
    message.signature = object.signature !== void 0 && object.signature !== null ? RecoverableEcdsaSignature.fromPartial(object.signature) : void 0;
    return message;
  }
};
function createBaseSignature2() {
  return {
    erc191: void 0,
    erc6492: void 0,
    installationKey: void 0,
    delegatedErc191: void 0,
    passkey: void 0
  };
}
var Signature2 = {
  encode(message, writer = import_minimal18.default.Writer.create()) {
    if (message.erc191 !== void 0) {
      RecoverableEcdsaSignature.encode(message.erc191, writer.uint32(10).fork()).ldelim();
    }
    if (message.erc6492 !== void 0) {
      SmartContractWalletSignature.encode(message.erc6492, writer.uint32(18).fork()).ldelim();
    }
    if (message.installationKey !== void 0) {
      RecoverableEd25519Signature.encode(message.installationKey, writer.uint32(26).fork()).ldelim();
    }
    if (message.delegatedErc191 !== void 0) {
      LegacyDelegatedSignature.encode(message.delegatedErc191, writer.uint32(34).fork()).ldelim();
    }
    if (message.passkey !== void 0) {
      RecoverablePasskeySignature.encode(message.passkey, writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal18.default.Reader ? input : new import_minimal18.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSignature2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.erc191 = RecoverableEcdsaSignature.decode(reader, reader.uint32());
          break;
        case 2:
          message.erc6492 = SmartContractWalletSignature.decode(reader, reader.uint32());
          break;
        case 3:
          message.installationKey = RecoverableEd25519Signature.decode(reader, reader.uint32());
          break;
        case 4:
          message.delegatedErc191 = LegacyDelegatedSignature.decode(reader, reader.uint32());
          break;
        case 5:
          message.passkey = RecoverablePasskeySignature.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      erc191: isSet17(object.erc191) ? RecoverableEcdsaSignature.fromJSON(object.erc191) : void 0,
      erc6492: isSet17(object.erc6492) ? SmartContractWalletSignature.fromJSON(object.erc6492) : void 0,
      installationKey: isSet17(object.installationKey) ? RecoverableEd25519Signature.fromJSON(object.installationKey) : void 0,
      delegatedErc191: isSet17(object.delegatedErc191) ? LegacyDelegatedSignature.fromJSON(object.delegatedErc191) : void 0,
      passkey: isSet17(object.passkey) ? RecoverablePasskeySignature.fromJSON(object.passkey) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.erc191 !== void 0 && (obj.erc191 = message.erc191 ? RecoverableEcdsaSignature.toJSON(message.erc191) : void 0);
    message.erc6492 !== void 0 && (obj.erc6492 = message.erc6492 ? SmartContractWalletSignature.toJSON(message.erc6492) : void 0);
    message.installationKey !== void 0 && (obj.installationKey = message.installationKey ? RecoverableEd25519Signature.toJSON(message.installationKey) : void 0);
    message.delegatedErc191 !== void 0 && (obj.delegatedErc191 = message.delegatedErc191 ? LegacyDelegatedSignature.toJSON(message.delegatedErc191) : void 0);
    message.passkey !== void 0 && (obj.passkey = message.passkey ? RecoverablePasskeySignature.toJSON(message.passkey) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseSignature2();
    message.erc191 = object.erc191 !== void 0 && object.erc191 !== null ? RecoverableEcdsaSignature.fromPartial(object.erc191) : void 0;
    message.erc6492 = object.erc6492 !== void 0 && object.erc6492 !== null ? SmartContractWalletSignature.fromPartial(object.erc6492) : void 0;
    message.installationKey = object.installationKey !== void 0 && object.installationKey !== null ? RecoverableEd25519Signature.fromPartial(object.installationKey) : void 0;
    message.delegatedErc191 = object.delegatedErc191 !== void 0 && object.delegatedErc191 !== null ? LegacyDelegatedSignature.fromPartial(object.delegatedErc191) : void 0;
    message.passkey = object.passkey !== void 0 && object.passkey !== null ? RecoverablePasskeySignature.fromPartial(object.passkey) : void 0;
    return message;
  }
};
var globalThis14 = (() => {
  if (typeof globalThis14 !== "undefined")
    return globalThis14;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6413(b642) {
  if (globalThis14.Buffer) {
    return Uint8Array.from(globalThis14.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis14.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes13(arr) {
  if (globalThis14.Buffer) {
    return globalThis14.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis14.btoa(bin.join(""));
  }
}
if (import_minimal18.default.util.Long !== long_default) {
  import_minimal18.default.util.Long = long_default;
  import_minimal18.default.configure();
}
function isSet17(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls/message_contents/commit_log.pb.js
var import_minimal19 = __toESM(require_minimal());
var CommitResult;
(function(CommitResult2) {
  CommitResult2[CommitResult2["COMMIT_RESULT_UNSPECIFIED"] = 0] = "COMMIT_RESULT_UNSPECIFIED";
  CommitResult2[CommitResult2["COMMIT_RESULT_APPLIED"] = 1] = "COMMIT_RESULT_APPLIED";
  CommitResult2[CommitResult2["COMMIT_RESULT_WRONG_EPOCH"] = 2] = "COMMIT_RESULT_WRONG_EPOCH";
  CommitResult2[CommitResult2["COMMIT_RESULT_UNDECRYPTABLE"] = 3] = "COMMIT_RESULT_UNDECRYPTABLE";
  CommitResult2[CommitResult2["COMMIT_RESULT_INVALID"] = 4] = "COMMIT_RESULT_INVALID";
  CommitResult2[CommitResult2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(CommitResult || (CommitResult = {}));
function createBaseCommitLogEntry() {
  return {
    sequenceId: long_default.UZERO,
    serializedCommitLogEntry: new Uint8Array(),
    signature: void 0
  };
}
var CommitLogEntry = {
  encode(message, writer = import_minimal19.default.Writer.create()) {
    if (!message.sequenceId.isZero()) {
      writer.uint32(8).uint64(message.sequenceId);
    }
    if (message.serializedCommitLogEntry.length !== 0) {
      writer.uint32(18).bytes(message.serializedCommitLogEntry);
    }
    if (message.signature !== void 0) {
      RecoverableEd25519Signature.encode(message.signature, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal19.default.Reader ? input : new import_minimal19.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCommitLogEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.sequenceId = reader.uint64();
          break;
        case 2:
          message.serializedCommitLogEntry = reader.bytes();
          break;
        case 3:
          message.signature = RecoverableEd25519Signature.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      sequenceId: isSet18(object.sequenceId) ? long_default.fromValue(object.sequenceId) : long_default.UZERO,
      serializedCommitLogEntry: isSet18(object.serializedCommitLogEntry) ? bytesFromBase6414(object.serializedCommitLogEntry) : new Uint8Array(),
      signature: isSet18(object.signature) ? RecoverableEd25519Signature.fromJSON(object.signature) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.sequenceId !== void 0 && (obj.sequenceId = (message.sequenceId || long_default.UZERO).toString());
    message.serializedCommitLogEntry !== void 0 && (obj.serializedCommitLogEntry = base64FromBytes14(message.serializedCommitLogEntry !== void 0 ? message.serializedCommitLogEntry : new Uint8Array()));
    message.signature !== void 0 && (obj.signature = message.signature ? RecoverableEd25519Signature.toJSON(message.signature) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseCommitLogEntry();
    message.sequenceId = object.sequenceId !== void 0 && object.sequenceId !== null ? long_default.fromValue(object.sequenceId) : long_default.UZERO;
    message.serializedCommitLogEntry = (_a = object.serializedCommitLogEntry) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.signature = object.signature !== void 0 && object.signature !== null ? RecoverableEd25519Signature.fromPartial(object.signature) : void 0;
    return message;
  }
};
var globalThis15 = (() => {
  if (typeof globalThis15 !== "undefined")
    return globalThis15;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6414(b642) {
  if (globalThis15.Buffer) {
    return Uint8Array.from(globalThis15.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis15.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes14(arr) {
  if (globalThis15.Buffer) {
    return globalThis15.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis15.btoa(bin.join(""));
  }
}
if (import_minimal19.default.util.Long !== long_default) {
  import_minimal19.default.util.Long = long_default;
  import_minimal19.default.configure();
}
function isSet18(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/google/protobuf/empty.pb.js
var import_minimal20 = __toESM(require_minimal());
function createBaseEmpty() {
  return {};
}
var Empty = {
  encode(_, writer = import_minimal20.default.Writer.create()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal20.default.Reader ? input : new import_minimal20.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEmpty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  fromPartial(_) {
    const message = createBaseEmpty();
    return message;
  }
};
if (import_minimal20.default.util.Long !== long_default) {
  import_minimal20.default.util.Long = long_default;
  import_minimal20.default.configure();
}

// node_modules/rxjs/dist/esm5/internal/util/isFunction.js
function isFunction(value) {
  return typeof value === "function";
}

// node_modules/rxjs/dist/esm5/internal/util/lift.js
function hasLift(source) {
  return isFunction(source === null || source === void 0 ? void 0 : source.lift);
}
function operate(init) {
  return function(source) {
    if (hasLift(source)) {
      return source.lift(function(liftedSource) {
        try {
          return init(liftedSource, this);
        } catch (err) {
          this.error(err);
        }
      });
    }
    throw new TypeError("Unable to lift unknown Observable type");
  };
}

// node_modules/rxjs/dist/esm5/internal/observable/innerFrom.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/util/isArrayLike.js
var isArrayLike = function(x) {
  return x && typeof x.length === "number" && typeof x !== "function";
};

// node_modules/rxjs/dist/esm5/internal/util/isPromise.js
function isPromise(value) {
  return isFunction(value === null || value === void 0 ? void 0 : value.then);
}

// node_modules/rxjs/dist/esm5/internal/Subscriber.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/Subscription.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/util/createErrorClass.js
function createErrorClass(createImpl) {
  var _super = function(instance) {
    Error.call(instance);
    instance.stack = new Error().stack;
  };
  var ctorFunc = createImpl(_super);
  ctorFunc.prototype = Object.create(Error.prototype);
  ctorFunc.prototype.constructor = ctorFunc;
  return ctorFunc;
}

// node_modules/rxjs/dist/esm5/internal/util/UnsubscriptionError.js
var UnsubscriptionError = createErrorClass(function(_super) {
  return function UnsubscriptionErrorImpl(errors) {
    _super(this);
    this.message = errors ? errors.length + " errors occurred during unsubscription:\n" + errors.map(function(err, i) {
      return i + 1 + ") " + err.toString();
    }).join("\n  ") : "";
    this.name = "UnsubscriptionError";
    this.errors = errors;
  };
});

// node_modules/rxjs/dist/esm5/internal/util/arrRemove.js
function arrRemove(arr, item) {
  if (arr) {
    var index = arr.indexOf(item);
    0 <= index && arr.splice(index, 1);
  }
}

// node_modules/rxjs/dist/esm5/internal/Subscription.js
var Subscription = function() {
  function Subscription2(initialTeardown) {
    this.initialTeardown = initialTeardown;
    this.closed = false;
    this._parentage = null;
    this._finalizers = null;
  }
  Subscription2.prototype.unsubscribe = function() {
    var e_1, _a, e_2, _b;
    var errors;
    if (!this.closed) {
      this.closed = true;
      var _parentage = this._parentage;
      if (_parentage) {
        this._parentage = null;
        if (Array.isArray(_parentage)) {
          try {
            for (var _parentage_1 = __values(_parentage), _parentage_1_1 = _parentage_1.next(); !_parentage_1_1.done; _parentage_1_1 = _parentage_1.next()) {
              var parent_1 = _parentage_1_1.value;
              parent_1.remove(this);
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (_parentage_1_1 && !_parentage_1_1.done && (_a = _parentage_1.return)) _a.call(_parentage_1);
            } finally {
              if (e_1) throw e_1.error;
            }
          }
        } else {
          _parentage.remove(this);
        }
      }
      var initialFinalizer = this.initialTeardown;
      if (isFunction(initialFinalizer)) {
        try {
          initialFinalizer();
        } catch (e) {
          errors = e instanceof UnsubscriptionError ? e.errors : [e];
        }
      }
      var _finalizers = this._finalizers;
      if (_finalizers) {
        this._finalizers = null;
        try {
          for (var _finalizers_1 = __values(_finalizers), _finalizers_1_1 = _finalizers_1.next(); !_finalizers_1_1.done; _finalizers_1_1 = _finalizers_1.next()) {
            var finalizer = _finalizers_1_1.value;
            try {
              execFinalizer(finalizer);
            } catch (err) {
              errors = errors !== null && errors !== void 0 ? errors : [];
              if (err instanceof UnsubscriptionError) {
                errors = __spreadArray(__spreadArray([], __read(errors)), __read(err.errors));
              } else {
                errors.push(err);
              }
            }
          }
        } catch (e_2_1) {
          e_2 = { error: e_2_1 };
        } finally {
          try {
            if (_finalizers_1_1 && !_finalizers_1_1.done && (_b = _finalizers_1.return)) _b.call(_finalizers_1);
          } finally {
            if (e_2) throw e_2.error;
          }
        }
      }
      if (errors) {
        throw new UnsubscriptionError(errors);
      }
    }
  };
  Subscription2.prototype.add = function(teardown) {
    var _a;
    if (teardown && teardown !== this) {
      if (this.closed) {
        execFinalizer(teardown);
      } else {
        if (teardown instanceof Subscription2) {
          if (teardown.closed || teardown._hasParent(this)) {
            return;
          }
          teardown._addParent(this);
        }
        (this._finalizers = (_a = this._finalizers) !== null && _a !== void 0 ? _a : []).push(teardown);
      }
    }
  };
  Subscription2.prototype._hasParent = function(parent) {
    var _parentage = this._parentage;
    return _parentage === parent || Array.isArray(_parentage) && _parentage.includes(parent);
  };
  Subscription2.prototype._addParent = function(parent) {
    var _parentage = this._parentage;
    this._parentage = Array.isArray(_parentage) ? (_parentage.push(parent), _parentage) : _parentage ? [_parentage, parent] : parent;
  };
  Subscription2.prototype._removeParent = function(parent) {
    var _parentage = this._parentage;
    if (_parentage === parent) {
      this._parentage = null;
    } else if (Array.isArray(_parentage)) {
      arrRemove(_parentage, parent);
    }
  };
  Subscription2.prototype.remove = function(teardown) {
    var _finalizers = this._finalizers;
    _finalizers && arrRemove(_finalizers, teardown);
    if (teardown instanceof Subscription2) {
      teardown._removeParent(this);
    }
  };
  Subscription2.EMPTY = function() {
    var empty = new Subscription2();
    empty.closed = true;
    return empty;
  }();
  return Subscription2;
}();
var EMPTY_SUBSCRIPTION = Subscription.EMPTY;
function isSubscription(value) {
  return value instanceof Subscription || value && "closed" in value && isFunction(value.remove) && isFunction(value.add) && isFunction(value.unsubscribe);
}
function execFinalizer(finalizer) {
  if (isFunction(finalizer)) {
    finalizer();
  } else {
    finalizer.unsubscribe();
  }
}

// node_modules/rxjs/dist/esm5/internal/config.js
var config = {
  onUnhandledError: null,
  onStoppedNotification: null,
  Promise: void 0,
  useDeprecatedSynchronousErrorHandling: false,
  useDeprecatedNextContext: false
};

// node_modules/rxjs/dist/esm5/internal/scheduler/timeoutProvider.js
init_tslib_es6();
var timeoutProvider = {
  setTimeout: function(handler, timeout2) {
    var args = [];
    for (var _i = 2; _i < arguments.length; _i++) {
      args[_i - 2] = arguments[_i];
    }
    var delegate = timeoutProvider.delegate;
    if (delegate === null || delegate === void 0 ? void 0 : delegate.setTimeout) {
      return delegate.setTimeout.apply(delegate, __spreadArray([handler, timeout2], __read(args)));
    }
    return setTimeout.apply(void 0, __spreadArray([handler, timeout2], __read(args)));
  },
  clearTimeout: function(handle) {
    var delegate = timeoutProvider.delegate;
    return ((delegate === null || delegate === void 0 ? void 0 : delegate.clearTimeout) || clearTimeout)(handle);
  },
  delegate: void 0
};

// node_modules/rxjs/dist/esm5/internal/util/reportUnhandledError.js
function reportUnhandledError(err) {
  timeoutProvider.setTimeout(function() {
    var onUnhandledError = config.onUnhandledError;
    if (onUnhandledError) {
      onUnhandledError(err);
    } else {
      throw err;
    }
  });
}

// node_modules/rxjs/dist/esm5/internal/util/noop.js
function noop() {
}

// node_modules/rxjs/dist/esm5/internal/NotificationFactories.js
var COMPLETE_NOTIFICATION = function() {
  return createNotification("C", void 0, void 0);
}();
function errorNotification(error) {
  return createNotification("E", void 0, error);
}
function nextNotification(value) {
  return createNotification("N", value, void 0);
}
function createNotification(kind, value, error) {
  return {
    kind,
    value,
    error
  };
}

// node_modules/rxjs/dist/esm5/internal/util/errorContext.js
var context = null;
function errorContext(cb) {
  if (config.useDeprecatedSynchronousErrorHandling) {
    var isRoot = !context;
    if (isRoot) {
      context = { errorThrown: false, error: null };
    }
    cb();
    if (isRoot) {
      var _a = context, errorThrown = _a.errorThrown, error = _a.error;
      context = null;
      if (errorThrown) {
        throw error;
      }
    }
  } else {
    cb();
  }
}
function captureError(err) {
  if (config.useDeprecatedSynchronousErrorHandling && context) {
    context.errorThrown = true;
    context.error = err;
  }
}

// node_modules/rxjs/dist/esm5/internal/Subscriber.js
var Subscriber = function(_super) {
  __extends(Subscriber2, _super);
  function Subscriber2(destination) {
    var _this = _super.call(this) || this;
    _this.isStopped = false;
    if (destination) {
      _this.destination = destination;
      if (isSubscription(destination)) {
        destination.add(_this);
      }
    } else {
      _this.destination = EMPTY_OBSERVER;
    }
    return _this;
  }
  Subscriber2.create = function(next, error, complete) {
    return new SafeSubscriber(next, error, complete);
  };
  Subscriber2.prototype.next = function(value) {
    if (this.isStopped) {
      handleStoppedNotification(nextNotification(value), this);
    } else {
      this._next(value);
    }
  };
  Subscriber2.prototype.error = function(err) {
    if (this.isStopped) {
      handleStoppedNotification(errorNotification(err), this);
    } else {
      this.isStopped = true;
      this._error(err);
    }
  };
  Subscriber2.prototype.complete = function() {
    if (this.isStopped) {
      handleStoppedNotification(COMPLETE_NOTIFICATION, this);
    } else {
      this.isStopped = true;
      this._complete();
    }
  };
  Subscriber2.prototype.unsubscribe = function() {
    if (!this.closed) {
      this.isStopped = true;
      _super.prototype.unsubscribe.call(this);
      this.destination = null;
    }
  };
  Subscriber2.prototype._next = function(value) {
    this.destination.next(value);
  };
  Subscriber2.prototype._error = function(err) {
    try {
      this.destination.error(err);
    } finally {
      this.unsubscribe();
    }
  };
  Subscriber2.prototype._complete = function() {
    try {
      this.destination.complete();
    } finally {
      this.unsubscribe();
    }
  };
  return Subscriber2;
}(Subscription);
var _bind = Function.prototype.bind;
function bind(fn, thisArg) {
  return _bind.call(fn, thisArg);
}
var ConsumerObserver = function() {
  function ConsumerObserver2(partialObserver) {
    this.partialObserver = partialObserver;
  }
  ConsumerObserver2.prototype.next = function(value) {
    var partialObserver = this.partialObserver;
    if (partialObserver.next) {
      try {
        partialObserver.next(value);
      } catch (error) {
        handleUnhandledError(error);
      }
    }
  };
  ConsumerObserver2.prototype.error = function(err) {
    var partialObserver = this.partialObserver;
    if (partialObserver.error) {
      try {
        partialObserver.error(err);
      } catch (error) {
        handleUnhandledError(error);
      }
    } else {
      handleUnhandledError(err);
    }
  };
  ConsumerObserver2.prototype.complete = function() {
    var partialObserver = this.partialObserver;
    if (partialObserver.complete) {
      try {
        partialObserver.complete();
      } catch (error) {
        handleUnhandledError(error);
      }
    }
  };
  return ConsumerObserver2;
}();
var SafeSubscriber = function(_super) {
  __extends(SafeSubscriber2, _super);
  function SafeSubscriber2(observerOrNext, error, complete) {
    var _this = _super.call(this) || this;
    var partialObserver;
    if (isFunction(observerOrNext) || !observerOrNext) {
      partialObserver = {
        next: observerOrNext !== null && observerOrNext !== void 0 ? observerOrNext : void 0,
        error: error !== null && error !== void 0 ? error : void 0,
        complete: complete !== null && complete !== void 0 ? complete : void 0
      };
    } else {
      var context_1;
      if (_this && config.useDeprecatedNextContext) {
        context_1 = Object.create(observerOrNext);
        context_1.unsubscribe = function() {
          return _this.unsubscribe();
        };
        partialObserver = {
          next: observerOrNext.next && bind(observerOrNext.next, context_1),
          error: observerOrNext.error && bind(observerOrNext.error, context_1),
          complete: observerOrNext.complete && bind(observerOrNext.complete, context_1)
        };
      } else {
        partialObserver = observerOrNext;
      }
    }
    _this.destination = new ConsumerObserver(partialObserver);
    return _this;
  }
  return SafeSubscriber2;
}(Subscriber);
function handleUnhandledError(error) {
  if (config.useDeprecatedSynchronousErrorHandling) {
    captureError(error);
  } else {
    reportUnhandledError(error);
  }
}
function defaultErrorHandler(err) {
  throw err;
}
function handleStoppedNotification(notification, subscriber) {
  var onStoppedNotification = config.onStoppedNotification;
  onStoppedNotification && timeoutProvider.setTimeout(function() {
    return onStoppedNotification(notification, subscriber);
  });
}
var EMPTY_OBSERVER = {
  closed: true,
  next: noop,
  error: defaultErrorHandler,
  complete: noop
};

// node_modules/rxjs/dist/esm5/internal/symbol/observable.js
var observable = function() {
  return typeof Symbol === "function" && Symbol.observable || "@@observable";
}();

// node_modules/rxjs/dist/esm5/internal/util/identity.js
function identity(x) {
  return x;
}

// node_modules/rxjs/dist/esm5/internal/util/pipe.js
function pipeFromArray(fns) {
  if (fns.length === 0) {
    return identity;
  }
  if (fns.length === 1) {
    return fns[0];
  }
  return function piped(input) {
    return fns.reduce(function(prev, fn) {
      return fn(prev);
    }, input);
  };
}

// node_modules/rxjs/dist/esm5/internal/Observable.js
var Observable = function() {
  function Observable2(subscribe) {
    if (subscribe) {
      this._subscribe = subscribe;
    }
  }
  Observable2.prototype.lift = function(operator) {
    var observable2 = new Observable2();
    observable2.source = this;
    observable2.operator = operator;
    return observable2;
  };
  Observable2.prototype.subscribe = function(observerOrNext, error, complete) {
    var _this = this;
    var subscriber = isSubscriber(observerOrNext) ? observerOrNext : new SafeSubscriber(observerOrNext, error, complete);
    errorContext(function() {
      var _a = _this, operator = _a.operator, source = _a.source;
      subscriber.add(operator ? operator.call(subscriber, source) : source ? _this._subscribe(subscriber) : _this._trySubscribe(subscriber));
    });
    return subscriber;
  };
  Observable2.prototype._trySubscribe = function(sink) {
    try {
      return this._subscribe(sink);
    } catch (err) {
      sink.error(err);
    }
  };
  Observable2.prototype.forEach = function(next, promiseCtor) {
    var _this = this;
    promiseCtor = getPromiseCtor(promiseCtor);
    return new promiseCtor(function(resolve, reject) {
      var subscriber = new SafeSubscriber({
        next: function(value) {
          try {
            next(value);
          } catch (err) {
            reject(err);
            subscriber.unsubscribe();
          }
        },
        error: reject,
        complete: resolve
      });
      _this.subscribe(subscriber);
    });
  };
  Observable2.prototype._subscribe = function(subscriber) {
    var _a;
    return (_a = this.source) === null || _a === void 0 ? void 0 : _a.subscribe(subscriber);
  };
  Observable2.prototype[observable] = function() {
    return this;
  };
  Observable2.prototype.pipe = function() {
    var operations = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      operations[_i] = arguments[_i];
    }
    return pipeFromArray(operations)(this);
  };
  Observable2.prototype.toPromise = function(promiseCtor) {
    var _this = this;
    promiseCtor = getPromiseCtor(promiseCtor);
    return new promiseCtor(function(resolve, reject) {
      var value;
      _this.subscribe(function(x) {
        return value = x;
      }, function(err) {
        return reject(err);
      }, function() {
        return resolve(value);
      });
    });
  };
  Observable2.create = function(subscribe) {
    return new Observable2(subscribe);
  };
  return Observable2;
}();
function getPromiseCtor(promiseCtor) {
  var _a;
  return (_a = promiseCtor !== null && promiseCtor !== void 0 ? promiseCtor : config.Promise) !== null && _a !== void 0 ? _a : Promise;
}
function isObserver(value) {
  return value && isFunction(value.next) && isFunction(value.error) && isFunction(value.complete);
}
function isSubscriber(value) {
  return value && value instanceof Subscriber || isObserver(value) && isSubscription(value);
}

// node_modules/rxjs/dist/esm5/internal/util/isInteropObservable.js
function isInteropObservable(input) {
  return isFunction(input[observable]);
}

// node_modules/rxjs/dist/esm5/internal/util/isAsyncIterable.js
function isAsyncIterable(obj) {
  return Symbol.asyncIterator && isFunction(obj === null || obj === void 0 ? void 0 : obj[Symbol.asyncIterator]);
}

// node_modules/rxjs/dist/esm5/internal/util/throwUnobservableError.js
function createInvalidObservableTypeError(input) {
  return new TypeError("You provided " + (input !== null && typeof input === "object" ? "an invalid object" : "'" + input + "'") + " where a stream was expected. You can provide an Observable, Promise, ReadableStream, Array, AsyncIterable, or Iterable.");
}

// node_modules/rxjs/dist/esm5/internal/symbol/iterator.js
function getSymbolIterator() {
  if (typeof Symbol !== "function" || !Symbol.iterator) {
    return "@@iterator";
  }
  return Symbol.iterator;
}
var iterator = getSymbolIterator();

// node_modules/rxjs/dist/esm5/internal/util/isIterable.js
function isIterable(input) {
  return isFunction(input === null || input === void 0 ? void 0 : input[iterator]);
}

// node_modules/rxjs/dist/esm5/internal/util/isReadableStreamLike.js
init_tslib_es6();
function readableStreamLikeToAsyncGenerator(readableStream) {
  return __asyncGenerator(this, arguments, function readableStreamLikeToAsyncGenerator_1() {
    var reader, _a, value, done;
    return __generator(this, function(_b) {
      switch (_b.label) {
        case 0:
          reader = readableStream.getReader();
          _b.label = 1;
        case 1:
          _b.trys.push([1, , 9, 10]);
          _b.label = 2;
        case 2:
          if (false) return [3, 8];
          return [4, __await(reader.read())];
        case 3:
          _a = _b.sent(), value = _a.value, done = _a.done;
          if (!done) return [3, 5];
          return [4, __await(void 0)];
        case 4:
          return [2, _b.sent()];
        case 5:
          return [4, __await(value)];
        case 6:
          return [4, _b.sent()];
        case 7:
          _b.sent();
          return [3, 2];
        case 8:
          return [3, 10];
        case 9:
          reader.releaseLock();
          return [7];
        case 10:
          return [2];
      }
    });
  });
}
function isReadableStreamLike(obj) {
  return isFunction(obj === null || obj === void 0 ? void 0 : obj.getReader);
}

// node_modules/rxjs/dist/esm5/internal/observable/innerFrom.js
function innerFrom(input) {
  if (input instanceof Observable) {
    return input;
  }
  if (input != null) {
    if (isInteropObservable(input)) {
      return fromInteropObservable(input);
    }
    if (isArrayLike(input)) {
      return fromArrayLike(input);
    }
    if (isPromise(input)) {
      return fromPromise(input);
    }
    if (isAsyncIterable(input)) {
      return fromAsyncIterable(input);
    }
    if (isIterable(input)) {
      return fromIterable(input);
    }
    if (isReadableStreamLike(input)) {
      return fromReadableStreamLike(input);
    }
  }
  throw createInvalidObservableTypeError(input);
}
function fromInteropObservable(obj) {
  return new Observable(function(subscriber) {
    var obs = obj[observable]();
    if (isFunction(obs.subscribe)) {
      return obs.subscribe(subscriber);
    }
    throw new TypeError("Provided object does not correctly implement Symbol.observable");
  });
}
function fromArrayLike(array) {
  return new Observable(function(subscriber) {
    for (var i = 0; i < array.length && !subscriber.closed; i++) {
      subscriber.next(array[i]);
    }
    subscriber.complete();
  });
}
function fromPromise(promise) {
  return new Observable(function(subscriber) {
    promise.then(function(value) {
      if (!subscriber.closed) {
        subscriber.next(value);
        subscriber.complete();
      }
    }, function(err) {
      return subscriber.error(err);
    }).then(null, reportUnhandledError);
  });
}
function fromIterable(iterable) {
  return new Observable(function(subscriber) {
    var e_1, _a;
    try {
      for (var iterable_1 = __values(iterable), iterable_1_1 = iterable_1.next(); !iterable_1_1.done; iterable_1_1 = iterable_1.next()) {
        var value = iterable_1_1.value;
        subscriber.next(value);
        if (subscriber.closed) {
          return;
        }
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (iterable_1_1 && !iterable_1_1.done && (_a = iterable_1.return)) _a.call(iterable_1);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
    subscriber.complete();
  });
}
function fromAsyncIterable(asyncIterable) {
  return new Observable(function(subscriber) {
    process(asyncIterable, subscriber).catch(function(err) {
      return subscriber.error(err);
    });
  });
}
function fromReadableStreamLike(readableStream) {
  return fromAsyncIterable(readableStreamLikeToAsyncGenerator(readableStream));
}
function process(asyncIterable, subscriber) {
  var asyncIterable_1, asyncIterable_1_1;
  var e_2, _a;
  return __awaiter(this, void 0, void 0, function() {
    var value, e_2_1;
    return __generator(this, function(_b) {
      switch (_b.label) {
        case 0:
          _b.trys.push([0, 5, 6, 11]);
          asyncIterable_1 = __asyncValues(asyncIterable);
          _b.label = 1;
        case 1:
          return [4, asyncIterable_1.next()];
        case 2:
          if (!(asyncIterable_1_1 = _b.sent(), !asyncIterable_1_1.done)) return [3, 4];
          value = asyncIterable_1_1.value;
          subscriber.next(value);
          if (subscriber.closed) {
            return [2];
          }
          _b.label = 3;
        case 3:
          return [3, 1];
        case 4:
          return [3, 11];
        case 5:
          e_2_1 = _b.sent();
          e_2 = { error: e_2_1 };
          return [3, 11];
        case 6:
          _b.trys.push([6, , 9, 10]);
          if (!(asyncIterable_1_1 && !asyncIterable_1_1.done && (_a = asyncIterable_1.return))) return [3, 8];
          return [4, _a.call(asyncIterable_1)];
        case 7:
          _b.sent();
          _b.label = 8;
        case 8:
          return [3, 10];
        case 9:
          if (e_2) throw e_2.error;
          return [7];
        case 10:
          return [7];
        case 11:
          subscriber.complete();
          return [2];
      }
    });
  });
}

// node_modules/rxjs/dist/esm5/internal/operators/OperatorSubscriber.js
init_tslib_es6();
function createOperatorSubscriber(destination, onNext, onComplete, onError, onFinalize) {
  return new OperatorSubscriber(destination, onNext, onComplete, onError, onFinalize);
}
var OperatorSubscriber = function(_super) {
  __extends(OperatorSubscriber2, _super);
  function OperatorSubscriber2(destination, onNext, onComplete, onError, onFinalize, shouldUnsubscribe) {
    var _this = _super.call(this, destination) || this;
    _this.onFinalize = onFinalize;
    _this.shouldUnsubscribe = shouldUnsubscribe;
    _this._next = onNext ? function(value) {
      try {
        onNext(value);
      } catch (err) {
        destination.error(err);
      }
    } : _super.prototype._next;
    _this._error = onError ? function(err) {
      try {
        onError(err);
      } catch (err2) {
        destination.error(err2);
      } finally {
        this.unsubscribe();
      }
    } : _super.prototype._error;
    _this._complete = onComplete ? function() {
      try {
        onComplete();
      } catch (err) {
        destination.error(err);
      } finally {
        this.unsubscribe();
      }
    } : _super.prototype._complete;
    return _this;
  }
  OperatorSubscriber2.prototype.unsubscribe = function() {
    var _a;
    if (!this.shouldUnsubscribe || this.shouldUnsubscribe()) {
      var closed_1 = this.closed;
      _super.prototype.unsubscribe.call(this);
      !closed_1 && ((_a = this.onFinalize) === null || _a === void 0 ? void 0 : _a.call(this));
    }
  };
  return OperatorSubscriber2;
}(Subscriber);

// node_modules/rxjs/dist/esm5/internal/scheduler/AsyncAction.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/scheduler/Action.js
init_tslib_es6();
var Action = function(_super) {
  __extends(Action2, _super);
  function Action2(scheduler, work) {
    return _super.call(this) || this;
  }
  Action2.prototype.schedule = function(state, delay2) {
    if (delay2 === void 0) {
      delay2 = 0;
    }
    return this;
  };
  return Action2;
}(Subscription);

// node_modules/rxjs/dist/esm5/internal/scheduler/intervalProvider.js
init_tslib_es6();
var intervalProvider = {
  setInterval: function(handler, timeout2) {
    var args = [];
    for (var _i = 2; _i < arguments.length; _i++) {
      args[_i - 2] = arguments[_i];
    }
    var delegate = intervalProvider.delegate;
    if (delegate === null || delegate === void 0 ? void 0 : delegate.setInterval) {
      return delegate.setInterval.apply(delegate, __spreadArray([handler, timeout2], __read(args)));
    }
    return setInterval.apply(void 0, __spreadArray([handler, timeout2], __read(args)));
  },
  clearInterval: function(handle) {
    var delegate = intervalProvider.delegate;
    return ((delegate === null || delegate === void 0 ? void 0 : delegate.clearInterval) || clearInterval)(handle);
  },
  delegate: void 0
};

// node_modules/rxjs/dist/esm5/internal/scheduler/AsyncAction.js
var AsyncAction = function(_super) {
  __extends(AsyncAction2, _super);
  function AsyncAction2(scheduler, work) {
    var _this = _super.call(this, scheduler, work) || this;
    _this.scheduler = scheduler;
    _this.work = work;
    _this.pending = false;
    return _this;
  }
  AsyncAction2.prototype.schedule = function(state, delay2) {
    var _a;
    if (delay2 === void 0) {
      delay2 = 0;
    }
    if (this.closed) {
      return this;
    }
    this.state = state;
    var id = this.id;
    var scheduler = this.scheduler;
    if (id != null) {
      this.id = this.recycleAsyncId(scheduler, id, delay2);
    }
    this.pending = true;
    this.delay = delay2;
    this.id = (_a = this.id) !== null && _a !== void 0 ? _a : this.requestAsyncId(scheduler, this.id, delay2);
    return this;
  };
  AsyncAction2.prototype.requestAsyncId = function(scheduler, _id, delay2) {
    if (delay2 === void 0) {
      delay2 = 0;
    }
    return intervalProvider.setInterval(scheduler.flush.bind(scheduler, this), delay2);
  };
  AsyncAction2.prototype.recycleAsyncId = function(_scheduler, id, delay2) {
    if (delay2 === void 0) {
      delay2 = 0;
    }
    if (delay2 != null && this.delay === delay2 && this.pending === false) {
      return id;
    }
    if (id != null) {
      intervalProvider.clearInterval(id);
    }
    return void 0;
  };
  AsyncAction2.prototype.execute = function(state, delay2) {
    if (this.closed) {
      return new Error("executing a cancelled action");
    }
    this.pending = false;
    var error = this._execute(state, delay2);
    if (error) {
      return error;
    } else if (this.pending === false && this.id != null) {
      this.id = this.recycleAsyncId(this.scheduler, this.id, null);
    }
  };
  AsyncAction2.prototype._execute = function(state, _delay) {
    var errored = false;
    var errorValue;
    try {
      this.work(state);
    } catch (e) {
      errored = true;
      errorValue = e ? e : new Error("Scheduled action threw falsy error");
    }
    if (errored) {
      this.unsubscribe();
      return errorValue;
    }
  };
  AsyncAction2.prototype.unsubscribe = function() {
    if (!this.closed) {
      var _a = this, id = _a.id, scheduler = _a.scheduler;
      var actions = scheduler.actions;
      this.work = this.state = this.scheduler = null;
      this.pending = false;
      arrRemove(actions, this);
      if (id != null) {
        this.id = this.recycleAsyncId(scheduler, id, null);
      }
      this.delay = null;
      _super.prototype.unsubscribe.call(this);
    }
  };
  return AsyncAction2;
}(Action);

// node_modules/rxjs/dist/esm5/internal/scheduler/AsyncScheduler.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/scheduler/dateTimestampProvider.js
var dateTimestampProvider = {
  now: function() {
    return (dateTimestampProvider.delegate || Date).now();
  },
  delegate: void 0
};

// node_modules/rxjs/dist/esm5/internal/Scheduler.js
var Scheduler = function() {
  function Scheduler2(schedulerActionCtor, now) {
    if (now === void 0) {
      now = Scheduler2.now;
    }
    this.schedulerActionCtor = schedulerActionCtor;
    this.now = now;
  }
  Scheduler2.prototype.schedule = function(work, delay2, state) {
    if (delay2 === void 0) {
      delay2 = 0;
    }
    return new this.schedulerActionCtor(this, work).schedule(state, delay2);
  };
  Scheduler2.now = dateTimestampProvider.now;
  return Scheduler2;
}();

// node_modules/rxjs/dist/esm5/internal/scheduler/AsyncScheduler.js
var AsyncScheduler = function(_super) {
  __extends(AsyncScheduler2, _super);
  function AsyncScheduler2(SchedulerAction, now) {
    if (now === void 0) {
      now = Scheduler.now;
    }
    var _this = _super.call(this, SchedulerAction, now) || this;
    _this.actions = [];
    _this._active = false;
    return _this;
  }
  AsyncScheduler2.prototype.flush = function(action) {
    var actions = this.actions;
    if (this._active) {
      actions.push(action);
      return;
    }
    var error;
    this._active = true;
    do {
      if (error = action.execute(action.state, action.delay)) {
        break;
      }
    } while (action = actions.shift());
    this._active = false;
    if (error) {
      while (action = actions.shift()) {
        action.unsubscribe();
      }
      throw error;
    }
  };
  return AsyncScheduler2;
}(Scheduler);

// node_modules/rxjs/dist/esm5/internal/scheduler/async.js
var asyncScheduler = new AsyncScheduler(AsyncAction);

// node_modules/rxjs/dist/esm5/internal/util/isScheduler.js
function isScheduler(value) {
  return value && isFunction(value.schedule);
}

// node_modules/rxjs/dist/esm5/internal/operators/bufferCount.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/bufferTime.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/util/args.js
function last(arr) {
  return arr[arr.length - 1];
}
function popScheduler(args) {
  return isScheduler(last(args)) ? args.pop() : void 0;
}

// node_modules/rxjs/dist/esm5/internal/util/executeSchedule.js
function executeSchedule(parentSubscription, scheduler, work, delay2, repeat2) {
  if (delay2 === void 0) {
    delay2 = 0;
  }
  if (repeat2 === void 0) {
    repeat2 = false;
  }
  var scheduleSubscription = scheduler.schedule(function() {
    work();
    if (repeat2) {
      parentSubscription.add(this.schedule(null, delay2));
    } else {
      this.unsubscribe();
    }
  }, delay2);
  parentSubscription.add(scheduleSubscription);
  if (!repeat2) {
    return scheduleSubscription;
  }
}

// node_modules/rxjs/dist/esm5/internal/operators/bufferToggle.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/util/argsArgArrayOrObject.js
var isArray = Array.isArray;
var objectProto = Object.prototype;

// node_modules/rxjs/dist/esm5/internal/operators/observeOn.js
function observeOn(scheduler, delay2) {
  if (delay2 === void 0) {
    delay2 = 0;
  }
  return operate(function(source, subscriber) {
    source.subscribe(createOperatorSubscriber(subscriber, function(value) {
      return executeSchedule(subscriber, scheduler, function() {
        return subscriber.next(value);
      }, delay2);
    }, function() {
      return executeSchedule(subscriber, scheduler, function() {
        return subscriber.complete();
      }, delay2);
    }, function(err) {
      return executeSchedule(subscriber, scheduler, function() {
        return subscriber.error(err);
      }, delay2);
    }));
  });
}

// node_modules/rxjs/dist/esm5/internal/operators/subscribeOn.js
function subscribeOn(scheduler, delay2) {
  if (delay2 === void 0) {
    delay2 = 0;
  }
  return operate(function(source, subscriber) {
    subscriber.add(scheduler.schedule(function() {
      return source.subscribe(subscriber);
    }, delay2));
  });
}

// node_modules/rxjs/dist/esm5/internal/scheduled/scheduleObservable.js
function scheduleObservable(input, scheduler) {
  return innerFrom(input).pipe(subscribeOn(scheduler), observeOn(scheduler));
}

// node_modules/rxjs/dist/esm5/internal/scheduled/schedulePromise.js
function schedulePromise(input, scheduler) {
  return innerFrom(input).pipe(subscribeOn(scheduler), observeOn(scheduler));
}

// node_modules/rxjs/dist/esm5/internal/scheduled/scheduleArray.js
function scheduleArray(input, scheduler) {
  return new Observable(function(subscriber) {
    var i = 0;
    return scheduler.schedule(function() {
      if (i === input.length) {
        subscriber.complete();
      } else {
        subscriber.next(input[i++]);
        if (!subscriber.closed) {
          this.schedule();
        }
      }
    });
  });
}

// node_modules/rxjs/dist/esm5/internal/scheduled/scheduleIterable.js
function scheduleIterable(input, scheduler) {
  return new Observable(function(subscriber) {
    var iterator2;
    executeSchedule(subscriber, scheduler, function() {
      iterator2 = input[iterator]();
      executeSchedule(subscriber, scheduler, function() {
        var _a;
        var value;
        var done;
        try {
          _a = iterator2.next(), value = _a.value, done = _a.done;
        } catch (err) {
          subscriber.error(err);
          return;
        }
        if (done) {
          subscriber.complete();
        } else {
          subscriber.next(value);
        }
      }, 0, true);
    });
    return function() {
      return isFunction(iterator2 === null || iterator2 === void 0 ? void 0 : iterator2.return) && iterator2.return();
    };
  });
}

// node_modules/rxjs/dist/esm5/internal/scheduled/scheduleAsyncIterable.js
function scheduleAsyncIterable(input, scheduler) {
  if (!input) {
    throw new Error("Iterable cannot be null");
  }
  return new Observable(function(subscriber) {
    executeSchedule(subscriber, scheduler, function() {
      var iterator2 = input[Symbol.asyncIterator]();
      executeSchedule(subscriber, scheduler, function() {
        iterator2.next().then(function(result) {
          if (result.done) {
            subscriber.complete();
          } else {
            subscriber.next(result.value);
          }
        });
      }, 0, true);
    });
  });
}

// node_modules/rxjs/dist/esm5/internal/scheduled/scheduleReadableStreamLike.js
function scheduleReadableStreamLike(input, scheduler) {
  return scheduleAsyncIterable(readableStreamLikeToAsyncGenerator(input), scheduler);
}

// node_modules/rxjs/dist/esm5/internal/scheduled/scheduled.js
function scheduled(input, scheduler) {
  if (input != null) {
    if (isInteropObservable(input)) {
      return scheduleObservable(input, scheduler);
    }
    if (isArrayLike(input)) {
      return scheduleArray(input, scheduler);
    }
    if (isPromise(input)) {
      return schedulePromise(input, scheduler);
    }
    if (isAsyncIterable(input)) {
      return scheduleAsyncIterable(input, scheduler);
    }
    if (isIterable(input)) {
      return scheduleIterable(input, scheduler);
    }
    if (isReadableStreamLike(input)) {
      return scheduleReadableStreamLike(input, scheduler);
    }
  }
  throw createInvalidObservableTypeError(input);
}

// node_modules/rxjs/dist/esm5/internal/observable/from.js
function from(input, scheduler) {
  return scheduler ? scheduled(input, scheduler) : innerFrom(input);
}

// node_modules/rxjs/dist/esm5/internal/util/mapOneOrManyArgs.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/map.js
function map(project, thisArg) {
  return operate(function(source, subscriber) {
    var index = 0;
    source.subscribe(createOperatorSubscriber(subscriber, function(value) {
      subscriber.next(project.call(thisArg, value, index++));
    }));
  });
}

// node_modules/rxjs/dist/esm5/internal/util/mapOneOrManyArgs.js
var isArray2 = Array.isArray;

// node_modules/rxjs/dist/esm5/internal/operators/combineLatest.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/util/argsOrArgArray.js
var isArray3 = Array.isArray;

// node_modules/rxjs/dist/esm5/internal/operators/combineLatestWith.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/concat.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/concatWith.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/Subject.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/util/ObjectUnsubscribedError.js
var ObjectUnsubscribedError = createErrorClass(function(_super) {
  return function ObjectUnsubscribedErrorImpl() {
    _super(this);
    this.name = "ObjectUnsubscribedError";
    this.message = "object unsubscribed";
  };
});

// node_modules/rxjs/dist/esm5/internal/Subject.js
var Subject = function(_super) {
  __extends(Subject2, _super);
  function Subject2() {
    var _this = _super.call(this) || this;
    _this.closed = false;
    _this.currentObservers = null;
    _this.observers = [];
    _this.isStopped = false;
    _this.hasError = false;
    _this.thrownError = null;
    return _this;
  }
  Subject2.prototype.lift = function(operator) {
    var subject = new AnonymousSubject(this, this);
    subject.operator = operator;
    return subject;
  };
  Subject2.prototype._throwIfClosed = function() {
    if (this.closed) {
      throw new ObjectUnsubscribedError();
    }
  };
  Subject2.prototype.next = function(value) {
    var _this = this;
    errorContext(function() {
      var e_1, _a;
      _this._throwIfClosed();
      if (!_this.isStopped) {
        if (!_this.currentObservers) {
          _this.currentObservers = Array.from(_this.observers);
        }
        try {
          for (var _b = __values(_this.currentObservers), _c = _b.next(); !_c.done; _c = _b.next()) {
            var observer = _c.value;
            observer.next(value);
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
      }
    });
  };
  Subject2.prototype.error = function(err) {
    var _this = this;
    errorContext(function() {
      _this._throwIfClosed();
      if (!_this.isStopped) {
        _this.hasError = _this.isStopped = true;
        _this.thrownError = err;
        var observers = _this.observers;
        while (observers.length) {
          observers.shift().error(err);
        }
      }
    });
  };
  Subject2.prototype.complete = function() {
    var _this = this;
    errorContext(function() {
      _this._throwIfClosed();
      if (!_this.isStopped) {
        _this.isStopped = true;
        var observers = _this.observers;
        while (observers.length) {
          observers.shift().complete();
        }
      }
    });
  };
  Subject2.prototype.unsubscribe = function() {
    this.isStopped = this.closed = true;
    this.observers = this.currentObservers = null;
  };
  Object.defineProperty(Subject2.prototype, "observed", {
    get: function() {
      var _a;
      return ((_a = this.observers) === null || _a === void 0 ? void 0 : _a.length) > 0;
    },
    enumerable: false,
    configurable: true
  });
  Subject2.prototype._trySubscribe = function(subscriber) {
    this._throwIfClosed();
    return _super.prototype._trySubscribe.call(this, subscriber);
  };
  Subject2.prototype._subscribe = function(subscriber) {
    this._throwIfClosed();
    this._checkFinalizedStatuses(subscriber);
    return this._innerSubscribe(subscriber);
  };
  Subject2.prototype._innerSubscribe = function(subscriber) {
    var _this = this;
    var _a = this, hasError = _a.hasError, isStopped = _a.isStopped, observers = _a.observers;
    if (hasError || isStopped) {
      return EMPTY_SUBSCRIPTION;
    }
    this.currentObservers = null;
    observers.push(subscriber);
    return new Subscription(function() {
      _this.currentObservers = null;
      arrRemove(observers, subscriber);
    });
  };
  Subject2.prototype._checkFinalizedStatuses = function(subscriber) {
    var _a = this, hasError = _a.hasError, thrownError = _a.thrownError, isStopped = _a.isStopped;
    if (hasError) {
      subscriber.error(thrownError);
    } else if (isStopped) {
      subscriber.complete();
    }
  };
  Subject2.prototype.asObservable = function() {
    var observable2 = new Observable();
    observable2.source = this;
    return observable2;
  };
  Subject2.create = function(destination, source) {
    return new AnonymousSubject(destination, source);
  };
  return Subject2;
}(Observable);
var AnonymousSubject = function(_super) {
  __extends(AnonymousSubject2, _super);
  function AnonymousSubject2(destination, source) {
    var _this = _super.call(this) || this;
    _this.destination = destination;
    _this.source = source;
    return _this;
  }
  AnonymousSubject2.prototype.next = function(value) {
    var _a, _b;
    (_b = (_a = this.destination) === null || _a === void 0 ? void 0 : _a.next) === null || _b === void 0 ? void 0 : _b.call(_a, value);
  };
  AnonymousSubject2.prototype.error = function(err) {
    var _a, _b;
    (_b = (_a = this.destination) === null || _a === void 0 ? void 0 : _a.error) === null || _b === void 0 ? void 0 : _b.call(_a, err);
  };
  AnonymousSubject2.prototype.complete = function() {
    var _a, _b;
    (_b = (_a = this.destination) === null || _a === void 0 ? void 0 : _a.complete) === null || _b === void 0 ? void 0 : _b.call(_a);
  };
  AnonymousSubject2.prototype._subscribe = function(subscriber) {
    var _a, _b;
    return (_b = (_a = this.source) === null || _a === void 0 ? void 0 : _a.subscribe(subscriber)) !== null && _b !== void 0 ? _b : EMPTY_SUBSCRIPTION;
  };
  return AnonymousSubject2;
}(Subject);

// node_modules/rxjs/dist/esm5/internal/observable/empty.js
var EMPTY = new Observable(function(subscriber) {
  return subscriber.complete();
});

// node_modules/rxjs/dist/esm5/internal/observable/of.js
function of() {
  var args = [];
  for (var _i = 0; _i < arguments.length; _i++) {
    args[_i] = arguments[_i];
  }
  var scheduler = popScheduler(args);
  return from(args, scheduler);
}

// node_modules/rxjs/dist/esm5/internal/observable/throwError.js
function throwError(errorOrErrorFactory, scheduler) {
  var errorFactory = isFunction(errorOrErrorFactory) ? errorOrErrorFactory : function() {
    return errorOrErrorFactory;
  };
  var init = function(subscriber) {
    return subscriber.error(errorFactory());
  };
  return new Observable(scheduler ? function(subscriber) {
    return scheduler.schedule(init, 0, subscriber);
  } : init);
}

// node_modules/rxjs/dist/esm5/internal/Notification.js
var NotificationKind;
(function(NotificationKind2) {
  NotificationKind2["NEXT"] = "N";
  NotificationKind2["ERROR"] = "E";
  NotificationKind2["COMPLETE"] = "C";
})(NotificationKind || (NotificationKind = {}));
var Notification = function() {
  function Notification2(kind, value, error) {
    this.kind = kind;
    this.value = value;
    this.error = error;
    this.hasValue = kind === "N";
  }
  Notification2.prototype.observe = function(observer) {
    return observeNotification(this, observer);
  };
  Notification2.prototype.do = function(nextHandler, errorHandler, completeHandler) {
    var _a = this, kind = _a.kind, value = _a.value, error = _a.error;
    return kind === "N" ? nextHandler === null || nextHandler === void 0 ? void 0 : nextHandler(value) : kind === "E" ? errorHandler === null || errorHandler === void 0 ? void 0 : errorHandler(error) : completeHandler === null || completeHandler === void 0 ? void 0 : completeHandler();
  };
  Notification2.prototype.accept = function(nextOrObserver, error, complete) {
    var _a;
    return isFunction((_a = nextOrObserver) === null || _a === void 0 ? void 0 : _a.next) ? this.observe(nextOrObserver) : this.do(nextOrObserver, error, complete);
  };
  Notification2.prototype.toObservable = function() {
    var _a = this, kind = _a.kind, value = _a.value, error = _a.error;
    var result = kind === "N" ? of(value) : kind === "E" ? throwError(function() {
      return error;
    }) : kind === "C" ? EMPTY : 0;
    if (!result) {
      throw new TypeError("Unexpected notification kind " + kind);
    }
    return result;
  };
  Notification2.createNext = function(value) {
    return new Notification2("N", value);
  };
  Notification2.createError = function(err) {
    return new Notification2("E", void 0, err);
  };
  Notification2.createComplete = function() {
    return Notification2.completeNotification;
  };
  Notification2.completeNotification = new Notification2("C");
  return Notification2;
}();
function observeNotification(notification, observer) {
  var _a, _b, _c;
  var _d = notification, kind = _d.kind, value = _d.value, error = _d.error;
  if (typeof kind !== "string") {
    throw new TypeError('Invalid notification, missing "kind"');
  }
  kind === "N" ? (_a = observer.next) === null || _a === void 0 ? void 0 : _a.call(observer, value) : kind === "E" ? (_b = observer.error) === null || _b === void 0 ? void 0 : _b.call(observer, error) : (_c = observer.complete) === null || _c === void 0 ? void 0 : _c.call(observer);
}

// node_modules/rxjs/dist/esm5/internal/util/ArgumentOutOfRangeError.js
var ArgumentOutOfRangeError = createErrorClass(function(_super) {
  return function ArgumentOutOfRangeErrorImpl() {
    _super(this);
    this.name = "ArgumentOutOfRangeError";
    this.message = "argument out of range";
  };
});

// node_modules/rxjs/dist/esm5/internal/util/EmptyError.js
var EmptyError = createErrorClass(function(_super) {
  return function EmptyErrorImpl() {
    _super(this);
    this.name = "EmptyError";
    this.message = "no elements in sequence";
  };
});

// node_modules/rxjs/dist/esm5/internal/operators/endWith.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/takeLast.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/merge.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/mergeWith.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/observable/ConnectableObservable.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/refCount.js
function refCount() {
  return operate(function(source, subscriber) {
    var connection = null;
    source._refCount++;
    var refCounter = createOperatorSubscriber(subscriber, void 0, void 0, void 0, function() {
      if (!source || source._refCount <= 0 || 0 < --source._refCount) {
        connection = null;
        return;
      }
      var sharedConnection = source._connection;
      var conn = connection;
      connection = null;
      if (sharedConnection && (!conn || sharedConnection === conn)) {
        sharedConnection.unsubscribe();
      }
      subscriber.unsubscribe();
    });
    source.subscribe(refCounter);
    if (!refCounter.closed) {
      connection = source.connect();
    }
  });
}

// node_modules/rxjs/dist/esm5/internal/observable/ConnectableObservable.js
var ConnectableObservable = function(_super) {
  __extends(ConnectableObservable2, _super);
  function ConnectableObservable2(source, subjectFactory) {
    var _this = _super.call(this) || this;
    _this.source = source;
    _this.subjectFactory = subjectFactory;
    _this._subject = null;
    _this._refCount = 0;
    _this._connection = null;
    if (hasLift(source)) {
      _this.lift = source.lift;
    }
    return _this;
  }
  ConnectableObservable2.prototype._subscribe = function(subscriber) {
    return this.getSubject().subscribe(subscriber);
  };
  ConnectableObservable2.prototype.getSubject = function() {
    var subject = this._subject;
    if (!subject || subject.isStopped) {
      this._subject = this.subjectFactory();
    }
    return this._subject;
  };
  ConnectableObservable2.prototype._teardown = function() {
    this._refCount = 0;
    var _connection = this._connection;
    this._subject = this._connection = null;
    _connection === null || _connection === void 0 ? void 0 : _connection.unsubscribe();
  };
  ConnectableObservable2.prototype.connect = function() {
    var _this = this;
    var connection = this._connection;
    if (!connection) {
      connection = this._connection = new Subscription();
      var subject_1 = this.getSubject();
      connection.add(this.source.subscribe(createOperatorSubscriber(subject_1, void 0, function() {
        _this._teardown();
        subject_1.complete();
      }, function(err) {
        _this._teardown();
        subject_1.error(err);
      }, function() {
        return _this._teardown();
      })));
      if (connection.closed) {
        this._connection = null;
        connection = Subscription.EMPTY;
      }
    }
    return connection;
  };
  ConnectableObservable2.prototype.refCount = function() {
    return refCount()(this);
  };
  return ConnectableObservable2;
}(Observable);

// node_modules/rxjs/dist/esm5/internal/operators/onErrorResumeNextWith.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/BehaviorSubject.js
init_tslib_es6();
var BehaviorSubject = function(_super) {
  __extends(BehaviorSubject2, _super);
  function BehaviorSubject2(_value) {
    var _this = _super.call(this) || this;
    _this._value = _value;
    return _this;
  }
  Object.defineProperty(BehaviorSubject2.prototype, "value", {
    get: function() {
      return this.getValue();
    },
    enumerable: false,
    configurable: true
  });
  BehaviorSubject2.prototype._subscribe = function(subscriber) {
    var subscription = _super.prototype._subscribe.call(this, subscriber);
    !subscription.closed && subscriber.next(this._value);
    return subscription;
  };
  BehaviorSubject2.prototype.getValue = function() {
    var _a = this, hasError = _a.hasError, thrownError = _a.thrownError, _value = _a._value;
    if (hasError) {
      throw thrownError;
    }
    this._throwIfClosed();
    return _value;
  };
  BehaviorSubject2.prototype.next = function(value) {
    _super.prototype.next.call(this, this._value = value);
  };
  return BehaviorSubject2;
}(Subject);

// node_modules/rxjs/dist/esm5/internal/AsyncSubject.js
init_tslib_es6();
var AsyncSubject = function(_super) {
  __extends(AsyncSubject2, _super);
  function AsyncSubject2() {
    var _this = _super !== null && _super.apply(this, arguments) || this;
    _this._value = null;
    _this._hasValue = false;
    _this._isComplete = false;
    return _this;
  }
  AsyncSubject2.prototype._checkFinalizedStatuses = function(subscriber) {
    var _a = this, hasError = _a.hasError, _hasValue = _a._hasValue, _value = _a._value, thrownError = _a.thrownError, isStopped = _a.isStopped, _isComplete = _a._isComplete;
    if (hasError) {
      subscriber.error(thrownError);
    } else if (isStopped || _isComplete) {
      _hasValue && subscriber.next(_value);
      subscriber.complete();
    }
  };
  AsyncSubject2.prototype.next = function(value) {
    if (!this.isStopped) {
      this._value = value;
      this._hasValue = true;
    }
  };
  AsyncSubject2.prototype.complete = function() {
    var _a = this, _hasValue = _a._hasValue, _value = _a._value, _isComplete = _a._isComplete;
    if (!_isComplete) {
      this._isComplete = true;
      _hasValue && _super.prototype.next.call(this, _value);
      _super.prototype.complete.call(this);
    }
  };
  return AsyncSubject2;
}(Subject);

// node_modules/rxjs/dist/esm5/internal/ReplaySubject.js
init_tslib_es6();
var ReplaySubject = function(_super) {
  __extends(ReplaySubject2, _super);
  function ReplaySubject2(_bufferSize, _windowTime, _timestampProvider) {
    if (_bufferSize === void 0) {
      _bufferSize = Infinity;
    }
    if (_windowTime === void 0) {
      _windowTime = Infinity;
    }
    if (_timestampProvider === void 0) {
      _timestampProvider = dateTimestampProvider;
    }
    var _this = _super.call(this) || this;
    _this._bufferSize = _bufferSize;
    _this._windowTime = _windowTime;
    _this._timestampProvider = _timestampProvider;
    _this._buffer = [];
    _this._infiniteTimeWindow = true;
    _this._infiniteTimeWindow = _windowTime === Infinity;
    _this._bufferSize = Math.max(1, _bufferSize);
    _this._windowTime = Math.max(1, _windowTime);
    return _this;
  }
  ReplaySubject2.prototype.next = function(value) {
    var _a = this, isStopped = _a.isStopped, _buffer = _a._buffer, _infiniteTimeWindow = _a._infiniteTimeWindow, _timestampProvider = _a._timestampProvider, _windowTime = _a._windowTime;
    if (!isStopped) {
      _buffer.push(value);
      !_infiniteTimeWindow && _buffer.push(_timestampProvider.now() + _windowTime);
    }
    this._trimBuffer();
    _super.prototype.next.call(this, value);
  };
  ReplaySubject2.prototype._subscribe = function(subscriber) {
    this._throwIfClosed();
    this._trimBuffer();
    var subscription = this._innerSubscribe(subscriber);
    var _a = this, _infiniteTimeWindow = _a._infiniteTimeWindow, _buffer = _a._buffer;
    var copy = _buffer.slice();
    for (var i = 0; i < copy.length && !subscriber.closed; i += _infiniteTimeWindow ? 1 : 2) {
      subscriber.next(copy[i]);
    }
    this._checkFinalizedStatuses(subscriber);
    return subscription;
  };
  ReplaySubject2.prototype._trimBuffer = function() {
    var _a = this, _bufferSize = _a._bufferSize, _timestampProvider = _a._timestampProvider, _buffer = _a._buffer, _infiniteTimeWindow = _a._infiniteTimeWindow;
    var adjustedBufferSize = (_infiniteTimeWindow ? 1 : 2) * _bufferSize;
    _bufferSize < Infinity && adjustedBufferSize < _buffer.length && _buffer.splice(0, _buffer.length - adjustedBufferSize);
    if (!_infiniteTimeWindow) {
      var now = _timestampProvider.now();
      var last3 = 0;
      for (var i = 1; i < _buffer.length && _buffer[i] <= now; i += 2) {
        last3 = i;
      }
      last3 && _buffer.splice(0, last3 + 1);
    }
  };
  return ReplaySubject2;
}(Subject);

// node_modules/rxjs/dist/esm5/internal/operators/race.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/raceWith.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/share.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/util/SequenceError.js
var SequenceError = createErrorClass(function(_super) {
  return function SequenceErrorImpl(message) {
    _super(this);
    this.name = "SequenceError";
    this.message = message;
  };
});

// node_modules/rxjs/dist/esm5/internal/util/NotFoundError.js
var NotFoundError = createErrorClass(function(_super) {
  return function NotFoundErrorImpl(message) {
    _super(this);
    this.name = "NotFoundError";
    this.message = message;
  };
});

// node_modules/rxjs/dist/esm5/internal/operators/timeout.js
var TimeoutError = createErrorClass(function(_super) {
  return function TimeoutErrorImpl(info) {
    if (info === void 0) {
      info = null;
    }
    _super(this);
    this.message = "Timeout has occurred";
    this.name = "TimeoutError";
    this.info = info;
  };
});

// node_modules/rxjs/dist/esm5/internal/operators/windowCount.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/windowToggle.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/withLatestFrom.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/zip.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/observable/zip.js
init_tslib_es6();

// node_modules/rxjs/dist/esm5/internal/operators/zipWith.js
init_tslib_es6();

// node_modules/@xmtp/proto/ts/dist/esm/mls/api/v1/mls.pb.js
var import_minimal21 = __toESM(require_minimal());
var protobufPackage17 = "xmtp.mls.api.v1";
var SortDirection2;
(function(SortDirection3) {
  SortDirection3[SortDirection3["SORT_DIRECTION_UNSPECIFIED"] = 0] = "SORT_DIRECTION_UNSPECIFIED";
  SortDirection3[SortDirection3["SORT_DIRECTION_ASCENDING"] = 1] = "SORT_DIRECTION_ASCENDING";
  SortDirection3[SortDirection3["SORT_DIRECTION_DESCENDING"] = 2] = "SORT_DIRECTION_DESCENDING";
  SortDirection3[SortDirection3["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(SortDirection2 || (SortDirection2 = {}));
function sortDirectionFromJSON(object) {
  switch (object) {
    case 0:
    case "SORT_DIRECTION_UNSPECIFIED":
      return SortDirection2.SORT_DIRECTION_UNSPECIFIED;
    case 1:
    case "SORT_DIRECTION_ASCENDING":
      return SortDirection2.SORT_DIRECTION_ASCENDING;
    case 2:
    case "SORT_DIRECTION_DESCENDING":
      return SortDirection2.SORT_DIRECTION_DESCENDING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SortDirection2.UNRECOGNIZED;
  }
}
function sortDirectionToJSON(object) {
  switch (object) {
    case SortDirection2.SORT_DIRECTION_UNSPECIFIED:
      return "SORT_DIRECTION_UNSPECIFIED";
    case SortDirection2.SORT_DIRECTION_ASCENDING:
      return "SORT_DIRECTION_ASCENDING";
    case SortDirection2.SORT_DIRECTION_DESCENDING:
      return "SORT_DIRECTION_DESCENDING";
    case SortDirection2.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseWelcomeMessage() {
  return { v1: void 0 };
}
var WelcomeMessage = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.v1 !== void 0) {
      WelcomeMessage_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseWelcomeMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = WelcomeMessage_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet19(object.v1) ? WelcomeMessage_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? WelcomeMessage_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseWelcomeMessage();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? WelcomeMessage_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseWelcomeMessage_V1() {
  return {
    id: long_default.UZERO,
    createdNs: long_default.UZERO,
    installationKey: new Uint8Array(),
    data: new Uint8Array(),
    hpkePublicKey: new Uint8Array(),
    wrapperAlgorithm: 0,
    welcomeMetadata: new Uint8Array()
  };
}
var WelcomeMessage_V1 = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (!message.id.isZero()) {
      writer.uint32(8).uint64(message.id);
    }
    if (!message.createdNs.isZero()) {
      writer.uint32(16).uint64(message.createdNs);
    }
    if (message.installationKey.length !== 0) {
      writer.uint32(26).bytes(message.installationKey);
    }
    if (message.data.length !== 0) {
      writer.uint32(34).bytes(message.data);
    }
    if (message.hpkePublicKey.length !== 0) {
      writer.uint32(42).bytes(message.hpkePublicKey);
    }
    if (message.wrapperAlgorithm !== 0) {
      writer.uint32(48).int32(message.wrapperAlgorithm);
    }
    if (message.welcomeMetadata.length !== 0) {
      writer.uint32(58).bytes(message.welcomeMetadata);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseWelcomeMessage_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.id = reader.uint64();
          break;
        case 2:
          message.createdNs = reader.uint64();
          break;
        case 3:
          message.installationKey = reader.bytes();
          break;
        case 4:
          message.data = reader.bytes();
          break;
        case 5:
          message.hpkePublicKey = reader.bytes();
          break;
        case 6:
          message.wrapperAlgorithm = reader.int32();
          break;
        case 7:
          message.welcomeMetadata = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      id: isSet19(object.id) ? long_default.fromValue(object.id) : long_default.UZERO,
      createdNs: isSet19(object.createdNs) ? long_default.fromValue(object.createdNs) : long_default.UZERO,
      installationKey: isSet19(object.installationKey) ? bytesFromBase6415(object.installationKey) : new Uint8Array(),
      data: isSet19(object.data) ? bytesFromBase6415(object.data) : new Uint8Array(),
      hpkePublicKey: isSet19(object.hpkePublicKey) ? bytesFromBase6415(object.hpkePublicKey) : new Uint8Array(),
      wrapperAlgorithm: isSet19(object.wrapperAlgorithm) ? welcomeWrapperAlgorithmFromJSON(object.wrapperAlgorithm) : 0,
      welcomeMetadata: isSet19(object.welcomeMetadata) ? bytesFromBase6415(object.welcomeMetadata) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.id !== void 0 && (obj.id = (message.id || long_default.UZERO).toString());
    message.createdNs !== void 0 && (obj.createdNs = (message.createdNs || long_default.UZERO).toString());
    message.installationKey !== void 0 && (obj.installationKey = base64FromBytes15(message.installationKey !== void 0 ? message.installationKey : new Uint8Array()));
    message.data !== void 0 && (obj.data = base64FromBytes15(message.data !== void 0 ? message.data : new Uint8Array()));
    message.hpkePublicKey !== void 0 && (obj.hpkePublicKey = base64FromBytes15(message.hpkePublicKey !== void 0 ? message.hpkePublicKey : new Uint8Array()));
    message.wrapperAlgorithm !== void 0 && (obj.wrapperAlgorithm = welcomeWrapperAlgorithmToJSON(message.wrapperAlgorithm));
    message.welcomeMetadata !== void 0 && (obj.welcomeMetadata = base64FromBytes15(message.welcomeMetadata !== void 0 ? message.welcomeMetadata : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d, _e;
    const message = createBaseWelcomeMessage_V1();
    message.id = object.id !== void 0 && object.id !== null ? long_default.fromValue(object.id) : long_default.UZERO;
    message.createdNs = object.createdNs !== void 0 && object.createdNs !== null ? long_default.fromValue(object.createdNs) : long_default.UZERO;
    message.installationKey = (_a = object.installationKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.data = (_b = object.data) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.hpkePublicKey = (_c = object.hpkePublicKey) !== null && _c !== void 0 ? _c : new Uint8Array();
    message.wrapperAlgorithm = (_d = object.wrapperAlgorithm) !== null && _d !== void 0 ? _d : 0;
    message.welcomeMetadata = (_e = object.welcomeMetadata) !== null && _e !== void 0 ? _e : new Uint8Array();
    return message;
  }
};
function createBaseWelcomeMessageInput() {
  return { v1: void 0 };
}
var WelcomeMessageInput = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.v1 !== void 0) {
      WelcomeMessageInput_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseWelcomeMessageInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = WelcomeMessageInput_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet19(object.v1) ? WelcomeMessageInput_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? WelcomeMessageInput_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseWelcomeMessageInput();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? WelcomeMessageInput_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseWelcomeMessageInput_V1() {
  return {
    installationKey: new Uint8Array(),
    data: new Uint8Array(),
    hpkePublicKey: new Uint8Array(),
    wrapperAlgorithm: 0,
    welcomeMetadata: new Uint8Array()
  };
}
var WelcomeMessageInput_V1 = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.installationKey.length !== 0) {
      writer.uint32(10).bytes(message.installationKey);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    if (message.hpkePublicKey.length !== 0) {
      writer.uint32(26).bytes(message.hpkePublicKey);
    }
    if (message.wrapperAlgorithm !== 0) {
      writer.uint32(32).int32(message.wrapperAlgorithm);
    }
    if (message.welcomeMetadata.length !== 0) {
      writer.uint32(58).bytes(message.welcomeMetadata);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseWelcomeMessageInput_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installationKey = reader.bytes();
          break;
        case 2:
          message.data = reader.bytes();
          break;
        case 3:
          message.hpkePublicKey = reader.bytes();
          break;
        case 4:
          message.wrapperAlgorithm = reader.int32();
          break;
        case 7:
          message.welcomeMetadata = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installationKey: isSet19(object.installationKey) ? bytesFromBase6415(object.installationKey) : new Uint8Array(),
      data: isSet19(object.data) ? bytesFromBase6415(object.data) : new Uint8Array(),
      hpkePublicKey: isSet19(object.hpkePublicKey) ? bytesFromBase6415(object.hpkePublicKey) : new Uint8Array(),
      wrapperAlgorithm: isSet19(object.wrapperAlgorithm) ? welcomeWrapperAlgorithmFromJSON(object.wrapperAlgorithm) : 0,
      welcomeMetadata: isSet19(object.welcomeMetadata) ? bytesFromBase6415(object.welcomeMetadata) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.installationKey !== void 0 && (obj.installationKey = base64FromBytes15(message.installationKey !== void 0 ? message.installationKey : new Uint8Array()));
    message.data !== void 0 && (obj.data = base64FromBytes15(message.data !== void 0 ? message.data : new Uint8Array()));
    message.hpkePublicKey !== void 0 && (obj.hpkePublicKey = base64FromBytes15(message.hpkePublicKey !== void 0 ? message.hpkePublicKey : new Uint8Array()));
    message.wrapperAlgorithm !== void 0 && (obj.wrapperAlgorithm = welcomeWrapperAlgorithmToJSON(message.wrapperAlgorithm));
    message.welcomeMetadata !== void 0 && (obj.welcomeMetadata = base64FromBytes15(message.welcomeMetadata !== void 0 ? message.welcomeMetadata : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d, _e;
    const message = createBaseWelcomeMessageInput_V1();
    message.installationKey = (_a = object.installationKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.data = (_b = object.data) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.hpkePublicKey = (_c = object.hpkePublicKey) !== null && _c !== void 0 ? _c : new Uint8Array();
    message.wrapperAlgorithm = (_d = object.wrapperAlgorithm) !== null && _d !== void 0 ? _d : 0;
    message.welcomeMetadata = (_e = object.welcomeMetadata) !== null && _e !== void 0 ? _e : new Uint8Array();
    return message;
  }
};
function createBaseWelcomeMetadata() {
  return { messageCursor: long_default.UZERO };
}
var WelcomeMetadata = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (!message.messageCursor.isZero()) {
      writer.uint32(8).uint64(message.messageCursor);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseWelcomeMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.messageCursor = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageCursor: isSet19(object.messageCursor) ? long_default.fromValue(object.messageCursor) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.messageCursor !== void 0 && (obj.messageCursor = (message.messageCursor || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    const message = createBaseWelcomeMetadata();
    message.messageCursor = object.messageCursor !== void 0 && object.messageCursor !== null ? long_default.fromValue(object.messageCursor) : long_default.UZERO;
    return message;
  }
};
function createBaseGroupMessage() {
  return { v1: void 0 };
}
var GroupMessage = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.v1 !== void 0) {
      GroupMessage_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = GroupMessage_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet19(object.v1) ? GroupMessage_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? GroupMessage_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseGroupMessage();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? GroupMessage_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseGroupMessage_V1() {
  return {
    id: long_default.UZERO,
    createdNs: long_default.UZERO,
    groupId: new Uint8Array(),
    data: new Uint8Array(),
    senderHmac: new Uint8Array(),
    shouldPush: false
  };
}
var GroupMessage_V1 = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (!message.id.isZero()) {
      writer.uint32(8).uint64(message.id);
    }
    if (!message.createdNs.isZero()) {
      writer.uint32(16).uint64(message.createdNs);
    }
    if (message.groupId.length !== 0) {
      writer.uint32(26).bytes(message.groupId);
    }
    if (message.data.length !== 0) {
      writer.uint32(34).bytes(message.data);
    }
    if (message.senderHmac.length !== 0) {
      writer.uint32(42).bytes(message.senderHmac);
    }
    if (message.shouldPush === true) {
      writer.uint32(48).bool(message.shouldPush);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupMessage_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.id = reader.uint64();
          break;
        case 2:
          message.createdNs = reader.uint64();
          break;
        case 3:
          message.groupId = reader.bytes();
          break;
        case 4:
          message.data = reader.bytes();
          break;
        case 5:
          message.senderHmac = reader.bytes();
          break;
        case 6:
          message.shouldPush = reader.bool();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      id: isSet19(object.id) ? long_default.fromValue(object.id) : long_default.UZERO,
      createdNs: isSet19(object.createdNs) ? long_default.fromValue(object.createdNs) : long_default.UZERO,
      groupId: isSet19(object.groupId) ? bytesFromBase6415(object.groupId) : new Uint8Array(),
      data: isSet19(object.data) ? bytesFromBase6415(object.data) : new Uint8Array(),
      senderHmac: isSet19(object.senderHmac) ? bytesFromBase6415(object.senderHmac) : new Uint8Array(),
      shouldPush: isSet19(object.shouldPush) ? Boolean(object.shouldPush) : false
    };
  },
  toJSON(message) {
    const obj = {};
    message.id !== void 0 && (obj.id = (message.id || long_default.UZERO).toString());
    message.createdNs !== void 0 && (obj.createdNs = (message.createdNs || long_default.UZERO).toString());
    message.groupId !== void 0 && (obj.groupId = base64FromBytes15(message.groupId !== void 0 ? message.groupId : new Uint8Array()));
    message.data !== void 0 && (obj.data = base64FromBytes15(message.data !== void 0 ? message.data : new Uint8Array()));
    message.senderHmac !== void 0 && (obj.senderHmac = base64FromBytes15(message.senderHmac !== void 0 ? message.senderHmac : new Uint8Array()));
    message.shouldPush !== void 0 && (obj.shouldPush = message.shouldPush);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d;
    const message = createBaseGroupMessage_V1();
    message.id = object.id !== void 0 && object.id !== null ? long_default.fromValue(object.id) : long_default.UZERO;
    message.createdNs = object.createdNs !== void 0 && object.createdNs !== null ? long_default.fromValue(object.createdNs) : long_default.UZERO;
    message.groupId = (_a = object.groupId) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.data = (_b = object.data) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.senderHmac = (_c = object.senderHmac) !== null && _c !== void 0 ? _c : new Uint8Array();
    message.shouldPush = (_d = object.shouldPush) !== null && _d !== void 0 ? _d : false;
    return message;
  }
};
function createBaseGroupMessageInput() {
  return { v1: void 0 };
}
var GroupMessageInput = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.v1 !== void 0) {
      GroupMessageInput_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupMessageInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = GroupMessageInput_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet19(object.v1) ? GroupMessageInput_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? GroupMessageInput_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseGroupMessageInput();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? GroupMessageInput_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseGroupMessageInput_V1() {
  return {
    data: new Uint8Array(),
    senderHmac: new Uint8Array(),
    shouldPush: false
  };
}
var GroupMessageInput_V1 = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    if (message.senderHmac.length !== 0) {
      writer.uint32(18).bytes(message.senderHmac);
    }
    if (message.shouldPush === true) {
      writer.uint32(24).bool(message.shouldPush);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupMessageInput_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.data = reader.bytes();
          break;
        case 2:
          message.senderHmac = reader.bytes();
          break;
        case 3:
          message.shouldPush = reader.bool();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      data: isSet19(object.data) ? bytesFromBase6415(object.data) : new Uint8Array(),
      senderHmac: isSet19(object.senderHmac) ? bytesFromBase6415(object.senderHmac) : new Uint8Array(),
      shouldPush: isSet19(object.shouldPush) ? Boolean(object.shouldPush) : false
    };
  },
  toJSON(message) {
    const obj = {};
    message.data !== void 0 && (obj.data = base64FromBytes15(message.data !== void 0 ? message.data : new Uint8Array()));
    message.senderHmac !== void 0 && (obj.senderHmac = base64FromBytes15(message.senderHmac !== void 0 ? message.senderHmac : new Uint8Array()));
    message.shouldPush !== void 0 && (obj.shouldPush = message.shouldPush);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseGroupMessageInput_V1();
    message.data = (_a = object.data) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.senderHmac = (_b = object.senderHmac) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.shouldPush = (_c = object.shouldPush) !== null && _c !== void 0 ? _c : false;
    return message;
  }
};
function createBaseSendGroupMessagesRequest() {
  return { messages: [] };
}
var SendGroupMessagesRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.messages) {
      GroupMessageInput.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendGroupMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.messages.push(GroupMessageInput.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      messages: Array.isArray(object === null || object === void 0 ? void 0 : object.messages) ? object.messages.map((e) => GroupMessageInput.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messages) {
      obj.messages = message.messages.map((e) => e ? GroupMessageInput.toJSON(e) : void 0);
    } else {
      obj.messages = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSendGroupMessagesRequest();
    message.messages = ((_a = object.messages) === null || _a === void 0 ? void 0 : _a.map((e) => GroupMessageInput.fromPartial(e))) || [];
    return message;
  }
};
function createBaseSendWelcomeMessagesRequest() {
  return { messages: [] };
}
var SendWelcomeMessagesRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.messages) {
      WelcomeMessageInput.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendWelcomeMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.messages.push(WelcomeMessageInput.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      messages: Array.isArray(object === null || object === void 0 ? void 0 : object.messages) ? object.messages.map((e) => WelcomeMessageInput.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messages) {
      obj.messages = message.messages.map((e) => e ? WelcomeMessageInput.toJSON(e) : void 0);
    } else {
      obj.messages = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSendWelcomeMessagesRequest();
    message.messages = ((_a = object.messages) === null || _a === void 0 ? void 0 : _a.map((e) => WelcomeMessageInput.fromPartial(e))) || [];
    return message;
  }
};
function createBaseKeyPackageUpload() {
  return { keyPackageTlsSerialized: new Uint8Array() };
}
var KeyPackageUpload = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.keyPackageTlsSerialized.length !== 0) {
      writer.uint32(10).bytes(message.keyPackageTlsSerialized);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseKeyPackageUpload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyPackageTlsSerialized = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyPackageTlsSerialized: isSet19(object.keyPackageTlsSerialized) ? bytesFromBase6415(object.keyPackageTlsSerialized) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.keyPackageTlsSerialized !== void 0 && (obj.keyPackageTlsSerialized = base64FromBytes15(message.keyPackageTlsSerialized !== void 0 ? message.keyPackageTlsSerialized : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseKeyPackageUpload();
    message.keyPackageTlsSerialized = (_a = object.keyPackageTlsSerialized) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseRegisterInstallationRequest() {
  return { keyPackage: void 0, isInboxIdCredential: false };
}
var RegisterInstallationRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.keyPackage !== void 0) {
      KeyPackageUpload.encode(message.keyPackage, writer.uint32(10).fork()).ldelim();
    }
    if (message.isInboxIdCredential === true) {
      writer.uint32(16).bool(message.isInboxIdCredential);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRegisterInstallationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyPackage = KeyPackageUpload.decode(reader, reader.uint32());
          break;
        case 2:
          message.isInboxIdCredential = reader.bool();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyPackage: isSet19(object.keyPackage) ? KeyPackageUpload.fromJSON(object.keyPackage) : void 0,
      isInboxIdCredential: isSet19(object.isInboxIdCredential) ? Boolean(object.isInboxIdCredential) : false
    };
  },
  toJSON(message) {
    const obj = {};
    message.keyPackage !== void 0 && (obj.keyPackage = message.keyPackage ? KeyPackageUpload.toJSON(message.keyPackage) : void 0);
    message.isInboxIdCredential !== void 0 && (obj.isInboxIdCredential = message.isInboxIdCredential);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseRegisterInstallationRequest();
    message.keyPackage = object.keyPackage !== void 0 && object.keyPackage !== null ? KeyPackageUpload.fromPartial(object.keyPackage) : void 0;
    message.isInboxIdCredential = (_a = object.isInboxIdCredential) !== null && _a !== void 0 ? _a : false;
    return message;
  }
};
function createBaseRegisterInstallationResponse() {
  return { installationKey: new Uint8Array() };
}
var RegisterInstallationResponse = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.installationKey.length !== 0) {
      writer.uint32(10).bytes(message.installationKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRegisterInstallationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installationKey = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installationKey: isSet19(object.installationKey) ? bytesFromBase6415(object.installationKey) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.installationKey !== void 0 && (obj.installationKey = base64FromBytes15(message.installationKey !== void 0 ? message.installationKey : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseRegisterInstallationResponse();
    message.installationKey = (_a = object.installationKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseUploadKeyPackageRequest() {
  return { keyPackage: void 0, isInboxIdCredential: false };
}
var UploadKeyPackageRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.keyPackage !== void 0) {
      KeyPackageUpload.encode(message.keyPackage, writer.uint32(10).fork()).ldelim();
    }
    if (message.isInboxIdCredential === true) {
      writer.uint32(16).bool(message.isInboxIdCredential);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUploadKeyPackageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyPackage = KeyPackageUpload.decode(reader, reader.uint32());
          break;
        case 2:
          message.isInboxIdCredential = reader.bool();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyPackage: isSet19(object.keyPackage) ? KeyPackageUpload.fromJSON(object.keyPackage) : void 0,
      isInboxIdCredential: isSet19(object.isInboxIdCredential) ? Boolean(object.isInboxIdCredential) : false
    };
  },
  toJSON(message) {
    const obj = {};
    message.keyPackage !== void 0 && (obj.keyPackage = message.keyPackage ? KeyPackageUpload.toJSON(message.keyPackage) : void 0);
    message.isInboxIdCredential !== void 0 && (obj.isInboxIdCredential = message.isInboxIdCredential);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseUploadKeyPackageRequest();
    message.keyPackage = object.keyPackage !== void 0 && object.keyPackage !== null ? KeyPackageUpload.fromPartial(object.keyPackage) : void 0;
    message.isInboxIdCredential = (_a = object.isInboxIdCredential) !== null && _a !== void 0 ? _a : false;
    return message;
  }
};
function createBaseFetchKeyPackagesRequest() {
  return { installationKeys: [] };
}
var FetchKeyPackagesRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.installationKeys) {
      writer.uint32(10).bytes(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFetchKeyPackagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installationKeys.push(reader.bytes());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installationKeys: Array.isArray(object === null || object === void 0 ? void 0 : object.installationKeys) ? object.installationKeys.map((e) => bytesFromBase6415(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.installationKeys) {
      obj.installationKeys = message.installationKeys.map((e) => base64FromBytes15(e !== void 0 ? e : new Uint8Array()));
    } else {
      obj.installationKeys = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseFetchKeyPackagesRequest();
    message.installationKeys = ((_a = object.installationKeys) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBaseFetchKeyPackagesResponse() {
  return { keyPackages: [] };
}
var FetchKeyPackagesResponse = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.keyPackages) {
      FetchKeyPackagesResponse_KeyPackage.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFetchKeyPackagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyPackages.push(FetchKeyPackagesResponse_KeyPackage.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyPackages: Array.isArray(object === null || object === void 0 ? void 0 : object.keyPackages) ? object.keyPackages.map((e) => FetchKeyPackagesResponse_KeyPackage.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.keyPackages) {
      obj.keyPackages = message.keyPackages.map((e) => e ? FetchKeyPackagesResponse_KeyPackage.toJSON(e) : void 0);
    } else {
      obj.keyPackages = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseFetchKeyPackagesResponse();
    message.keyPackages = ((_a = object.keyPackages) === null || _a === void 0 ? void 0 : _a.map((e) => FetchKeyPackagesResponse_KeyPackage.fromPartial(e))) || [];
    return message;
  }
};
function createBaseFetchKeyPackagesResponse_KeyPackage() {
  return { keyPackageTlsSerialized: new Uint8Array() };
}
var FetchKeyPackagesResponse_KeyPackage = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.keyPackageTlsSerialized.length !== 0) {
      writer.uint32(10).bytes(message.keyPackageTlsSerialized);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFetchKeyPackagesResponse_KeyPackage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyPackageTlsSerialized = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyPackageTlsSerialized: isSet19(object.keyPackageTlsSerialized) ? bytesFromBase6415(object.keyPackageTlsSerialized) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.keyPackageTlsSerialized !== void 0 && (obj.keyPackageTlsSerialized = base64FromBytes15(message.keyPackageTlsSerialized !== void 0 ? message.keyPackageTlsSerialized : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseFetchKeyPackagesResponse_KeyPackage();
    message.keyPackageTlsSerialized = (_a = object.keyPackageTlsSerialized) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseRevokeInstallationRequest() {
  return { installationKey: new Uint8Array(), walletSignature: void 0 };
}
var RevokeInstallationRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.installationKey.length !== 0) {
      writer.uint32(10).bytes(message.installationKey);
    }
    if (message.walletSignature !== void 0) {
      Signature.encode(message.walletSignature, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRevokeInstallationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installationKey = reader.bytes();
          break;
        case 2:
          message.walletSignature = Signature.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installationKey: isSet19(object.installationKey) ? bytesFromBase6415(object.installationKey) : new Uint8Array(),
      walletSignature: isSet19(object.walletSignature) ? Signature.fromJSON(object.walletSignature) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.installationKey !== void 0 && (obj.installationKey = base64FromBytes15(message.installationKey !== void 0 ? message.installationKey : new Uint8Array()));
    message.walletSignature !== void 0 && (obj.walletSignature = message.walletSignature ? Signature.toJSON(message.walletSignature) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseRevokeInstallationRequest();
    message.installationKey = (_a = object.installationKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.walletSignature = object.walletSignature !== void 0 && object.walletSignature !== null ? Signature.fromPartial(object.walletSignature) : void 0;
    return message;
  }
};
function createBaseGetIdentityUpdatesRequest() {
  return { accountAddresses: [], startTimeNs: long_default.UZERO };
}
var GetIdentityUpdatesRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.accountAddresses) {
      writer.uint32(10).string(v);
    }
    if (!message.startTimeNs.isZero()) {
      writer.uint32(16).uint64(message.startTimeNs);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIdentityUpdatesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.accountAddresses.push(reader.string());
          break;
        case 2:
          message.startTimeNs = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      accountAddresses: Array.isArray(object === null || object === void 0 ? void 0 : object.accountAddresses) ? object.accountAddresses.map((e) => String(e)) : [],
      startTimeNs: isSet19(object.startTimeNs) ? long_default.fromValue(object.startTimeNs) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.accountAddresses) {
      obj.accountAddresses = message.accountAddresses.map((e) => e);
    } else {
      obj.accountAddresses = [];
    }
    message.startTimeNs !== void 0 && (obj.startTimeNs = (message.startTimeNs || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetIdentityUpdatesRequest();
    message.accountAddresses = ((_a = object.accountAddresses) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    message.startTimeNs = object.startTimeNs !== void 0 && object.startTimeNs !== null ? long_default.fromValue(object.startTimeNs) : long_default.UZERO;
    return message;
  }
};
function createBaseGetIdentityUpdatesResponse() {
  return { updates: [] };
}
var GetIdentityUpdatesResponse = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.updates) {
      GetIdentityUpdatesResponse_WalletUpdates.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIdentityUpdatesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.updates.push(GetIdentityUpdatesResponse_WalletUpdates.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      updates: Array.isArray(object === null || object === void 0 ? void 0 : object.updates) ? object.updates.map((e) => GetIdentityUpdatesResponse_WalletUpdates.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.updates) {
      obj.updates = message.updates.map((e) => e ? GetIdentityUpdatesResponse_WalletUpdates.toJSON(e) : void 0);
    } else {
      obj.updates = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetIdentityUpdatesResponse();
    message.updates = ((_a = object.updates) === null || _a === void 0 ? void 0 : _a.map((e) => GetIdentityUpdatesResponse_WalletUpdates.fromPartial(e))) || [];
    return message;
  }
};
function createBaseGetIdentityUpdatesResponse_NewInstallationUpdate() {
  return {
    installationKey: new Uint8Array(),
    credentialIdentity: new Uint8Array()
  };
}
var GetIdentityUpdatesResponse_NewInstallationUpdate = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.installationKey.length !== 0) {
      writer.uint32(10).bytes(message.installationKey);
    }
    if (message.credentialIdentity.length !== 0) {
      writer.uint32(18).bytes(message.credentialIdentity);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIdentityUpdatesResponse_NewInstallationUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installationKey = reader.bytes();
          break;
        case 2:
          message.credentialIdentity = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installationKey: isSet19(object.installationKey) ? bytesFromBase6415(object.installationKey) : new Uint8Array(),
      credentialIdentity: isSet19(object.credentialIdentity) ? bytesFromBase6415(object.credentialIdentity) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.installationKey !== void 0 && (obj.installationKey = base64FromBytes15(message.installationKey !== void 0 ? message.installationKey : new Uint8Array()));
    message.credentialIdentity !== void 0 && (obj.credentialIdentity = base64FromBytes15(message.credentialIdentity !== void 0 ? message.credentialIdentity : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseGetIdentityUpdatesResponse_NewInstallationUpdate();
    message.installationKey = (_a = object.installationKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.credentialIdentity = (_b = object.credentialIdentity) !== null && _b !== void 0 ? _b : new Uint8Array();
    return message;
  }
};
function createBaseGetIdentityUpdatesResponse_RevokedInstallationUpdate() {
  return { installationKey: new Uint8Array() };
}
var GetIdentityUpdatesResponse_RevokedInstallationUpdate = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.installationKey.length !== 0) {
      writer.uint32(10).bytes(message.installationKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIdentityUpdatesResponse_RevokedInstallationUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installationKey = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installationKey: isSet19(object.installationKey) ? bytesFromBase6415(object.installationKey) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.installationKey !== void 0 && (obj.installationKey = base64FromBytes15(message.installationKey !== void 0 ? message.installationKey : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetIdentityUpdatesResponse_RevokedInstallationUpdate();
    message.installationKey = (_a = object.installationKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseGetIdentityUpdatesResponse_Update() {
  return {
    timestampNs: long_default.UZERO,
    newInstallation: void 0,
    revokedInstallation: void 0
  };
}
var GetIdentityUpdatesResponse_Update = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (!message.timestampNs.isZero()) {
      writer.uint32(8).uint64(message.timestampNs);
    }
    if (message.newInstallation !== void 0) {
      GetIdentityUpdatesResponse_NewInstallationUpdate.encode(message.newInstallation, writer.uint32(18).fork()).ldelim();
    }
    if (message.revokedInstallation !== void 0) {
      GetIdentityUpdatesResponse_RevokedInstallationUpdate.encode(message.revokedInstallation, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIdentityUpdatesResponse_Update();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.timestampNs = reader.uint64();
          break;
        case 2:
          message.newInstallation = GetIdentityUpdatesResponse_NewInstallationUpdate.decode(reader, reader.uint32());
          break;
        case 3:
          message.revokedInstallation = GetIdentityUpdatesResponse_RevokedInstallationUpdate.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      timestampNs: isSet19(object.timestampNs) ? long_default.fromValue(object.timestampNs) : long_default.UZERO,
      newInstallation: isSet19(object.newInstallation) ? GetIdentityUpdatesResponse_NewInstallationUpdate.fromJSON(object.newInstallation) : void 0,
      revokedInstallation: isSet19(object.revokedInstallation) ? GetIdentityUpdatesResponse_RevokedInstallationUpdate.fromJSON(object.revokedInstallation) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.timestampNs !== void 0 && (obj.timestampNs = (message.timestampNs || long_default.UZERO).toString());
    message.newInstallation !== void 0 && (obj.newInstallation = message.newInstallation ? GetIdentityUpdatesResponse_NewInstallationUpdate.toJSON(message.newInstallation) : void 0);
    message.revokedInstallation !== void 0 && (obj.revokedInstallation = message.revokedInstallation ? GetIdentityUpdatesResponse_RevokedInstallationUpdate.toJSON(message.revokedInstallation) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseGetIdentityUpdatesResponse_Update();
    message.timestampNs = object.timestampNs !== void 0 && object.timestampNs !== null ? long_default.fromValue(object.timestampNs) : long_default.UZERO;
    message.newInstallation = object.newInstallation !== void 0 && object.newInstallation !== null ? GetIdentityUpdatesResponse_NewInstallationUpdate.fromPartial(object.newInstallation) : void 0;
    message.revokedInstallation = object.revokedInstallation !== void 0 && object.revokedInstallation !== null ? GetIdentityUpdatesResponse_RevokedInstallationUpdate.fromPartial(object.revokedInstallation) : void 0;
    return message;
  }
};
function createBaseGetIdentityUpdatesResponse_WalletUpdates() {
  return { updates: [] };
}
var GetIdentityUpdatesResponse_WalletUpdates = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.updates) {
      GetIdentityUpdatesResponse_Update.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIdentityUpdatesResponse_WalletUpdates();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.updates.push(GetIdentityUpdatesResponse_Update.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      updates: Array.isArray(object === null || object === void 0 ? void 0 : object.updates) ? object.updates.map((e) => GetIdentityUpdatesResponse_Update.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.updates) {
      obj.updates = message.updates.map((e) => e ? GetIdentityUpdatesResponse_Update.toJSON(e) : void 0);
    } else {
      obj.updates = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGetIdentityUpdatesResponse_WalletUpdates();
    message.updates = ((_a = object.updates) === null || _a === void 0 ? void 0 : _a.map((e) => GetIdentityUpdatesResponse_Update.fromPartial(e))) || [];
    return message;
  }
};
function createBasePagingInfo() {
  return { direction: 0, limit: 0, idCursor: long_default.UZERO };
}
var PagingInfo = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.direction !== 0) {
      writer.uint32(8).int32(message.direction);
    }
    if (message.limit !== 0) {
      writer.uint32(16).uint32(message.limit);
    }
    if (!message.idCursor.isZero()) {
      writer.uint32(24).uint64(message.idCursor);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePagingInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.direction = reader.int32();
          break;
        case 2:
          message.limit = reader.uint32();
          break;
        case 3:
          message.idCursor = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      direction: isSet19(object.direction) ? sortDirectionFromJSON(object.direction) : 0,
      limit: isSet19(object.limit) ? Number(object.limit) : 0,
      idCursor: isSet19(object.idCursor) ? long_default.fromValue(object.idCursor) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.direction !== void 0 && (obj.direction = sortDirectionToJSON(message.direction));
    message.limit !== void 0 && (obj.limit = Math.round(message.limit));
    message.idCursor !== void 0 && (obj.idCursor = (message.idCursor || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBasePagingInfo();
    message.direction = (_a = object.direction) !== null && _a !== void 0 ? _a : 0;
    message.limit = (_b = object.limit) !== null && _b !== void 0 ? _b : 0;
    message.idCursor = object.idCursor !== void 0 && object.idCursor !== null ? long_default.fromValue(object.idCursor) : long_default.UZERO;
    return message;
  }
};
function createBaseQueryGroupMessagesRequest() {
  return { groupId: new Uint8Array(), pagingInfo: void 0 };
}
var QueryGroupMessagesRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.groupId.length !== 0) {
      writer.uint32(10).bytes(message.groupId);
    }
    if (message.pagingInfo !== void 0) {
      PagingInfo.encode(message.pagingInfo, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryGroupMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.groupId = reader.bytes();
          break;
        case 2:
          message.pagingInfo = PagingInfo.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      groupId: isSet19(object.groupId) ? bytesFromBase6415(object.groupId) : new Uint8Array(),
      pagingInfo: isSet19(object.pagingInfo) ? PagingInfo.fromJSON(object.pagingInfo) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.groupId !== void 0 && (obj.groupId = base64FromBytes15(message.groupId !== void 0 ? message.groupId : new Uint8Array()));
    message.pagingInfo !== void 0 && (obj.pagingInfo = message.pagingInfo ? PagingInfo.toJSON(message.pagingInfo) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseQueryGroupMessagesRequest();
    message.groupId = (_a = object.groupId) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.pagingInfo = object.pagingInfo !== void 0 && object.pagingInfo !== null ? PagingInfo.fromPartial(object.pagingInfo) : void 0;
    return message;
  }
};
function createBaseQueryGroupMessagesResponse() {
  return { messages: [], pagingInfo: void 0 };
}
var QueryGroupMessagesResponse = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.messages) {
      GroupMessage.encode(v, writer.uint32(10).fork()).ldelim();
    }
    if (message.pagingInfo !== void 0) {
      PagingInfo.encode(message.pagingInfo, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryGroupMessagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.messages.push(GroupMessage.decode(reader, reader.uint32()));
          break;
        case 2:
          message.pagingInfo = PagingInfo.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      messages: Array.isArray(object === null || object === void 0 ? void 0 : object.messages) ? object.messages.map((e) => GroupMessage.fromJSON(e)) : [],
      pagingInfo: isSet19(object.pagingInfo) ? PagingInfo.fromJSON(object.pagingInfo) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messages) {
      obj.messages = message.messages.map((e) => e ? GroupMessage.toJSON(e) : void 0);
    } else {
      obj.messages = [];
    }
    message.pagingInfo !== void 0 && (obj.pagingInfo = message.pagingInfo ? PagingInfo.toJSON(message.pagingInfo) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseQueryGroupMessagesResponse();
    message.messages = ((_a = object.messages) === null || _a === void 0 ? void 0 : _a.map((e) => GroupMessage.fromPartial(e))) || [];
    message.pagingInfo = object.pagingInfo !== void 0 && object.pagingInfo !== null ? PagingInfo.fromPartial(object.pagingInfo) : void 0;
    return message;
  }
};
function createBaseQueryWelcomeMessagesRequest() {
  return { installationKey: new Uint8Array(), pagingInfo: void 0 };
}
var QueryWelcomeMessagesRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.installationKey.length !== 0) {
      writer.uint32(10).bytes(message.installationKey);
    }
    if (message.pagingInfo !== void 0) {
      PagingInfo.encode(message.pagingInfo, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryWelcomeMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installationKey = reader.bytes();
          break;
        case 2:
          message.pagingInfo = PagingInfo.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installationKey: isSet19(object.installationKey) ? bytesFromBase6415(object.installationKey) : new Uint8Array(),
      pagingInfo: isSet19(object.pagingInfo) ? PagingInfo.fromJSON(object.pagingInfo) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.installationKey !== void 0 && (obj.installationKey = base64FromBytes15(message.installationKey !== void 0 ? message.installationKey : new Uint8Array()));
    message.pagingInfo !== void 0 && (obj.pagingInfo = message.pagingInfo ? PagingInfo.toJSON(message.pagingInfo) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseQueryWelcomeMessagesRequest();
    message.installationKey = (_a = object.installationKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.pagingInfo = object.pagingInfo !== void 0 && object.pagingInfo !== null ? PagingInfo.fromPartial(object.pagingInfo) : void 0;
    return message;
  }
};
function createBaseQueryWelcomeMessagesResponse() {
  return { messages: [], pagingInfo: void 0 };
}
var QueryWelcomeMessagesResponse = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.messages) {
      WelcomeMessage.encode(v, writer.uint32(10).fork()).ldelim();
    }
    if (message.pagingInfo !== void 0) {
      PagingInfo.encode(message.pagingInfo, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryWelcomeMessagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.messages.push(WelcomeMessage.decode(reader, reader.uint32()));
          break;
        case 2:
          message.pagingInfo = PagingInfo.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      messages: Array.isArray(object === null || object === void 0 ? void 0 : object.messages) ? object.messages.map((e) => WelcomeMessage.fromJSON(e)) : [],
      pagingInfo: isSet19(object.pagingInfo) ? PagingInfo.fromJSON(object.pagingInfo) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messages) {
      obj.messages = message.messages.map((e) => e ? WelcomeMessage.toJSON(e) : void 0);
    } else {
      obj.messages = [];
    }
    message.pagingInfo !== void 0 && (obj.pagingInfo = message.pagingInfo ? PagingInfo.toJSON(message.pagingInfo) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseQueryWelcomeMessagesResponse();
    message.messages = ((_a = object.messages) === null || _a === void 0 ? void 0 : _a.map((e) => WelcomeMessage.fromPartial(e))) || [];
    message.pagingInfo = object.pagingInfo !== void 0 && object.pagingInfo !== null ? PagingInfo.fromPartial(object.pagingInfo) : void 0;
    return message;
  }
};
function createBaseSubscribeGroupMessagesRequest() {
  return { filters: [] };
}
var SubscribeGroupMessagesRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.filters) {
      SubscribeGroupMessagesRequest_Filter.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeGroupMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.filters.push(SubscribeGroupMessagesRequest_Filter.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      filters: Array.isArray(object === null || object === void 0 ? void 0 : object.filters) ? object.filters.map((e) => SubscribeGroupMessagesRequest_Filter.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.filters) {
      obj.filters = message.filters.map((e) => e ? SubscribeGroupMessagesRequest_Filter.toJSON(e) : void 0);
    } else {
      obj.filters = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSubscribeGroupMessagesRequest();
    message.filters = ((_a = object.filters) === null || _a === void 0 ? void 0 : _a.map((e) => SubscribeGroupMessagesRequest_Filter.fromPartial(e))) || [];
    return message;
  }
};
function createBaseSubscribeGroupMessagesRequest_Filter() {
  return { groupId: new Uint8Array(), idCursor: long_default.UZERO };
}
var SubscribeGroupMessagesRequest_Filter = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.groupId.length !== 0) {
      writer.uint32(10).bytes(message.groupId);
    }
    if (!message.idCursor.isZero()) {
      writer.uint32(16).uint64(message.idCursor);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeGroupMessagesRequest_Filter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.groupId = reader.bytes();
          break;
        case 2:
          message.idCursor = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      groupId: isSet19(object.groupId) ? bytesFromBase6415(object.groupId) : new Uint8Array(),
      idCursor: isSet19(object.idCursor) ? long_default.fromValue(object.idCursor) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.groupId !== void 0 && (obj.groupId = base64FromBytes15(message.groupId !== void 0 ? message.groupId : new Uint8Array()));
    message.idCursor !== void 0 && (obj.idCursor = (message.idCursor || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSubscribeGroupMessagesRequest_Filter();
    message.groupId = (_a = object.groupId) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.idCursor = object.idCursor !== void 0 && object.idCursor !== null ? long_default.fromValue(object.idCursor) : long_default.UZERO;
    return message;
  }
};
function createBaseSubscribeWelcomeMessagesRequest() {
  return { filters: [] };
}
var SubscribeWelcomeMessagesRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.filters) {
      SubscribeWelcomeMessagesRequest_Filter.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeWelcomeMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.filters.push(SubscribeWelcomeMessagesRequest_Filter.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      filters: Array.isArray(object === null || object === void 0 ? void 0 : object.filters) ? object.filters.map((e) => SubscribeWelcomeMessagesRequest_Filter.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.filters) {
      obj.filters = message.filters.map((e) => e ? SubscribeWelcomeMessagesRequest_Filter.toJSON(e) : void 0);
    } else {
      obj.filters = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSubscribeWelcomeMessagesRequest();
    message.filters = ((_a = object.filters) === null || _a === void 0 ? void 0 : _a.map((e) => SubscribeWelcomeMessagesRequest_Filter.fromPartial(e))) || [];
    return message;
  }
};
function createBaseSubscribeWelcomeMessagesRequest_Filter() {
  return { installationKey: new Uint8Array(), idCursor: long_default.UZERO };
}
var SubscribeWelcomeMessagesRequest_Filter = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.installationKey.length !== 0) {
      writer.uint32(10).bytes(message.installationKey);
    }
    if (!message.idCursor.isZero()) {
      writer.uint32(16).uint64(message.idCursor);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeWelcomeMessagesRequest_Filter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installationKey = reader.bytes();
          break;
        case 2:
          message.idCursor = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installationKey: isSet19(object.installationKey) ? bytesFromBase6415(object.installationKey) : new Uint8Array(),
      idCursor: isSet19(object.idCursor) ? long_default.fromValue(object.idCursor) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.installationKey !== void 0 && (obj.installationKey = base64FromBytes15(message.installationKey !== void 0 ? message.installationKey : new Uint8Array()));
    message.idCursor !== void 0 && (obj.idCursor = (message.idCursor || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSubscribeWelcomeMessagesRequest_Filter();
    message.installationKey = (_a = object.installationKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.idCursor = object.idCursor !== void 0 && object.idCursor !== null ? long_default.fromValue(object.idCursor) : long_default.UZERO;
    return message;
  }
};
function createBaseBatchPublishCommitLogRequest() {
  return { requests: [] };
}
var BatchPublishCommitLogRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.requests) {
      PublishCommitLogRequest.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseBatchPublishCommitLogRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.requests.push(PublishCommitLogRequest.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      requests: Array.isArray(object === null || object === void 0 ? void 0 : object.requests) ? object.requests.map((e) => PublishCommitLogRequest.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.requests) {
      obj.requests = message.requests.map((e) => e ? PublishCommitLogRequest.toJSON(e) : void 0);
    } else {
      obj.requests = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseBatchPublishCommitLogRequest();
    message.requests = ((_a = object.requests) === null || _a === void 0 ? void 0 : _a.map((e) => PublishCommitLogRequest.fromPartial(e))) || [];
    return message;
  }
};
function createBasePublishCommitLogRequest() {
  return {
    groupId: new Uint8Array(),
    serializedCommitLogEntry: new Uint8Array(),
    signature: void 0
  };
}
var PublishCommitLogRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.groupId.length !== 0) {
      writer.uint32(10).bytes(message.groupId);
    }
    if (message.serializedCommitLogEntry.length !== 0) {
      writer.uint32(18).bytes(message.serializedCommitLogEntry);
    }
    if (message.signature !== void 0) {
      RecoverableEd25519Signature.encode(message.signature, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePublishCommitLogRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.groupId = reader.bytes();
          break;
        case 2:
          message.serializedCommitLogEntry = reader.bytes();
          break;
        case 3:
          message.signature = RecoverableEd25519Signature.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      groupId: isSet19(object.groupId) ? bytesFromBase6415(object.groupId) : new Uint8Array(),
      serializedCommitLogEntry: isSet19(object.serializedCommitLogEntry) ? bytesFromBase6415(object.serializedCommitLogEntry) : new Uint8Array(),
      signature: isSet19(object.signature) ? RecoverableEd25519Signature.fromJSON(object.signature) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.groupId !== void 0 && (obj.groupId = base64FromBytes15(message.groupId !== void 0 ? message.groupId : new Uint8Array()));
    message.serializedCommitLogEntry !== void 0 && (obj.serializedCommitLogEntry = base64FromBytes15(message.serializedCommitLogEntry !== void 0 ? message.serializedCommitLogEntry : new Uint8Array()));
    message.signature !== void 0 && (obj.signature = message.signature ? RecoverableEd25519Signature.toJSON(message.signature) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBasePublishCommitLogRequest();
    message.groupId = (_a = object.groupId) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.serializedCommitLogEntry = (_b = object.serializedCommitLogEntry) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.signature = object.signature !== void 0 && object.signature !== null ? RecoverableEd25519Signature.fromPartial(object.signature) : void 0;
    return message;
  }
};
function createBaseQueryCommitLogRequest() {
  return { groupId: new Uint8Array(), pagingInfo: void 0 };
}
var QueryCommitLogRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.groupId.length !== 0) {
      writer.uint32(10).bytes(message.groupId);
    }
    if (message.pagingInfo !== void 0) {
      PagingInfo.encode(message.pagingInfo, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryCommitLogRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.groupId = reader.bytes();
          break;
        case 2:
          message.pagingInfo = PagingInfo.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      groupId: isSet19(object.groupId) ? bytesFromBase6415(object.groupId) : new Uint8Array(),
      pagingInfo: isSet19(object.pagingInfo) ? PagingInfo.fromJSON(object.pagingInfo) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.groupId !== void 0 && (obj.groupId = base64FromBytes15(message.groupId !== void 0 ? message.groupId : new Uint8Array()));
    message.pagingInfo !== void 0 && (obj.pagingInfo = message.pagingInfo ? PagingInfo.toJSON(message.pagingInfo) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseQueryCommitLogRequest();
    message.groupId = (_a = object.groupId) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.pagingInfo = object.pagingInfo !== void 0 && object.pagingInfo !== null ? PagingInfo.fromPartial(object.pagingInfo) : void 0;
    return message;
  }
};
function createBaseQueryCommitLogResponse() {
  return {
    groupId: new Uint8Array(),
    commitLogEntries: [],
    pagingInfo: void 0
  };
}
var QueryCommitLogResponse = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    if (message.groupId.length !== 0) {
      writer.uint32(10).bytes(message.groupId);
    }
    for (const v of message.commitLogEntries) {
      CommitLogEntry.encode(v, writer.uint32(18).fork()).ldelim();
    }
    if (message.pagingInfo !== void 0) {
      PagingInfo.encode(message.pagingInfo, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseQueryCommitLogResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.groupId = reader.bytes();
          break;
        case 2:
          message.commitLogEntries.push(CommitLogEntry.decode(reader, reader.uint32()));
          break;
        case 3:
          message.pagingInfo = PagingInfo.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      groupId: isSet19(object.groupId) ? bytesFromBase6415(object.groupId) : new Uint8Array(),
      commitLogEntries: Array.isArray(object === null || object === void 0 ? void 0 : object.commitLogEntries) ? object.commitLogEntries.map((e) => CommitLogEntry.fromJSON(e)) : [],
      pagingInfo: isSet19(object.pagingInfo) ? PagingInfo.fromJSON(object.pagingInfo) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.groupId !== void 0 && (obj.groupId = base64FromBytes15(message.groupId !== void 0 ? message.groupId : new Uint8Array()));
    if (message.commitLogEntries) {
      obj.commitLogEntries = message.commitLogEntries.map((e) => e ? CommitLogEntry.toJSON(e) : void 0);
    } else {
      obj.commitLogEntries = [];
    }
    message.pagingInfo !== void 0 && (obj.pagingInfo = message.pagingInfo ? PagingInfo.toJSON(message.pagingInfo) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseQueryCommitLogResponse();
    message.groupId = (_a = object.groupId) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.commitLogEntries = ((_b = object.commitLogEntries) === null || _b === void 0 ? void 0 : _b.map((e) => CommitLogEntry.fromPartial(e))) || [];
    message.pagingInfo = object.pagingInfo !== void 0 && object.pagingInfo !== null ? PagingInfo.fromPartial(object.pagingInfo) : void 0;
    return message;
  }
};
function createBaseBatchQueryCommitLogRequest() {
  return { requests: [] };
}
var BatchQueryCommitLogRequest = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.requests) {
      QueryCommitLogRequest.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseBatchQueryCommitLogRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.requests.push(QueryCommitLogRequest.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      requests: Array.isArray(object === null || object === void 0 ? void 0 : object.requests) ? object.requests.map((e) => QueryCommitLogRequest.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.requests) {
      obj.requests = message.requests.map((e) => e ? QueryCommitLogRequest.toJSON(e) : void 0);
    } else {
      obj.requests = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseBatchQueryCommitLogRequest();
    message.requests = ((_a = object.requests) === null || _a === void 0 ? void 0 : _a.map((e) => QueryCommitLogRequest.fromPartial(e))) || [];
    return message;
  }
};
function createBaseBatchQueryCommitLogResponse() {
  return { responses: [] };
}
var BatchQueryCommitLogResponse = {
  encode(message, writer = import_minimal21.default.Writer.create()) {
    for (const v of message.responses) {
      QueryCommitLogResponse.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal21.default.Reader ? input : new import_minimal21.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseBatchQueryCommitLogResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.responses.push(QueryCommitLogResponse.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      responses: Array.isArray(object === null || object === void 0 ? void 0 : object.responses) ? object.responses.map((e) => QueryCommitLogResponse.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.responses) {
      obj.responses = message.responses.map((e) => e ? QueryCommitLogResponse.toJSON(e) : void 0);
    } else {
      obj.responses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseBatchQueryCommitLogResponse();
    message.responses = ((_a = object.responses) === null || _a === void 0 ? void 0 : _a.map((e) => QueryCommitLogResponse.fromPartial(e))) || [];
    return message;
  }
};
var MlsApiClientImpl = class {
  constructor(rpc) {
    this.rpc = rpc;
    this.SendGroupMessages = this.SendGroupMessages.bind(this);
    this.SendWelcomeMessages = this.SendWelcomeMessages.bind(this);
    this.RegisterInstallation = this.RegisterInstallation.bind(this);
    this.UploadKeyPackage = this.UploadKeyPackage.bind(this);
    this.FetchKeyPackages = this.FetchKeyPackages.bind(this);
    this.RevokeInstallation = this.RevokeInstallation.bind(this);
    this.GetIdentityUpdates = this.GetIdentityUpdates.bind(this);
    this.QueryGroupMessages = this.QueryGroupMessages.bind(this);
    this.QueryWelcomeMessages = this.QueryWelcomeMessages.bind(this);
    this.SubscribeGroupMessages = this.SubscribeGroupMessages.bind(this);
    this.SubscribeWelcomeMessages = this.SubscribeWelcomeMessages.bind(this);
    this.BatchPublishCommitLog = this.BatchPublishCommitLog.bind(this);
    this.BatchQueryCommitLog = this.BatchQueryCommitLog.bind(this);
  }
  SendGroupMessages(request) {
    const data = SendGroupMessagesRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls.api.v1.MlsApi", "SendGroupMessages", data);
    return promise.then((data2) => Empty.decode(new import_minimal21.default.Reader(data2)));
  }
  SendWelcomeMessages(request) {
    const data = SendWelcomeMessagesRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls.api.v1.MlsApi", "SendWelcomeMessages", data);
    return promise.then((data2) => Empty.decode(new import_minimal21.default.Reader(data2)));
  }
  RegisterInstallation(request) {
    const data = RegisterInstallationRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls.api.v1.MlsApi", "RegisterInstallation", data);
    return promise.then((data2) => RegisterInstallationResponse.decode(new import_minimal21.default.Reader(data2)));
  }
  UploadKeyPackage(request) {
    const data = UploadKeyPackageRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls.api.v1.MlsApi", "UploadKeyPackage", data);
    return promise.then((data2) => Empty.decode(new import_minimal21.default.Reader(data2)));
  }
  FetchKeyPackages(request) {
    const data = FetchKeyPackagesRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls.api.v1.MlsApi", "FetchKeyPackages", data);
    return promise.then((data2) => FetchKeyPackagesResponse.decode(new import_minimal21.default.Reader(data2)));
  }
  RevokeInstallation(request) {
    const data = RevokeInstallationRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls.api.v1.MlsApi", "RevokeInstallation", data);
    return promise.then((data2) => Empty.decode(new import_minimal21.default.Reader(data2)));
  }
  GetIdentityUpdates(request) {
    const data = GetIdentityUpdatesRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls.api.v1.MlsApi", "GetIdentityUpdates", data);
    return promise.then((data2) => GetIdentityUpdatesResponse.decode(new import_minimal21.default.Reader(data2)));
  }
  QueryGroupMessages(request) {
    const data = QueryGroupMessagesRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls.api.v1.MlsApi", "QueryGroupMessages", data);
    return promise.then((data2) => QueryGroupMessagesResponse.decode(new import_minimal21.default.Reader(data2)));
  }
  QueryWelcomeMessages(request) {
    const data = QueryWelcomeMessagesRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls.api.v1.MlsApi", "QueryWelcomeMessages", data);
    return promise.then((data2) => QueryWelcomeMessagesResponse.decode(new import_minimal21.default.Reader(data2)));
  }
  SubscribeGroupMessages(request) {
    const data = SubscribeGroupMessagesRequest.encode(request).finish();
    const result = this.rpc.serverStreamingRequest("xmtp.mls.api.v1.MlsApi", "SubscribeGroupMessages", data);
    return result.pipe(map((data2) => GroupMessage.decode(new import_minimal21.default.Reader(data2))));
  }
  SubscribeWelcomeMessages(request) {
    const data = SubscribeWelcomeMessagesRequest.encode(request).finish();
    const result = this.rpc.serverStreamingRequest("xmtp.mls.api.v1.MlsApi", "SubscribeWelcomeMessages", data);
    return result.pipe(map((data2) => WelcomeMessage.decode(new import_minimal21.default.Reader(data2))));
  }
  BatchPublishCommitLog(request) {
    const data = BatchPublishCommitLogRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls.api.v1.MlsApi", "BatchPublishCommitLog", data);
    return promise.then((data2) => Empty.decode(new import_minimal21.default.Reader(data2)));
  }
  BatchQueryCommitLog(request) {
    const data = BatchQueryCommitLogRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls.api.v1.MlsApi", "BatchQueryCommitLog", data);
    return promise.then((data2) => BatchQueryCommitLogResponse.decode(new import_minimal21.default.Reader(data2)));
  }
};
var globalThis16 = (() => {
  if (typeof globalThis16 !== "undefined")
    return globalThis16;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6415(b642) {
  if (globalThis16.Buffer) {
    return Uint8Array.from(globalThis16.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis16.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes15(arr) {
  if (globalThis16.Buffer) {
    return globalThis16.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis16.btoa(bin.join(""));
  }
}
if (import_minimal21.default.util.Long !== long_default) {
  import_minimal21.default.util.Long = long_default;
  import_minimal21.default.configure();
}
function isSet19(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls/database/intents.pb.js
var intents_pb_exports = {};
__export(intents_pb_exports, {
  AccountAddresses: () => AccountAddresses,
  AddMembersData: () => AddMembersData,
  AddMembersData_V1: () => AddMembersData_V1,
  AddressesOrInstallationIds: () => AddressesOrInstallationIds,
  AdminListUpdateType: () => AdminListUpdateType,
  InstallationIds: () => InstallationIds,
  PermissionPolicyOption: () => PermissionPolicyOption,
  PermissionUpdateType: () => PermissionUpdateType,
  PostCommitAction: () => PostCommitAction,
  PostCommitAction_Installation: () => PostCommitAction_Installation,
  PostCommitAction_SendWelcomes: () => PostCommitAction_SendWelcomes,
  ReaddInstallationsData: () => ReaddInstallationsData,
  ReaddInstallationsData_V1: () => ReaddInstallationsData_V1,
  RemoveMembersData: () => RemoveMembersData,
  RemoveMembersData_V1: () => RemoveMembersData_V1,
  SendMessageData: () => SendMessageData,
  SendMessageData_V1: () => SendMessageData_V1,
  UpdateAdminListsData: () => UpdateAdminListsData,
  UpdateAdminListsData_V1: () => UpdateAdminListsData_V1,
  UpdateGroupMembershipData: () => UpdateGroupMembershipData,
  UpdateGroupMembershipData_V1: () => UpdateGroupMembershipData_V1,
  UpdateGroupMembershipData_V1_MembershipUpdatesEntry: () => UpdateGroupMembershipData_V1_MembershipUpdatesEntry,
  UpdateMetadataData: () => UpdateMetadataData,
  UpdateMetadataData_V1: () => UpdateMetadataData_V1,
  UpdatePermissionData: () => UpdatePermissionData,
  UpdatePermissionData_V1: () => UpdatePermissionData_V1,
  adminListUpdateTypeFromJSON: () => adminListUpdateTypeFromJSON,
  adminListUpdateTypeToJSON: () => adminListUpdateTypeToJSON,
  permissionPolicyOptionFromJSON: () => permissionPolicyOptionFromJSON,
  permissionPolicyOptionToJSON: () => permissionPolicyOptionToJSON,
  permissionUpdateTypeFromJSON: () => permissionUpdateTypeFromJSON,
  permissionUpdateTypeToJSON: () => permissionUpdateTypeToJSON,
  protobufPackage: () => protobufPackage18
});
var import_minimal22 = __toESM(require_minimal());
var protobufPackage18 = "xmtp.mls.database";
var AdminListUpdateType;
(function(AdminListUpdateType2) {
  AdminListUpdateType2[AdminListUpdateType2["ADMIN_LIST_UPDATE_TYPE_UNSPECIFIED"] = 0] = "ADMIN_LIST_UPDATE_TYPE_UNSPECIFIED";
  AdminListUpdateType2[AdminListUpdateType2["ADMIN_LIST_UPDATE_TYPE_ADD_ADMIN"] = 1] = "ADMIN_LIST_UPDATE_TYPE_ADD_ADMIN";
  AdminListUpdateType2[AdminListUpdateType2["ADMIN_LIST_UPDATE_TYPE_REMOVE_ADMIN"] = 2] = "ADMIN_LIST_UPDATE_TYPE_REMOVE_ADMIN";
  AdminListUpdateType2[AdminListUpdateType2["ADMIN_LIST_UPDATE_TYPE_ADD_SUPER_ADMIN"] = 3] = "ADMIN_LIST_UPDATE_TYPE_ADD_SUPER_ADMIN";
  AdminListUpdateType2[AdminListUpdateType2["ADMIN_LIST_UPDATE_TYPE_REMOVE_SUPER_ADMIN"] = 4] = "ADMIN_LIST_UPDATE_TYPE_REMOVE_SUPER_ADMIN";
  AdminListUpdateType2[AdminListUpdateType2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(AdminListUpdateType || (AdminListUpdateType = {}));
function adminListUpdateTypeFromJSON(object) {
  switch (object) {
    case 0:
    case "ADMIN_LIST_UPDATE_TYPE_UNSPECIFIED":
      return AdminListUpdateType.ADMIN_LIST_UPDATE_TYPE_UNSPECIFIED;
    case 1:
    case "ADMIN_LIST_UPDATE_TYPE_ADD_ADMIN":
      return AdminListUpdateType.ADMIN_LIST_UPDATE_TYPE_ADD_ADMIN;
    case 2:
    case "ADMIN_LIST_UPDATE_TYPE_REMOVE_ADMIN":
      return AdminListUpdateType.ADMIN_LIST_UPDATE_TYPE_REMOVE_ADMIN;
    case 3:
    case "ADMIN_LIST_UPDATE_TYPE_ADD_SUPER_ADMIN":
      return AdminListUpdateType.ADMIN_LIST_UPDATE_TYPE_ADD_SUPER_ADMIN;
    case 4:
    case "ADMIN_LIST_UPDATE_TYPE_REMOVE_SUPER_ADMIN":
      return AdminListUpdateType.ADMIN_LIST_UPDATE_TYPE_REMOVE_SUPER_ADMIN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AdminListUpdateType.UNRECOGNIZED;
  }
}
function adminListUpdateTypeToJSON(object) {
  switch (object) {
    case AdminListUpdateType.ADMIN_LIST_UPDATE_TYPE_UNSPECIFIED:
      return "ADMIN_LIST_UPDATE_TYPE_UNSPECIFIED";
    case AdminListUpdateType.ADMIN_LIST_UPDATE_TYPE_ADD_ADMIN:
      return "ADMIN_LIST_UPDATE_TYPE_ADD_ADMIN";
    case AdminListUpdateType.ADMIN_LIST_UPDATE_TYPE_REMOVE_ADMIN:
      return "ADMIN_LIST_UPDATE_TYPE_REMOVE_ADMIN";
    case AdminListUpdateType.ADMIN_LIST_UPDATE_TYPE_ADD_SUPER_ADMIN:
      return "ADMIN_LIST_UPDATE_TYPE_ADD_SUPER_ADMIN";
    case AdminListUpdateType.ADMIN_LIST_UPDATE_TYPE_REMOVE_SUPER_ADMIN:
      return "ADMIN_LIST_UPDATE_TYPE_REMOVE_SUPER_ADMIN";
    case AdminListUpdateType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
var PermissionUpdateType;
(function(PermissionUpdateType2) {
  PermissionUpdateType2[PermissionUpdateType2["PERMISSION_UPDATE_TYPE_UNSPECIFIED"] = 0] = "PERMISSION_UPDATE_TYPE_UNSPECIFIED";
  PermissionUpdateType2[PermissionUpdateType2["PERMISSION_UPDATE_TYPE_ADD_MEMBER"] = 1] = "PERMISSION_UPDATE_TYPE_ADD_MEMBER";
  PermissionUpdateType2[PermissionUpdateType2["PERMISSION_UPDATE_TYPE_REMOVE_MEMBER"] = 2] = "PERMISSION_UPDATE_TYPE_REMOVE_MEMBER";
  PermissionUpdateType2[PermissionUpdateType2["PERMISSION_UPDATE_TYPE_ADD_ADMIN"] = 3] = "PERMISSION_UPDATE_TYPE_ADD_ADMIN";
  PermissionUpdateType2[PermissionUpdateType2["PERMISSION_UPDATE_TYPE_REMOVE_ADMIN"] = 4] = "PERMISSION_UPDATE_TYPE_REMOVE_ADMIN";
  PermissionUpdateType2[PermissionUpdateType2["PERMISSION_UPDATE_TYPE_UPDATE_METADATA"] = 5] = "PERMISSION_UPDATE_TYPE_UPDATE_METADATA";
  PermissionUpdateType2[PermissionUpdateType2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(PermissionUpdateType || (PermissionUpdateType = {}));
function permissionUpdateTypeFromJSON(object) {
  switch (object) {
    case 0:
    case "PERMISSION_UPDATE_TYPE_UNSPECIFIED":
      return PermissionUpdateType.PERMISSION_UPDATE_TYPE_UNSPECIFIED;
    case 1:
    case "PERMISSION_UPDATE_TYPE_ADD_MEMBER":
      return PermissionUpdateType.PERMISSION_UPDATE_TYPE_ADD_MEMBER;
    case 2:
    case "PERMISSION_UPDATE_TYPE_REMOVE_MEMBER":
      return PermissionUpdateType.PERMISSION_UPDATE_TYPE_REMOVE_MEMBER;
    case 3:
    case "PERMISSION_UPDATE_TYPE_ADD_ADMIN":
      return PermissionUpdateType.PERMISSION_UPDATE_TYPE_ADD_ADMIN;
    case 4:
    case "PERMISSION_UPDATE_TYPE_REMOVE_ADMIN":
      return PermissionUpdateType.PERMISSION_UPDATE_TYPE_REMOVE_ADMIN;
    case 5:
    case "PERMISSION_UPDATE_TYPE_UPDATE_METADATA":
      return PermissionUpdateType.PERMISSION_UPDATE_TYPE_UPDATE_METADATA;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PermissionUpdateType.UNRECOGNIZED;
  }
}
function permissionUpdateTypeToJSON(object) {
  switch (object) {
    case PermissionUpdateType.PERMISSION_UPDATE_TYPE_UNSPECIFIED:
      return "PERMISSION_UPDATE_TYPE_UNSPECIFIED";
    case PermissionUpdateType.PERMISSION_UPDATE_TYPE_ADD_MEMBER:
      return "PERMISSION_UPDATE_TYPE_ADD_MEMBER";
    case PermissionUpdateType.PERMISSION_UPDATE_TYPE_REMOVE_MEMBER:
      return "PERMISSION_UPDATE_TYPE_REMOVE_MEMBER";
    case PermissionUpdateType.PERMISSION_UPDATE_TYPE_ADD_ADMIN:
      return "PERMISSION_UPDATE_TYPE_ADD_ADMIN";
    case PermissionUpdateType.PERMISSION_UPDATE_TYPE_REMOVE_ADMIN:
      return "PERMISSION_UPDATE_TYPE_REMOVE_ADMIN";
    case PermissionUpdateType.PERMISSION_UPDATE_TYPE_UPDATE_METADATA:
      return "PERMISSION_UPDATE_TYPE_UPDATE_METADATA";
    case PermissionUpdateType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
var PermissionPolicyOption;
(function(PermissionPolicyOption2) {
  PermissionPolicyOption2[PermissionPolicyOption2["PERMISSION_POLICY_OPTION_UNSPECIFIED"] = 0] = "PERMISSION_POLICY_OPTION_UNSPECIFIED";
  PermissionPolicyOption2[PermissionPolicyOption2["PERMISSION_POLICY_OPTION_ALLOW"] = 1] = "PERMISSION_POLICY_OPTION_ALLOW";
  PermissionPolicyOption2[PermissionPolicyOption2["PERMISSION_POLICY_OPTION_DENY"] = 2] = "PERMISSION_POLICY_OPTION_DENY";
  PermissionPolicyOption2[PermissionPolicyOption2["PERMISSION_POLICY_OPTION_ADMIN_ONLY"] = 3] = "PERMISSION_POLICY_OPTION_ADMIN_ONLY";
  PermissionPolicyOption2[PermissionPolicyOption2["PERMISSION_POLICY_OPTION_SUPER_ADMIN_ONLY"] = 4] = "PERMISSION_POLICY_OPTION_SUPER_ADMIN_ONLY";
  PermissionPolicyOption2[PermissionPolicyOption2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(PermissionPolicyOption || (PermissionPolicyOption = {}));
function permissionPolicyOptionFromJSON(object) {
  switch (object) {
    case 0:
    case "PERMISSION_POLICY_OPTION_UNSPECIFIED":
      return PermissionPolicyOption.PERMISSION_POLICY_OPTION_UNSPECIFIED;
    case 1:
    case "PERMISSION_POLICY_OPTION_ALLOW":
      return PermissionPolicyOption.PERMISSION_POLICY_OPTION_ALLOW;
    case 2:
    case "PERMISSION_POLICY_OPTION_DENY":
      return PermissionPolicyOption.PERMISSION_POLICY_OPTION_DENY;
    case 3:
    case "PERMISSION_POLICY_OPTION_ADMIN_ONLY":
      return PermissionPolicyOption.PERMISSION_POLICY_OPTION_ADMIN_ONLY;
    case 4:
    case "PERMISSION_POLICY_OPTION_SUPER_ADMIN_ONLY":
      return PermissionPolicyOption.PERMISSION_POLICY_OPTION_SUPER_ADMIN_ONLY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PermissionPolicyOption.UNRECOGNIZED;
  }
}
function permissionPolicyOptionToJSON(object) {
  switch (object) {
    case PermissionPolicyOption.PERMISSION_POLICY_OPTION_UNSPECIFIED:
      return "PERMISSION_POLICY_OPTION_UNSPECIFIED";
    case PermissionPolicyOption.PERMISSION_POLICY_OPTION_ALLOW:
      return "PERMISSION_POLICY_OPTION_ALLOW";
    case PermissionPolicyOption.PERMISSION_POLICY_OPTION_DENY:
      return "PERMISSION_POLICY_OPTION_DENY";
    case PermissionPolicyOption.PERMISSION_POLICY_OPTION_ADMIN_ONLY:
      return "PERMISSION_POLICY_OPTION_ADMIN_ONLY";
    case PermissionPolicyOption.PERMISSION_POLICY_OPTION_SUPER_ADMIN_ONLY:
      return "PERMISSION_POLICY_OPTION_SUPER_ADMIN_ONLY";
    case PermissionPolicyOption.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseSendMessageData() {
  return { v1: void 0 };
}
var SendMessageData = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.v1 !== void 0) {
      SendMessageData_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendMessageData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = SendMessageData_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet20(object.v1) ? SendMessageData_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? SendMessageData_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseSendMessageData();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? SendMessageData_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseSendMessageData_V1() {
  return { payloadBytes: new Uint8Array() };
}
var SendMessageData_V1 = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.payloadBytes.length !== 0) {
      writer.uint32(10).bytes(message.payloadBytes);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendMessageData_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.payloadBytes = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      payloadBytes: isSet20(object.payloadBytes) ? bytesFromBase6416(object.payloadBytes) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.payloadBytes !== void 0 && (obj.payloadBytes = base64FromBytes16(message.payloadBytes !== void 0 ? message.payloadBytes : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseSendMessageData_V1();
    message.payloadBytes = (_a = object.payloadBytes) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseAccountAddresses() {
  return { accountAddresses: [] };
}
var AccountAddresses = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    for (const v of message.accountAddresses) {
      writer.uint32(10).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAccountAddresses();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.accountAddresses.push(reader.string());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      accountAddresses: Array.isArray(object === null || object === void 0 ? void 0 : object.accountAddresses) ? object.accountAddresses.map((e) => String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.accountAddresses) {
      obj.accountAddresses = message.accountAddresses.map((e) => e);
    } else {
      obj.accountAddresses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseAccountAddresses();
    message.accountAddresses = ((_a = object.accountAddresses) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBaseInstallationIds() {
  return { installationIds: [] };
}
var InstallationIds = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    for (const v of message.installationIds) {
      writer.uint32(10).bytes(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInstallationIds();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installationIds.push(reader.bytes());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installationIds: Array.isArray(object === null || object === void 0 ? void 0 : object.installationIds) ? object.installationIds.map((e) => bytesFromBase6416(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.installationIds) {
      obj.installationIds = message.installationIds.map((e) => base64FromBytes16(e !== void 0 ? e : new Uint8Array()));
    } else {
      obj.installationIds = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseInstallationIds();
    message.installationIds = ((_a = object.installationIds) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBaseAddressesOrInstallationIds() {
  return { accountAddresses: void 0, installationIds: void 0 };
}
var AddressesOrInstallationIds = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.accountAddresses !== void 0) {
      AccountAddresses.encode(message.accountAddresses, writer.uint32(10).fork()).ldelim();
    }
    if (message.installationIds !== void 0) {
      InstallationIds.encode(message.installationIds, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddressesOrInstallationIds();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.accountAddresses = AccountAddresses.decode(reader, reader.uint32());
          break;
        case 2:
          message.installationIds = InstallationIds.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      accountAddresses: isSet20(object.accountAddresses) ? AccountAddresses.fromJSON(object.accountAddresses) : void 0,
      installationIds: isSet20(object.installationIds) ? InstallationIds.fromJSON(object.installationIds) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.accountAddresses !== void 0 && (obj.accountAddresses = message.accountAddresses ? AccountAddresses.toJSON(message.accountAddresses) : void 0);
    message.installationIds !== void 0 && (obj.installationIds = message.installationIds ? InstallationIds.toJSON(message.installationIds) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseAddressesOrInstallationIds();
    message.accountAddresses = object.accountAddresses !== void 0 && object.accountAddresses !== null ? AccountAddresses.fromPartial(object.accountAddresses) : void 0;
    message.installationIds = object.installationIds !== void 0 && object.installationIds !== null ? InstallationIds.fromPartial(object.installationIds) : void 0;
    return message;
  }
};
function createBaseAddMembersData() {
  return { v1: void 0 };
}
var AddMembersData = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.v1 !== void 0) {
      AddMembersData_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddMembersData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = AddMembersData_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet20(object.v1) ? AddMembersData_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? AddMembersData_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseAddMembersData();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? AddMembersData_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseAddMembersData_V1() {
  return { addressesOrInstallationIds: void 0 };
}
var AddMembersData_V1 = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.addressesOrInstallationIds !== void 0) {
      AddressesOrInstallationIds.encode(message.addressesOrInstallationIds, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddMembersData_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.addressesOrInstallationIds = AddressesOrInstallationIds.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      addressesOrInstallationIds: isSet20(object.addressesOrInstallationIds) ? AddressesOrInstallationIds.fromJSON(object.addressesOrInstallationIds) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.addressesOrInstallationIds !== void 0 && (obj.addressesOrInstallationIds = message.addressesOrInstallationIds ? AddressesOrInstallationIds.toJSON(message.addressesOrInstallationIds) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseAddMembersData_V1();
    message.addressesOrInstallationIds = object.addressesOrInstallationIds !== void 0 && object.addressesOrInstallationIds !== null ? AddressesOrInstallationIds.fromPartial(object.addressesOrInstallationIds) : void 0;
    return message;
  }
};
function createBaseRemoveMembersData() {
  return { v1: void 0 };
}
var RemoveMembersData = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.v1 !== void 0) {
      RemoveMembersData_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRemoveMembersData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = RemoveMembersData_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet20(object.v1) ? RemoveMembersData_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? RemoveMembersData_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseRemoveMembersData();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? RemoveMembersData_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseRemoveMembersData_V1() {
  return { addressesOrInstallationIds: void 0 };
}
var RemoveMembersData_V1 = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.addressesOrInstallationIds !== void 0) {
      AddressesOrInstallationIds.encode(message.addressesOrInstallationIds, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRemoveMembersData_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.addressesOrInstallationIds = AddressesOrInstallationIds.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      addressesOrInstallationIds: isSet20(object.addressesOrInstallationIds) ? AddressesOrInstallationIds.fromJSON(object.addressesOrInstallationIds) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.addressesOrInstallationIds !== void 0 && (obj.addressesOrInstallationIds = message.addressesOrInstallationIds ? AddressesOrInstallationIds.toJSON(message.addressesOrInstallationIds) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseRemoveMembersData_V1();
    message.addressesOrInstallationIds = object.addressesOrInstallationIds !== void 0 && object.addressesOrInstallationIds !== null ? AddressesOrInstallationIds.fromPartial(object.addressesOrInstallationIds) : void 0;
    return message;
  }
};
function createBaseUpdateGroupMembershipData() {
  return { v1: void 0 };
}
var UpdateGroupMembershipData = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.v1 !== void 0) {
      UpdateGroupMembershipData_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdateGroupMembershipData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = UpdateGroupMembershipData_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet20(object.v1) ? UpdateGroupMembershipData_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? UpdateGroupMembershipData_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseUpdateGroupMembershipData();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? UpdateGroupMembershipData_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseUpdateGroupMembershipData_V1() {
  return { membershipUpdates: {}, removedMembers: [], failedInstallations: [] };
}
var UpdateGroupMembershipData_V1 = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    Object.entries(message.membershipUpdates).forEach(([key, value]) => {
      UpdateGroupMembershipData_V1_MembershipUpdatesEntry.encode({ key, value }, writer.uint32(10).fork()).ldelim();
    });
    for (const v of message.removedMembers) {
      writer.uint32(18).string(v);
    }
    for (const v of message.failedInstallations) {
      writer.uint32(26).bytes(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdateGroupMembershipData_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          const entry1 = UpdateGroupMembershipData_V1_MembershipUpdatesEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.membershipUpdates[entry1.key] = entry1.value;
          }
          break;
        case 2:
          message.removedMembers.push(reader.string());
          break;
        case 3:
          message.failedInstallations.push(reader.bytes());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      membershipUpdates: isObject4(object.membershipUpdates) ? Object.entries(object.membershipUpdates).reduce((acc, [key, value]) => {
        acc[key] = long_default.fromValue(value);
        return acc;
      }, {}) : {},
      removedMembers: Array.isArray(object === null || object === void 0 ? void 0 : object.removedMembers) ? object.removedMembers.map((e) => String(e)) : [],
      failedInstallations: Array.isArray(object === null || object === void 0 ? void 0 : object.failedInstallations) ? object.failedInstallations.map((e) => bytesFromBase6416(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    obj.membershipUpdates = {};
    if (message.membershipUpdates) {
      Object.entries(message.membershipUpdates).forEach(([k, v]) => {
        obj.membershipUpdates[k] = v.toString();
      });
    }
    if (message.removedMembers) {
      obj.removedMembers = message.removedMembers.map((e) => e);
    } else {
      obj.removedMembers = [];
    }
    if (message.failedInstallations) {
      obj.failedInstallations = message.failedInstallations.map((e) => base64FromBytes16(e !== void 0 ? e : new Uint8Array()));
    } else {
      obj.failedInstallations = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseUpdateGroupMembershipData_V1();
    message.membershipUpdates = Object.entries((_a = object.membershipUpdates) !== null && _a !== void 0 ? _a : {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = long_default.fromValue(value);
      }
      return acc;
    }, {});
    message.removedMembers = ((_b = object.removedMembers) === null || _b === void 0 ? void 0 : _b.map((e) => e)) || [];
    message.failedInstallations = ((_c = object.failedInstallations) === null || _c === void 0 ? void 0 : _c.map((e) => e)) || [];
    return message;
  }
};
function createBaseUpdateGroupMembershipData_V1_MembershipUpdatesEntry() {
  return { key: "", value: long_default.UZERO };
}
var UpdateGroupMembershipData_V1_MembershipUpdatesEntry = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (!message.value.isZero()) {
      writer.uint32(16).uint64(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdateGroupMembershipData_V1_MembershipUpdatesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.key = reader.string();
          break;
        case 2:
          message.value = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet20(object.key) ? String(object.key) : "",
      value: isSet20(object.value) ? long_default.fromValue(object.value) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.key !== void 0 && (obj.key = message.key);
    message.value !== void 0 && (obj.value = (message.value || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseUpdateGroupMembershipData_V1_MembershipUpdatesEntry();
    message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
    message.value = object.value !== void 0 && object.value !== null ? long_default.fromValue(object.value) : long_default.UZERO;
    return message;
  }
};
function createBaseReaddInstallationsData() {
  return { v1: void 0 };
}
var ReaddInstallationsData = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.v1 !== void 0) {
      ReaddInstallationsData_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseReaddInstallationsData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = ReaddInstallationsData_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet20(object.v1) ? ReaddInstallationsData_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? ReaddInstallationsData_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseReaddInstallationsData();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? ReaddInstallationsData_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseReaddInstallationsData_V1() {
  return { readdedInstallations: [] };
}
var ReaddInstallationsData_V1 = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    for (const v of message.readdedInstallations) {
      writer.uint32(10).bytes(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseReaddInstallationsData_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.readdedInstallations.push(reader.bytes());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      readdedInstallations: Array.isArray(object === null || object === void 0 ? void 0 : object.readdedInstallations) ? object.readdedInstallations.map((e) => bytesFromBase6416(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.readdedInstallations) {
      obj.readdedInstallations = message.readdedInstallations.map((e) => base64FromBytes16(e !== void 0 ? e : new Uint8Array()));
    } else {
      obj.readdedInstallations = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseReaddInstallationsData_V1();
    message.readdedInstallations = ((_a = object.readdedInstallations) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBaseUpdateMetadataData() {
  return { v1: void 0 };
}
var UpdateMetadataData = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.v1 !== void 0) {
      UpdateMetadataData_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdateMetadataData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = UpdateMetadataData_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet20(object.v1) ? UpdateMetadataData_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? UpdateMetadataData_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseUpdateMetadataData();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? UpdateMetadataData_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseUpdateMetadataData_V1() {
  return { fieldName: "", fieldValue: "" };
}
var UpdateMetadataData_V1 = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.fieldName !== "") {
      writer.uint32(10).string(message.fieldName);
    }
    if (message.fieldValue !== "") {
      writer.uint32(18).string(message.fieldValue);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdateMetadataData_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.fieldName = reader.string();
          break;
        case 2:
          message.fieldValue = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      fieldName: isSet20(object.fieldName) ? String(object.fieldName) : "",
      fieldValue: isSet20(object.fieldValue) ? String(object.fieldValue) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.fieldName !== void 0 && (obj.fieldName = message.fieldName);
    message.fieldValue !== void 0 && (obj.fieldValue = message.fieldValue);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseUpdateMetadataData_V1();
    message.fieldName = (_a = object.fieldName) !== null && _a !== void 0 ? _a : "";
    message.fieldValue = (_b = object.fieldValue) !== null && _b !== void 0 ? _b : "";
    return message;
  }
};
function createBaseUpdateAdminListsData() {
  return { v1: void 0 };
}
var UpdateAdminListsData = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.v1 !== void 0) {
      UpdateAdminListsData_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdateAdminListsData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = UpdateAdminListsData_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet20(object.v1) ? UpdateAdminListsData_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? UpdateAdminListsData_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseUpdateAdminListsData();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? UpdateAdminListsData_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseUpdateAdminListsData_V1() {
  return { adminListUpdateType: 0, inboxId: "" };
}
var UpdateAdminListsData_V1 = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.adminListUpdateType !== 0) {
      writer.uint32(8).int32(message.adminListUpdateType);
    }
    if (message.inboxId !== "") {
      writer.uint32(18).string(message.inboxId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdateAdminListsData_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.adminListUpdateType = reader.int32();
          break;
        case 2:
          message.inboxId = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      adminListUpdateType: isSet20(object.adminListUpdateType) ? adminListUpdateTypeFromJSON(object.adminListUpdateType) : 0,
      inboxId: isSet20(object.inboxId) ? String(object.inboxId) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.adminListUpdateType !== void 0 && (obj.adminListUpdateType = adminListUpdateTypeToJSON(message.adminListUpdateType));
    message.inboxId !== void 0 && (obj.inboxId = message.inboxId);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseUpdateAdminListsData_V1();
    message.adminListUpdateType = (_a = object.adminListUpdateType) !== null && _a !== void 0 ? _a : 0;
    message.inboxId = (_b = object.inboxId) !== null && _b !== void 0 ? _b : "";
    return message;
  }
};
function createBaseUpdatePermissionData() {
  return { v1: void 0 };
}
var UpdatePermissionData = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.v1 !== void 0) {
      UpdatePermissionData_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdatePermissionData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = UpdatePermissionData_V1.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet20(object.v1) ? UpdatePermissionData_V1.fromJSON(object.v1) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? UpdatePermissionData_V1.toJSON(message.v1) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseUpdatePermissionData();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? UpdatePermissionData_V1.fromPartial(object.v1) : void 0;
    return message;
  }
};
function createBaseUpdatePermissionData_V1() {
  return {
    permissionUpdateType: 0,
    permissionPolicyOption: 0,
    metadataFieldName: void 0
  };
}
var UpdatePermissionData_V1 = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.permissionUpdateType !== 0) {
      writer.uint32(8).int32(message.permissionUpdateType);
    }
    if (message.permissionPolicyOption !== 0) {
      writer.uint32(16).int32(message.permissionPolicyOption);
    }
    if (message.metadataFieldName !== void 0) {
      writer.uint32(26).string(message.metadataFieldName);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdatePermissionData_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.permissionUpdateType = reader.int32();
          break;
        case 2:
          message.permissionPolicyOption = reader.int32();
          break;
        case 3:
          message.metadataFieldName = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      permissionUpdateType: isSet20(object.permissionUpdateType) ? permissionUpdateTypeFromJSON(object.permissionUpdateType) : 0,
      permissionPolicyOption: isSet20(object.permissionPolicyOption) ? permissionPolicyOptionFromJSON(object.permissionPolicyOption) : 0,
      metadataFieldName: isSet20(object.metadataFieldName) ? String(object.metadataFieldName) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.permissionUpdateType !== void 0 && (obj.permissionUpdateType = permissionUpdateTypeToJSON(message.permissionUpdateType));
    message.permissionPolicyOption !== void 0 && (obj.permissionPolicyOption = permissionPolicyOptionToJSON(message.permissionPolicyOption));
    message.metadataFieldName !== void 0 && (obj.metadataFieldName = message.metadataFieldName);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseUpdatePermissionData_V1();
    message.permissionUpdateType = (_a = object.permissionUpdateType) !== null && _a !== void 0 ? _a : 0;
    message.permissionPolicyOption = (_b = object.permissionPolicyOption) !== null && _b !== void 0 ? _b : 0;
    message.metadataFieldName = (_c = object.metadataFieldName) !== null && _c !== void 0 ? _c : void 0;
    return message;
  }
};
function createBasePostCommitAction() {
  return { sendWelcomes: void 0 };
}
var PostCommitAction = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.sendWelcomes !== void 0) {
      PostCommitAction_SendWelcomes.encode(message.sendWelcomes, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePostCommitAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.sendWelcomes = PostCommitAction_SendWelcomes.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      sendWelcomes: isSet20(object.sendWelcomes) ? PostCommitAction_SendWelcomes.fromJSON(object.sendWelcomes) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.sendWelcomes !== void 0 && (obj.sendWelcomes = message.sendWelcomes ? PostCommitAction_SendWelcomes.toJSON(message.sendWelcomes) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBasePostCommitAction();
    message.sendWelcomes = object.sendWelcomes !== void 0 && object.sendWelcomes !== null ? PostCommitAction_SendWelcomes.fromPartial(object.sendWelcomes) : void 0;
    return message;
  }
};
function createBasePostCommitAction_Installation() {
  return {
    installationKey: new Uint8Array(),
    hpkePublicKey: new Uint8Array(),
    welcomeWrapperAlgorithm: 0
  };
}
var PostCommitAction_Installation = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    if (message.installationKey.length !== 0) {
      writer.uint32(10).bytes(message.installationKey);
    }
    if (message.hpkePublicKey.length !== 0) {
      writer.uint32(18).bytes(message.hpkePublicKey);
    }
    if (message.welcomeWrapperAlgorithm !== 0) {
      writer.uint32(24).int32(message.welcomeWrapperAlgorithm);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePostCommitAction_Installation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installationKey = reader.bytes();
          break;
        case 2:
          message.hpkePublicKey = reader.bytes();
          break;
        case 3:
          message.welcomeWrapperAlgorithm = reader.int32();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installationKey: isSet20(object.installationKey) ? bytesFromBase6416(object.installationKey) : new Uint8Array(),
      hpkePublicKey: isSet20(object.hpkePublicKey) ? bytesFromBase6416(object.hpkePublicKey) : new Uint8Array(),
      welcomeWrapperAlgorithm: isSet20(object.welcomeWrapperAlgorithm) ? welcomeWrapperAlgorithmFromJSON(object.welcomeWrapperAlgorithm) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.installationKey !== void 0 && (obj.installationKey = base64FromBytes16(message.installationKey !== void 0 ? message.installationKey : new Uint8Array()));
    message.hpkePublicKey !== void 0 && (obj.hpkePublicKey = base64FromBytes16(message.hpkePublicKey !== void 0 ? message.hpkePublicKey : new Uint8Array()));
    message.welcomeWrapperAlgorithm !== void 0 && (obj.welcomeWrapperAlgorithm = welcomeWrapperAlgorithmToJSON(message.welcomeWrapperAlgorithm));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBasePostCommitAction_Installation();
    message.installationKey = (_a = object.installationKey) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.hpkePublicKey = (_b = object.hpkePublicKey) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.welcomeWrapperAlgorithm = (_c = object.welcomeWrapperAlgorithm) !== null && _c !== void 0 ? _c : 0;
    return message;
  }
};
function createBasePostCommitAction_SendWelcomes() {
  return { installations: [], welcomeMessage: new Uint8Array() };
}
var PostCommitAction_SendWelcomes = {
  encode(message, writer = import_minimal22.default.Writer.create()) {
    for (const v of message.installations) {
      PostCommitAction_Installation.encode(v, writer.uint32(10).fork()).ldelim();
    }
    if (message.welcomeMessage.length !== 0) {
      writer.uint32(18).bytes(message.welcomeMessage);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal22.default.Reader ? input : new import_minimal22.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePostCommitAction_SendWelcomes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installations.push(PostCommitAction_Installation.decode(reader, reader.uint32()));
          break;
        case 2:
          message.welcomeMessage = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installations: Array.isArray(object === null || object === void 0 ? void 0 : object.installations) ? object.installations.map((e) => PostCommitAction_Installation.fromJSON(e)) : [],
      welcomeMessage: isSet20(object.welcomeMessage) ? bytesFromBase6416(object.welcomeMessage) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.installations) {
      obj.installations = message.installations.map((e) => e ? PostCommitAction_Installation.toJSON(e) : void 0);
    } else {
      obj.installations = [];
    }
    message.welcomeMessage !== void 0 && (obj.welcomeMessage = base64FromBytes16(message.welcomeMessage !== void 0 ? message.welcomeMessage : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBasePostCommitAction_SendWelcomes();
    message.installations = ((_a = object.installations) === null || _a === void 0 ? void 0 : _a.map((e) => PostCommitAction_Installation.fromPartial(e))) || [];
    message.welcomeMessage = (_b = object.welcomeMessage) !== null && _b !== void 0 ? _b : new Uint8Array();
    return message;
  }
};
var globalThis17 = (() => {
  if (typeof globalThis17 !== "undefined")
    return globalThis17;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6416(b642) {
  if (globalThis17.Buffer) {
    return Uint8Array.from(globalThis17.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis17.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes16(arr) {
  if (globalThis17.Buffer) {
    return globalThis17.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis17.btoa(bin.join(""));
  }
}
if (import_minimal22.default.util.Long !== long_default) {
  import_minimal22.default.util.Long = long_default;
  import_minimal22.default.configure();
}
function isObject4(value) {
  return typeof value === "object" && value !== null;
}
function isSet20(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls/message_contents/content.pb.js
var content_pb_exports2 = {};
__export(content_pb_exports2, {
  Compression: () => Compression2,
  ContentTypeId: () => ContentTypeId2,
  EncodedContent: () => EncodedContent2,
  EncodedContent_ParametersEntry: () => EncodedContent_ParametersEntry2,
  PlaintextEnvelope: () => PlaintextEnvelope,
  PlaintextEnvelope_V1: () => PlaintextEnvelope_V1,
  PlaintextEnvelope_V2: () => PlaintextEnvelope_V2,
  compressionFromJSON: () => compressionFromJSON2,
  compressionToJSON: () => compressionToJSON2,
  protobufPackage: () => protobufPackage19
});

// node_modules/@xmtp/proto/ts/dist/esm/device_sync/group_backup.pb.js
var import_minimal23 = __toESM(require_minimal());
var GroupMembershipStateSave;
(function(GroupMembershipStateSave2) {
  GroupMembershipStateSave2[GroupMembershipStateSave2["GROUP_MEMBERSHIP_STATE_SAVE_UNSPECIFIED"] = 0] = "GROUP_MEMBERSHIP_STATE_SAVE_UNSPECIFIED";
  GroupMembershipStateSave2[GroupMembershipStateSave2["GROUP_MEMBERSHIP_STATE_SAVE_ALLOWED"] = 1] = "GROUP_MEMBERSHIP_STATE_SAVE_ALLOWED";
  GroupMembershipStateSave2[GroupMembershipStateSave2["GROUP_MEMBERSHIP_STATE_SAVE_REJECTED"] = 2] = "GROUP_MEMBERSHIP_STATE_SAVE_REJECTED";
  GroupMembershipStateSave2[GroupMembershipStateSave2["GROUP_MEMBERSHIP_STATE_SAVE_PENDING"] = 3] = "GROUP_MEMBERSHIP_STATE_SAVE_PENDING";
  GroupMembershipStateSave2[GroupMembershipStateSave2["GROUP_MEMBERSHIP_STATE_SAVE_RESTORED"] = 4] = "GROUP_MEMBERSHIP_STATE_SAVE_RESTORED";
  GroupMembershipStateSave2[GroupMembershipStateSave2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(GroupMembershipStateSave || (GroupMembershipStateSave = {}));
var ConversationTypeSave;
(function(ConversationTypeSave2) {
  ConversationTypeSave2[ConversationTypeSave2["CONVERSATION_TYPE_SAVE_UNSPECIFIED"] = 0] = "CONVERSATION_TYPE_SAVE_UNSPECIFIED";
  ConversationTypeSave2[ConversationTypeSave2["CONVERSATION_TYPE_SAVE_GROUP"] = 1] = "CONVERSATION_TYPE_SAVE_GROUP";
  ConversationTypeSave2[ConversationTypeSave2["CONVERSATION_TYPE_SAVE_DM"] = 2] = "CONVERSATION_TYPE_SAVE_DM";
  ConversationTypeSave2[ConversationTypeSave2["CONVERSATION_TYPE_SAVE_SYNC"] = 3] = "CONVERSATION_TYPE_SAVE_SYNC";
  ConversationTypeSave2[ConversationTypeSave2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(ConversationTypeSave || (ConversationTypeSave = {}));
var globalThis18 = (() => {
  if (typeof globalThis18 !== "undefined")
    return globalThis18;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
if (import_minimal23.default.util.Long !== long_default) {
  import_minimal23.default.util.Long = long_default;
  import_minimal23.default.configure();
}

// node_modules/@xmtp/proto/ts/dist/esm/device_sync/message_backup.pb.js
var import_minimal24 = __toESM(require_minimal());
var GroupMessageKindSave;
(function(GroupMessageKindSave2) {
  GroupMessageKindSave2[GroupMessageKindSave2["GROUP_MESSAGE_KIND_SAVE_UNSPECIFIED"] = 0] = "GROUP_MESSAGE_KIND_SAVE_UNSPECIFIED";
  GroupMessageKindSave2[GroupMessageKindSave2["GROUP_MESSAGE_KIND_SAVE_APPLICATION"] = 1] = "GROUP_MESSAGE_KIND_SAVE_APPLICATION";
  GroupMessageKindSave2[GroupMessageKindSave2["GROUP_MESSAGE_KIND_SAVE_MEMBERSHIP_CHANGE"] = 2] = "GROUP_MESSAGE_KIND_SAVE_MEMBERSHIP_CHANGE";
  GroupMessageKindSave2[GroupMessageKindSave2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(GroupMessageKindSave || (GroupMessageKindSave = {}));
var DeliveryStatusSave;
(function(DeliveryStatusSave2) {
  DeliveryStatusSave2[DeliveryStatusSave2["DELIVERY_STATUS_SAVE_UNSPECIFIED"] = 0] = "DELIVERY_STATUS_SAVE_UNSPECIFIED";
  DeliveryStatusSave2[DeliveryStatusSave2["DELIVERY_STATUS_SAVE_UNPUBLISHED"] = 1] = "DELIVERY_STATUS_SAVE_UNPUBLISHED";
  DeliveryStatusSave2[DeliveryStatusSave2["DELIVERY_STATUS_SAVE_PUBLISHED"] = 2] = "DELIVERY_STATUS_SAVE_PUBLISHED";
  DeliveryStatusSave2[DeliveryStatusSave2["DELIVERY_STATUS_SAVE_FAILED"] = 3] = "DELIVERY_STATUS_SAVE_FAILED";
  DeliveryStatusSave2[DeliveryStatusSave2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(DeliveryStatusSave || (DeliveryStatusSave = {}));
var ContentTypeSave;
(function(ContentTypeSave2) {
  ContentTypeSave2[ContentTypeSave2["CONTENT_TYPE_SAVE_UNSPECIFIED"] = 0] = "CONTENT_TYPE_SAVE_UNSPECIFIED";
  ContentTypeSave2[ContentTypeSave2["CONTENT_TYPE_SAVE_UNKNOWN"] = 1] = "CONTENT_TYPE_SAVE_UNKNOWN";
  ContentTypeSave2[ContentTypeSave2["CONTENT_TYPE_SAVE_TEXT"] = 2] = "CONTENT_TYPE_SAVE_TEXT";
  ContentTypeSave2[ContentTypeSave2["CONTENT_TYPE_SAVE_GROUP_MEMBERSHIP_CHANGE"] = 3] = "CONTENT_TYPE_SAVE_GROUP_MEMBERSHIP_CHANGE";
  ContentTypeSave2[ContentTypeSave2["CONTENT_TYPE_SAVE_GROUP_UPDATED"] = 4] = "CONTENT_TYPE_SAVE_GROUP_UPDATED";
  ContentTypeSave2[ContentTypeSave2["CONTENT_TYPE_SAVE_REACTION"] = 5] = "CONTENT_TYPE_SAVE_REACTION";
  ContentTypeSave2[ContentTypeSave2["CONTENT_TYPE_SAVE_READ_RECEIPT"] = 6] = "CONTENT_TYPE_SAVE_READ_RECEIPT";
  ContentTypeSave2[ContentTypeSave2["CONTENT_TYPE_SAVE_REPLY"] = 7] = "CONTENT_TYPE_SAVE_REPLY";
  ContentTypeSave2[ContentTypeSave2["CONTENT_TYPE_SAVE_ATTACHMENT"] = 8] = "CONTENT_TYPE_SAVE_ATTACHMENT";
  ContentTypeSave2[ContentTypeSave2["CONTENT_TYPE_SAVE_REMOTE_ATTACHMENT"] = 9] = "CONTENT_TYPE_SAVE_REMOTE_ATTACHMENT";
  ContentTypeSave2[ContentTypeSave2["CONTENT_TYPE_SAVE_TRANSACTION_REFERENCE"] = 10] = "CONTENT_TYPE_SAVE_TRANSACTION_REFERENCE";
  ContentTypeSave2[ContentTypeSave2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(ContentTypeSave || (ContentTypeSave = {}));
var globalThis19 = (() => {
  if (typeof globalThis19 !== "undefined")
    return globalThis19;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
if (import_minimal24.default.util.Long !== long_default) {
  import_minimal24.default.util.Long = long_default;
  import_minimal24.default.configure();
}

// node_modules/@xmtp/proto/ts/dist/esm/device_sync/consent_backup.pb.js
var import_minimal25 = __toESM(require_minimal());
var ConsentTypeSave;
(function(ConsentTypeSave2) {
  ConsentTypeSave2[ConsentTypeSave2["CONSENT_TYPE_SAVE_UNSPECIFIED"] = 0] = "CONSENT_TYPE_SAVE_UNSPECIFIED";
  ConsentTypeSave2[ConsentTypeSave2["CONSENT_TYPE_SAVE_CONVERSATION_ID"] = 1] = "CONSENT_TYPE_SAVE_CONVERSATION_ID";
  ConsentTypeSave2[ConsentTypeSave2["CONSENT_TYPE_SAVE_INBOX_ID"] = 2] = "CONSENT_TYPE_SAVE_INBOX_ID";
  ConsentTypeSave2[ConsentTypeSave2["CONSENT_TYPE_SAVE_ADDRESS"] = 3] = "CONSENT_TYPE_SAVE_ADDRESS";
  ConsentTypeSave2[ConsentTypeSave2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(ConsentTypeSave || (ConsentTypeSave = {}));
var ConsentStateSave;
(function(ConsentStateSave2) {
  ConsentStateSave2[ConsentStateSave2["CONSENT_STATE_SAVE_UNSPECIFIED"] = 0] = "CONSENT_STATE_SAVE_UNSPECIFIED";
  ConsentStateSave2[ConsentStateSave2["CONSENT_STATE_SAVE_UNKNOWN"] = 1] = "CONSENT_STATE_SAVE_UNKNOWN";
  ConsentStateSave2[ConsentStateSave2["CONSENT_STATE_SAVE_ALLOWED"] = 2] = "CONSENT_STATE_SAVE_ALLOWED";
  ConsentStateSave2[ConsentStateSave2["CONSENT_STATE_SAVE_DENIED"] = 3] = "CONSENT_STATE_SAVE_DENIED";
  ConsentStateSave2[ConsentStateSave2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(ConsentStateSave || (ConsentStateSave = {}));
if (import_minimal25.default.util.Long !== long_default) {
  import_minimal25.default.util.Long = long_default;
  import_minimal25.default.configure();
}

// node_modules/@xmtp/proto/ts/dist/esm/device_sync/event_backup.pb.js
var import_minimal26 = __toESM(require_minimal());
var EventLevelSave;
(function(EventLevelSave2) {
  EventLevelSave2[EventLevelSave2["EVENT_LEVEL_SAVE_UNSPECIFIED"] = 0] = "EVENT_LEVEL_SAVE_UNSPECIFIED";
  EventLevelSave2[EventLevelSave2["EVENT_LEVEL_SAVE_NONE"] = 1] = "EVENT_LEVEL_SAVE_NONE";
  EventLevelSave2[EventLevelSave2["EVENT_LEVEL_SAVE_SUCCESS"] = 2] = "EVENT_LEVEL_SAVE_SUCCESS";
  EventLevelSave2[EventLevelSave2["EVENT_LEVEL_SAVE_WARN"] = 3] = "EVENT_LEVEL_SAVE_WARN";
  EventLevelSave2[EventLevelSave2["EVENT_LEVEL_SAVE_ERROR"] = 4] = "EVENT_LEVEL_SAVE_ERROR";
  EventLevelSave2[EventLevelSave2["EVENT_LEVEL_SAVE_FAULT"] = 5] = "EVENT_LEVEL_SAVE_FAULT";
  EventLevelSave2[EventLevelSave2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(EventLevelSave || (EventLevelSave = {}));
var globalThis20 = (() => {
  if (typeof globalThis20 !== "undefined")
    return globalThis20;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
if (import_minimal26.default.util.Long !== long_default) {
  import_minimal26.default.util.Long = long_default;
  import_minimal26.default.configure();
}

// node_modules/@xmtp/proto/ts/dist/esm/device_sync/device_sync.pb.js
var import_minimal27 = __toESM(require_minimal());
var BackupElementSelection;
(function(BackupElementSelection2) {
  BackupElementSelection2[BackupElementSelection2["BACKUP_ELEMENT_SELECTION_UNSPECIFIED"] = 0] = "BACKUP_ELEMENT_SELECTION_UNSPECIFIED";
  BackupElementSelection2[BackupElementSelection2["BACKUP_ELEMENT_SELECTION_MESSAGES"] = 1] = "BACKUP_ELEMENT_SELECTION_MESSAGES";
  BackupElementSelection2[BackupElementSelection2["BACKUP_ELEMENT_SELECTION_CONSENT"] = 2] = "BACKUP_ELEMENT_SELECTION_CONSENT";
  BackupElementSelection2[BackupElementSelection2["BACKUP_ELEMENT_SELECTION_EVENT"] = 3] = "BACKUP_ELEMENT_SELECTION_EVENT";
  BackupElementSelection2[BackupElementSelection2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(BackupElementSelection || (BackupElementSelection = {}));
function backupElementSelectionFromJSON(object) {
  switch (object) {
    case 0:
    case "BACKUP_ELEMENT_SELECTION_UNSPECIFIED":
      return BackupElementSelection.BACKUP_ELEMENT_SELECTION_UNSPECIFIED;
    case 1:
    case "BACKUP_ELEMENT_SELECTION_MESSAGES":
      return BackupElementSelection.BACKUP_ELEMENT_SELECTION_MESSAGES;
    case 2:
    case "BACKUP_ELEMENT_SELECTION_CONSENT":
      return BackupElementSelection.BACKUP_ELEMENT_SELECTION_CONSENT;
    case 3:
    case "BACKUP_ELEMENT_SELECTION_EVENT":
      return BackupElementSelection.BACKUP_ELEMENT_SELECTION_EVENT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return BackupElementSelection.UNRECOGNIZED;
  }
}
function backupElementSelectionToJSON(object) {
  switch (object) {
    case BackupElementSelection.BACKUP_ELEMENT_SELECTION_UNSPECIFIED:
      return "BACKUP_ELEMENT_SELECTION_UNSPECIFIED";
    case BackupElementSelection.BACKUP_ELEMENT_SELECTION_MESSAGES:
      return "BACKUP_ELEMENT_SELECTION_MESSAGES";
    case BackupElementSelection.BACKUP_ELEMENT_SELECTION_CONSENT:
      return "BACKUP_ELEMENT_SELECTION_CONSENT";
    case BackupElementSelection.BACKUP_ELEMENT_SELECTION_EVENT:
      return "BACKUP_ELEMENT_SELECTION_EVENT";
    case BackupElementSelection.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseBackupMetadataSave() {
  return {
    elements: [],
    exportedAtNs: long_default.ZERO,
    startNs: void 0,
    endNs: void 0
  };
}
var BackupMetadataSave = {
  encode(message, writer = import_minimal27.default.Writer.create()) {
    writer.uint32(18).fork();
    for (const v of message.elements) {
      writer.int32(v);
    }
    writer.ldelim();
    if (!message.exportedAtNs.isZero()) {
      writer.uint32(24).int64(message.exportedAtNs);
    }
    if (message.startNs !== void 0) {
      writer.uint32(32).int64(message.startNs);
    }
    if (message.endNs !== void 0) {
      writer.uint32(40).int64(message.endNs);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal27.default.Reader ? input : new import_minimal27.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseBackupMetadataSave();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2:
          if ((tag & 7) === 2) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.elements.push(reader.int32());
            }
          } else {
            message.elements.push(reader.int32());
          }
          break;
        case 3:
          message.exportedAtNs = reader.int64();
          break;
        case 4:
          message.startNs = reader.int64();
          break;
        case 5:
          message.endNs = reader.int64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      elements: Array.isArray(object === null || object === void 0 ? void 0 : object.elements) ? object.elements.map((e) => backupElementSelectionFromJSON(e)) : [],
      exportedAtNs: isSet21(object.exportedAtNs) ? long_default.fromValue(object.exportedAtNs) : long_default.ZERO,
      startNs: isSet21(object.startNs) ? long_default.fromValue(object.startNs) : void 0,
      endNs: isSet21(object.endNs) ? long_default.fromValue(object.endNs) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.elements) {
      obj.elements = message.elements.map((e) => backupElementSelectionToJSON(e));
    } else {
      obj.elements = [];
    }
    message.exportedAtNs !== void 0 && (obj.exportedAtNs = (message.exportedAtNs || long_default.ZERO).toString());
    message.startNs !== void 0 && (obj.startNs = (message.startNs || void 0).toString());
    message.endNs !== void 0 && (obj.endNs = (message.endNs || void 0).toString());
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseBackupMetadataSave();
    message.elements = ((_a = object.elements) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    message.exportedAtNs = object.exportedAtNs !== void 0 && object.exportedAtNs !== null ? long_default.fromValue(object.exportedAtNs) : long_default.ZERO;
    message.startNs = object.startNs !== void 0 && object.startNs !== null ? long_default.fromValue(object.startNs) : void 0;
    message.endNs = object.endNs !== void 0 && object.endNs !== null ? long_default.fromValue(object.endNs) : void 0;
    return message;
  }
};
function createBaseBackupOptions() {
  return { elements: [], startNs: void 0, endNs: void 0 };
}
var BackupOptions = {
  encode(message, writer = import_minimal27.default.Writer.create()) {
    writer.uint32(10).fork();
    for (const v of message.elements) {
      writer.int32(v);
    }
    writer.ldelim();
    if (message.startNs !== void 0) {
      writer.uint32(16).int64(message.startNs);
    }
    if (message.endNs !== void 0) {
      writer.uint32(24).int64(message.endNs);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal27.default.Reader ? input : new import_minimal27.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseBackupOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if ((tag & 7) === 2) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.elements.push(reader.int32());
            }
          } else {
            message.elements.push(reader.int32());
          }
          break;
        case 2:
          message.startNs = reader.int64();
          break;
        case 3:
          message.endNs = reader.int64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      elements: Array.isArray(object === null || object === void 0 ? void 0 : object.elements) ? object.elements.map((e) => backupElementSelectionFromJSON(e)) : [],
      startNs: isSet21(object.startNs) ? long_default.fromValue(object.startNs) : void 0,
      endNs: isSet21(object.endNs) ? long_default.fromValue(object.endNs) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.elements) {
      obj.elements = message.elements.map((e) => backupElementSelectionToJSON(e));
    } else {
      obj.elements = [];
    }
    message.startNs !== void 0 && (obj.startNs = (message.startNs || void 0).toString());
    message.endNs !== void 0 && (obj.endNs = (message.endNs || void 0).toString());
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseBackupOptions();
    message.elements = ((_a = object.elements) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    message.startNs = object.startNs !== void 0 && object.startNs !== null ? long_default.fromValue(object.startNs) : void 0;
    message.endNs = object.endNs !== void 0 && object.endNs !== null ? long_default.fromValue(object.endNs) : void 0;
    return message;
  }
};
if (import_minimal27.default.util.Long !== long_default) {
  import_minimal27.default.util.Long = long_default;
  import_minimal27.default.configure();
}
function isSet21(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/device_sync/content.pb.js
var import_minimal28 = __toESM(require_minimal());
function createBaseV1UserPreferenceUpdate() {
  return { contents: [] };
}
var V1UserPreferenceUpdate = {
  encode(message, writer = import_minimal28.default.Writer.create()) {
    for (const v of message.contents) {
      writer.uint32(10).bytes(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal28.default.Reader ? input : new import_minimal28.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseV1UserPreferenceUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.contents.push(reader.bytes());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      contents: Array.isArray(object === null || object === void 0 ? void 0 : object.contents) ? object.contents.map((e) => bytesFromBase6417(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.contents) {
      obj.contents = message.contents.map((e) => base64FromBytes17(e !== void 0 ? e : new Uint8Array()));
    } else {
      obj.contents = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseV1UserPreferenceUpdate();
    message.contents = ((_a = object.contents) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
function createBaseDeviceSyncRequest() {
  return { requestId: "", pinCode: "", kind: 0, options: void 0 };
}
var DeviceSyncRequest = {
  encode(message, writer = import_minimal28.default.Writer.create()) {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.pinCode !== "") {
      writer.uint32(18).string(message.pinCode);
    }
    if (message.kind !== 0) {
      writer.uint32(24).int32(message.kind);
    }
    if (message.options !== void 0) {
      BackupOptions.encode(message.options, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal28.default.Reader ? input : new import_minimal28.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDeviceSyncRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.requestId = reader.string();
          break;
        case 2:
          message.pinCode = reader.string();
          break;
        case 3:
          message.kind = reader.int32();
          break;
        case 4:
          message.options = BackupOptions.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      requestId: isSet22(object.requestId) ? String(object.requestId) : "",
      pinCode: isSet22(object.pinCode) ? String(object.pinCode) : "",
      kind: isSet22(object.kind) ? backupElementSelectionFromJSON(object.kind) : 0,
      options: isSet22(object.options) ? BackupOptions.fromJSON(object.options) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.requestId !== void 0 && (obj.requestId = message.requestId);
    message.pinCode !== void 0 && (obj.pinCode = message.pinCode);
    message.kind !== void 0 && (obj.kind = backupElementSelectionToJSON(message.kind));
    message.options !== void 0 && (obj.options = message.options ? BackupOptions.toJSON(message.options) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseDeviceSyncRequest();
    message.requestId = (_a = object.requestId) !== null && _a !== void 0 ? _a : "";
    message.pinCode = (_b = object.pinCode) !== null && _b !== void 0 ? _b : "";
    message.kind = (_c = object.kind) !== null && _c !== void 0 ? _c : 0;
    message.options = object.options !== void 0 && object.options !== null ? BackupOptions.fromPartial(object.options) : void 0;
    return message;
  }
};
function createBaseDeviceSyncReply() {
  return {
    requestId: "",
    url: "",
    encryptionKey: void 0,
    timestampNs: long_default.UZERO,
    kind: 0,
    metadata: void 0
  };
}
var DeviceSyncReply = {
  encode(message, writer = import_minimal28.default.Writer.create()) {
    if (message.requestId !== "") {
      writer.uint32(10).string(message.requestId);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    if (message.encryptionKey !== void 0) {
      DeviceSyncKeyType.encode(message.encryptionKey, writer.uint32(26).fork()).ldelim();
    }
    if (!message.timestampNs.isZero()) {
      writer.uint32(32).uint64(message.timestampNs);
    }
    if (message.kind !== 0) {
      writer.uint32(40).int32(message.kind);
    }
    if (message.metadata !== void 0) {
      BackupMetadataSave.encode(message.metadata, writer.uint32(50).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal28.default.Reader ? input : new import_minimal28.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDeviceSyncReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.requestId = reader.string();
          break;
        case 2:
          message.url = reader.string();
          break;
        case 3:
          message.encryptionKey = DeviceSyncKeyType.decode(reader, reader.uint32());
          break;
        case 4:
          message.timestampNs = reader.uint64();
          break;
        case 5:
          message.kind = reader.int32();
          break;
        case 6:
          message.metadata = BackupMetadataSave.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      requestId: isSet22(object.requestId) ? String(object.requestId) : "",
      url: isSet22(object.url) ? String(object.url) : "",
      encryptionKey: isSet22(object.encryptionKey) ? DeviceSyncKeyType.fromJSON(object.encryptionKey) : void 0,
      timestampNs: isSet22(object.timestampNs) ? long_default.fromValue(object.timestampNs) : long_default.UZERO,
      kind: isSet22(object.kind) ? backupElementSelectionFromJSON(object.kind) : 0,
      metadata: isSet22(object.metadata) ? BackupMetadataSave.fromJSON(object.metadata) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.requestId !== void 0 && (obj.requestId = message.requestId);
    message.url !== void 0 && (obj.url = message.url);
    message.encryptionKey !== void 0 && (obj.encryptionKey = message.encryptionKey ? DeviceSyncKeyType.toJSON(message.encryptionKey) : void 0);
    message.timestampNs !== void 0 && (obj.timestampNs = (message.timestampNs || long_default.UZERO).toString());
    message.kind !== void 0 && (obj.kind = backupElementSelectionToJSON(message.kind));
    message.metadata !== void 0 && (obj.metadata = message.metadata ? BackupMetadataSave.toJSON(message.metadata) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseDeviceSyncReply();
    message.requestId = (_a = object.requestId) !== null && _a !== void 0 ? _a : "";
    message.url = (_b = object.url) !== null && _b !== void 0 ? _b : "";
    message.encryptionKey = object.encryptionKey !== void 0 && object.encryptionKey !== null ? DeviceSyncKeyType.fromPartial(object.encryptionKey) : void 0;
    message.timestampNs = object.timestampNs !== void 0 && object.timestampNs !== null ? long_default.fromValue(object.timestampNs) : long_default.UZERO;
    message.kind = (_c = object.kind) !== null && _c !== void 0 ? _c : 0;
    message.metadata = object.metadata !== void 0 && object.metadata !== null ? BackupMetadataSave.fromPartial(object.metadata) : void 0;
    return message;
  }
};
function createBaseDeviceSyncKeyType() {
  return { aes256Gcm: void 0 };
}
var DeviceSyncKeyType = {
  encode(message, writer = import_minimal28.default.Writer.create()) {
    if (message.aes256Gcm !== void 0) {
      writer.uint32(10).bytes(message.aes256Gcm);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal28.default.Reader ? input : new import_minimal28.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDeviceSyncKeyType();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.aes256Gcm = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      aes256Gcm: isSet22(object.aes256Gcm) ? bytesFromBase6417(object.aes256Gcm) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.aes256Gcm !== void 0 && (obj.aes256Gcm = message.aes256Gcm !== void 0 ? base64FromBytes17(message.aes256Gcm) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseDeviceSyncKeyType();
    message.aes256Gcm = (_a = object.aes256Gcm) !== null && _a !== void 0 ? _a : void 0;
    return message;
  }
};
var globalThis21 = (() => {
  if (typeof globalThis21 !== "undefined")
    return globalThis21;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6417(b642) {
  if (globalThis21.Buffer) {
    return Uint8Array.from(globalThis21.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis21.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes17(arr) {
  if (globalThis21.Buffer) {
    return globalThis21.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis21.btoa(bin.join(""));
  }
}
if (import_minimal28.default.util.Long !== long_default) {
  import_minimal28.default.util.Long = long_default;
  import_minimal28.default.configure();
}
function isSet22(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls/message_contents/content.pb.js
var import_minimal29 = __toESM(require_minimal());
var protobufPackage19 = "xmtp.mls.message_contents";
var Compression2;
(function(Compression3) {
  Compression3[Compression3["COMPRESSION_DEFLATE"] = 0] = "COMPRESSION_DEFLATE";
  Compression3[Compression3["COMPRESSION_GZIP"] = 1] = "COMPRESSION_GZIP";
  Compression3[Compression3["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(Compression2 || (Compression2 = {}));
function compressionFromJSON2(object) {
  switch (object) {
    case 0:
    case "COMPRESSION_DEFLATE":
      return Compression2.COMPRESSION_DEFLATE;
    case 1:
    case "COMPRESSION_GZIP":
      return Compression2.COMPRESSION_GZIP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Compression2.UNRECOGNIZED;
  }
}
function compressionToJSON2(object) {
  switch (object) {
    case Compression2.COMPRESSION_DEFLATE:
      return "COMPRESSION_DEFLATE";
    case Compression2.COMPRESSION_GZIP:
      return "COMPRESSION_GZIP";
    case Compression2.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseContentTypeId2() {
  return { authorityId: "", typeId: "", versionMajor: 0, versionMinor: 0 };
}
var ContentTypeId2 = {
  encode(message, writer = import_minimal29.default.Writer.create()) {
    if (message.authorityId !== "") {
      writer.uint32(10).string(message.authorityId);
    }
    if (message.typeId !== "") {
      writer.uint32(18).string(message.typeId);
    }
    if (message.versionMajor !== 0) {
      writer.uint32(24).uint32(message.versionMajor);
    }
    if (message.versionMinor !== 0) {
      writer.uint32(32).uint32(message.versionMinor);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal29.default.Reader ? input : new import_minimal29.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseContentTypeId2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.authorityId = reader.string();
          break;
        case 2:
          message.typeId = reader.string();
          break;
        case 3:
          message.versionMajor = reader.uint32();
          break;
        case 4:
          message.versionMinor = reader.uint32();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      authorityId: isSet23(object.authorityId) ? String(object.authorityId) : "",
      typeId: isSet23(object.typeId) ? String(object.typeId) : "",
      versionMajor: isSet23(object.versionMajor) ? Number(object.versionMajor) : 0,
      versionMinor: isSet23(object.versionMinor) ? Number(object.versionMinor) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.authorityId !== void 0 && (obj.authorityId = message.authorityId);
    message.typeId !== void 0 && (obj.typeId = message.typeId);
    message.versionMajor !== void 0 && (obj.versionMajor = Math.round(message.versionMajor));
    message.versionMinor !== void 0 && (obj.versionMinor = Math.round(message.versionMinor));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d;
    const message = createBaseContentTypeId2();
    message.authorityId = (_a = object.authorityId) !== null && _a !== void 0 ? _a : "";
    message.typeId = (_b = object.typeId) !== null && _b !== void 0 ? _b : "";
    message.versionMajor = (_c = object.versionMajor) !== null && _c !== void 0 ? _c : 0;
    message.versionMinor = (_d = object.versionMinor) !== null && _d !== void 0 ? _d : 0;
    return message;
  }
};
function createBaseEncodedContent2() {
  return {
    type: void 0,
    parameters: {},
    fallback: void 0,
    compression: void 0,
    content: new Uint8Array()
  };
}
var EncodedContent2 = {
  encode(message, writer = import_minimal29.default.Writer.create()) {
    if (message.type !== void 0) {
      ContentTypeId2.encode(message.type, writer.uint32(10).fork()).ldelim();
    }
    Object.entries(message.parameters).forEach(([key, value]) => {
      EncodedContent_ParametersEntry2.encode({ key, value }, writer.uint32(18).fork()).ldelim();
    });
    if (message.fallback !== void 0) {
      writer.uint32(26).string(message.fallback);
    }
    if (message.compression !== void 0) {
      writer.uint32(40).int32(message.compression);
    }
    if (message.content.length !== 0) {
      writer.uint32(34).bytes(message.content);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal29.default.Reader ? input : new import_minimal29.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncodedContent2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.type = ContentTypeId2.decode(reader, reader.uint32());
          break;
        case 2:
          const entry2 = EncodedContent_ParametersEntry2.decode(reader, reader.uint32());
          if (entry2.value !== void 0) {
            message.parameters[entry2.key] = entry2.value;
          }
          break;
        case 3:
          message.fallback = reader.string();
          break;
        case 5:
          message.compression = reader.int32();
          break;
        case 4:
          message.content = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      type: isSet23(object.type) ? ContentTypeId2.fromJSON(object.type) : void 0,
      parameters: isObject5(object.parameters) ? Object.entries(object.parameters).reduce((acc, [key, value]) => {
        acc[key] = String(value);
        return acc;
      }, {}) : {},
      fallback: isSet23(object.fallback) ? String(object.fallback) : void 0,
      compression: isSet23(object.compression) ? compressionFromJSON2(object.compression) : void 0,
      content: isSet23(object.content) ? bytesFromBase6418(object.content) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.type !== void 0 && (obj.type = message.type ? ContentTypeId2.toJSON(message.type) : void 0);
    obj.parameters = {};
    if (message.parameters) {
      Object.entries(message.parameters).forEach(([k, v]) => {
        obj.parameters[k] = v;
      });
    }
    message.fallback !== void 0 && (obj.fallback = message.fallback);
    message.compression !== void 0 && (obj.compression = message.compression !== void 0 ? compressionToJSON2(message.compression) : void 0);
    message.content !== void 0 && (obj.content = base64FromBytes18(message.content !== void 0 ? message.content : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d;
    const message = createBaseEncodedContent2();
    message.type = object.type !== void 0 && object.type !== null ? ContentTypeId2.fromPartial(object.type) : void 0;
    message.parameters = Object.entries((_a = object.parameters) !== null && _a !== void 0 ? _a : {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = String(value);
      }
      return acc;
    }, {});
    message.fallback = (_b = object.fallback) !== null && _b !== void 0 ? _b : void 0;
    message.compression = (_c = object.compression) !== null && _c !== void 0 ? _c : void 0;
    message.content = (_d = object.content) !== null && _d !== void 0 ? _d : new Uint8Array();
    return message;
  }
};
function createBaseEncodedContent_ParametersEntry2() {
  return { key: "", value: "" };
}
var EncodedContent_ParametersEntry2 = {
  encode(message, writer = import_minimal29.default.Writer.create()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal29.default.Reader ? input : new import_minimal29.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEncodedContent_ParametersEntry2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.key = reader.string();
          break;
        case 2:
          message.value = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet23(object.key) ? String(object.key) : "",
      value: isSet23(object.value) ? String(object.value) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.key !== void 0 && (obj.key = message.key);
    message.value !== void 0 && (obj.value = message.value);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseEncodedContent_ParametersEntry2();
    message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
    message.value = (_b = object.value) !== null && _b !== void 0 ? _b : "";
    return message;
  }
};
function createBasePlaintextEnvelope() {
  return { v1: void 0, v2: void 0 };
}
var PlaintextEnvelope = {
  encode(message, writer = import_minimal29.default.Writer.create()) {
    if (message.v1 !== void 0) {
      PlaintextEnvelope_V1.encode(message.v1, writer.uint32(10).fork()).ldelim();
    }
    if (message.v2 !== void 0) {
      PlaintextEnvelope_V2.encode(message.v2, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal29.default.Reader ? input : new import_minimal29.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePlaintextEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.v1 = PlaintextEnvelope_V1.decode(reader, reader.uint32());
          break;
        case 2:
          message.v2 = PlaintextEnvelope_V2.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      v1: isSet23(object.v1) ? PlaintextEnvelope_V1.fromJSON(object.v1) : void 0,
      v2: isSet23(object.v2) ? PlaintextEnvelope_V2.fromJSON(object.v2) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.v1 !== void 0 && (obj.v1 = message.v1 ? PlaintextEnvelope_V1.toJSON(message.v1) : void 0);
    message.v2 !== void 0 && (obj.v2 = message.v2 ? PlaintextEnvelope_V2.toJSON(message.v2) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBasePlaintextEnvelope();
    message.v1 = object.v1 !== void 0 && object.v1 !== null ? PlaintextEnvelope_V1.fromPartial(object.v1) : void 0;
    message.v2 = object.v2 !== void 0 && object.v2 !== null ? PlaintextEnvelope_V2.fromPartial(object.v2) : void 0;
    return message;
  }
};
function createBasePlaintextEnvelope_V1() {
  return { content: new Uint8Array(), idempotencyKey: "" };
}
var PlaintextEnvelope_V1 = {
  encode(message, writer = import_minimal29.default.Writer.create()) {
    if (message.content.length !== 0) {
      writer.uint32(10).bytes(message.content);
    }
    if (message.idempotencyKey !== "") {
      writer.uint32(18).string(message.idempotencyKey);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal29.default.Reader ? input : new import_minimal29.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePlaintextEnvelope_V1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.content = reader.bytes();
          break;
        case 2:
          message.idempotencyKey = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      content: isSet23(object.content) ? bytesFromBase6418(object.content) : new Uint8Array(),
      idempotencyKey: isSet23(object.idempotencyKey) ? String(object.idempotencyKey) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.content !== void 0 && (obj.content = base64FromBytes18(message.content !== void 0 ? message.content : new Uint8Array()));
    message.idempotencyKey !== void 0 && (obj.idempotencyKey = message.idempotencyKey);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBasePlaintextEnvelope_V1();
    message.content = (_a = object.content) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.idempotencyKey = (_b = object.idempotencyKey) !== null && _b !== void 0 ? _b : "";
    return message;
  }
};
function createBasePlaintextEnvelope_V2() {
  return {
    idempotencyKey: "",
    content: void 0,
    deviceSyncRequest: void 0,
    deviceSyncReply: void 0,
    userPreferenceUpdate: void 0
  };
}
var PlaintextEnvelope_V2 = {
  encode(message, writer = import_minimal29.default.Writer.create()) {
    if (message.idempotencyKey !== "") {
      writer.uint32(10).string(message.idempotencyKey);
    }
    if (message.content !== void 0) {
      writer.uint32(18).bytes(message.content);
    }
    if (message.deviceSyncRequest !== void 0) {
      DeviceSyncRequest.encode(message.deviceSyncRequest, writer.uint32(26).fork()).ldelim();
    }
    if (message.deviceSyncReply !== void 0) {
      DeviceSyncReply.encode(message.deviceSyncReply, writer.uint32(34).fork()).ldelim();
    }
    if (message.userPreferenceUpdate !== void 0) {
      V1UserPreferenceUpdate.encode(message.userPreferenceUpdate, writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal29.default.Reader ? input : new import_minimal29.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePlaintextEnvelope_V2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.idempotencyKey = reader.string();
          break;
        case 2:
          message.content = reader.bytes();
          break;
        case 3:
          message.deviceSyncRequest = DeviceSyncRequest.decode(reader, reader.uint32());
          break;
        case 4:
          message.deviceSyncReply = DeviceSyncReply.decode(reader, reader.uint32());
          break;
        case 5:
          message.userPreferenceUpdate = V1UserPreferenceUpdate.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      idempotencyKey: isSet23(object.idempotencyKey) ? String(object.idempotencyKey) : "",
      content: isSet23(object.content) ? bytesFromBase6418(object.content) : void 0,
      deviceSyncRequest: isSet23(object.deviceSyncRequest) ? DeviceSyncRequest.fromJSON(object.deviceSyncRequest) : void 0,
      deviceSyncReply: isSet23(object.deviceSyncReply) ? DeviceSyncReply.fromJSON(object.deviceSyncReply) : void 0,
      userPreferenceUpdate: isSet23(object.userPreferenceUpdate) ? V1UserPreferenceUpdate.fromJSON(object.userPreferenceUpdate) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.idempotencyKey !== void 0 && (obj.idempotencyKey = message.idempotencyKey);
    message.content !== void 0 && (obj.content = message.content !== void 0 ? base64FromBytes18(message.content) : void 0);
    message.deviceSyncRequest !== void 0 && (obj.deviceSyncRequest = message.deviceSyncRequest ? DeviceSyncRequest.toJSON(message.deviceSyncRequest) : void 0);
    message.deviceSyncReply !== void 0 && (obj.deviceSyncReply = message.deviceSyncReply ? DeviceSyncReply.toJSON(message.deviceSyncReply) : void 0);
    message.userPreferenceUpdate !== void 0 && (obj.userPreferenceUpdate = message.userPreferenceUpdate ? V1UserPreferenceUpdate.toJSON(message.userPreferenceUpdate) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBasePlaintextEnvelope_V2();
    message.idempotencyKey = (_a = object.idempotencyKey) !== null && _a !== void 0 ? _a : "";
    message.content = (_b = object.content) !== null && _b !== void 0 ? _b : void 0;
    message.deviceSyncRequest = object.deviceSyncRequest !== void 0 && object.deviceSyncRequest !== null ? DeviceSyncRequest.fromPartial(object.deviceSyncRequest) : void 0;
    message.deviceSyncReply = object.deviceSyncReply !== void 0 && object.deviceSyncReply !== null ? DeviceSyncReply.fromPartial(object.deviceSyncReply) : void 0;
    message.userPreferenceUpdate = object.userPreferenceUpdate !== void 0 && object.userPreferenceUpdate !== null ? V1UserPreferenceUpdate.fromPartial(object.userPreferenceUpdate) : void 0;
    return message;
  }
};
var globalThis22 = (() => {
  if (typeof globalThis22 !== "undefined")
    return globalThis22;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6418(b642) {
  if (globalThis22.Buffer) {
    return Uint8Array.from(globalThis22.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis22.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes18(arr) {
  if (globalThis22.Buffer) {
    return globalThis22.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis22.btoa(bin.join(""));
  }
}
if (import_minimal29.default.util.Long !== long_default) {
  import_minimal29.default.util.Long = long_default;
  import_minimal29.default.configure();
}
function isObject5(value) {
  return typeof value === "object" && value !== null;
}
function isSet23(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls/message_contents/group_membership.pb.js
var group_membership_pb_exports = {};
__export(group_membership_pb_exports, {
  GroupMembership: () => GroupMembership,
  GroupMembership_MembersEntry: () => GroupMembership_MembersEntry,
  protobufPackage: () => protobufPackage20
});
var import_minimal30 = __toESM(require_minimal());
var protobufPackage20 = "xmtp.mls.message_contents";
function createBaseGroupMembership() {
  return { members: {}, failedInstallations: [] };
}
var GroupMembership = {
  encode(message, writer = import_minimal30.default.Writer.create()) {
    Object.entries(message.members).forEach(([key, value]) => {
      GroupMembership_MembersEntry.encode({ key, value }, writer.uint32(10).fork()).ldelim();
    });
    for (const v of message.failedInstallations) {
      writer.uint32(18).bytes(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal30.default.Reader ? input : new import_minimal30.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupMembership();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          const entry1 = GroupMembership_MembersEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.members[entry1.key] = entry1.value;
          }
          break;
        case 2:
          message.failedInstallations.push(reader.bytes());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      members: isObject6(object.members) ? Object.entries(object.members).reduce((acc, [key, value]) => {
        acc[key] = long_default.fromValue(value);
        return acc;
      }, {}) : {},
      failedInstallations: Array.isArray(object === null || object === void 0 ? void 0 : object.failedInstallations) ? object.failedInstallations.map((e) => bytesFromBase6419(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    obj.members = {};
    if (message.members) {
      Object.entries(message.members).forEach(([k, v]) => {
        obj.members[k] = v.toString();
      });
    }
    if (message.failedInstallations) {
      obj.failedInstallations = message.failedInstallations.map((e) => base64FromBytes19(e !== void 0 ? e : new Uint8Array()));
    } else {
      obj.failedInstallations = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseGroupMembership();
    message.members = Object.entries((_a = object.members) !== null && _a !== void 0 ? _a : {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = long_default.fromValue(value);
      }
      return acc;
    }, {});
    message.failedInstallations = ((_b = object.failedInstallations) === null || _b === void 0 ? void 0 : _b.map((e) => e)) || [];
    return message;
  }
};
function createBaseGroupMembership_MembersEntry() {
  return { key: "", value: long_default.UZERO };
}
var GroupMembership_MembersEntry = {
  encode(message, writer = import_minimal30.default.Writer.create()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (!message.value.isZero()) {
      writer.uint32(16).uint64(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal30.default.Reader ? input : new import_minimal30.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupMembership_MembersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.key = reader.string();
          break;
        case 2:
          message.value = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet24(object.key) ? String(object.key) : "",
      value: isSet24(object.value) ? long_default.fromValue(object.value) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.key !== void 0 && (obj.key = message.key);
    message.value !== void 0 && (obj.value = (message.value || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGroupMembership_MembersEntry();
    message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
    message.value = object.value !== void 0 && object.value !== null ? long_default.fromValue(object.value) : long_default.UZERO;
    return message;
  }
};
var globalThis23 = (() => {
  if (typeof globalThis23 !== "undefined")
    return globalThis23;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6419(b642) {
  if (globalThis23.Buffer) {
    return Uint8Array.from(globalThis23.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis23.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes19(arr) {
  if (globalThis23.Buffer) {
    return globalThis23.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis23.btoa(bin.join(""));
  }
}
if (import_minimal30.default.util.Long !== long_default) {
  import_minimal30.default.util.Long = long_default;
  import_minimal30.default.configure();
}
function isObject6(value) {
  return typeof value === "object" && value !== null;
}
function isSet24(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls/message_contents/group_metadata.pb.js
var group_metadata_pb_exports = {};
__export(group_metadata_pb_exports, {
  ConversationType: () => ConversationType,
  DmMembers: () => DmMembers,
  GroupMetadataV1: () => GroupMetadataV1,
  Inbox: () => Inbox,
  conversationTypeFromJSON: () => conversationTypeFromJSON,
  conversationTypeToJSON: () => conversationTypeToJSON,
  protobufPackage: () => protobufPackage21
});

// node_modules/@xmtp/proto/ts/dist/esm/mls/message_contents/oneshot.pb.js
var import_minimal31 = __toESM(require_minimal());
function createBaseOneshotMessage() {
  return { readdRequest: void 0 };
}
var OneshotMessage = {
  encode(message, writer = import_minimal31.default.Writer.create()) {
    if (message.readdRequest !== void 0) {
      ReaddRequest.encode(message.readdRequest, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal31.default.Reader ? input : new import_minimal31.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseOneshotMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.readdRequest = ReaddRequest.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      readdRequest: isSet25(object.readdRequest) ? ReaddRequest.fromJSON(object.readdRequest) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.readdRequest !== void 0 && (obj.readdRequest = message.readdRequest ? ReaddRequest.toJSON(message.readdRequest) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseOneshotMessage();
    message.readdRequest = object.readdRequest !== void 0 && object.readdRequest !== null ? ReaddRequest.fromPartial(object.readdRequest) : void 0;
    return message;
  }
};
function createBaseReaddRequest() {
  return { groupId: new Uint8Array(), latestCommitSequenceId: long_default.UZERO };
}
var ReaddRequest = {
  encode(message, writer = import_minimal31.default.Writer.create()) {
    if (message.groupId.length !== 0) {
      writer.uint32(10).bytes(message.groupId);
    }
    if (!message.latestCommitSequenceId.isZero()) {
      writer.uint32(16).uint64(message.latestCommitSequenceId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal31.default.Reader ? input : new import_minimal31.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseReaddRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.groupId = reader.bytes();
          break;
        case 2:
          message.latestCommitSequenceId = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      groupId: isSet25(object.groupId) ? bytesFromBase6420(object.groupId) : new Uint8Array(),
      latestCommitSequenceId: isSet25(object.latestCommitSequenceId) ? long_default.fromValue(object.latestCommitSequenceId) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.groupId !== void 0 && (obj.groupId = base64FromBytes20(message.groupId !== void 0 ? message.groupId : new Uint8Array()));
    message.latestCommitSequenceId !== void 0 && (obj.latestCommitSequenceId = (message.latestCommitSequenceId || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseReaddRequest();
    message.groupId = (_a = object.groupId) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.latestCommitSequenceId = object.latestCommitSequenceId !== void 0 && object.latestCommitSequenceId !== null ? long_default.fromValue(object.latestCommitSequenceId) : long_default.UZERO;
    return message;
  }
};
var globalThis24 = (() => {
  if (typeof globalThis24 !== "undefined")
    return globalThis24;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6420(b642) {
  if (globalThis24.Buffer) {
    return Uint8Array.from(globalThis24.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis24.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes20(arr) {
  if (globalThis24.Buffer) {
    return globalThis24.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis24.btoa(bin.join(""));
  }
}
if (import_minimal31.default.util.Long !== long_default) {
  import_minimal31.default.util.Long = long_default;
  import_minimal31.default.configure();
}
function isSet25(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls/message_contents/group_metadata.pb.js
var import_minimal32 = __toESM(require_minimal());
var protobufPackage21 = "xmtp.mls.message_contents";
var ConversationType;
(function(ConversationType2) {
  ConversationType2[ConversationType2["CONVERSATION_TYPE_UNSPECIFIED"] = 0] = "CONVERSATION_TYPE_UNSPECIFIED";
  ConversationType2[ConversationType2["CONVERSATION_TYPE_GROUP"] = 1] = "CONVERSATION_TYPE_GROUP";
  ConversationType2[ConversationType2["CONVERSATION_TYPE_DM"] = 2] = "CONVERSATION_TYPE_DM";
  ConversationType2[ConversationType2["CONVERSATION_TYPE_SYNC"] = 3] = "CONVERSATION_TYPE_SYNC";
  ConversationType2[ConversationType2["CONVERSATION_TYPE_ONESHOT"] = 4] = "CONVERSATION_TYPE_ONESHOT";
  ConversationType2[ConversationType2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(ConversationType || (ConversationType = {}));
function conversationTypeFromJSON(object) {
  switch (object) {
    case 0:
    case "CONVERSATION_TYPE_UNSPECIFIED":
      return ConversationType.CONVERSATION_TYPE_UNSPECIFIED;
    case 1:
    case "CONVERSATION_TYPE_GROUP":
      return ConversationType.CONVERSATION_TYPE_GROUP;
    case 2:
    case "CONVERSATION_TYPE_DM":
      return ConversationType.CONVERSATION_TYPE_DM;
    case 3:
    case "CONVERSATION_TYPE_SYNC":
      return ConversationType.CONVERSATION_TYPE_SYNC;
    case 4:
    case "CONVERSATION_TYPE_ONESHOT":
      return ConversationType.CONVERSATION_TYPE_ONESHOT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ConversationType.UNRECOGNIZED;
  }
}
function conversationTypeToJSON(object) {
  switch (object) {
    case ConversationType.CONVERSATION_TYPE_UNSPECIFIED:
      return "CONVERSATION_TYPE_UNSPECIFIED";
    case ConversationType.CONVERSATION_TYPE_GROUP:
      return "CONVERSATION_TYPE_GROUP";
    case ConversationType.CONVERSATION_TYPE_DM:
      return "CONVERSATION_TYPE_DM";
    case ConversationType.CONVERSATION_TYPE_SYNC:
      return "CONVERSATION_TYPE_SYNC";
    case ConversationType.CONVERSATION_TYPE_ONESHOT:
      return "CONVERSATION_TYPE_ONESHOT";
    case ConversationType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseGroupMetadataV1() {
  return {
    conversationType: 0,
    creatorAccountAddress: "",
    creatorInboxId: "",
    dmMembers: void 0,
    oneshotMessage: void 0
  };
}
var GroupMetadataV1 = {
  encode(message, writer = import_minimal32.default.Writer.create()) {
    if (message.conversationType !== 0) {
      writer.uint32(8).int32(message.conversationType);
    }
    if (message.creatorAccountAddress !== "") {
      writer.uint32(18).string(message.creatorAccountAddress);
    }
    if (message.creatorInboxId !== "") {
      writer.uint32(26).string(message.creatorInboxId);
    }
    if (message.dmMembers !== void 0) {
      DmMembers.encode(message.dmMembers, writer.uint32(34).fork()).ldelim();
    }
    if (message.oneshotMessage !== void 0) {
      OneshotMessage.encode(message.oneshotMessage, writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal32.default.Reader ? input : new import_minimal32.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupMetadataV1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.conversationType = reader.int32();
          break;
        case 2:
          message.creatorAccountAddress = reader.string();
          break;
        case 3:
          message.creatorInboxId = reader.string();
          break;
        case 4:
          message.dmMembers = DmMembers.decode(reader, reader.uint32());
          break;
        case 5:
          message.oneshotMessage = OneshotMessage.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      conversationType: isSet26(object.conversationType) ? conversationTypeFromJSON(object.conversationType) : 0,
      creatorAccountAddress: isSet26(object.creatorAccountAddress) ? String(object.creatorAccountAddress) : "",
      creatorInboxId: isSet26(object.creatorInboxId) ? String(object.creatorInboxId) : "",
      dmMembers: isSet26(object.dmMembers) ? DmMembers.fromJSON(object.dmMembers) : void 0,
      oneshotMessage: isSet26(object.oneshotMessage) ? OneshotMessage.fromJSON(object.oneshotMessage) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.conversationType !== void 0 && (obj.conversationType = conversationTypeToJSON(message.conversationType));
    message.creatorAccountAddress !== void 0 && (obj.creatorAccountAddress = message.creatorAccountAddress);
    message.creatorInboxId !== void 0 && (obj.creatorInboxId = message.creatorInboxId);
    message.dmMembers !== void 0 && (obj.dmMembers = message.dmMembers ? DmMembers.toJSON(message.dmMembers) : void 0);
    message.oneshotMessage !== void 0 && (obj.oneshotMessage = message.oneshotMessage ? OneshotMessage.toJSON(message.oneshotMessage) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseGroupMetadataV1();
    message.conversationType = (_a = object.conversationType) !== null && _a !== void 0 ? _a : 0;
    message.creatorAccountAddress = (_b = object.creatorAccountAddress) !== null && _b !== void 0 ? _b : "";
    message.creatorInboxId = (_c = object.creatorInboxId) !== null && _c !== void 0 ? _c : "";
    message.dmMembers = object.dmMembers !== void 0 && object.dmMembers !== null ? DmMembers.fromPartial(object.dmMembers) : void 0;
    message.oneshotMessage = object.oneshotMessage !== void 0 && object.oneshotMessage !== null ? OneshotMessage.fromPartial(object.oneshotMessage) : void 0;
    return message;
  }
};
function createBaseInbox() {
  return { inboxId: "" };
}
var Inbox = {
  encode(message, writer = import_minimal32.default.Writer.create()) {
    if (message.inboxId !== "") {
      writer.uint32(10).string(message.inboxId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal32.default.Reader ? input : new import_minimal32.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInbox();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.inboxId = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      inboxId: isSet26(object.inboxId) ? String(object.inboxId) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.inboxId !== void 0 && (obj.inboxId = message.inboxId);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseInbox();
    message.inboxId = (_a = object.inboxId) !== null && _a !== void 0 ? _a : "";
    return message;
  }
};
function createBaseDmMembers() {
  return { dmMemberOne: void 0, dmMemberTwo: void 0 };
}
var DmMembers = {
  encode(message, writer = import_minimal32.default.Writer.create()) {
    if (message.dmMemberOne !== void 0) {
      Inbox.encode(message.dmMemberOne, writer.uint32(10).fork()).ldelim();
    }
    if (message.dmMemberTwo !== void 0) {
      Inbox.encode(message.dmMemberTwo, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal32.default.Reader ? input : new import_minimal32.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDmMembers();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.dmMemberOne = Inbox.decode(reader, reader.uint32());
          break;
        case 2:
          message.dmMemberTwo = Inbox.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      dmMemberOne: isSet26(object.dmMemberOne) ? Inbox.fromJSON(object.dmMemberOne) : void 0,
      dmMemberTwo: isSet26(object.dmMemberTwo) ? Inbox.fromJSON(object.dmMemberTwo) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.dmMemberOne !== void 0 && (obj.dmMemberOne = message.dmMemberOne ? Inbox.toJSON(message.dmMemberOne) : void 0);
    message.dmMemberTwo !== void 0 && (obj.dmMemberTwo = message.dmMemberTwo ? Inbox.toJSON(message.dmMemberTwo) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseDmMembers();
    message.dmMemberOne = object.dmMemberOne !== void 0 && object.dmMemberOne !== null ? Inbox.fromPartial(object.dmMemberOne) : void 0;
    message.dmMemberTwo = object.dmMemberTwo !== void 0 && object.dmMemberTwo !== null ? Inbox.fromPartial(object.dmMemberTwo) : void 0;
    return message;
  }
};
if (import_minimal32.default.util.Long !== long_default) {
  import_minimal32.default.util.Long = long_default;
  import_minimal32.default.configure();
}
function isSet26(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls/message_contents/group_mutable_metadata.pb.js
var group_mutable_metadata_pb_exports = {};
__export(group_mutable_metadata_pb_exports, {
  GroupMutableMetadataV1: () => GroupMutableMetadataV1,
  GroupMutableMetadataV1_AttributesEntry: () => GroupMutableMetadataV1_AttributesEntry,
  Inboxes: () => Inboxes,
  protobufPackage: () => protobufPackage22
});
var import_minimal33 = __toESM(require_minimal());
var protobufPackage22 = "xmtp.mls.message_contents";
function createBaseGroupMutableMetadataV1() {
  return { attributes: {}, adminList: void 0, superAdminList: void 0 };
}
var GroupMutableMetadataV1 = {
  encode(message, writer = import_minimal33.default.Writer.create()) {
    Object.entries(message.attributes).forEach(([key, value]) => {
      GroupMutableMetadataV1_AttributesEntry.encode({ key, value }, writer.uint32(10).fork()).ldelim();
    });
    if (message.adminList !== void 0) {
      Inboxes.encode(message.adminList, writer.uint32(18).fork()).ldelim();
    }
    if (message.superAdminList !== void 0) {
      Inboxes.encode(message.superAdminList, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal33.default.Reader ? input : new import_minimal33.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupMutableMetadataV1();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          const entry1 = GroupMutableMetadataV1_AttributesEntry.decode(reader, reader.uint32());
          if (entry1.value !== void 0) {
            message.attributes[entry1.key] = entry1.value;
          }
          break;
        case 2:
          message.adminList = Inboxes.decode(reader, reader.uint32());
          break;
        case 3:
          message.superAdminList = Inboxes.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      attributes: isObject7(object.attributes) ? Object.entries(object.attributes).reduce((acc, [key, value]) => {
        acc[key] = String(value);
        return acc;
      }, {}) : {},
      adminList: isSet27(object.adminList) ? Inboxes.fromJSON(object.adminList) : void 0,
      superAdminList: isSet27(object.superAdminList) ? Inboxes.fromJSON(object.superAdminList) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    obj.attributes = {};
    if (message.attributes) {
      Object.entries(message.attributes).forEach(([k, v]) => {
        obj.attributes[k] = v;
      });
    }
    message.adminList !== void 0 && (obj.adminList = message.adminList ? Inboxes.toJSON(message.adminList) : void 0);
    message.superAdminList !== void 0 && (obj.superAdminList = message.superAdminList ? Inboxes.toJSON(message.superAdminList) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGroupMutableMetadataV1();
    message.attributes = Object.entries((_a = object.attributes) !== null && _a !== void 0 ? _a : {}).reduce((acc, [key, value]) => {
      if (value !== void 0) {
        acc[key] = String(value);
      }
      return acc;
    }, {});
    message.adminList = object.adminList !== void 0 && object.adminList !== null ? Inboxes.fromPartial(object.adminList) : void 0;
    message.superAdminList = object.superAdminList !== void 0 && object.superAdminList !== null ? Inboxes.fromPartial(object.superAdminList) : void 0;
    return message;
  }
};
function createBaseGroupMutableMetadataV1_AttributesEntry() {
  return { key: "", value: "" };
}
var GroupMutableMetadataV1_AttributesEntry = {
  encode(message, writer = import_minimal33.default.Writer.create()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal33.default.Reader ? input : new import_minimal33.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupMutableMetadataV1_AttributesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.key = reader.string();
          break;
        case 2:
          message.value = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet27(object.key) ? String(object.key) : "",
      value: isSet27(object.value) ? String(object.value) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.key !== void 0 && (obj.key = message.key);
    message.value !== void 0 && (obj.value = message.value);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseGroupMutableMetadataV1_AttributesEntry();
    message.key = (_a = object.key) !== null && _a !== void 0 ? _a : "";
    message.value = (_b = object.value) !== null && _b !== void 0 ? _b : "";
    return message;
  }
};
function createBaseInboxes() {
  return { inboxIds: [] };
}
var Inboxes = {
  encode(message, writer = import_minimal33.default.Writer.create()) {
    for (const v of message.inboxIds) {
      writer.uint32(10).string(v);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal33.default.Reader ? input : new import_minimal33.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInboxes();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.inboxIds.push(reader.string());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      inboxIds: Array.isArray(object === null || object === void 0 ? void 0 : object.inboxIds) ? object.inboxIds.map((e) => String(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.inboxIds) {
      obj.inboxIds = message.inboxIds.map((e) => e);
    } else {
      obj.inboxIds = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseInboxes();
    message.inboxIds = ((_a = object.inboxIds) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    return message;
  }
};
if (import_minimal33.default.util.Long !== long_default) {
  import_minimal33.default.util.Long = long_default;
  import_minimal33.default.configure();
}
function isObject7(value) {
  return typeof value === "object" && value !== null;
}
function isSet27(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls/message_contents/transcript_messages.pb.js
var transcript_messages_pb_exports = {};
__export(transcript_messages_pb_exports, {
  GroupMembershipChanges: () => GroupMembershipChanges,
  GroupUpdated: () => GroupUpdated,
  GroupUpdated_Inbox: () => GroupUpdated_Inbox,
  GroupUpdated_MetadataFieldChange: () => GroupUpdated_MetadataFieldChange,
  MembershipChange: () => MembershipChange,
  protobufPackage: () => protobufPackage23
});
var import_minimal34 = __toESM(require_minimal());
var protobufPackage23 = "xmtp.mls.message_contents";
function createBaseMembershipChange() {
  return {
    installationIds: [],
    accountAddress: "",
    initiatedByAccountAddress: ""
  };
}
var MembershipChange = {
  encode(message, writer = import_minimal34.default.Writer.create()) {
    for (const v of message.installationIds) {
      writer.uint32(10).bytes(v);
    }
    if (message.accountAddress !== "") {
      writer.uint32(18).string(message.accountAddress);
    }
    if (message.initiatedByAccountAddress !== "") {
      writer.uint32(26).string(message.initiatedByAccountAddress);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal34.default.Reader ? input : new import_minimal34.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMembershipChange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.installationIds.push(reader.bytes());
          break;
        case 2:
          message.accountAddress = reader.string();
          break;
        case 3:
          message.initiatedByAccountAddress = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      installationIds: Array.isArray(object === null || object === void 0 ? void 0 : object.installationIds) ? object.installationIds.map((e) => bytesFromBase6421(e)) : [],
      accountAddress: isSet28(object.accountAddress) ? String(object.accountAddress) : "",
      initiatedByAccountAddress: isSet28(object.initiatedByAccountAddress) ? String(object.initiatedByAccountAddress) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.installationIds) {
      obj.installationIds = message.installationIds.map((e) => base64FromBytes21(e !== void 0 ? e : new Uint8Array()));
    } else {
      obj.installationIds = [];
    }
    message.accountAddress !== void 0 && (obj.accountAddress = message.accountAddress);
    message.initiatedByAccountAddress !== void 0 && (obj.initiatedByAccountAddress = message.initiatedByAccountAddress);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseMembershipChange();
    message.installationIds = ((_a = object.installationIds) === null || _a === void 0 ? void 0 : _a.map((e) => e)) || [];
    message.accountAddress = (_b = object.accountAddress) !== null && _b !== void 0 ? _b : "";
    message.initiatedByAccountAddress = (_c = object.initiatedByAccountAddress) !== null && _c !== void 0 ? _c : "";
    return message;
  }
};
function createBaseGroupMembershipChanges() {
  return {
    membersAdded: [],
    membersRemoved: [],
    installationsAdded: [],
    installationsRemoved: []
  };
}
var GroupMembershipChanges = {
  encode(message, writer = import_minimal34.default.Writer.create()) {
    for (const v of message.membersAdded) {
      MembershipChange.encode(v, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.membersRemoved) {
      MembershipChange.encode(v, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.installationsAdded) {
      MembershipChange.encode(v, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.installationsRemoved) {
      MembershipChange.encode(v, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal34.default.Reader ? input : new import_minimal34.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupMembershipChanges();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.membersAdded.push(MembershipChange.decode(reader, reader.uint32()));
          break;
        case 2:
          message.membersRemoved.push(MembershipChange.decode(reader, reader.uint32()));
          break;
        case 3:
          message.installationsAdded.push(MembershipChange.decode(reader, reader.uint32()));
          break;
        case 4:
          message.installationsRemoved.push(MembershipChange.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      membersAdded: Array.isArray(object === null || object === void 0 ? void 0 : object.membersAdded) ? object.membersAdded.map((e) => MembershipChange.fromJSON(e)) : [],
      membersRemoved: Array.isArray(object === null || object === void 0 ? void 0 : object.membersRemoved) ? object.membersRemoved.map((e) => MembershipChange.fromJSON(e)) : [],
      installationsAdded: Array.isArray(object === null || object === void 0 ? void 0 : object.installationsAdded) ? object.installationsAdded.map((e) => MembershipChange.fromJSON(e)) : [],
      installationsRemoved: Array.isArray(object === null || object === void 0 ? void 0 : object.installationsRemoved) ? object.installationsRemoved.map((e) => MembershipChange.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.membersAdded) {
      obj.membersAdded = message.membersAdded.map((e) => e ? MembershipChange.toJSON(e) : void 0);
    } else {
      obj.membersAdded = [];
    }
    if (message.membersRemoved) {
      obj.membersRemoved = message.membersRemoved.map((e) => e ? MembershipChange.toJSON(e) : void 0);
    } else {
      obj.membersRemoved = [];
    }
    if (message.installationsAdded) {
      obj.installationsAdded = message.installationsAdded.map((e) => e ? MembershipChange.toJSON(e) : void 0);
    } else {
      obj.installationsAdded = [];
    }
    if (message.installationsRemoved) {
      obj.installationsRemoved = message.installationsRemoved.map((e) => e ? MembershipChange.toJSON(e) : void 0);
    } else {
      obj.installationsRemoved = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d;
    const message = createBaseGroupMembershipChanges();
    message.membersAdded = ((_a = object.membersAdded) === null || _a === void 0 ? void 0 : _a.map((e) => MembershipChange.fromPartial(e))) || [];
    message.membersRemoved = ((_b = object.membersRemoved) === null || _b === void 0 ? void 0 : _b.map((e) => MembershipChange.fromPartial(e))) || [];
    message.installationsAdded = ((_c = object.installationsAdded) === null || _c === void 0 ? void 0 : _c.map((e) => MembershipChange.fromPartial(e))) || [];
    message.installationsRemoved = ((_d = object.installationsRemoved) === null || _d === void 0 ? void 0 : _d.map((e) => MembershipChange.fromPartial(e))) || [];
    return message;
  }
};
function createBaseGroupUpdated() {
  return {
    initiatedByInboxId: "",
    addedInboxes: [],
    removedInboxes: [],
    metadataFieldChanges: []
  };
}
var GroupUpdated = {
  encode(message, writer = import_minimal34.default.Writer.create()) {
    if (message.initiatedByInboxId !== "") {
      writer.uint32(10).string(message.initiatedByInboxId);
    }
    for (const v of message.addedInboxes) {
      GroupUpdated_Inbox.encode(v, writer.uint32(18).fork()).ldelim();
    }
    for (const v of message.removedInboxes) {
      GroupUpdated_Inbox.encode(v, writer.uint32(26).fork()).ldelim();
    }
    for (const v of message.metadataFieldChanges) {
      GroupUpdated_MetadataFieldChange.encode(v, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal34.default.Reader ? input : new import_minimal34.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupUpdated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.initiatedByInboxId = reader.string();
          break;
        case 2:
          message.addedInboxes.push(GroupUpdated_Inbox.decode(reader, reader.uint32()));
          break;
        case 3:
          message.removedInboxes.push(GroupUpdated_Inbox.decode(reader, reader.uint32()));
          break;
        case 4:
          message.metadataFieldChanges.push(GroupUpdated_MetadataFieldChange.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      initiatedByInboxId: isSet28(object.initiatedByInboxId) ? String(object.initiatedByInboxId) : "",
      addedInboxes: Array.isArray(object === null || object === void 0 ? void 0 : object.addedInboxes) ? object.addedInboxes.map((e) => GroupUpdated_Inbox.fromJSON(e)) : [],
      removedInboxes: Array.isArray(object === null || object === void 0 ? void 0 : object.removedInboxes) ? object.removedInboxes.map((e) => GroupUpdated_Inbox.fromJSON(e)) : [],
      metadataFieldChanges: Array.isArray(object === null || object === void 0 ? void 0 : object.metadataFieldChanges) ? object.metadataFieldChanges.map((e) => GroupUpdated_MetadataFieldChange.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    message.initiatedByInboxId !== void 0 && (obj.initiatedByInboxId = message.initiatedByInboxId);
    if (message.addedInboxes) {
      obj.addedInboxes = message.addedInboxes.map((e) => e ? GroupUpdated_Inbox.toJSON(e) : void 0);
    } else {
      obj.addedInboxes = [];
    }
    if (message.removedInboxes) {
      obj.removedInboxes = message.removedInboxes.map((e) => e ? GroupUpdated_Inbox.toJSON(e) : void 0);
    } else {
      obj.removedInboxes = [];
    }
    if (message.metadataFieldChanges) {
      obj.metadataFieldChanges = message.metadataFieldChanges.map((e) => e ? GroupUpdated_MetadataFieldChange.toJSON(e) : void 0);
    } else {
      obj.metadataFieldChanges = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d;
    const message = createBaseGroupUpdated();
    message.initiatedByInboxId = (_a = object.initiatedByInboxId) !== null && _a !== void 0 ? _a : "";
    message.addedInboxes = ((_b = object.addedInboxes) === null || _b === void 0 ? void 0 : _b.map((e) => GroupUpdated_Inbox.fromPartial(e))) || [];
    message.removedInboxes = ((_c = object.removedInboxes) === null || _c === void 0 ? void 0 : _c.map((e) => GroupUpdated_Inbox.fromPartial(e))) || [];
    message.metadataFieldChanges = ((_d = object.metadataFieldChanges) === null || _d === void 0 ? void 0 : _d.map((e) => GroupUpdated_MetadataFieldChange.fromPartial(e))) || [];
    return message;
  }
};
function createBaseGroupUpdated_Inbox() {
  return { inboxId: "" };
}
var GroupUpdated_Inbox = {
  encode(message, writer = import_minimal34.default.Writer.create()) {
    if (message.inboxId !== "") {
      writer.uint32(10).string(message.inboxId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal34.default.Reader ? input : new import_minimal34.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupUpdated_Inbox();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.inboxId = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      inboxId: isSet28(object.inboxId) ? String(object.inboxId) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.inboxId !== void 0 && (obj.inboxId = message.inboxId);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseGroupUpdated_Inbox();
    message.inboxId = (_a = object.inboxId) !== null && _a !== void 0 ? _a : "";
    return message;
  }
};
function createBaseGroupUpdated_MetadataFieldChange() {
  return { fieldName: "", oldValue: void 0, newValue: void 0 };
}
var GroupUpdated_MetadataFieldChange = {
  encode(message, writer = import_minimal34.default.Writer.create()) {
    if (message.fieldName !== "") {
      writer.uint32(10).string(message.fieldName);
    }
    if (message.oldValue !== void 0) {
      writer.uint32(18).string(message.oldValue);
    }
    if (message.newValue !== void 0) {
      writer.uint32(26).string(message.newValue);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal34.default.Reader ? input : new import_minimal34.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGroupUpdated_MetadataFieldChange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.fieldName = reader.string();
          break;
        case 2:
          message.oldValue = reader.string();
          break;
        case 3:
          message.newValue = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      fieldName: isSet28(object.fieldName) ? String(object.fieldName) : "",
      oldValue: isSet28(object.oldValue) ? String(object.oldValue) : void 0,
      newValue: isSet28(object.newValue) ? String(object.newValue) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.fieldName !== void 0 && (obj.fieldName = message.fieldName);
    message.oldValue !== void 0 && (obj.oldValue = message.oldValue);
    message.newValue !== void 0 && (obj.newValue = message.newValue);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseGroupUpdated_MetadataFieldChange();
    message.fieldName = (_a = object.fieldName) !== null && _a !== void 0 ? _a : "";
    message.oldValue = (_b = object.oldValue) !== null && _b !== void 0 ? _b : void 0;
    message.newValue = (_c = object.newValue) !== null && _c !== void 0 ? _c : void 0;
    return message;
  }
};
var globalThis25 = (() => {
  if (typeof globalThis25 !== "undefined")
    return globalThis25;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6421(b642) {
  if (globalThis25.Buffer) {
    return Uint8Array.from(globalThis25.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis25.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes21(arr) {
  if (globalThis25.Buffer) {
    return globalThis25.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis25.btoa(bin.join(""));
  }
}
if (import_minimal34.default.util.Long !== long_default) {
  import_minimal34.default.util.Long = long_default;
  import_minimal34.default.configure();
}
function isSet28(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls_validation/v1/service.pb.js
var service_pb_exports = {};
__export(service_pb_exports, {
  GetAssociationStateRequest: () => GetAssociationStateRequest,
  GetAssociationStateResponse: () => GetAssociationStateResponse,
  ValidateGroupMessagesRequest: () => ValidateGroupMessagesRequest,
  ValidateGroupMessagesRequest_GroupMessage: () => ValidateGroupMessagesRequest_GroupMessage,
  ValidateGroupMessagesResponse: () => ValidateGroupMessagesResponse,
  ValidateGroupMessagesResponse_ValidationResponse: () => ValidateGroupMessagesResponse_ValidationResponse,
  ValidateInboxIdKeyPackagesRequest: () => ValidateInboxIdKeyPackagesRequest,
  ValidateInboxIdKeyPackagesRequest_KeyPackage: () => ValidateInboxIdKeyPackagesRequest_KeyPackage,
  ValidateInboxIdKeyPackagesResponse: () => ValidateInboxIdKeyPackagesResponse,
  ValidateInboxIdKeyPackagesResponse_Response: () => ValidateInboxIdKeyPackagesResponse_Response,
  ValidateKeyPackagesRequest: () => ValidateKeyPackagesRequest,
  ValidateKeyPackagesRequest_KeyPackage: () => ValidateKeyPackagesRequest_KeyPackage,
  ValidateKeyPackagesResponse: () => ValidateKeyPackagesResponse,
  ValidateKeyPackagesResponse_ValidationResponse: () => ValidateKeyPackagesResponse_ValidationResponse,
  ValidationApiClientImpl: () => ValidationApiClientImpl,
  protobufPackage: () => protobufPackage24
});

// node_modules/@xmtp/proto/ts/dist/esm/identity/credential.pb.js
var import_minimal35 = __toESM(require_minimal());
function createBaseMlsCredential() {
  return { inboxId: "" };
}
var MlsCredential = {
  encode(message, writer = import_minimal35.default.Writer.create()) {
    if (message.inboxId !== "") {
      writer.uint32(10).string(message.inboxId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal35.default.Reader ? input : new import_minimal35.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMlsCredential();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.inboxId = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      inboxId: isSet29(object.inboxId) ? String(object.inboxId) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    message.inboxId !== void 0 && (obj.inboxId = message.inboxId);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseMlsCredential();
    message.inboxId = (_a = object.inboxId) !== null && _a !== void 0 ? _a : "";
    return message;
  }
};
if (import_minimal35.default.util.Long !== long_default) {
  import_minimal35.default.util.Long = long_default;
  import_minimal35.default.configure();
}
function isSet29(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/identity/associations/association.pb.js
var import_minimal36 = __toESM(require_minimal());
var IdentifierKind;
(function(IdentifierKind2) {
  IdentifierKind2[IdentifierKind2["IDENTIFIER_KIND_UNSPECIFIED"] = 0] = "IDENTIFIER_KIND_UNSPECIFIED";
  IdentifierKind2[IdentifierKind2["IDENTIFIER_KIND_ETHEREUM"] = 1] = "IDENTIFIER_KIND_ETHEREUM";
  IdentifierKind2[IdentifierKind2["IDENTIFIER_KIND_PASSKEY"] = 2] = "IDENTIFIER_KIND_PASSKEY";
  IdentifierKind2[IdentifierKind2["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(IdentifierKind || (IdentifierKind = {}));
function identifierKindFromJSON(object) {
  switch (object) {
    case 0:
    case "IDENTIFIER_KIND_UNSPECIFIED":
      return IdentifierKind.IDENTIFIER_KIND_UNSPECIFIED;
    case 1:
    case "IDENTIFIER_KIND_ETHEREUM":
      return IdentifierKind.IDENTIFIER_KIND_ETHEREUM;
    case 2:
    case "IDENTIFIER_KIND_PASSKEY":
      return IdentifierKind.IDENTIFIER_KIND_PASSKEY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return IdentifierKind.UNRECOGNIZED;
  }
}
function identifierKindToJSON(object) {
  switch (object) {
    case IdentifierKind.IDENTIFIER_KIND_UNSPECIFIED:
      return "IDENTIFIER_KIND_UNSPECIFIED";
    case IdentifierKind.IDENTIFIER_KIND_ETHEREUM:
      return "IDENTIFIER_KIND_ETHEREUM";
    case IdentifierKind.IDENTIFIER_KIND_PASSKEY:
      return "IDENTIFIER_KIND_PASSKEY";
    case IdentifierKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}
function createBaseMemberIdentifier() {
  return {
    ethereumAddress: void 0,
    installationPublicKey: void 0,
    passkey: void 0
  };
}
var MemberIdentifier = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    if (message.ethereumAddress !== void 0) {
      writer.uint32(10).string(message.ethereumAddress);
    }
    if (message.installationPublicKey !== void 0) {
      writer.uint32(18).bytes(message.installationPublicKey);
    }
    if (message.passkey !== void 0) {
      Passkey.encode(message.passkey, writer.uint32(26).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMemberIdentifier();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.ethereumAddress = reader.string();
          break;
        case 2:
          message.installationPublicKey = reader.bytes();
          break;
        case 3:
          message.passkey = Passkey.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      ethereumAddress: isSet30(object.ethereumAddress) ? String(object.ethereumAddress) : void 0,
      installationPublicKey: isSet30(object.installationPublicKey) ? bytesFromBase6422(object.installationPublicKey) : void 0,
      passkey: isSet30(object.passkey) ? Passkey.fromJSON(object.passkey) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.ethereumAddress !== void 0 && (obj.ethereumAddress = message.ethereumAddress);
    message.installationPublicKey !== void 0 && (obj.installationPublicKey = message.installationPublicKey !== void 0 ? base64FromBytes22(message.installationPublicKey) : void 0);
    message.passkey !== void 0 && (obj.passkey = message.passkey ? Passkey.toJSON(message.passkey) : void 0);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseMemberIdentifier();
    message.ethereumAddress = (_a = object.ethereumAddress) !== null && _a !== void 0 ? _a : void 0;
    message.installationPublicKey = (_b = object.installationPublicKey) !== null && _b !== void 0 ? _b : void 0;
    message.passkey = object.passkey !== void 0 && object.passkey !== null ? Passkey.fromPartial(object.passkey) : void 0;
    return message;
  }
};
function createBasePasskey() {
  return { key: new Uint8Array(), relyingParty: void 0 };
}
var Passkey = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    if (message.key.length !== 0) {
      writer.uint32(10).bytes(message.key);
    }
    if (message.relyingParty !== void 0) {
      writer.uint32(18).string(message.relyingParty);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePasskey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.key = reader.bytes();
          break;
        case 2:
          message.relyingParty = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet30(object.key) ? bytesFromBase6422(object.key) : new Uint8Array(),
      relyingParty: isSet30(object.relyingParty) ? String(object.relyingParty) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.key !== void 0 && (obj.key = base64FromBytes22(message.key !== void 0 ? message.key : new Uint8Array()));
    message.relyingParty !== void 0 && (obj.relyingParty = message.relyingParty);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBasePasskey();
    message.key = (_a = object.key) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.relyingParty = (_b = object.relyingParty) !== null && _b !== void 0 ? _b : void 0;
    return message;
  }
};
function createBaseMember() {
  return {
    identifier: void 0,
    addedByEntity: void 0,
    clientTimestampNs: void 0,
    addedOnChainId: void 0
  };
}
var Member = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    if (message.identifier !== void 0) {
      MemberIdentifier.encode(message.identifier, writer.uint32(10).fork()).ldelim();
    }
    if (message.addedByEntity !== void 0) {
      MemberIdentifier.encode(message.addedByEntity, writer.uint32(18).fork()).ldelim();
    }
    if (message.clientTimestampNs !== void 0) {
      writer.uint32(24).uint64(message.clientTimestampNs);
    }
    if (message.addedOnChainId !== void 0) {
      writer.uint32(32).uint64(message.addedOnChainId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMember();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.identifier = MemberIdentifier.decode(reader, reader.uint32());
          break;
        case 2:
          message.addedByEntity = MemberIdentifier.decode(reader, reader.uint32());
          break;
        case 3:
          message.clientTimestampNs = reader.uint64();
          break;
        case 4:
          message.addedOnChainId = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      identifier: isSet30(object.identifier) ? MemberIdentifier.fromJSON(object.identifier) : void 0,
      addedByEntity: isSet30(object.addedByEntity) ? MemberIdentifier.fromJSON(object.addedByEntity) : void 0,
      clientTimestampNs: isSet30(object.clientTimestampNs) ? long_default.fromValue(object.clientTimestampNs) : void 0,
      addedOnChainId: isSet30(object.addedOnChainId) ? long_default.fromValue(object.addedOnChainId) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.identifier !== void 0 && (obj.identifier = message.identifier ? MemberIdentifier.toJSON(message.identifier) : void 0);
    message.addedByEntity !== void 0 && (obj.addedByEntity = message.addedByEntity ? MemberIdentifier.toJSON(message.addedByEntity) : void 0);
    message.clientTimestampNs !== void 0 && (obj.clientTimestampNs = (message.clientTimestampNs || void 0).toString());
    message.addedOnChainId !== void 0 && (obj.addedOnChainId = (message.addedOnChainId || void 0).toString());
    return obj;
  },
  fromPartial(object) {
    const message = createBaseMember();
    message.identifier = object.identifier !== void 0 && object.identifier !== null ? MemberIdentifier.fromPartial(object.identifier) : void 0;
    message.addedByEntity = object.addedByEntity !== void 0 && object.addedByEntity !== null ? MemberIdentifier.fromPartial(object.addedByEntity) : void 0;
    message.clientTimestampNs = object.clientTimestampNs !== void 0 && object.clientTimestampNs !== null ? long_default.fromValue(object.clientTimestampNs) : void 0;
    message.addedOnChainId = object.addedOnChainId !== void 0 && object.addedOnChainId !== null ? long_default.fromValue(object.addedOnChainId) : void 0;
    return message;
  }
};
function createBaseCreateInbox() {
  return {
    initialIdentifier: "",
    nonce: long_default.UZERO,
    initialIdentifierSignature: void 0,
    initialIdentifierKind: 0,
    relyingParty: void 0
  };
}
var CreateInbox = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    if (message.initialIdentifier !== "") {
      writer.uint32(10).string(message.initialIdentifier);
    }
    if (!message.nonce.isZero()) {
      writer.uint32(16).uint64(message.nonce);
    }
    if (message.initialIdentifierSignature !== void 0) {
      Signature2.encode(message.initialIdentifierSignature, writer.uint32(26).fork()).ldelim();
    }
    if (message.initialIdentifierKind !== 0) {
      writer.uint32(32).int32(message.initialIdentifierKind);
    }
    if (message.relyingParty !== void 0) {
      writer.uint32(42).string(message.relyingParty);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateInbox();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.initialIdentifier = reader.string();
          break;
        case 2:
          message.nonce = reader.uint64();
          break;
        case 3:
          message.initialIdentifierSignature = Signature2.decode(reader, reader.uint32());
          break;
        case 4:
          message.initialIdentifierKind = reader.int32();
          break;
        case 5:
          message.relyingParty = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      initialIdentifier: isSet30(object.initialIdentifier) ? String(object.initialIdentifier) : "",
      nonce: isSet30(object.nonce) ? long_default.fromValue(object.nonce) : long_default.UZERO,
      initialIdentifierSignature: isSet30(object.initialIdentifierSignature) ? Signature2.fromJSON(object.initialIdentifierSignature) : void 0,
      initialIdentifierKind: isSet30(object.initialIdentifierKind) ? identifierKindFromJSON(object.initialIdentifierKind) : 0,
      relyingParty: isSet30(object.relyingParty) ? String(object.relyingParty) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.initialIdentifier !== void 0 && (obj.initialIdentifier = message.initialIdentifier);
    message.nonce !== void 0 && (obj.nonce = (message.nonce || long_default.UZERO).toString());
    message.initialIdentifierSignature !== void 0 && (obj.initialIdentifierSignature = message.initialIdentifierSignature ? Signature2.toJSON(message.initialIdentifierSignature) : void 0);
    message.initialIdentifierKind !== void 0 && (obj.initialIdentifierKind = identifierKindToJSON(message.initialIdentifierKind));
    message.relyingParty !== void 0 && (obj.relyingParty = message.relyingParty);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseCreateInbox();
    message.initialIdentifier = (_a = object.initialIdentifier) !== null && _a !== void 0 ? _a : "";
    message.nonce = object.nonce !== void 0 && object.nonce !== null ? long_default.fromValue(object.nonce) : long_default.UZERO;
    message.initialIdentifierSignature = object.initialIdentifierSignature !== void 0 && object.initialIdentifierSignature !== null ? Signature2.fromPartial(object.initialIdentifierSignature) : void 0;
    message.initialIdentifierKind = (_b = object.initialIdentifierKind) !== null && _b !== void 0 ? _b : 0;
    message.relyingParty = (_c = object.relyingParty) !== null && _c !== void 0 ? _c : void 0;
    return message;
  }
};
function createBaseAddAssociation() {
  return {
    newMemberIdentifier: void 0,
    existingMemberSignature: void 0,
    newMemberSignature: void 0,
    relyingParty: void 0
  };
}
var AddAssociation = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    if (message.newMemberIdentifier !== void 0) {
      MemberIdentifier.encode(message.newMemberIdentifier, writer.uint32(10).fork()).ldelim();
    }
    if (message.existingMemberSignature !== void 0) {
      Signature2.encode(message.existingMemberSignature, writer.uint32(18).fork()).ldelim();
    }
    if (message.newMemberSignature !== void 0) {
      Signature2.encode(message.newMemberSignature, writer.uint32(26).fork()).ldelim();
    }
    if (message.relyingParty !== void 0) {
      writer.uint32(34).string(message.relyingParty);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddAssociation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.newMemberIdentifier = MemberIdentifier.decode(reader, reader.uint32());
          break;
        case 2:
          message.existingMemberSignature = Signature2.decode(reader, reader.uint32());
          break;
        case 3:
          message.newMemberSignature = Signature2.decode(reader, reader.uint32());
          break;
        case 4:
          message.relyingParty = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      newMemberIdentifier: isSet30(object.newMemberIdentifier) ? MemberIdentifier.fromJSON(object.newMemberIdentifier) : void 0,
      existingMemberSignature: isSet30(object.existingMemberSignature) ? Signature2.fromJSON(object.existingMemberSignature) : void 0,
      newMemberSignature: isSet30(object.newMemberSignature) ? Signature2.fromJSON(object.newMemberSignature) : void 0,
      relyingParty: isSet30(object.relyingParty) ? String(object.relyingParty) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.newMemberIdentifier !== void 0 && (obj.newMemberIdentifier = message.newMemberIdentifier ? MemberIdentifier.toJSON(message.newMemberIdentifier) : void 0);
    message.existingMemberSignature !== void 0 && (obj.existingMemberSignature = message.existingMemberSignature ? Signature2.toJSON(message.existingMemberSignature) : void 0);
    message.newMemberSignature !== void 0 && (obj.newMemberSignature = message.newMemberSignature ? Signature2.toJSON(message.newMemberSignature) : void 0);
    message.relyingParty !== void 0 && (obj.relyingParty = message.relyingParty);
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseAddAssociation();
    message.newMemberIdentifier = object.newMemberIdentifier !== void 0 && object.newMemberIdentifier !== null ? MemberIdentifier.fromPartial(object.newMemberIdentifier) : void 0;
    message.existingMemberSignature = object.existingMemberSignature !== void 0 && object.existingMemberSignature !== null ? Signature2.fromPartial(object.existingMemberSignature) : void 0;
    message.newMemberSignature = object.newMemberSignature !== void 0 && object.newMemberSignature !== null ? Signature2.fromPartial(object.newMemberSignature) : void 0;
    message.relyingParty = (_a = object.relyingParty) !== null && _a !== void 0 ? _a : void 0;
    return message;
  }
};
function createBaseRevokeAssociation() {
  return { memberToRevoke: void 0, recoveryIdentifierSignature: void 0 };
}
var RevokeAssociation = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    if (message.memberToRevoke !== void 0) {
      MemberIdentifier.encode(message.memberToRevoke, writer.uint32(10).fork()).ldelim();
    }
    if (message.recoveryIdentifierSignature !== void 0) {
      Signature2.encode(message.recoveryIdentifierSignature, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRevokeAssociation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.memberToRevoke = MemberIdentifier.decode(reader, reader.uint32());
          break;
        case 2:
          message.recoveryIdentifierSignature = Signature2.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      memberToRevoke: isSet30(object.memberToRevoke) ? MemberIdentifier.fromJSON(object.memberToRevoke) : void 0,
      recoveryIdentifierSignature: isSet30(object.recoveryIdentifierSignature) ? Signature2.fromJSON(object.recoveryIdentifierSignature) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.memberToRevoke !== void 0 && (obj.memberToRevoke = message.memberToRevoke ? MemberIdentifier.toJSON(message.memberToRevoke) : void 0);
    message.recoveryIdentifierSignature !== void 0 && (obj.recoveryIdentifierSignature = message.recoveryIdentifierSignature ? Signature2.toJSON(message.recoveryIdentifierSignature) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseRevokeAssociation();
    message.memberToRevoke = object.memberToRevoke !== void 0 && object.memberToRevoke !== null ? MemberIdentifier.fromPartial(object.memberToRevoke) : void 0;
    message.recoveryIdentifierSignature = object.recoveryIdentifierSignature !== void 0 && object.recoveryIdentifierSignature !== null ? Signature2.fromPartial(object.recoveryIdentifierSignature) : void 0;
    return message;
  }
};
function createBaseChangeRecoveryAddress() {
  return {
    newRecoveryIdentifier: "",
    existingRecoveryIdentifierSignature: void 0,
    newRecoveryIdentifierKind: 0,
    relyingParty: void 0
  };
}
var ChangeRecoveryAddress = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    if (message.newRecoveryIdentifier !== "") {
      writer.uint32(10).string(message.newRecoveryIdentifier);
    }
    if (message.existingRecoveryIdentifierSignature !== void 0) {
      Signature2.encode(message.existingRecoveryIdentifierSignature, writer.uint32(18).fork()).ldelim();
    }
    if (message.newRecoveryIdentifierKind !== 0) {
      writer.uint32(24).int32(message.newRecoveryIdentifierKind);
    }
    if (message.relyingParty !== void 0) {
      writer.uint32(34).string(message.relyingParty);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseChangeRecoveryAddress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.newRecoveryIdentifier = reader.string();
          break;
        case 2:
          message.existingRecoveryIdentifierSignature = Signature2.decode(reader, reader.uint32());
          break;
        case 3:
          message.newRecoveryIdentifierKind = reader.int32();
          break;
        case 4:
          message.relyingParty = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      newRecoveryIdentifier: isSet30(object.newRecoveryIdentifier) ? String(object.newRecoveryIdentifier) : "",
      existingRecoveryIdentifierSignature: isSet30(object.existingRecoveryIdentifierSignature) ? Signature2.fromJSON(object.existingRecoveryIdentifierSignature) : void 0,
      newRecoveryIdentifierKind: isSet30(object.newRecoveryIdentifierKind) ? identifierKindFromJSON(object.newRecoveryIdentifierKind) : 0,
      relyingParty: isSet30(object.relyingParty) ? String(object.relyingParty) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.newRecoveryIdentifier !== void 0 && (obj.newRecoveryIdentifier = message.newRecoveryIdentifier);
    message.existingRecoveryIdentifierSignature !== void 0 && (obj.existingRecoveryIdentifierSignature = message.existingRecoveryIdentifierSignature ? Signature2.toJSON(message.existingRecoveryIdentifierSignature) : void 0);
    message.newRecoveryIdentifierKind !== void 0 && (obj.newRecoveryIdentifierKind = identifierKindToJSON(message.newRecoveryIdentifierKind));
    message.relyingParty !== void 0 && (obj.relyingParty = message.relyingParty);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseChangeRecoveryAddress();
    message.newRecoveryIdentifier = (_a = object.newRecoveryIdentifier) !== null && _a !== void 0 ? _a : "";
    message.existingRecoveryIdentifierSignature = object.existingRecoveryIdentifierSignature !== void 0 && object.existingRecoveryIdentifierSignature !== null ? Signature2.fromPartial(object.existingRecoveryIdentifierSignature) : void 0;
    message.newRecoveryIdentifierKind = (_b = object.newRecoveryIdentifierKind) !== null && _b !== void 0 ? _b : 0;
    message.relyingParty = (_c = object.relyingParty) !== null && _c !== void 0 ? _c : void 0;
    return message;
  }
};
function createBaseIdentityAction() {
  return {
    createInbox: void 0,
    add: void 0,
    revoke: void 0,
    changeRecoveryAddress: void 0
  };
}
var IdentityAction = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    if (message.createInbox !== void 0) {
      CreateInbox.encode(message.createInbox, writer.uint32(10).fork()).ldelim();
    }
    if (message.add !== void 0) {
      AddAssociation.encode(message.add, writer.uint32(18).fork()).ldelim();
    }
    if (message.revoke !== void 0) {
      RevokeAssociation.encode(message.revoke, writer.uint32(26).fork()).ldelim();
    }
    if (message.changeRecoveryAddress !== void 0) {
      ChangeRecoveryAddress.encode(message.changeRecoveryAddress, writer.uint32(34).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseIdentityAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.createInbox = CreateInbox.decode(reader, reader.uint32());
          break;
        case 2:
          message.add = AddAssociation.decode(reader, reader.uint32());
          break;
        case 3:
          message.revoke = RevokeAssociation.decode(reader, reader.uint32());
          break;
        case 4:
          message.changeRecoveryAddress = ChangeRecoveryAddress.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      createInbox: isSet30(object.createInbox) ? CreateInbox.fromJSON(object.createInbox) : void 0,
      add: isSet30(object.add) ? AddAssociation.fromJSON(object.add) : void 0,
      revoke: isSet30(object.revoke) ? RevokeAssociation.fromJSON(object.revoke) : void 0,
      changeRecoveryAddress: isSet30(object.changeRecoveryAddress) ? ChangeRecoveryAddress.fromJSON(object.changeRecoveryAddress) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.createInbox !== void 0 && (obj.createInbox = message.createInbox ? CreateInbox.toJSON(message.createInbox) : void 0);
    message.add !== void 0 && (obj.add = message.add ? AddAssociation.toJSON(message.add) : void 0);
    message.revoke !== void 0 && (obj.revoke = message.revoke ? RevokeAssociation.toJSON(message.revoke) : void 0);
    message.changeRecoveryAddress !== void 0 && (obj.changeRecoveryAddress = message.changeRecoveryAddress ? ChangeRecoveryAddress.toJSON(message.changeRecoveryAddress) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseIdentityAction();
    message.createInbox = object.createInbox !== void 0 && object.createInbox !== null ? CreateInbox.fromPartial(object.createInbox) : void 0;
    message.add = object.add !== void 0 && object.add !== null ? AddAssociation.fromPartial(object.add) : void 0;
    message.revoke = object.revoke !== void 0 && object.revoke !== null ? RevokeAssociation.fromPartial(object.revoke) : void 0;
    message.changeRecoveryAddress = object.changeRecoveryAddress !== void 0 && object.changeRecoveryAddress !== null ? ChangeRecoveryAddress.fromPartial(object.changeRecoveryAddress) : void 0;
    return message;
  }
};
function createBaseIdentityUpdate() {
  return { actions: [], clientTimestampNs: long_default.UZERO, inboxId: "" };
}
var IdentityUpdate = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    for (const v of message.actions) {
      IdentityAction.encode(v, writer.uint32(10).fork()).ldelim();
    }
    if (!message.clientTimestampNs.isZero()) {
      writer.uint32(16).uint64(message.clientTimestampNs);
    }
    if (message.inboxId !== "") {
      writer.uint32(26).string(message.inboxId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseIdentityUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.actions.push(IdentityAction.decode(reader, reader.uint32()));
          break;
        case 2:
          message.clientTimestampNs = reader.uint64();
          break;
        case 3:
          message.inboxId = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      actions: Array.isArray(object === null || object === void 0 ? void 0 : object.actions) ? object.actions.map((e) => IdentityAction.fromJSON(e)) : [],
      clientTimestampNs: isSet30(object.clientTimestampNs) ? long_default.fromValue(object.clientTimestampNs) : long_default.UZERO,
      inboxId: isSet30(object.inboxId) ? String(object.inboxId) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.actions) {
      obj.actions = message.actions.map((e) => e ? IdentityAction.toJSON(e) : void 0);
    } else {
      obj.actions = [];
    }
    message.clientTimestampNs !== void 0 && (obj.clientTimestampNs = (message.clientTimestampNs || long_default.UZERO).toString());
    message.inboxId !== void 0 && (obj.inboxId = message.inboxId);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseIdentityUpdate();
    message.actions = ((_a = object.actions) === null || _a === void 0 ? void 0 : _a.map((e) => IdentityAction.fromPartial(e))) || [];
    message.clientTimestampNs = object.clientTimestampNs !== void 0 && object.clientTimestampNs !== null ? long_default.fromValue(object.clientTimestampNs) : long_default.UZERO;
    message.inboxId = (_b = object.inboxId) !== null && _b !== void 0 ? _b : "";
    return message;
  }
};
function createBaseMemberMap() {
  return { key: void 0, value: void 0 };
}
var MemberMap = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    if (message.key !== void 0) {
      MemberIdentifier.encode(message.key, writer.uint32(10).fork()).ldelim();
    }
    if (message.value !== void 0) {
      Member.encode(message.value, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMemberMap();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.key = MemberIdentifier.decode(reader, reader.uint32());
          break;
        case 2:
          message.value = Member.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet30(object.key) ? MemberIdentifier.fromJSON(object.key) : void 0,
      value: isSet30(object.value) ? Member.fromJSON(object.value) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.key !== void 0 && (obj.key = message.key ? MemberIdentifier.toJSON(message.key) : void 0);
    message.value !== void 0 && (obj.value = message.value ? Member.toJSON(message.value) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseMemberMap();
    message.key = object.key !== void 0 && object.key !== null ? MemberIdentifier.fromPartial(object.key) : void 0;
    message.value = object.value !== void 0 && object.value !== null ? Member.fromPartial(object.value) : void 0;
    return message;
  }
};
function createBaseAssociationState() {
  return {
    inboxId: "",
    members: [],
    recoveryIdentifier: "",
    seenSignatures: [],
    recoveryIdentifierKind: 0,
    relyingParty: void 0
  };
}
var AssociationState = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    if (message.inboxId !== "") {
      writer.uint32(10).string(message.inboxId);
    }
    for (const v of message.members) {
      MemberMap.encode(v, writer.uint32(18).fork()).ldelim();
    }
    if (message.recoveryIdentifier !== "") {
      writer.uint32(26).string(message.recoveryIdentifier);
    }
    for (const v of message.seenSignatures) {
      writer.uint32(34).bytes(v);
    }
    if (message.recoveryIdentifierKind !== 0) {
      writer.uint32(40).int32(message.recoveryIdentifierKind);
    }
    if (message.relyingParty !== void 0) {
      writer.uint32(50).string(message.relyingParty);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAssociationState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.inboxId = reader.string();
          break;
        case 2:
          message.members.push(MemberMap.decode(reader, reader.uint32()));
          break;
        case 3:
          message.recoveryIdentifier = reader.string();
          break;
        case 4:
          message.seenSignatures.push(reader.bytes());
          break;
        case 5:
          message.recoveryIdentifierKind = reader.int32();
          break;
        case 6:
          message.relyingParty = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      inboxId: isSet30(object.inboxId) ? String(object.inboxId) : "",
      members: Array.isArray(object === null || object === void 0 ? void 0 : object.members) ? object.members.map((e) => MemberMap.fromJSON(e)) : [],
      recoveryIdentifier: isSet30(object.recoveryIdentifier) ? String(object.recoveryIdentifier) : "",
      seenSignatures: Array.isArray(object === null || object === void 0 ? void 0 : object.seenSignatures) ? object.seenSignatures.map((e) => bytesFromBase6422(e)) : [],
      recoveryIdentifierKind: isSet30(object.recoveryIdentifierKind) ? identifierKindFromJSON(object.recoveryIdentifierKind) : 0,
      relyingParty: isSet30(object.relyingParty) ? String(object.relyingParty) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.inboxId !== void 0 && (obj.inboxId = message.inboxId);
    if (message.members) {
      obj.members = message.members.map((e) => e ? MemberMap.toJSON(e) : void 0);
    } else {
      obj.members = [];
    }
    message.recoveryIdentifier !== void 0 && (obj.recoveryIdentifier = message.recoveryIdentifier);
    if (message.seenSignatures) {
      obj.seenSignatures = message.seenSignatures.map((e) => base64FromBytes22(e !== void 0 ? e : new Uint8Array()));
    } else {
      obj.seenSignatures = [];
    }
    message.recoveryIdentifierKind !== void 0 && (obj.recoveryIdentifierKind = identifierKindToJSON(message.recoveryIdentifierKind));
    message.relyingParty !== void 0 && (obj.relyingParty = message.relyingParty);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d, _e, _f;
    const message = createBaseAssociationState();
    message.inboxId = (_a = object.inboxId) !== null && _a !== void 0 ? _a : "";
    message.members = ((_b = object.members) === null || _b === void 0 ? void 0 : _b.map((e) => MemberMap.fromPartial(e))) || [];
    message.recoveryIdentifier = (_c = object.recoveryIdentifier) !== null && _c !== void 0 ? _c : "";
    message.seenSignatures = ((_d = object.seenSignatures) === null || _d === void 0 ? void 0 : _d.map((e) => e)) || [];
    message.recoveryIdentifierKind = (_e = object.recoveryIdentifierKind) !== null && _e !== void 0 ? _e : 0;
    message.relyingParty = (_f = object.relyingParty) !== null && _f !== void 0 ? _f : void 0;
    return message;
  }
};
function createBaseAssociationStateDiff() {
  return { newMembers: [], removedMembers: [] };
}
var AssociationStateDiff = {
  encode(message, writer = import_minimal36.default.Writer.create()) {
    for (const v of message.newMembers) {
      MemberIdentifier.encode(v, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.removedMembers) {
      MemberIdentifier.encode(v, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal36.default.Reader ? input : new import_minimal36.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAssociationStateDiff();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.newMembers.push(MemberIdentifier.decode(reader, reader.uint32()));
          break;
        case 2:
          message.removedMembers.push(MemberIdentifier.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      newMembers: Array.isArray(object === null || object === void 0 ? void 0 : object.newMembers) ? object.newMembers.map((e) => MemberIdentifier.fromJSON(e)) : [],
      removedMembers: Array.isArray(object === null || object === void 0 ? void 0 : object.removedMembers) ? object.removedMembers.map((e) => MemberIdentifier.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.newMembers) {
      obj.newMembers = message.newMembers.map((e) => e ? MemberIdentifier.toJSON(e) : void 0);
    } else {
      obj.newMembers = [];
    }
    if (message.removedMembers) {
      obj.removedMembers = message.removedMembers.map((e) => e ? MemberIdentifier.toJSON(e) : void 0);
    } else {
      obj.removedMembers = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseAssociationStateDiff();
    message.newMembers = ((_a = object.newMembers) === null || _a === void 0 ? void 0 : _a.map((e) => MemberIdentifier.fromPartial(e))) || [];
    message.removedMembers = ((_b = object.removedMembers) === null || _b === void 0 ? void 0 : _b.map((e) => MemberIdentifier.fromPartial(e))) || [];
    return message;
  }
};
var globalThis26 = (() => {
  if (typeof globalThis26 !== "undefined")
    return globalThis26;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6422(b642) {
  if (globalThis26.Buffer) {
    return Uint8Array.from(globalThis26.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis26.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes22(arr) {
  if (globalThis26.Buffer) {
    return globalThis26.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis26.btoa(bin.join(""));
  }
}
if (import_minimal36.default.util.Long !== long_default) {
  import_minimal36.default.util.Long = long_default;
  import_minimal36.default.configure();
}
function isSet30(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/identity/api/v1/identity.pb.js
var import_minimal37 = __toESM(require_minimal());
function createBaseVerifySmartContractWalletSignaturesRequest() {
  return { signatures: [] };
}
var VerifySmartContractWalletSignaturesRequest = {
  encode(message, writer = import_minimal37.default.Writer.create()) {
    for (const v of message.signatures) {
      VerifySmartContractWalletSignatureRequestSignature.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal37.default.Reader ? input : new import_minimal37.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseVerifySmartContractWalletSignaturesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.signatures.push(VerifySmartContractWalletSignatureRequestSignature.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      signatures: Array.isArray(object === null || object === void 0 ? void 0 : object.signatures) ? object.signatures.map((e) => VerifySmartContractWalletSignatureRequestSignature.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.signatures) {
      obj.signatures = message.signatures.map((e) => e ? VerifySmartContractWalletSignatureRequestSignature.toJSON(e) : void 0);
    } else {
      obj.signatures = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseVerifySmartContractWalletSignaturesRequest();
    message.signatures = ((_a = object.signatures) === null || _a === void 0 ? void 0 : _a.map((e) => VerifySmartContractWalletSignatureRequestSignature.fromPartial(e))) || [];
    return message;
  }
};
function createBaseVerifySmartContractWalletSignatureRequestSignature() {
  return {
    accountId: "",
    blockNumber: void 0,
    signature: new Uint8Array(),
    hash: new Uint8Array()
  };
}
var VerifySmartContractWalletSignatureRequestSignature = {
  encode(message, writer = import_minimal37.default.Writer.create()) {
    if (message.accountId !== "") {
      writer.uint32(10).string(message.accountId);
    }
    if (message.blockNumber !== void 0) {
      writer.uint32(16).uint64(message.blockNumber);
    }
    if (message.signature.length !== 0) {
      writer.uint32(26).bytes(message.signature);
    }
    if (message.hash.length !== 0) {
      writer.uint32(34).bytes(message.hash);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal37.default.Reader ? input : new import_minimal37.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseVerifySmartContractWalletSignatureRequestSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.accountId = reader.string();
          break;
        case 2:
          message.blockNumber = reader.uint64();
          break;
        case 3:
          message.signature = reader.bytes();
          break;
        case 4:
          message.hash = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      accountId: isSet31(object.accountId) ? String(object.accountId) : "",
      blockNumber: isSet31(object.blockNumber) ? long_default.fromValue(object.blockNumber) : void 0,
      signature: isSet31(object.signature) ? bytesFromBase6423(object.signature) : new Uint8Array(),
      hash: isSet31(object.hash) ? bytesFromBase6423(object.hash) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.accountId !== void 0 && (obj.accountId = message.accountId);
    message.blockNumber !== void 0 && (obj.blockNumber = (message.blockNumber || void 0).toString());
    message.signature !== void 0 && (obj.signature = base64FromBytes23(message.signature !== void 0 ? message.signature : new Uint8Array()));
    message.hash !== void 0 && (obj.hash = base64FromBytes23(message.hash !== void 0 ? message.hash : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseVerifySmartContractWalletSignatureRequestSignature();
    message.accountId = (_a = object.accountId) !== null && _a !== void 0 ? _a : "";
    message.blockNumber = object.blockNumber !== void 0 && object.blockNumber !== null ? long_default.fromValue(object.blockNumber) : void 0;
    message.signature = (_b = object.signature) !== null && _b !== void 0 ? _b : new Uint8Array();
    message.hash = (_c = object.hash) !== null && _c !== void 0 ? _c : new Uint8Array();
    return message;
  }
};
function createBaseVerifySmartContractWalletSignaturesResponse() {
  return { responses: [] };
}
var VerifySmartContractWalletSignaturesResponse = {
  encode(message, writer = import_minimal37.default.Writer.create()) {
    for (const v of message.responses) {
      VerifySmartContractWalletSignaturesResponse_ValidationResponse.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal37.default.Reader ? input : new import_minimal37.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseVerifySmartContractWalletSignaturesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.responses.push(VerifySmartContractWalletSignaturesResponse_ValidationResponse.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      responses: Array.isArray(object === null || object === void 0 ? void 0 : object.responses) ? object.responses.map((e) => VerifySmartContractWalletSignaturesResponse_ValidationResponse.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.responses) {
      obj.responses = message.responses.map((e) => e ? VerifySmartContractWalletSignaturesResponse_ValidationResponse.toJSON(e) : void 0);
    } else {
      obj.responses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseVerifySmartContractWalletSignaturesResponse();
    message.responses = ((_a = object.responses) === null || _a === void 0 ? void 0 : _a.map((e) => VerifySmartContractWalletSignaturesResponse_ValidationResponse.fromPartial(e))) || [];
    return message;
  }
};
function createBaseVerifySmartContractWalletSignaturesResponse_ValidationResponse() {
  return { isValid: false, blockNumber: void 0, error: void 0 };
}
var VerifySmartContractWalletSignaturesResponse_ValidationResponse = {
  encode(message, writer = import_minimal37.default.Writer.create()) {
    if (message.isValid === true) {
      writer.uint32(8).bool(message.isValid);
    }
    if (message.blockNumber !== void 0) {
      writer.uint32(16).uint64(message.blockNumber);
    }
    if (message.error !== void 0) {
      writer.uint32(26).string(message.error);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal37.default.Reader ? input : new import_minimal37.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseVerifySmartContractWalletSignaturesResponse_ValidationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.isValid = reader.bool();
          break;
        case 2:
          message.blockNumber = reader.uint64();
          break;
        case 3:
          message.error = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      isValid: isSet31(object.isValid) ? Boolean(object.isValid) : false,
      blockNumber: isSet31(object.blockNumber) ? long_default.fromValue(object.blockNumber) : void 0,
      error: isSet31(object.error) ? String(object.error) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.isValid !== void 0 && (obj.isValid = message.isValid);
    message.blockNumber !== void 0 && (obj.blockNumber = (message.blockNumber || void 0).toString());
    message.error !== void 0 && (obj.error = message.error);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseVerifySmartContractWalletSignaturesResponse_ValidationResponse();
    message.isValid = (_a = object.isValid) !== null && _a !== void 0 ? _a : false;
    message.blockNumber = object.blockNumber !== void 0 && object.blockNumber !== null ? long_default.fromValue(object.blockNumber) : void 0;
    message.error = (_b = object.error) !== null && _b !== void 0 ? _b : void 0;
    return message;
  }
};
var globalThis27 = (() => {
  if (typeof globalThis27 !== "undefined")
    return globalThis27;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6423(b642) {
  if (globalThis27.Buffer) {
    return Uint8Array.from(globalThis27.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis27.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes23(arr) {
  if (globalThis27.Buffer) {
    return globalThis27.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis27.btoa(bin.join(""));
  }
}
if (import_minimal37.default.util.Long !== long_default) {
  import_minimal37.default.util.Long = long_default;
  import_minimal37.default.configure();
}
function isSet31(value) {
  return value !== null && value !== void 0;
}

// node_modules/@xmtp/proto/ts/dist/esm/mls_validation/v1/service.pb.js
var import_minimal38 = __toESM(require_minimal());
var protobufPackage24 = "xmtp.mls_validation.v1";
function createBaseValidateInboxIdKeyPackagesRequest() {
  return { keyPackages: [] };
}
var ValidateInboxIdKeyPackagesRequest = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    for (const v of message.keyPackages) {
      ValidateInboxIdKeyPackagesRequest_KeyPackage.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateInboxIdKeyPackagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyPackages.push(ValidateInboxIdKeyPackagesRequest_KeyPackage.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyPackages: Array.isArray(object === null || object === void 0 ? void 0 : object.keyPackages) ? object.keyPackages.map((e) => ValidateInboxIdKeyPackagesRequest_KeyPackage.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.keyPackages) {
      obj.keyPackages = message.keyPackages.map((e) => e ? ValidateInboxIdKeyPackagesRequest_KeyPackage.toJSON(e) : void 0);
    } else {
      obj.keyPackages = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseValidateInboxIdKeyPackagesRequest();
    message.keyPackages = ((_a = object.keyPackages) === null || _a === void 0 ? void 0 : _a.map((e) => ValidateInboxIdKeyPackagesRequest_KeyPackage.fromPartial(e))) || [];
    return message;
  }
};
function createBaseValidateInboxIdKeyPackagesRequest_KeyPackage() {
  return {
    keyPackageBytesTlsSerialized: new Uint8Array(),
    isInboxIdCredential: false
  };
}
var ValidateInboxIdKeyPackagesRequest_KeyPackage = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    if (message.keyPackageBytesTlsSerialized.length !== 0) {
      writer.uint32(10).bytes(message.keyPackageBytesTlsSerialized);
    }
    if (message.isInboxIdCredential === true) {
      writer.uint32(16).bool(message.isInboxIdCredential);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateInboxIdKeyPackagesRequest_KeyPackage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyPackageBytesTlsSerialized = reader.bytes();
          break;
        case 2:
          message.isInboxIdCredential = reader.bool();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyPackageBytesTlsSerialized: isSet32(object.keyPackageBytesTlsSerialized) ? bytesFromBase6424(object.keyPackageBytesTlsSerialized) : new Uint8Array(),
      isInboxIdCredential: isSet32(object.isInboxIdCredential) ? Boolean(object.isInboxIdCredential) : false
    };
  },
  toJSON(message) {
    const obj = {};
    message.keyPackageBytesTlsSerialized !== void 0 && (obj.keyPackageBytesTlsSerialized = base64FromBytes24(message.keyPackageBytesTlsSerialized !== void 0 ? message.keyPackageBytesTlsSerialized : new Uint8Array()));
    message.isInboxIdCredential !== void 0 && (obj.isInboxIdCredential = message.isInboxIdCredential);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseValidateInboxIdKeyPackagesRequest_KeyPackage();
    message.keyPackageBytesTlsSerialized = (_a = object.keyPackageBytesTlsSerialized) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.isInboxIdCredential = (_b = object.isInboxIdCredential) !== null && _b !== void 0 ? _b : false;
    return message;
  }
};
function createBaseValidateInboxIdKeyPackagesResponse() {
  return { responses: [] };
}
var ValidateInboxIdKeyPackagesResponse = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    for (const v of message.responses) {
      ValidateInboxIdKeyPackagesResponse_Response.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateInboxIdKeyPackagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.responses.push(ValidateInboxIdKeyPackagesResponse_Response.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      responses: Array.isArray(object === null || object === void 0 ? void 0 : object.responses) ? object.responses.map((e) => ValidateInboxIdKeyPackagesResponse_Response.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.responses) {
      obj.responses = message.responses.map((e) => e ? ValidateInboxIdKeyPackagesResponse_Response.toJSON(e) : void 0);
    } else {
      obj.responses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseValidateInboxIdKeyPackagesResponse();
    message.responses = ((_a = object.responses) === null || _a === void 0 ? void 0 : _a.map((e) => ValidateInboxIdKeyPackagesResponse_Response.fromPartial(e))) || [];
    return message;
  }
};
function createBaseValidateInboxIdKeyPackagesResponse_Response() {
  return {
    isOk: false,
    errorMessage: "",
    credential: void 0,
    installationPublicKey: new Uint8Array(),
    expiration: long_default.UZERO
  };
}
var ValidateInboxIdKeyPackagesResponse_Response = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    if (message.isOk === true) {
      writer.uint32(8).bool(message.isOk);
    }
    if (message.errorMessage !== "") {
      writer.uint32(18).string(message.errorMessage);
    }
    if (message.credential !== void 0) {
      MlsCredential.encode(message.credential, writer.uint32(26).fork()).ldelim();
    }
    if (message.installationPublicKey.length !== 0) {
      writer.uint32(34).bytes(message.installationPublicKey);
    }
    if (!message.expiration.isZero()) {
      writer.uint32(40).uint64(message.expiration);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateInboxIdKeyPackagesResponse_Response();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.isOk = reader.bool();
          break;
        case 2:
          message.errorMessage = reader.string();
          break;
        case 3:
          message.credential = MlsCredential.decode(reader, reader.uint32());
          break;
        case 4:
          message.installationPublicKey = reader.bytes();
          break;
        case 5:
          message.expiration = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      isOk: isSet32(object.isOk) ? Boolean(object.isOk) : false,
      errorMessage: isSet32(object.errorMessage) ? String(object.errorMessage) : "",
      credential: isSet32(object.credential) ? MlsCredential.fromJSON(object.credential) : void 0,
      installationPublicKey: isSet32(object.installationPublicKey) ? bytesFromBase6424(object.installationPublicKey) : new Uint8Array(),
      expiration: isSet32(object.expiration) ? long_default.fromValue(object.expiration) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.isOk !== void 0 && (obj.isOk = message.isOk);
    message.errorMessage !== void 0 && (obj.errorMessage = message.errorMessage);
    message.credential !== void 0 && (obj.credential = message.credential ? MlsCredential.toJSON(message.credential) : void 0);
    message.installationPublicKey !== void 0 && (obj.installationPublicKey = base64FromBytes24(message.installationPublicKey !== void 0 ? message.installationPublicKey : new Uint8Array()));
    message.expiration !== void 0 && (obj.expiration = (message.expiration || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c;
    const message = createBaseValidateInboxIdKeyPackagesResponse_Response();
    message.isOk = (_a = object.isOk) !== null && _a !== void 0 ? _a : false;
    message.errorMessage = (_b = object.errorMessage) !== null && _b !== void 0 ? _b : "";
    message.credential = object.credential !== void 0 && object.credential !== null ? MlsCredential.fromPartial(object.credential) : void 0;
    message.installationPublicKey = (_c = object.installationPublicKey) !== null && _c !== void 0 ? _c : new Uint8Array();
    message.expiration = object.expiration !== void 0 && object.expiration !== null ? long_default.fromValue(object.expiration) : long_default.UZERO;
    return message;
  }
};
function createBaseValidateKeyPackagesRequest() {
  return { keyPackages: [] };
}
var ValidateKeyPackagesRequest = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    for (const v of message.keyPackages) {
      ValidateKeyPackagesRequest_KeyPackage.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateKeyPackagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyPackages.push(ValidateKeyPackagesRequest_KeyPackage.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyPackages: Array.isArray(object === null || object === void 0 ? void 0 : object.keyPackages) ? object.keyPackages.map((e) => ValidateKeyPackagesRequest_KeyPackage.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.keyPackages) {
      obj.keyPackages = message.keyPackages.map((e) => e ? ValidateKeyPackagesRequest_KeyPackage.toJSON(e) : void 0);
    } else {
      obj.keyPackages = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseValidateKeyPackagesRequest();
    message.keyPackages = ((_a = object.keyPackages) === null || _a === void 0 ? void 0 : _a.map((e) => ValidateKeyPackagesRequest_KeyPackage.fromPartial(e))) || [];
    return message;
  }
};
function createBaseValidateKeyPackagesRequest_KeyPackage() {
  return {
    keyPackageBytesTlsSerialized: new Uint8Array(),
    isInboxIdCredential: false
  };
}
var ValidateKeyPackagesRequest_KeyPackage = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    if (message.keyPackageBytesTlsSerialized.length !== 0) {
      writer.uint32(10).bytes(message.keyPackageBytesTlsSerialized);
    }
    if (message.isInboxIdCredential === true) {
      writer.uint32(16).bool(message.isInboxIdCredential);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateKeyPackagesRequest_KeyPackage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.keyPackageBytesTlsSerialized = reader.bytes();
          break;
        case 2:
          message.isInboxIdCredential = reader.bool();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      keyPackageBytesTlsSerialized: isSet32(object.keyPackageBytesTlsSerialized) ? bytesFromBase6424(object.keyPackageBytesTlsSerialized) : new Uint8Array(),
      isInboxIdCredential: isSet32(object.isInboxIdCredential) ? Boolean(object.isInboxIdCredential) : false
    };
  },
  toJSON(message) {
    const obj = {};
    message.keyPackageBytesTlsSerialized !== void 0 && (obj.keyPackageBytesTlsSerialized = base64FromBytes24(message.keyPackageBytesTlsSerialized !== void 0 ? message.keyPackageBytesTlsSerialized : new Uint8Array()));
    message.isInboxIdCredential !== void 0 && (obj.isInboxIdCredential = message.isInboxIdCredential);
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseValidateKeyPackagesRequest_KeyPackage();
    message.keyPackageBytesTlsSerialized = (_a = object.keyPackageBytesTlsSerialized) !== null && _a !== void 0 ? _a : new Uint8Array();
    message.isInboxIdCredential = (_b = object.isInboxIdCredential) !== null && _b !== void 0 ? _b : false;
    return message;
  }
};
function createBaseValidateKeyPackagesResponse() {
  return { responses: [] };
}
var ValidateKeyPackagesResponse = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    for (const v of message.responses) {
      ValidateKeyPackagesResponse_ValidationResponse.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateKeyPackagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.responses.push(ValidateKeyPackagesResponse_ValidationResponse.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      responses: Array.isArray(object === null || object === void 0 ? void 0 : object.responses) ? object.responses.map((e) => ValidateKeyPackagesResponse_ValidationResponse.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.responses) {
      obj.responses = message.responses.map((e) => e ? ValidateKeyPackagesResponse_ValidationResponse.toJSON(e) : void 0);
    } else {
      obj.responses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseValidateKeyPackagesResponse();
    message.responses = ((_a = object.responses) === null || _a === void 0 ? void 0 : _a.map((e) => ValidateKeyPackagesResponse_ValidationResponse.fromPartial(e))) || [];
    return message;
  }
};
function createBaseValidateKeyPackagesResponse_ValidationResponse() {
  return {
    isOk: false,
    errorMessage: "",
    installationId: new Uint8Array(),
    accountAddress: "",
    credentialIdentityBytes: new Uint8Array(),
    expiration: long_default.UZERO
  };
}
var ValidateKeyPackagesResponse_ValidationResponse = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    if (message.isOk === true) {
      writer.uint32(8).bool(message.isOk);
    }
    if (message.errorMessage !== "") {
      writer.uint32(18).string(message.errorMessage);
    }
    if (message.installationId.length !== 0) {
      writer.uint32(26).bytes(message.installationId);
    }
    if (message.accountAddress !== "") {
      writer.uint32(34).string(message.accountAddress);
    }
    if (message.credentialIdentityBytes.length !== 0) {
      writer.uint32(42).bytes(message.credentialIdentityBytes);
    }
    if (!message.expiration.isZero()) {
      writer.uint32(48).uint64(message.expiration);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateKeyPackagesResponse_ValidationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.isOk = reader.bool();
          break;
        case 2:
          message.errorMessage = reader.string();
          break;
        case 3:
          message.installationId = reader.bytes();
          break;
        case 4:
          message.accountAddress = reader.string();
          break;
        case 5:
          message.credentialIdentityBytes = reader.bytes();
          break;
        case 6:
          message.expiration = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      isOk: isSet32(object.isOk) ? Boolean(object.isOk) : false,
      errorMessage: isSet32(object.errorMessage) ? String(object.errorMessage) : "",
      installationId: isSet32(object.installationId) ? bytesFromBase6424(object.installationId) : new Uint8Array(),
      accountAddress: isSet32(object.accountAddress) ? String(object.accountAddress) : "",
      credentialIdentityBytes: isSet32(object.credentialIdentityBytes) ? bytesFromBase6424(object.credentialIdentityBytes) : new Uint8Array(),
      expiration: isSet32(object.expiration) ? long_default.fromValue(object.expiration) : long_default.UZERO
    };
  },
  toJSON(message) {
    const obj = {};
    message.isOk !== void 0 && (obj.isOk = message.isOk);
    message.errorMessage !== void 0 && (obj.errorMessage = message.errorMessage);
    message.installationId !== void 0 && (obj.installationId = base64FromBytes24(message.installationId !== void 0 ? message.installationId : new Uint8Array()));
    message.accountAddress !== void 0 && (obj.accountAddress = message.accountAddress);
    message.credentialIdentityBytes !== void 0 && (obj.credentialIdentityBytes = base64FromBytes24(message.credentialIdentityBytes !== void 0 ? message.credentialIdentityBytes : new Uint8Array()));
    message.expiration !== void 0 && (obj.expiration = (message.expiration || long_default.UZERO).toString());
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d, _e;
    const message = createBaseValidateKeyPackagesResponse_ValidationResponse();
    message.isOk = (_a = object.isOk) !== null && _a !== void 0 ? _a : false;
    message.errorMessage = (_b = object.errorMessage) !== null && _b !== void 0 ? _b : "";
    message.installationId = (_c = object.installationId) !== null && _c !== void 0 ? _c : new Uint8Array();
    message.accountAddress = (_d = object.accountAddress) !== null && _d !== void 0 ? _d : "";
    message.credentialIdentityBytes = (_e = object.credentialIdentityBytes) !== null && _e !== void 0 ? _e : new Uint8Array();
    message.expiration = object.expiration !== void 0 && object.expiration !== null ? long_default.fromValue(object.expiration) : long_default.UZERO;
    return message;
  }
};
function createBaseValidateGroupMessagesRequest() {
  return { groupMessages: [] };
}
var ValidateGroupMessagesRequest = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    for (const v of message.groupMessages) {
      ValidateGroupMessagesRequest_GroupMessage.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateGroupMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.groupMessages.push(ValidateGroupMessagesRequest_GroupMessage.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      groupMessages: Array.isArray(object === null || object === void 0 ? void 0 : object.groupMessages) ? object.groupMessages.map((e) => ValidateGroupMessagesRequest_GroupMessage.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.groupMessages) {
      obj.groupMessages = message.groupMessages.map((e) => e ? ValidateGroupMessagesRequest_GroupMessage.toJSON(e) : void 0);
    } else {
      obj.groupMessages = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseValidateGroupMessagesRequest();
    message.groupMessages = ((_a = object.groupMessages) === null || _a === void 0 ? void 0 : _a.map((e) => ValidateGroupMessagesRequest_GroupMessage.fromPartial(e))) || [];
    return message;
  }
};
function createBaseValidateGroupMessagesRequest_GroupMessage() {
  return { groupMessageBytesTlsSerialized: new Uint8Array() };
}
var ValidateGroupMessagesRequest_GroupMessage = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    if (message.groupMessageBytesTlsSerialized.length !== 0) {
      writer.uint32(10).bytes(message.groupMessageBytesTlsSerialized);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateGroupMessagesRequest_GroupMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.groupMessageBytesTlsSerialized = reader.bytes();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      groupMessageBytesTlsSerialized: isSet32(object.groupMessageBytesTlsSerialized) ? bytesFromBase6424(object.groupMessageBytesTlsSerialized) : new Uint8Array()
    };
  },
  toJSON(message) {
    const obj = {};
    message.groupMessageBytesTlsSerialized !== void 0 && (obj.groupMessageBytesTlsSerialized = base64FromBytes24(message.groupMessageBytesTlsSerialized !== void 0 ? message.groupMessageBytesTlsSerialized : new Uint8Array()));
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseValidateGroupMessagesRequest_GroupMessage();
    message.groupMessageBytesTlsSerialized = (_a = object.groupMessageBytesTlsSerialized) !== null && _a !== void 0 ? _a : new Uint8Array();
    return message;
  }
};
function createBaseValidateGroupMessagesResponse() {
  return { responses: [] };
}
var ValidateGroupMessagesResponse = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    for (const v of message.responses) {
      ValidateGroupMessagesResponse_ValidationResponse.encode(v, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateGroupMessagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.responses.push(ValidateGroupMessagesResponse_ValidationResponse.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      responses: Array.isArray(object === null || object === void 0 ? void 0 : object.responses) ? object.responses.map((e) => ValidateGroupMessagesResponse_ValidationResponse.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.responses) {
      obj.responses = message.responses.map((e) => e ? ValidateGroupMessagesResponse_ValidationResponse.toJSON(e) : void 0);
    } else {
      obj.responses = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a;
    const message = createBaseValidateGroupMessagesResponse();
    message.responses = ((_a = object.responses) === null || _a === void 0 ? void 0 : _a.map((e) => ValidateGroupMessagesResponse_ValidationResponse.fromPartial(e))) || [];
    return message;
  }
};
function createBaseValidateGroupMessagesResponse_ValidationResponse() {
  return { isOk: false, errorMessage: "", groupId: "", isCommit: false };
}
var ValidateGroupMessagesResponse_ValidationResponse = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    if (message.isOk === true) {
      writer.uint32(8).bool(message.isOk);
    }
    if (message.errorMessage !== "") {
      writer.uint32(18).string(message.errorMessage);
    }
    if (message.groupId !== "") {
      writer.uint32(26).string(message.groupId);
    }
    if (message.isCommit === true) {
      writer.uint32(32).bool(message.isCommit);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseValidateGroupMessagesResponse_ValidationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.isOk = reader.bool();
          break;
        case 2:
          message.errorMessage = reader.string();
          break;
        case 3:
          message.groupId = reader.string();
          break;
        case 4:
          message.isCommit = reader.bool();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      isOk: isSet32(object.isOk) ? Boolean(object.isOk) : false,
      errorMessage: isSet32(object.errorMessage) ? String(object.errorMessage) : "",
      groupId: isSet32(object.groupId) ? String(object.groupId) : "",
      isCommit: isSet32(object.isCommit) ? Boolean(object.isCommit) : false
    };
  },
  toJSON(message) {
    const obj = {};
    message.isOk !== void 0 && (obj.isOk = message.isOk);
    message.errorMessage !== void 0 && (obj.errorMessage = message.errorMessage);
    message.groupId !== void 0 && (obj.groupId = message.groupId);
    message.isCommit !== void 0 && (obj.isCommit = message.isCommit);
    return obj;
  },
  fromPartial(object) {
    var _a, _b, _c, _d;
    const message = createBaseValidateGroupMessagesResponse_ValidationResponse();
    message.isOk = (_a = object.isOk) !== null && _a !== void 0 ? _a : false;
    message.errorMessage = (_b = object.errorMessage) !== null && _b !== void 0 ? _b : "";
    message.groupId = (_c = object.groupId) !== null && _c !== void 0 ? _c : "";
    message.isCommit = (_d = object.isCommit) !== null && _d !== void 0 ? _d : false;
    return message;
  }
};
function createBaseGetAssociationStateRequest() {
  return { oldUpdates: [], newUpdates: [] };
}
var GetAssociationStateRequest = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    for (const v of message.oldUpdates) {
      IdentityUpdate.encode(v, writer.uint32(10).fork()).ldelim();
    }
    for (const v of message.newUpdates) {
      IdentityUpdate.encode(v, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetAssociationStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.oldUpdates.push(IdentityUpdate.decode(reader, reader.uint32()));
          break;
        case 2:
          message.newUpdates.push(IdentityUpdate.decode(reader, reader.uint32()));
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      oldUpdates: Array.isArray(object === null || object === void 0 ? void 0 : object.oldUpdates) ? object.oldUpdates.map((e) => IdentityUpdate.fromJSON(e)) : [],
      newUpdates: Array.isArray(object === null || object === void 0 ? void 0 : object.newUpdates) ? object.newUpdates.map((e) => IdentityUpdate.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.oldUpdates) {
      obj.oldUpdates = message.oldUpdates.map((e) => e ? IdentityUpdate.toJSON(e) : void 0);
    } else {
      obj.oldUpdates = [];
    }
    if (message.newUpdates) {
      obj.newUpdates = message.newUpdates.map((e) => e ? IdentityUpdate.toJSON(e) : void 0);
    } else {
      obj.newUpdates = [];
    }
    return obj;
  },
  fromPartial(object) {
    var _a, _b;
    const message = createBaseGetAssociationStateRequest();
    message.oldUpdates = ((_a = object.oldUpdates) === null || _a === void 0 ? void 0 : _a.map((e) => IdentityUpdate.fromPartial(e))) || [];
    message.newUpdates = ((_b = object.newUpdates) === null || _b === void 0 ? void 0 : _b.map((e) => IdentityUpdate.fromPartial(e))) || [];
    return message;
  }
};
function createBaseGetAssociationStateResponse() {
  return { associationState: void 0, stateDiff: void 0 };
}
var GetAssociationStateResponse = {
  encode(message, writer = import_minimal38.default.Writer.create()) {
    if (message.associationState !== void 0) {
      AssociationState.encode(message.associationState, writer.uint32(10).fork()).ldelim();
    }
    if (message.stateDiff !== void 0) {
      AssociationStateDiff.encode(message.stateDiff, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof import_minimal38.default.Reader ? input : new import_minimal38.default.Reader(input);
    let end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetAssociationStateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.associationState = AssociationState.decode(reader, reader.uint32());
          break;
        case 2:
          message.stateDiff = AssociationStateDiff.decode(reader, reader.uint32());
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromJSON(object) {
    return {
      associationState: isSet32(object.associationState) ? AssociationState.fromJSON(object.associationState) : void 0,
      stateDiff: isSet32(object.stateDiff) ? AssociationStateDiff.fromJSON(object.stateDiff) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    message.associationState !== void 0 && (obj.associationState = message.associationState ? AssociationState.toJSON(message.associationState) : void 0);
    message.stateDiff !== void 0 && (obj.stateDiff = message.stateDiff ? AssociationStateDiff.toJSON(message.stateDiff) : void 0);
    return obj;
  },
  fromPartial(object) {
    const message = createBaseGetAssociationStateResponse();
    message.associationState = object.associationState !== void 0 && object.associationState !== null ? AssociationState.fromPartial(object.associationState) : void 0;
    message.stateDiff = object.stateDiff !== void 0 && object.stateDiff !== null ? AssociationStateDiff.fromPartial(object.stateDiff) : void 0;
    return message;
  }
};
var ValidationApiClientImpl = class {
  constructor(rpc) {
    this.rpc = rpc;
    this.ValidateGroupMessages = this.ValidateGroupMessages.bind(this);
    this.GetAssociationState = this.GetAssociationState.bind(this);
    this.ValidateInboxIdKeyPackages = this.ValidateInboxIdKeyPackages.bind(this);
    this.VerifySmartContractWalletSignatures = this.VerifySmartContractWalletSignatures.bind(this);
  }
  ValidateGroupMessages(request) {
    const data = ValidateGroupMessagesRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls_validation.v1.ValidationApi", "ValidateGroupMessages", data);
    return promise.then((data2) => ValidateGroupMessagesResponse.decode(new import_minimal38.default.Reader(data2)));
  }
  GetAssociationState(request) {
    const data = GetAssociationStateRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls_validation.v1.ValidationApi", "GetAssociationState", data);
    return promise.then((data2) => GetAssociationStateResponse.decode(new import_minimal38.default.Reader(data2)));
  }
  ValidateInboxIdKeyPackages(request) {
    const data = ValidateKeyPackagesRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls_validation.v1.ValidationApi", "ValidateInboxIdKeyPackages", data);
    return promise.then((data2) => ValidateInboxIdKeyPackagesResponse.decode(new import_minimal38.default.Reader(data2)));
  }
  VerifySmartContractWalletSignatures(request) {
    const data = VerifySmartContractWalletSignaturesRequest.encode(request).finish();
    const promise = this.rpc.request("xmtp.mls_validation.v1.ValidationApi", "VerifySmartContractWalletSignatures", data);
    return promise.then((data2) => VerifySmartContractWalletSignaturesResponse.decode(new import_minimal38.default.Reader(data2)));
  }
};
var globalThis28 = (() => {
  if (typeof globalThis28 !== "undefined")
    return globalThis28;
  if (typeof self !== "undefined")
    return self;
  if (typeof window !== "undefined")
    return window;
  if (typeof global !== "undefined")
    return global;
  throw "Unable to locate global object";
})();
function bytesFromBase6424(b642) {
  if (globalThis28.Buffer) {
    return Uint8Array.from(globalThis28.Buffer.from(b642, "base64"));
  } else {
    const bin = globalThis28.atob(b642);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes24(arr) {
  if (globalThis28.Buffer) {
    return globalThis28.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(String.fromCharCode(byte));
    });
    return globalThis28.btoa(bin.join(""));
  }
}
if (import_minimal38.default.util.Long !== long_default) {
  import_minimal38.default.util.Long = long_default;
  import_minimal38.default.configure();
}
function isSet32(value) {
  return value !== null && value !== void 0;
}
export {
  authn_pb_exports as authn,
  ciphertext_pb_exports as ciphertext,
  composite_pb_exports as composite,
  contact_pb_exports as contact,
  content_pb_exports as content,
  conversation_reference_pb_exports as conversationReference,
  ecies_pb_exports as ecies,
  fetch_pb_exports as fetcher,
  frames_pb_exports as frames,
  invitation_pb_exports as invitation,
  keystore_pb_exports as keystore,
  message_pb_exports as message,
  message_api_pb_exports as messageApi,
  mls_pb_exports as mlsApi,
  content_pb_exports2 as mlsContent,
  intents_pb_exports as mlsDatabaseIntent,
  group_membership_pb_exports as mlsGroupMembership,
  group_metadata_pb_exports as mlsGroupMetadata,
  group_mutable_metadata_pb_exports as mlsGroupMutableMetadata,
  transcript_messages_pb_exports as mlsTranscriptMessages,
  service_pb_exports as mlsValidationService,
  private_key_pb_exports as privateKey,
  private_preferences_pb_exports as privatePreferences,
  public_key_pb_exports as publicKey,
  signature_pb_exports as signature,
  signed_payload_pb_exports as signedPayload
};
/*! Bundled license information:

long/index.js:
  (**
   * @license
   * Copyright 2009 The Closure Library Authors
   * Copyright 2020 Daniel Wirtz / The long.js Authors.
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *     http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *
   * SPDX-License-Identifier: Apache-2.0
   *)
*/
//# sourceMappingURL=@xmtp_proto.js.map
